---
name: sdd-run
model: sonnet
description: Implement an SDD change by executing its tasks.md in order, checking tasks off as they are verified. Use when the user runs /sdd-run.
---

Read `sdd/workflow/README.md` (shared rules for all SDD phases), then execute the phase defined in `sdd/workflow/run.md`. The argument, if any, is whatever was passed to this skill invocation.
