# revenue-reviews

§18, §7.20-7.21). Su `needs:` es `messaging-ai` porque es quien introduce el `MockAIAdapter`: hoy no hay puerto de IA compartido —`maintenance` declaró el suyo propio (`IncidentClassifier`, su D1)— y estrenar un tercero aquí obligaría a reconciliar los tres después (no está en el plan original, separada de `revenue` el 2026-08-16)
