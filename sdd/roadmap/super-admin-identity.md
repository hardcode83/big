# super-admin-identity

hoy no cabe en el esquema**. `users.tenant_id` es `NOT NULL` (migración baseline, `4a5faad7796b`) y `UserModel` lleva `TenantScopedMixin`, así que un superusuario «con visibilidad sobre todos los tenants y no perteneciente a ninguno» —requisito declarado por Jose el 2026-08-29— exige una migración y una excepción explícita, documentada, a la regla 1 de `steering/security.md` (`tenant_id` siempre del token, sin bypass). Decide también si `GRANTABLE_ROLES` (hoy `frozenset(UserRole) - {UserRole.SUPER_ADMIN}`, `auth/domain/entities.py:14`) se abre para dar de alta a otro `SUPER_ADMIN` por API —hoy solo existe el que siembra `cli/bootstrap.py`— o si eso queda fuera de alcance (no está en el plan original, separada de `super-admin-console` el 2026-08-31 al abrir su `/sdd:new`: censo completo en `sdd/roadmap/super-admin-console.md`) …
