# incident-list-property-projection

hoy `GET /api/v1/incidents` devuelve `property_id` como UUID pelado, así que `/tech` emite hasta `per_page` peticiones extra a `GET /incidents/{id}/context` por página renderizada (N+1). Proyectar `property_name` y `property_internal_code` en `IncidentResponse` —o servir una proyección estrecha de listado— cierra el N+1 **y** recorta el precio que hoy paga la excepción 6 del censo de la regla 11 de `steering/security.md`: esa proyección es *coarse* y arrastra `access_notes` a la caché del cliente fila a fila, que es lo que la fila de la excepción advierte. **Decide** si ensancha `IncidentResponse` o es recurso aparte, y si la proyección de listado excluye `access_notes` estructuralmente (no está en el plan original, candidata de `tech-app` el 2026-08-30; ver el §Risks del design de `tech-app`) …
