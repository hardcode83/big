# ci-runner-oci

versionado (`docs/ci-runner-rollback.md` + `git revert`) para volver a `ubuntu-latest`. Motivación: minutos de GitHub Actions y exceso de gates — el adelgazamiento de gates queda fuera de alcance. No está en el plan original, añadido tras `tunnel-host-surface-hardening` (2026-09-03).
