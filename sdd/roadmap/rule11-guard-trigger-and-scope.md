# rule11-guard-trigger-and-scope

su entrada y su gatillo son conjuntos disjuntos. `backend/tests/test_rule11_ownership.py` escanea `sdd/**`, `docs/**` y los `.py` de `backend/{app,alembic/versions,tests}`, y `backend-tests.yml:187` decide el área del diff con `case "$f" in backend/* | .github/workflows/backend-tests.yml)`, así que un commit de sola prosa —la forma de **todo** commit de `/sdd:archive`— no ejecuta la suite que lo contiene. **La consecuencia ya ocurrió**: el archivado de `notification-writers-gap` (`f86a83f`, 2026-08-30) dejó tres bloques infractores en `sdd/specs/access-notifications.md` (372, 525 y 689) con `backend-tests-suite` **skipped** en el run 33409418091, así que `main` está verde en CI y rojo en local — medido dos veces, la segunda ejecutando la propia detección del guardián sobre `origin/main@96756a0`: 3 infractores y ninguno más en todo el árbol. Y muerde a quien no lo rompió: la PR de `guest-portal-messaging` sí toca `backend/**`, así que saldría roja por prosa ajena, y su ship está parado por eso. **Es la segunda vez que este proyecto comete este error exacto y la primera está escrita** — la entrada de `compose-ports-guard`, en este mismo fichero, ya dijo que una guardia así «no puede vivir en `backend/tests/`»—, así que la salida no hay que inventarla: workflow propio de un job, sin `paths:` en `on:` (prohibido por `specs/backend-ci.md`), invocando un target de `make`, el patrón de `api-contract.yml` y `compose-ports.yml`. Lleva además el **alcance como dato**: hoy el guardián escanea `sdd/roadmap.md`, y una entrada que describía trabajo pendiente con el mismo vocabulario que usa la autoridad disparó su eje de propiedad sin ser una afirmación de censo, forzando una edición de este fichero que el panel de `guest-portal-messaging` levantó cuatro veces como violación de la regla 1. **Lo que NO hace, y consta**: redefinir `OWNERSHIP_PATTERNS` —13 redacciones literales que ya se sortearon solas con una paráfrasis en inglés, residual 1 del propio guardián— ni meter los miembros de `NotificationType` en el eje de sumideros (residual 8); las dos quedan como candidatas (no está en el plan original, candidata del `design.md` de `guest-portal-messaging` y registrada el 2026-08-31 al abrir su `/sdd:new`)
