# tech-cycle-completion

falta `reject` —PRD §6 le concede «aceptar/rechazar tickets» y no hay operación; `cancel` no vale, es del manager y lleva a un terminal, mientras que rechazar devuelve la incidencia al manager para reasignación y `assign` ya admite `ASSIGNED` como origen (R1)—, falta el **campo ETA** que PRD §12 pide, faltan los **materiales** junto a `final_cost`, y falta decidir «en ruta»: `TimelineEventType.TECHNICIAN_EN_ROUTE` existe en el vocabulario y `specs/maintenance.md` dice literalmente que nadie lo escribe («no hay transición "en ruta" en el ciclo entregado»), con `start` (`ACCEPTED → IN_PROGRESS`) escribiendo `TECHNICIAN_STARTED`. O es estado nuevo, o `start` pasa a significarlo y el evento huérfano se retira — no se deja. Toca la tabla de transiciones de R1, `AUDITABLE_FIELDS` de `INCIDENT` (R9 audita once campos y ninguno de los nuevos) y el contrato publicado (no está en el plan original, separada de `tech-app` el 2026-08-19 al abrir su `/sdd:new`; ver `sdd/roadmap/tech-app.md`)
