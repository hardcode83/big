# frontend-ci

[INFRA] **gate de CI para la suite del frontend** (vitest + `eslint` + `tsc --noEmit`), espejo de `backend-tests.yml`. Hoy **ningún workflow invoca vitest**: solo existen `backend-tests.yml`, `deploy-dev.yml`, `infra-dev.yml` y `multiarch-build-check.yml`, y los dos últimos se limitan a `npm run build`. Consecuencia: cualquier "suite del frontend en verde" es la ejecución local de una persona, no evidencia que nadie más pueda reproducir — exactamente el razonamiento con el que se creó `backend-tests.yml` para el backend. **No es hipotético**: `app-version-badge-date` se encontró 27 tests rotos en `main` (Node 25 empezó a exponer un `localStorage` global inservible que el guard de `test/setup.ts` veía *truthy*) y nada en CI los habría delatado nunca; se arreglaron ahí de rebote, fuera de su alcance. Lo levantó el revisor de cicd en el `/sdd:review` de ese change. Mismo encuadre que el backend: se ejecuta y reporta, sin marcar obligatorio mientras el plan del repositorio no dé protección de rama (`specs/backend-ci.md` §Estado) (no está en el plan original, añadido tras `app-version-badge-date`)
