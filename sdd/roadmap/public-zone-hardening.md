# public-zone-hardening

(1) el switcher ES/EN refresca los Server Components de la landing sin recarga manual; (2) `/login` expone «← Volver a la landing» con su clave i18n; (3) workspace/cleaner/tech shells montan un `UserMenu` con «Cerrar sesión» que purga tokens, cookie `autohostai.session.present` y caché de TanStack Query y vuelve a `/`; (4) `LoginForm` redirige a `/dashboard` | `/cleaner` | `/tech` según el rol de `/auth/me` cuando no hay `?returnTo=`. Cierra los cinco síntomas de UX observados el 2026-08-26 sobre `autohostai.digitalsec.work`. No es del plan original; entra como hotfix tras `landing-public` y antes de `hardening-release` porque (3) resuelve el caso en el que la cookie «presente» sobrevive a un logout imposible (hoy no hay logout), y la suite E2E de `hardening-release` necesita un `logout` real para poder firmar el DoD §28 sin bypass. **No endurece `AuthGuard` por rol** (cualquier autenticado entra a `/cleaner` o `/tech`): esa parte es del alcance de `cleaner-app`/`tech-app`, queda como Out of scope aquí
