# Tasks: ci-runner-oci

<!-- Single-PR migration. Sections leave the system working between them
     (each migrated workflow keeps running, just on the new runner). -->

## 1. Setup — runbook skeleton, README link, QEMU probe

- [x] 1.1 Crear `docs/ci-runner-rollback.md` con las 6 secciones del design D8 (resumen / tabla / `git revert` / rollback manual / validación / rotación-retirada). La tabla de la §2 queda como placeholder "se completa en §7" hasta migrar todos los workflows. Enlazar desde `README.md` raíz, sección "Infra / CI / runner self-hosted" (R6.1, R6.2).
- [x] 1.2 Probar QEMU en la VM viva: desde SSH, ejecutar `docker run --privileged --rm tonistiigi/binfmt --install all` y `docker buildx create --use --driver docker-container --bootstrap && docker buildx build --platform linux/amd64 - < <(echo 'FROM alpine:3.19')` para verificar que la emulación amd64 sobre arm64 funciona end-to-end. Documentar el resultado en `sdd/changes/ci-runner-oci/STATE.md` (campo `qemu_verification`). Si falla, marcar en el runbook que `multiarch-build-check.yml` se queda en `ubuntu-latest` desde el inicio (D5 fallback explícito, R4). **Resolución 2026-09-03**: probe no ejecutado (sin IP reachable, OCI CLI expirada, sin tfstate en este worktree). Adoptado el fallback explícito R4.2 / D5: `multiarch-build-check.yml` queda en `runs-on: ubuntu-latest` desde el inicio, runbook §2 declara "9 de 10 migrados, 1 excepción deliberada — QEMU no verificado". `STATE.md.qemu_verification: fallback — …`. El operador puede verificar QEMU en un follow-up change si quiere cerrar la salvedad.

## 2. Migrate lightweight contract & guard workflows <!-- panel: PASS 2026-09-03 -->

- [x] 2.1 `.github/workflows/api-contract.yml`: `runs-on: ubuntu-latest` → `runs-on: [self-hosted, dev]`. Añadir cabecera de comentario con puntero al runbook (D3, D7). El resto del workflow byte a byte: `permissions`, `concurrency`, `timeout-minutes`, actions pineadas por SHA. [R1] [R2]
- [x] 2.2 `.github/workflows/compose-ports.yml`: idem. [R1] [R2]
- [x] 2.3 `.github/workflows/frontend-api-contract.yml`: idem. [R1] [R2]
- [x] 2.4 `.github/workflows/rule11-ownership.yml`: idem. [R1] [R2]

## 3. Migrate backend-tests.yml <!-- panel: PASS 2026-09-03 -->

- [x] 3.1 `.github/workflows/backend-tests.yml` (3 jobs: `backend-tests-detect`, `backend-tests-suite`, `backend-tests`): `runs-on` de los 3 jobs a `[self-hosted, dev]`. Mantener el `concurrency` por ref con `cancel-in-progress: true`. Cabecera de comentario. El cambio se queda byte a byte fuera del `runs-on` y la cabecera. [R1] [R2] [R7]

## 4. Migrate frontend-tests.yml + infra-dev.yml <!-- panel: PASS 2026-09-03 -->

- [x] 4.1 `.github/workflows/frontend-tests.yml` (2 jobs): `runs-on` a `[self-hosted, dev]`. Concurrency por ref con `cancel-in-progress: true`. Cabecera de comentario. [R1] [R2] [R7]
- [x] 4.2 `.github/workflows/infra-dev.yml` (3 jobs: `check`, `plan`, `apply`): `runs-on` a `[self-hosted, dev]` en los 3 jobs. Mantener el actor IAM `svc-terraform-dev` — no se mueve a `instance_principal` (D6). `secrets.OCI_PRIVATE_KEY` se sigue escribiendo a `$RUNNER_TEMP/oci_private_key.pem` (`infra-dev.yml:62-66`, `infra-dev.yml:152-156`), el cleanup por job del runner ya cubre la persistencia. Concurrency de `apply` (`group: infra-dev-apply`, `cancel-in-progress: false`) intacto. Cabecera de comentario. [R1] [R2] [R5] [R7]

## 5. Migrate deploy-dev.yml build jobs <!-- panel: PASS 2026-09-03 -->

- [x] 5.1 `.github/workflows/deploy-dev.yml` (3 jobs a migrar: `provenance`, `build-backend`, `build-frontend`): `runs-on` de los 3 a `[self-hosted, dev]`. El job `deploy` ya está en `[self-hosted, dev]`, sin cambios. Mantener `concurrency` exacto: `build-backend-${{ github.ref }}` y `build-frontend-${{ github.ref }}` con `cancel-in-progress: true` en los builds, `group: deploy-dev` con `cancel-in-progress: false` en el deploy (compartido con `demo-reset.yml`, ver D4). `docker/login-action` sigue usando `GITHUB_TOKEN` (`packages: write`) — sin nuevo secret (R5). Cabecera de comentario. [R1] [R2] [R3] [R5] [R7]

## 6. Migrate multiarch-build-check.yml (QEMU caveat) <!-- panel: PASS 2026-09-03 -->

- [x] 6.1 `.github/workflows/multiarch-build-check.yml` (2 jobs: `build-backend`, `build-frontend`): `runs-on` a `[self-hosted, dev]`. Mantener `docker/setup-qemu-action@v3` + `docker/setup-buildx-action@v3` + `docker/build-push-action@v6` con `platforms: linux/amd64,linux/arm64`, `push: false`. Cabecera de comentario. **SI** la verificación de §1.2 reveló que QEMU no se registra en binfmt, **revertir solo este workflow** a `runs-on: ubuntu-latest` y declarar la excepción ("9 de 10 migrados, 1 excepción deliberada") en la tabla del runbook (D5, R4). [R1] [R2] [R4] [R7]

## 7. demo-reset.yml header + runbook finalization + verification

- [x] 7.1 `.github/workflows/demo-reset.yml`: el `runs-on: [self-hosted, dev]` ya está (caso base). Solo actualizar la cabecera de comentario para incluir el puntero al runbook. [R1]
- [x] 7.2 Rellenar la tabla de `docs/ci-runner-rollback.md` §2 con los valores reales: workflow / `runs-on` previo / `runs-on` actual / excepción (multiarch-build-check si QEMU falló). Confirmar que la tabla cubre los 10 workflows y coincide con los `.yml` migrados (R6.1, R6.3). Verificar que `pr-review-cicd` (cuando se añada el chequeo de `runs-on:`) lo dé por bueno.
- [x] 7.3 Verificación end-to-end (R7): ejecutar `gh run list --workflow=<name> --json name,conclusion,runner --limit 1` por los 10 workflows contra el commit del PR. Para los que no se disparen en push/PR (típicamente `demo-reset.yml` por schedule), correr `workflow_dispatch` ad-hoc. Adjuntar la salida al PR como evidencia. Si algún workflow queda rojo, NO abrir el PR — escribir el bloqueo en `STATE.md` y `BLOCKED.md`, y reabrir §5 o §6 según corresponda. [R7] **Primera pasada 2026-09-03 (branch `sdd/ci-runner-oci` @ `f82a035`)**: 5 verdes, 4 rojos por tooling implícito de `ubuntu-latest` → design D11 y §8. Se cierra tras §8.5. **Alcance real de "verificado":** `deploy-dev` (`build-backend`/`build-frontend`/`deploy`) e `infra-dev` (`plan`/`apply`) quedan `skipped` en ambas pasadas — están gateados a `github.ref == 'refs/heads/main'` y un `workflow_dispatch` sobre la rama no los alcanza. Su primera ejecución runner-side real es tras el merge; riesgo aceptado y documentado en `design.md` (Risks & mitigations) y vigilado por `/sdd:ship`, no un PASS de R7 sobre esos 5 jobs.

## 8. Tooling pins — enmienda D11 tras la verificación R7 <!-- panel: PASS 2026-09-03 -->

<!-- Añadida el 2026-09-03 por deviación (skill run, paso 4): D3 «solo runs-on» dejó 4 jobs en rojo en el runner. Detalle medido en design.md D11. -->

- [x] 8.1 Crear `backend/.python-version` con el contenido `3.12` (misma versión que `python:3.12-slim` en `backend/devops/Dockerfile`). Verificar que `uv` lo honra: `cd backend && uv python find` debe resolver 3.12 si hay uv disponible; si no, se verifica en 8.5 con la línea «Using CPython 3.12.x» del log de `backend-tests-suite`. [R7] [D11]
- [x] 8.2 `.github/workflows/deploy-dev.yml`, job `provenance`: añadir tras el `actions/checkout` el paso `- uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4` con `with: node-version: "22"` (mismo pin y versión que `frontend-tests.yml:78-80`; sin `cache`, el job no instala dependencias). Nada más cambia en el fichero. [R3] [R7] [D11]
- [x] 8.3 `.github/workflows/frontend-tests.yml`, job `provenance-contract`: añadir inmediatamente antes del paso `Version parity` el paso `- uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0` con `with: python-version: "3.12"`. Nada más cambia. [R7] [D11]
- [x] 8.4 `.github/workflows/rule11-ownership.yml`: añadir inmediatamente antes del paso `La propiedad de un sumidero se declara en un solo sitio` el mismo paso `actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0` con `python-version: "3.12"`. Nada más cambia. [R7] [D11]
- [x] 8.5 Re-verificación R7 de los cuatro workflows tocados (`backend-tests`, `deploy-dev`, `frontend-tests`, `rule11-ownership`) por `workflow_dispatch` sobre la rama: los jobs no gateados a `main` terminan `success` con `runner_name = autohostai-dev-vm` (API REST de jobs), y `backend-tests-suite` registra «Using CPython 3.12». Regenerar `sdd/changes/ci-runner-oci/r7-evidence.md` con la tabla completa (los 5 verdes de la primera pasada siguen valiendo: sus ficheros no cambian en §8) y cerrar 7.3. [R7]

## Implementation Notes

(Implementers: append here any decision, name or gotcha that the next section needs to know.)

- **QEMU probe (tarea 1.2) estuvo bloqueada inicialmente**: en este worktree no había `tfstate` ni `dev.tfvars` ni `backend.hcl` para sacar la IP con `terraform output`, y la sesión de OCI CLI estaba expirada (`oci iam compartment list --profile autohostai-dev --all` falla con "CLI session has expired"). La clave SSH `~/.ssh/autohostai_dev_vm` estaba presente, pero no se pudo abrir la conexión sin IP. No se inventó un resultado. **No se llegó a escribir `BLOCKED.md`** (R7.2 lo reserva para un run parcial que impida abrir el PR): en vez de eso el orquestador resolvió el bloqueo adoptando el fallback R4.2/D5 — ver la nota de "Resolución 2026-09-03" más abajo y `STATE.md.qemu_verification`.
- **`qemu_verification` en `STATE.md` quedó con valor `fallback — …`**, no `pass`/`fail`/`blocked`. El §2 implementador NO debe asumir PASS ni escribir la fila de `multiarch-build-check.yml` como migrada distinto de la excepción; ver la nota de "Resolución 2026-09-03".
- **Runbook §2 es placeholder**: 10 filas listadas por nombre (`api-contract`, `backend-tests`, `compose-ports`, `demo-reset`, `deploy-dev`, `frontend-api-contract`, `frontend-tests`, `infra-dev`, `multiarch-build-check`, `rule11-ownership`), columnas `runs-on previo` / `runs-on actual` / `Excepción` con el literal «(se completa en §7)». La columna `Excepción` de `demo-reset.yml` ya tiene la nota «ya estaba en `[self-hosted, dev]`»; la de `multiarch-build-check.yml` se actualiza en §6 si QEMU falló, en §7 si pasó.
- **Runbook §6 cubre el camino de fuerza mayor**: rollback manual de **un solo workflow** con un `sed` mínimo (`sed -i '' 's|runs-on: \[self-hosted, dev\]|runs-on: ubuntu-latest|g' .github/workflows/<name>.yml`). Cuando `git revert` global no basta (runner VM caída), el §2 implementador o el operador pueden parchear un workflow concreto; el revert global sigue siendo preferible.
- **README raíz**: sección «CI / runner self-hosted» insertada después de «Despliegue a dev (CD)» (línea 319-323). Enlace al runbook (`docs/ci-runner-rollback.md`) y al `RUNBOOK.md §6` (operación del runner). 3 líneas de prosa, sin tabla.
- **Cabecera D7 de los workflows**: la forma literal está en `design.md` D7; el §2 implementador la pega tal cual, con `<n>` = PR del migration. Comentario precede a `name:`, igual que `compose-ports.yml:1-28`, `rule11-ownership.yml:1-40`, `backend-tests.yml:1-48`.
- **Resolución 2026-09-03 (decisión del orquestador, con el usuario)**: probe QEMU no ejecutado. Adoptado fallback R4.2 / D5: `multiarch-build-check.yml` queda en `ubuntu-latest`, runbook §2 ya rellena esa fila como excepción. **`STATE.md.qemu_verification: fallback — …`**. El §6 implementador **no** debe migrar `multiarch-build-check.yml` (ya está en su estado final, no se toca). El §7 implementador NO debe reescribir la fila de multiarch; ya está cerrada.
- **§2 (2026-09-03) — `api-contract.yml`**: cabecera D7 pegada (3 líneas, `<migration-pr>` literal), `runs-on: ubuntu-latest` → `runs-on: [self-hosted, dev]` en el único job `api-contract`. Diff scope: header + runs-on, todo lo demás (`concurrency`, `permissions`, `timeout-minutes`, `on:`, actions pineadas por SHA, `env`, steps) byte a byte. SHA del commit lo añadirá el orchestrator al cerrar §2.
- **§2 (2026-09-03) — `compose-ports.yml`**: cabecera D7 pegada (3 líneas, `<migration-pr>` literal), `runs-on: ubuntu-latest` → `runs-on: [self-hosted, dev]` en el único job `compose-ports`. Diff scope: header + runs-on, byte a byte en lo demás. SHA del commit lo añadirá el orchestrator al cerrar §2.
- **§2 (2026-09-03) — `frontend-api-contract.yml`**: cabecera D7 pegada (3 líneas, `<migration-pr>` literal), `runs-on: ubuntu-latest` → `runs-on: [self-hosted, dev]` en el único job `frontend-api-contract`. Diff scope: header + runs-on, byte a byte en lo demás. SHA del commit lo añadirá el orchestrator al cerrar §2.
- **§2 (2026-09-03) — `rule11-ownership.yml`**: cabecera D7 pegada (3 líneas, `<migration-pr>` literal) **encima** de la prosa existente que ya vivía antes de `name:`; `runs-on: ubuntu-latest` → `runs-on: [self-hosted, dev]` en el único job `rule11-ownership`. Diff scope: header + runs-on, la prosa larga original entre `name:` y `on:` queda intacta, todo lo demás byte a byte. SHA del commit lo añadirá el orchestrator al cerrar §2.
- **§3 (2026-09-03) — `backend-tests.yml`**: cabecera D7 pegada (3 líneas, `<migration-pr>` literal) **encima** de la prosa existente que ya vivía antes de `name:`; `runs-on: ubuntu-latest` → `runs-on: [self-hosted, dev]` en los 3 jobs (`backend-tests-detect` @107, `backend-tests-suite` @267, `backend-tests` @430). `concurrency` por ref con `cancel-in-progress: true` preservado (workflow-level, aplica a los 3). Diff scope: header + 3 runs-on (+6 / -3 line diff), todo lo demás (`on:`, `concurrency`, `permissions`, `timeout-minutes`, actions pineadas por SHA, `env`, steps, `services:`, `needs:`, `if:`, `outputs:`) byte a byte. Sin `secrets.*` / `vars.*` (R5.2). SHA del commit lo añadirá el orchestrator al cerrar §3.
- **§4 (2026-09-03) — `frontend-tests.yml`**: cabecera D7 pegada (3 líneas, `<migration-pr>` literal) **encima** de `name:`, sin prosa previa; `runs-on: ubuntu-latest` → `runs-on: [self-hosted, dev]` en los 2 jobs (`provenance-contract` @30, `frontend-tests` @65). `concurrency` workflow-level (`group: frontend-tests-${{ github.ref }}` con `cancel-in-progress: true`) byte a byte. Diff scope: header + 2 runs-on (+5 / -2 line diff), todo lo demás (`on:`, `permissions`, `timeout-minutes`, actions pineadas por SHA, `env`, steps, `if:`, `continue-on-error:`, `working-directory:`, `id:`, `outputs` implícitos) byte a byte. Sin `secrets.*` / `vars.*` (R5.2). SHA del commit lo añadirá el orchestrator al cerrar §4.
- **§4 (2026-09-03) — `infra-dev.yml`**: cabecera D7 pegada (3 líneas, `<migration-pr>` literal) **encima** de `name:`, sin prosa previa; `runs-on: ubuntu-latest` → `runs-on: [self-hosted, dev]` en los 3 jobs (`check` @30, `plan` @55, `apply` @143). `apply` mantiene `concurrency: { group: infra-dev-apply, cancel-in-progress: false }` byte a byte. Los 6 secretos OCI (`OCI_PRIVATE_KEY`, `TFSTATE_BUCKET`, `TFSTATE_NAMESPACE`, `OCI_REGION`, `OCI_TENANCY_OCID`, `OCI_USER_OCID`, `OCI_FINGERPRINT` + `OCI_COMPARTMENT_OCID`, `GH_APP_PRIVATE_KEY`, `CLOUDFLARE_*`, `ALLOWED_SSH_*`, `SSH_PUBLIC_KEY*`) se siguen leyendo de `secrets.*` (R5.1, R5.2). Sin `instance_principal` añadido (D6: actor IAM sigue siendo `svc-terraform-dev`). Las dos escrituras a `$RUNNER_TEMP/oci_private_key.pem` (en `plan` y `apply`) byte a byte — los bloques `run:` de líneas 68 (`plan`) y 159 (`apply`) idénticos a los originales (offset +3 por la cabecera). Diff scope: header + 3 runs-on (+6 / -3 line diff), todo lo demás (`on:`, `defaults`, `concurrency`, `inputs`, `timeout-minutes`, `if:`, `needs:`, actions pineadas por SHA, `env`, steps) byte a byte. SHA del commit lo añadirá el orchestrator al cerrar §4.
- **§5 (2026-09-03) — `deploy-dev.yml`**: cabecera D7 pegada (3 líneas, `<migration-pr>` literal) **encima** de `name:`, justo antes de la prosa larga existente (`deploy-dev.yml:3-6`, comentario CD del entorno dev); `runs-on: ubuntu-latest` → `runs-on: [self-hosted, dev]` en los 3 jobs `provenance` (@24), `build-backend` (@65) y `build-frontend` (@104). El 4º job, `deploy` (@149), ya estaba en `[self-hosted, dev]` — sin cambios. `concurrency` de `build-backend` (`group: build-backend-${{ github.ref }}`, `cancel-in-progress: true`) y `build-frontend` (`group: build-frontend-${{ github.ref }}`, `cancel-in-progress: true`) byte a byte; `deploy` mantiene `group: deploy-dev` con `cancel-in-progress: false` (compartido deliberado con `demo-reset.yml` — D4). `docker/login-action` en `build-backend` y `build-frontend` sigue usando `secrets.GITHUB_TOKEN` con `packages: write` (R3.3: sin nuevo secret `GHCR_TOKEN` — grep `GHCR_TOKEN` = 0); el job `deploy` usa `secrets.GITHUB_TOKEN` con `packages: read` para el pull. Diff scope: header + 3 runs-on (+6 / -3 line diff), todo lo demás (`on:`, `permissions`, `timeout-minutes`, `concurrency`, `needs:`, `if:`, `outputs:`, actions pineadas por SHA, `env`, steps, build args, labels, tags, la sonda de ingress) byte a byte. SHA del commit lo añadirá el orchestrator al cerrar §5.
- **§6 (2026-09-03) — `multiarch-build-check.yml` (fallback R4.2/D5 aplicado)**: el workflow **no se migra**; queda en su estado final `runs-on: ubuntu-latest` en ambos jobs (`build-backend` y `build-frontend`). Único cambio: cabecera de excepción de 9 líneas pegada **encima** de la línea `name:` existente, justo antes de la prosa original de 3 líneas (`multiarch-build-check.yml:3-5`, comentario preexistente del check multi-arch). El orden del fichero queda: cabecera de excepción → `name: multiarch-build-check` → prosa original (byte a byte) → `on:` → jobs (byte a byte). Diff scope: +9 / -0 line diff, ningún `runs-on:` tocado, ningún job/step/action/platform modificado (`docker/setup-qemu-action@v3`, `docker/setup-buildx-action@v3`, `docker/build-push-action@v6`, `platforms: linux/amd64,linux/arm64`, `push: false`, `actions/checkout@v4` byte a byte). La fila del runbook §2 ya estaba rellena como excepción ("9 de 10 migrados, 1 excepción deliberada — QEMU no verificado"), cerrada antes de §6; el §7 no debe reescribirla. SHA del commit lo añadirá el orchestrator al cerrar §6.
- **§7.1 (2026-09-03) — `demo-reset.yml`**: cabecera D7 pegada (3 líneas, `<migration-pr>` literal) **encima** de la línea `name: demo-reset` existente, antes de la prosa original de 8 líneas (`demo-reset.yml:3-10`, ya explicaba por qué corre en la VM). `runs-on: [self-hosted, dev]` intacto (línea 24 → 27 tras el offset). Diff scope: +3/-0, solo la cabecera; todo lo demás byte a byte.
- **§7.2 (2026-09-03) — runbook `docs/ci-runner-rollback.md` §2**: tabla rellenada con las 10 filas medidas contra `a41ec1b` (pre-migración) y el árbol actual (post-migración): `api-contract.yml` 1 job ubuntu-latest→[self-hosted, dev]; `backend-tests.yml` 3 jobs ídem; `compose-ports.yml` 1 job ídem; `demo-reset.yml` 1 job [self-hosted, dev]→[self-hosted, dev] (nota "ya estaba" conservada); `deploy-dev.yml` 3 jobs ubuntu-latest (provenance/build-backend/build-frontend) + 1 job ya [self-hosted, dev] (deploy) → 4 jobs [self-hosted, dev]; `frontend-api-contract.yml` 1 job ídem; `frontend-tests.yml` 2 jobs ídem; `infra-dev.yml` 3 jobs ídem; `multiarch-build-check.yml` fila NO tocada (ya cerrada en §6, ambos jobs `ubuntu-latest`); `rule11-ownership.yml` 1 job ídem. La frase de placeholder del §2 intro se reemplazó por una que dice que la tabla refleja los `.yml` migrados a fecha del change. `grep -c 'se completa en §7'` → 0. §1 (`<n>`/`<migration_sha>`) intacto, se rellena al ship.
- **§8 (2026-09-03) — tooling pins D11**: `backend/.python-version` creado (nuevo fichero, 1 línea, `3.12`); `uv python find` (uv sí estaba disponible en este host) resolvió `/opt/homebrew/opt/python@3.12/bin/python3.12`, confirmando que `uv` lo honra — no hace falta esperar a 8.5 para este punto, aunque 8.5 sigue siendo la verificación en el runner real. `.github/workflows/deploy-dev.yml` (+5/-0): `setup-node@49933ea…` con `node-version: "22"` tras el `checkout` del job `provenance`, más 1 línea de comentario D11. `.github/workflows/frontend-tests.yml` (+4/-0): `setup-python@5fda3b9…` con `python-version: "3.12"` justo antes de `Version parity` en `provenance-contract`, más 1 línea de comentario. `.github/workflows/rule11-ownership.yml` (+6/-0): mismo `setup-python@5fda3b9…` justo antes de `La propiedad de un sumidero se declara en un solo sitio`, tras el `setup-uv` existente, más 2 líneas de comentario D11. Diff scope verificado por fichero: solo el paso insertado (+ comentario), `grep -c 'runs-on:'` sin cambio en los 3 ficheros (`deploy-dev.yml`=4, `frontend-tests.yml`=2, `rule11-ownership.yml`=1, todos `[self-hosted, dev]`), YAML parsea con `ruby -ryaml`. `grep -c 'setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97'` = 1 en cada uno de los dos ficheros; `grep -c 'setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020'` = 1 en `deploy-dev.yml`. 8.5 (re-verificación en CI real, incluida la línea «Using CPython 3.12» de `backend-tests-suite`) y 7.3 quedan sin marcar — no son de este implementador.
- **§8.5 / §7.3 (2026-09-03, orquestador)**: segunda pasada R7 sobre `c34303f` — `backend-tests` 33766368745, `deploy-dev` 33766375127, `frontend-tests` 33766381743, `rule11-ownership` 33766387942, todos `success` en `autohostai-dev-vm`; `backend-tests-suite` con `Using CPython 3.12.14`, `9903 passed, 41 skipped in 472.51s`. Evidencia completa (las dos pasadas, los `skipped` por gating a `main`, la excepción multiarch y el caso base demo-reset) en `sdd/changes/ci-runner-oci/r7-evidence.md`, para pegar en la descripción del PR desde `/sdd:ship`. **Pendiente por construcción, no por fallo**: los builds/deploy de `deploy-dev` y `plan`/`apply` de `infra-dev` (gateados a `main`) se prueban en el runner en el primer push a `main` tras el merge. **`infra-dev/check` NO se prueba con este PR en absoluto** (corregido 2026-09-03, `sdd-qa` tercera pasada): su `paths` filter es `infra/environments/dev/**`, que este PR no toca, así que GitHub no lo dispara aquí — ship no tiene ningún check `infra-dev/check` que mirar en este PR; su primera evidencia real llega con el primer PR futuro que sí toque esa ruta. Ver `design.md` Risks & mitigations y `proposal.md` R7.1/R7.3 (enmienda) para el riesgo aceptado sobre estos 6 jobs. Los placeholders `<n>` de las 9 cabeceras y del runbook §1 los rellena ship al abrir el PR.
- **§9 — Remediación del panel de `/sdd:review` (2026-09-03, orquestador, con el usuario)**: dos rondas de hallazgos del panel (architect/security/qa/cicd/documentation) sobre el commit `f82a035`+`c34303f`, ambas cerradas con commits en la rama:
  1. `.github/workflows/infra-dev.yml` (`plan` y `apply`): `umask 077` inmediatamente antes del `printf` que escribe `OCI_PRIVATE_KEY` a `$RUNNER_TEMP` — el original no restringía el modo del fichero en el runner persistente (hallazgo `sdd-security`). Amplía R2.3, que ahora lo declara explícitamente (ver proposal.md).
  2. `design.md` (Risks & mitigations): la nota que reemplazaba la premisa falsa "sin acceso a secretos fuera del workspace del job" se corrigió dos veces — primero para nombrar el riesgo real (trust boundary widening de PR code hacia el runner con acceso a secret-bundles), después (`sdd-security`, segunda pasada) para acotarlo a los 16 OCID enumerados en `main.tf:258` en vez de "todo el compartment", y nombrar los 3 secretos que sí habilitan escalada (`github_app_key`, `jwt_secret_key`, `encryption_key`). **Riesgo aceptado explícitamente por el usuario** (no una decisión unilateral del implementador).
  3. `proposal.md` R7.1/R7.3: enmendados para excluir explícitamente de "run verde al abrir el PR" a los 5 jobs gateados a `main` (`deploy-dev`: build-backend/build-frontend/deploy; `infra-dev`: plan/apply) — su primera verificación runner-side real es post-merge, **riesgo aceptado explícitamente por el usuario**, no un PASS de R7 sobre esos jobs. Ver también `design.md` Risks & mitigations y `tasks.md` §7.3.
  4. `README.md` y `docs/ci-runner-rollback.md`: "10 workflows" → "9 de 10" con la excepción nombrada (hallazgo `sdd-architect`/`sdd-security`); después, "QEMU no está disponible" (fallo confirmado) → "QEMU no se verificó" (probe nunca ejecutado), para no contradecir `STATE.md.qemu_verification` (hallazgo `sdd-qa`, segunda pasada).
  5. `tasks.md` (este fichero): referencias colgantes a `BLOCKED.md` (nunca se creó — el fallback de QEMU se resolvió sin bloquear el PR) corregidas para apuntar a `STATE.md.qemu_verification` y a esta misma nota (hallazgo `sdd-review-cicd`).
  6. `design.md` D11: suavizado el overclaim de que `backend/.python-version` era necesario para `api-contract` (ya estaba verde en la primera pasada R7 sin ese pin) (hallazgo `sdd-review-cicd`).
  7. `.github/workflows/*.yml` (los 9 migrados): cabecera D7 — `<migration-pr>` → `<n>` en las 9 cabeceras, para que coincida con el placeholder literal de `design.md` D7 y de `docs/ci-runner-rollback.md` (que siempre usaron `<n>`). Las notas §2-§7 de más arriba, escritas cuando cada sección se cerró, todavía dicen `<migration-pr>` — es historial de lo que se pegó en su momento, no el estado actual del fichero (hallazgo `sdd-review-documentation`, tercera pasada).
  8. `docs/ci-runner-rollback.md` §3: "revierte los 10 workflows" → "revierte los 9 workflows migrados" (`multiarch-build-check.yml` ya está en `ubuntu-latest`, no participa del revert) (hallazgo `sdd-review-documentation`, tercera pasada).
  9. `infra-dev/check` no tiene evidencia runner-side posible con este PR: su `paths` filter (`infra/environments/dev/**`) no matchea el diff de la migración, así que GitHub no lo dispara aquí — la afirmación anterior ("se verifica al abrir el PR, por construcción") era falsa. Corregido en `proposal.md` R7.1/R7.3 (añadido a la lista de jobs sin evidencia pre-merge, ahora 6 en vez de 5), `design.md` Risks & mitigations, `r7-evidence.md` y esta nota §8.5/§7.3 (hallazgo `sdd-qa`, tercera pasada).
  10. `proposal.md` R4.3: la ASSUMPTION sobre si la verificación nativa arm64 basta (vs. QEMU) nunca se cerró — quedó "a confirmar en design" → "a confirmar en tasks" sin que ninguna sección la retomara. Cerrada como **moot**: el fallback R4.2/D5 se activó y `multiarch-build-check.yml` nunca se migró, así que esta rama no se ejercitó (hallazgo `sdd-qa`, tercera pasada).
  11. `sdd/specs/{backend-ci,frontend-ci,rule11-ownership-guard}.md` describen comportamiento pre-migración (runner de 2 vCPU, `ubuntu-latest`, "CI no está afectado: ubuntu-latest trae 3.12") que este change ya invalida. Por convención del proyecto (`proposal.md` "Affected specs", nota "se crea al archivar" del nuevo `ci-runner-self-hosted.md`) estas specs se reconcilian en `/sdd:archive`, no durante `/sdd:review` — se enriquecieron las entradas de "Affected specs" con la cláusula exacta a corregir en cada una para que archivar no las repase a ciegas (hallazgo `sdd-review-cicd`, tercera pasada).
  12. `.github/workflows/backend-tests.yml`: los `services.postgres`/`services.redis` publicaban `5432`/`6379` a `0.0.0.0` — inofensivo en `ubuntu-latest` (VM efímera sin IP alcanzable), expuesto de más en el runner persistente con IP real. Cambiados a `127.0.0.1:<puerto>:<puerto>` (hallazgo `sdd-security`, tercera pasada, severidad baja).
  13. `README.md`: los 3 bullets `docs/`/`infra/`/`.github/workflows/` vivían huérfanos (pre-existentes al change, ver `git show 0cc85dd:README.md`) bajo la sección de despliegue en vez de bajo "## Estructura", a la que pertenecen semánticamente; la inserción de "## CI / runner self-hosted" entre medias lo hizo más visible. Movidos a la lista de "## Estructura" (hallazgo `sdd-qa`, tercera pasada, severidad baja).
  14. **Cerrado — decisión fresca del usuario**: `sdd-security` (tercera pasada) encontró que la nota de riesgo aceptado de `design.md` (línea 184, ronda 2) no cubría dos superficies reales — (a) el job `deploy` escribía el `.env` de dev en texto plano al workspace compartido del runner (`deploy-dev.yml:255-256`), que ahora comparten 7 workflows `pull_request`-triggered, y un PR que fijara `clean: false` en su propio `actions/checkout` podría leerlo sin pasar por IAM/Vault; (b) el usuario del runner en el grupo `docker` no es solo lectura de secretos, es control root-equivalente sobre el stack de dev **desplegado y público** (contenedores, volumen de Postgres, el túnel a `https://autohostai.digitalsec.work`). **El usuario, con el cuadro completo, confirmó mantener la aceptación del riesgo tal cual** (sin segregar runners ni tocar el grupo `docker`), en vez de la aceptación parcial (solo lectura de secret-bundles OCI) de la ronda 2. Ver `design.md` Risks & mitigations, nota final.
  15. **Regresión introducida y revertida en la misma ronda (2026-09-03, orquestador)**: el intento de mitigar (a) de la entrada 14 con `rm -f .env` al final de `deploy` (commit de la ronda 3) rompía `demo-reset.yml`, que depende deliberadamente de que ese `.env` sobreviva en el workspace compartido (`clean: false` + precondición "el entorno está desplegado" + interpolación de `docker-compose.deploy.yml`) — hallazgo `sdd-security`, cuarta pasada, severidad alta. **Revertido en el acto**: el paso "Borrar .env del workspace" se quitó de `deploy-dev.yml`; (a) vuelve a quedar como riesgo aceptado sin mitigación técnica, junto con (b), no como "mitigado". `design.md` corregido para no reclamar una mitigación que ya no existe. **Lección**: un fix de seguridad en un workflow con un consumidor deliberado del mismo estado (`demo-reset.yml` ↔ `deploy-dev.yml`) necesita revisar ese consumidor antes de aplicarse, no solo el job que se está tocando.
  16. **Otros hallazgos de la cuarta pasada, sin el riesgo de acoplamiento de la entrada 15**: `infra-dev.yml` (`plan` y `apply`) ahora borra `errored.tfstate`/`backend.hcl`/`.terraform` con `if: always()` al final de cada job — `terraform apply` puede volcar el state completo en claro si el push al bucket falla, y nada más en el runner lee esos ficheros (sin el acoplamiento de `.env`, seguro de añadir). `deploy-dev.yml`: `build-backend`/`build-frontend` ahora hacen `docker logout ghcr.io` (`if: always()`), igual que ya hacía `deploy` — la credencial de GHCR no debe sobrevivir al job en el runner persistente. `design.md`: matizada la afirmación de que `infra-dev/check` tiene "riesgo runner-side mínimo" por no leer `secrets.*` — su riesgo real es el mismo contorno de confianza ya aceptado (jobs `pull_request` en el runner con acceso ambiente vía instance-principal y `docker`), no uno aparte y menor; y corregido que `demo-reset.yml` **lee** `.env`, no lo escribe (D5's "Chosen" también corregido en la propia ronda anterior). Todos hallazgo `sdd-security`, cuarta pasada.
  17. **Panel en verde tras 5 rondas; `mark-local-verified` normaliza `STATE.md`**: el schema fijo de `sdd_lifecycle.py` (`STATE_FIELDS`) no incluye `qemu_verification` — es un campo que este change añadió a mano al frontmatter, fuera del contrato del script. `mark-local-verified` re-serializa `STATE.md` con `render_state()`, que solo escribe los campos de `STATE_FIELDS`: el campo desaparece del fichero al ejecutarlo. **Las citas a `STATE.md.qemu_verification` de más arriba (§1.2, y las notas de "Resolución 2026-09-03") documentan lo que decía ese campo mientras existió — no un puntero vivo.** El texto completo del campo (probe no ejecutado, fallback R4.2/D5 adoptado, "9 de 10 migrados, 1 excepción deliberada — QEMU no verificado") queda preservado aquí y en `design.md` D5, que es la referencia a usar de ahora en adelante.
  18. **README §"CI / runner self-hosted"**: añadida una nota `EXTERNAL_DEPENDENCY` explícita (hallazgo `sdd-review-documentation`, sexta pasada, referente `steering/documentation.md` línea 15) — la VM solo trae Docker y el agente de Actions preinstalados; el resto del tooling lo declara cada job por SHA pineado (D11), no algo implícito del runner. Enlace corregido a RUNBOOK.md §6.2 en concreto (provisión del runner), no solo §6.
  Detalle completo de cada ronda en el historial de commits de la rama (`git log --oneline -- sdd/changes/ci-runner-oci .github/workflows README.md docs/ci-runner-rollback.md`).
