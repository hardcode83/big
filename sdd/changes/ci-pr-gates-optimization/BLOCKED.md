# BLOCKED — ci-pr-gates-optimization

- **phase**: run
  **type**: deferred
  **what & why**: la tarea 2.3 requiere observar `cache-hit: true`/`cache-hit: false` en el log real del step `Restore Playwright browser cache` de `frontend-tests.yml` a lo largo de dos ejecuciones consecutivas de GitHub Actions (mismo lockfile, luego lockfile regenerado). `/sdd:run` no dispara ejecuciones reales de GitHub Actions ni abre PRs (ver `tasks.md` sección 7/8: la verificación end-to-end queda diferida a `/sdd:ship`). El plan de verificación y el resultado esperado de ambos escenarios ya están documentados en `sdd/changes/ci-pr-gates-optimization/metrics.md` (sección "Playwright cache verification (R2.4)").
  **exact resume command**: `/sdd:ship ci-pr-gates-optimization` — sección 8 de `tasks.md` ("E2E GitHub Actions — deferred-to-PR_OPEN") ejecuta esta verificación sobre el PR real y actualiza `metrics.md` con el resultado observado.

- **phase**: run
  **type**: deferred
  **what & why**: la sección 6 (archive prep) de `tasks.md` prepara los borradores de actualización de los cinco specs afectados por este change (`backend-ci.md`, `frontend-ci.md`, `multiarch-build-check.md` —a crear—, `compose-ports.md` —a crear o a fundir en `local-environment.md`— y `rule11-ownership-guard.md`) en `sdd/changes/ci-pr-gates-optimization/spec-changes/`, **sin escribirlos en `sdd/specs/*`**. Por regla del proyecto, `/sdd:archive` es la única fase que crea o actualiza specs vivas; `/sdd:run` no escribe en `sdd/specs/*` (verificado: `git status --porcelain sdd/specs/` vacío tras la sección 6).
  **exact resume command**: `/sdd:archive ci-pr-gates-optimization` consume los borradores de `sdd/changes/ci-pr-gates-optimization/spec-changes/` y los aplica a `sdd/specs/`.

- **phase**: run (§7)
  **type**: deferred / expected-sync-risk
  **what & why**: `origin/main` (`3e0aa4bb`) ha avanzado desde el punto de rama `888edfa4` y toca `.github/workflows/deploy-dev.yml` y `multiarch-build-check.yml` (entre otros, `ci-runner-oci` y el trabajo de SMTP). El diff propio de este change sobre esos dos ficheros es únicamente los 8 renglones `cache-from`/`cache-to` (4 pares). **Dry-run del merge a 3 vías ahora mismo: limpio** (`git merge-file` sobre base `888edfa4` / árbol de trabajo / `origin/main` → rc=0, cero conflictos, sin sincronizar nada). **El merge resultante conserva a la vez los cambios de `origin/main`** (runner self-hosted, `Logout GHCR`, `.env` en `$HOME`, `SMTP_*`, `PUBLIC_HOSTNAME`) **y las líneas Buildx de este change** (las 4 cache lines caen dentro de los bloques `with:` de `docker/build-push-action` de main). **No existe decisión humana pendiente mientras el merge siga siendo limpio.**
  **exact resume command**: `/sdd:ship ci-pr-gates-optimization` sincroniza `origin/main` en la rama y **debe volver a verificar tras el sync-base** que el resultado sigue siendo correcto — que `git diff origin/main -- .github/workflows/deploy-dev.yml .github/workflows/multiarch-build-check.yml` muestra SOLO los 4 pares de cache sobre lo que ya trae main. Si para entonces `origin/main` ha avanzado tocando esos mismos bloques `with:` y el merge deja de ser limpio, resolver conservando ambos lados.

- **phase**: review → run (§10 remediación, 2026-09-05)
  **type**: deferred
  **what & why**: el panel de `/sdd:review` a escala de feature quedó FAIL con 5 findings (F1 metrics.md caché multiarch pre-change falsa; F2 drafts spec-changes con anclas pre-§9; F3 cita D5→D4; F4 latente detector rule11 vs SCOPE recursivo; D-F2 tasks.md `**`→`*`). **Todos remediados** en la sección 10 de `tasks.md` — **panel §10 PASS 2026-09-05** (architect/security/qa/cicd/documentation, tras 2 fix-rounds; i18n/tenancy no aplicaban al diff). F4 resuelto por decisión de usuario: anclas explícitas conservadas + test machine-checked `relevant-SCOPE(rule11) ⊆ detect-surface` en `scripts/test_rule11_ownership.py` (design D1.1.a). El código de los workflows nunca tuvo defecto (todos los EARS R1–R8 met); los findings eran de exactitud de artefactos. **Pendiente único:** re-ejecutar `/sdd:review` para establecer la aprobación local — diferido a propósito esta sesión (el usuario pidió STOP tras el run, sin avanzar a review/ship/archive).
  **exact resume command**: `/sdd:review ci-pr-gates-optimization` (re-audita con la remediación §10 incorporada; si PASS → `local_review: APPROVED` + `READY_FOR_PR`).

- **phase**: review → run (§11 remediación, 2026-09-05)
  **type**: deferred
  **what & why**: **RESUELTO en la sección 11 de `tasks.md` — panel §11 PASS 2026-09-05** (architect/security/qa/cicd; i18n/tenancy/documentation no aplican al diff). SEC-1/SEC-2/SEC-3 + QA-R2.4 remediados en una ronda, con auditoría exhaustiva de la clase de regresión (design D1.1.b/c, proposal R1.8/R5.1/R5.5/R2.4) y tests anti-regresión (`scripts/test_detect_surface.py`). Los findings pre-existentes de `multiarch-build-check.yml` (cicd) quedan **fuera de scope por decisión de usuario** (candidatos a un change de higiene de supply-chain aparte). Registro histórico del FAIL #2 (el re-`/sdd:review` tras §10) — architect/i18n/tenancy/documentation PASS; security/qa/cicd FAIL con residuales más profundos de la misma invariante de superficie de detección, ya cerrados:
    - **SEC-1 (medium, en-scope, R5.1/D1.1):** `scripts/compose-ports.py` corre `docker compose config` **sin `-f`** (`config_command()` líneas 107-117) → descubrimiento por defecto de Compose (`compose.yaml`>`compose.yml`>`docker-compose.yaml`>`docker-compose.yml`+`*.override.*`). `compose-ports-detect` solo ancla `docker-compose*` → un PR que añada `compose.yaml` (que **tiene precedencia**) lo inspecciona el guard pero el detect da `compose=false` → OMITIDA verde. Mismo fail-open que F4, no cubierto. Fix: anclar la familia `compose*.yml|compose*.yaml|compose.override.*` (o pasar `-f` explícito en el script).
    - **SEC-2 (medium, en-scope, R1.2):** `check-version-parity` (lee `VERSION`+`backend/pyproject.toml`+`frontend/package.json`) corre **solo** en frontend-tests-suite, que este change ahora gatea → regresión: un bump de `VERSION` o `backend/pyproject.toml` sin tocar frontend ya no se re-verifica (antes corría siempre) y `deploy-dev` lo hornea en `org.opencontainers.image.version`. Documentado como "frontera/residual" en D1.1, pero security objeta enviar la regresión. Fix limpio: extraer `check-version-parity` a un job/workflow **always-run** propio (patrón de `frontend-api-contract.yml`), que cierra la regresión sin erosionar R7.
    - **SEC-3 (low, en-scope, D1.1.a):** el test F4 corre solo en rule11-suite y compose-suite (`pytest scripts/`), **ambos condicionales** → un PR que añada `sdd/adr/x.md` no casa ningún anchor, ambas suites se saltan, el test **no corre en el PR que abre la brecha** (el rojo aparece un PR tarde). D1.1.a prometía FALLAR en el PR introductor. Fix: correr la invariante de superficie **incondicionalmente** (paso en `rule11-ownership-detect` o job tiny always-run).
    - **QA (low, en-scope, R2.4):** `proposal.md` R2.4 ("SHALL NOT volver a invocar el instalador de Playwright en cache hit") nunca se enmendó para reflejar la anulación **razonada y deliberada de D3** (el install corre siempre para verificar `--with-deps`; tasks.md 2.2 lo exige). El texto del criterio canónico no cuadra con el código. Fix: enmendar R2.4 (o nota de decisión) para que el criterio describa el comportamiento implementado.
    - **cicd (2, PRE-EXISTENTES, out-of-scope):** `multiarch-build-check.yml` usa tags flotantes `@v3/@v4/@v6` sin pin SHA y no declara `permissions:`. El diff propio de este change en ese fichero es **solo** las 4 líneas de caché; ambos issues son previos. El tag flotante **ya está documentado** en tasks.md §7 como *"Future-change candidate (fuera de scope … excede R8.5, que solo prohíbe *cambiar* pins existentes)"*. Decisión de scope pendiente del usuario: pinnearlos ahora (el SHA de v6 ya existe en `deploy-dev.yml`) o dejarlos al change de higiene aparte.
  **estado**: cerrado por §11 (panel PASS). **Pendiente único**: re-ejecutar `/sdd:review` para establecer la aprobación local — diferido a propósito (el usuario pidió STOP tras el run, sin `/sdd:review` automático).
  **exact resume command**: `/sdd:review ci-pr-gates-optimization` (re-audita con §9/§10/§11 incorporados; si PASS → `local_review: APPROVED` + `READY_FOR_PR`).

- **phase**: review → run (§12 remediación, 2026-09-05)
  **type**: deferred
  **what & why**: 4.º `/sdd:review` a escala de feature. 6/7 PASS (architect/qa/cicd/documentation/i18n/tenancy); **security FAIL** con 2 findings, ambos remediados esta ronda:
    - **SEC-medium (en-scope, R5.5(a)/D1.1.c) — RESUELTO:** `frontend_surface()` en `scripts/check-detect-surface.py` calculaba su superficie con un regex `.py`-only (`_SCRIPTS_PY = scripts/[…]+\.py`), mientras `frontend-tests-detect` ancla solo dos scripts `.py` **nombrados** (no `scripts/*` en bloque, como sí hacen compose/rule11). Punto ciego: el día que `frontend-tests-suite` corriera un guard no-`.py` bajo `scripts/` (`bash scripts/foo.sh`, `node scripts/x.mjs`), el check always-run seguiría dando exit 0 mientras un PR que tocara solo ese fichero recibía `**OMITIDA**` verde sin correr la suite — la misma clase fail-open que §9/§11 cierran, reabierta solo para el gate frontend. No explotable hoy (la suite no referencia ningún script no-`.py` sin anclar), pero el guard que debía hacerlo *no-reabrible* no lo cubría. **Fix (decisión de usuario: superficie extension-agnostic + test):** regex renombrado a `_SCRIPTS_REF` sin sufijo `.py` (captura cualquier token `scripts/...`), manteniendo las anclas frontend estrechas (preserva el objetivo de optimización). Dos tests prove-the-red en `scripts/test_detect_surface.py`: `test_frontend_surface_regex_is_extension_agnostic` (la superficie ahora *ve* `scripts/foo.sh`) + `test_frontend_guard_fails_the_red_on_an_unanchored_non_py_script` (esa ruta queda uncovered → guard fail-closed). Suite: **261 passed** (antes 259; +2); los 3 detectores siguen OK; `check("frontend") == []`.
    - **SEC-low (pre-existente, out-of-scope) — ACEPTADO Y DOCUMENTADO:** este change añade `cache-to: type=gha,mode=max` a `multiarch-build-check.yml`, que sigue con tags flotantes (`@v3/@v4/@v6`) y sin `permissions:`. El pinneo ya estaba **diferido por decisión de usuario** (tasks.md:189); la novedad es el canal de escritura de cache. **Fix (decisión de usuario: documentar como riesgo aceptado):** fila nueva en la tabla "Risks & mitigations" de `design.md` (§12, SEC-low) — blast radius acotado (GHA aísla caches por `ref`; `pull_request`+`paths:`; `push: false`), pendiente de un change de higiene de supply-chain dedicado.
  **estado**: cerrado por §12 (ambos findings remediados). **Pendiente único**: re-ejecutar el panel `/sdd:review` para confirmar security PASS y establecer la aprobación local.
  **exact resume command**: `/sdd:review ci-pr-gates-optimization` (re-audita con §9/§10/§11/§12 incorporados; si PASS → `local_review: APPROVED` + `READY_FOR_PR`).

- **phase**: review → run (§13 remediación, 2026-09-07)
  **type**: decision
  **what & why**: el `/sdd:review` round 7 (2026-09-07) dio 6/7 PASS y **2 findings reales**: (documentation) README raíz sin `make check-detect-surface` — **RESUELTO** en §13.3 (`README.md:35`); (security medium, R5.5(a)/D1.1.c) `_SCRIPTS_REF`/`frontend_surface()` soltaba en silencio ciertas grafías de una referencia a script de raíz. La §13 + 2 fix-rounds remediaron **3 variantes** de la clase de under-match (grafías `"$ROOT"/`,`$(…)/`,`${VAR:-.}/`; dir real no anclado `lib/scripts/`; sufijo-hermano `notfrontend/`/`sub.github/`), sustituyendo el regex por la función `_root_script_refs` (deny-list por segmento completo). **El panel de §13 sigue FAIL**: la 2.ª (y última permitida) ronda de re-review de security halló un **4.º finding posicional de la MISMA clase** — un directorio **anidado** cuyo último segmento es exactamente `frontend`/`.github` (`a/frontend/scripts/x.sh`, `vendor/.github/scripts/x.sh`) se descarta en silencio porque la exclusión no exige que el segmento esté en la RAÍZ. **Límite de 2 fix-rounds/sección alcanzado → STOP, decisión del usuario requerida.** Contexto para decidir: cada finding de esta clase ha sido **latente** (la suite frontend de hoy no referencia ninguna de esas rutas; `check("frontend")==[]` y `frontend_surface()==['.github/scripts/extract-pr.sh','Makefile','scripts/validate-provenance-contract.py']` en TODAS las rondas). El parser textual de superficie persigue grafías de un input **hipotético futuro**. La raíz del problema es la decisión «anclas estrechas» de `frontend-tests-detect` (ancla 2 scripts `.py` nombrados en vez de `scripts/*` en bloque como compose/rule11), tomada deliberadamente por el ahorro de R7 (design D1.1.a). Opciones: **(A)** anclar `scripts/*` en `frontend-tests-detect` → dissuelve la clase entera (cualquier `scripts/…` queda anclado, el parser de grafías deja de ser el guardián), coste R7 mínimo (compose/rule11 ya anclan `scripts/*`); **(B)** un 3.er parche a `_root_script_refs` exigiendo raíz de ruta (puede aflorar otra variante posicional); **(C)** aceptar el estado actual (cierra prefijo/sufijo/hermano; el anidado es un edge obscuro que la suite no toca) y documentarlo como known-limitation. Fix-round-2 queda aplicado en disco (mejora estricta, `266 passed`, `check("frontend")==[]`).
  **exact resume command**: decisión del usuario. Si **(A)**: editar el `case` de `frontend-tests-detect` en `.github/workflows/frontend-tests.yml` añadiendo `scripts/*`, actualizar design D1.1.a/proposal R1.2 (frontera), simplificar/retirar el parser de grafías de `frontend_surface()`, re-correr el panel de §13. Si **(B)**: parchear `_root_script_refs` (exigir que el segmento excluido sea el primero de la ruta) + test anidado, re-correr security. Si **(C)**: documentar la known-limitation en el docstring + `design.md`, marcar el finding como riesgo aceptado, re-correr el panel de §13. Tras cerrar §13: `/sdd:review ci-pr-gates-optimization`.
  **RESOLUCIÓN (2026-09-07)**: el usuario eligió **(A)**. Implementado como tareas **13.5/13.6**: `frontend-tests-detect` ancla `scripts/*` en bloque; `check-detect-surface.py` reescrito (`_root_script_refs` reporta por ruta real, quita solo prefijos virtuales; `_SCRIPTS_REF`/`_GH_SCRIPT` eliminados); tests migrados a semántica de ruta real; `design.md` D1.1.d + `proposal.md` R1.2 sincronizados. Dos fix-rounds de robustez del parser (doble-barra → `/+`; `..`/`.` → `posixpath.normpath`) cerraron los residuales de normalización que el re-review de security fue hallando; security confirmó **0 fail-open** reales. **§13 panel PASS 2026-09-07** (gate `reviewer_panel.py --phase run` exit 0: architect/security/qa/cicd PASS; documentation phase-excluded en run; i18n/tenancy NO MATCH). Ambos findings del round 7 quedan remediados (documentation README en 13.3; security en 13.1→13.6). **Estado: cerrado.** Único pendiente lateral (igual que §10/§11/§12): re-correr `/sdd:review ci-pr-gates-optimization` a escala de feature para establecer la aprobación local (re-audita con documentation incluido — valida el README de 13.3 — y con §9–§13; si PASS → `local_review: APPROVED` + `READY_FOR_PR`).
