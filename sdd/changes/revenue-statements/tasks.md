# Tasks: revenue-statements

> Fuente de verdad del diseño: `sdd/changes/revenue-statements/design.md`.
> Cobertura: las siete R del proposal y las cuatro OQ obligatorias.
> Convención de tests: cada tarea incluye su test (testing.md §"Cada tarea de
> implementación incluye su test"); `domain/` con invariante real va TDD-first.

## 1. Domain layer — invariantes y excepciones <!-- panel: PASS 2026-08-27 -->

- [x] 1.1 **TDD-first**: en `backend/app/statements/domain/entities.py` añadir la tabla `_TRANSITIONS` (ClassVar), `update_notes(notes, now)`, `mark_ready(now)`, `mark_sent(now)` y el helper `_check_transition(operation)` sobre `OwnerStatement`. Tests en `backend/tests/statements/test_entities.py` que cubran: las tres transiciones legales, el rechazo de saltar `DRAFT → SENT`, el rechazo de transicionar desde `SENT`, el rechazo de `update_notes` con `None` / vacío tras `strip()` / con `U+0000`. **Files**: `backend/app/statements/domain/entities.py`, `backend/tests/statements/test_entities.py`. [R4.1, R4.4, R4.5, R4.6]
- [x] 1.2 Crear `backend/app/statements/domain/exceptions.py` con: `OwnerStatementInvalidTransitionError`, `OwnerStatementValidationError` (necesaria para los rechazos de `update_notes` — R4.5/D1), `OwnerStatementNotFoundError`, `ExpenseNotFoundError`, `ExpenseAlreadyConsolidatedError`, `NamedExpenseInClosedPeriodError`, `MixedCurrencyPeriodError`. Jerarquía plana con base `StatementsDomainError(Exception)` siguiendo el patrón de `pricing/domain/exceptions.py`. **Files**: `backend/app/statements/domain/exceptions.py` (nuevo). [D6.2, D6.3, D9]

## 2. Audit vocabulary y permisos RBAC <!-- panel: PASS 2026-08-27 (architect + qa PASS; security's 2 findings both FIXED in fix-rounds — OWNER_STATEMENT added to REDACT_ONLY_FIELDS, ordinal wording removed from action docstring) -->

- [x] 2.1 En `backend/app/audit/domain/actions.py` añadir `ENTITY_OWNER_STATEMENT` y `ENTITY_EXPENSE` a `ENTITY_TYPES`, y seis acciones (`OWNER_STATEMENT_GENERATED`, `OWNER_STATEMENT_STATUS_CHANGED`, `OWNER_STATEMENT_NOTES_UPDATED`, `EXPENSE_CREATED`, `EXPENSE_UPDATED`, `EXPENSE_DELETED`) a `ACTIONS`. **Files**: `backend/app/audit/domain/actions.py`. [R7.3, D7]
- [x] 2.2 En `backend/app/audit/domain/value_objects.py` añadir `AUDITABLE_FIELDS["OWNER_STATEMENT"] = {"status", "notes"}`, `AUDITABLE_FIELDS["EXPENSE"] = {"category", "amount", "currency", "date", "statement_id", "incident_id", "approved_by", "receipt_storage_key"}`, y `REDACT_ONLY_FIELDS["EXPENSE"] = {"description"}`. Test parametrizado en `backend/tests/test_audit_allowlists.py` (o donde esté el test existente del censo) que falle con cuerpo claro si el nombre se quita o cambia. **Files**: `backend/app/audit/domain/value_objects.py`, test existente. [R7.4, D7]
- [x] 2.3 En `backend/app/auth/domain/policy.py` añadir `Permission.READ_OWNER_STATEMENTS`, `Permission.MANAGE_OWNER_STATEMENTS`, los `_STATEMENTS_READ` y `_STATEMENTS_MANAGE` bundles, y la asignación a `TENANT_OWNER` y `PROPERTY_MANAGER` (los demás quedan fuera). Test en `backend/tests/auth/test_policy.py` que verifique la asignación por rol. **Files**: `backend/app/auth/domain/policy.py`, `backend/tests/auth/test_policy.py`. [R7.1, D8]

## 3. Repositories — puertos y adaptadores SQLAlchemy <!-- panel: PASS 2026-08-27 (architect's 2 findings FIXED — typo "EXPENSE" → "OTHER" with `OwnerApprovalRelatedType.OTHER.value`, tenant_id added to `find_pending_owner_approval_for` per regla 1; security's 4 findings FIXED — same two plus TOCTOU `with_for_update()` row lock in `Expense.save`, and `scalar_one_or_none()` for missing-row symmetry) -->

- [x] 3.1 En `backend/app/statements/domain/repositories.py` añadir el Protocol `OwnerStatementRepository` con: `get`, `list_paginated(filters)`, `add`, `save`. El `ExpenseReader` existente queda intacto. `list_for_period` y `bulk_associate_to_statement` **no** pertenecen aquí — son del lado de Expense (D6.1 fija `Expense.statement_id` en el bulk; D6.1 lee `Expense` por período). **Files**: `backend/app/statements/domain/repositories.py`. [D2]
- [x] 3.2 En el mismo archivo añadir el Protocol `ExpenseRepository` con: `get`, `list_paginated(filters)`, `add`, `save`, `list_for_period(tenant_id, property_id, period_start, period_end)` (D6.1, consumido por `MonetaryAggregator`), `list_pending_for_property(tenant_id, property_id)` (D2), `bulk_associate_to_statement(tenant_id, ids, statement_id)` (D6.1, idempotente por `statement_id IS NULL`), `find_pending_owner_approval_for(tenant_id, expense_id)` (D13, devuelve el `OwnerApproval(OTHER, status=PENDING)` si lo hay — `tenant_id` es **obligatorio** per regla 1 de `steering/security.md`, no del D13 original). **Files**: `backend/app/statements/domain/repositories.py`. [D2, D6.1, D13]
- [x] 3.3 En `backend/app/statements/infrastructure/repositories.py` añadir `SqlAlchemyOwnerStatementRepository` (con los cuatro métodos de 3.1) y `SqlAlchemyExpenseRepository` (con los ocho de 3.2). Cada método toma `tenant_id` explícito además del filtro. La query de `bulk_associate_to_statement` es un `UPDATE expenses SET statement_id = :new WHERE id = ANY(:ids) AND tenant_id = :t AND statement_id IS NULL` (idempotente). **Files**: `backend/app/statements/infrastructure/repositories.py`. [D2, D6.1]
- [x] 3.4 En el mismo adaptador, el `update` de `Expense` aplica la inmutabilidad de D6.2: rechaza con `ExpenseAlreadyConsolidatedError(field_name)` los `amount`/`currency`/`category`/`date`/`property_id`/`statement_id`/`approved_by` cuando `statement_id IS NOT NULL`; permite `description` y `receipt_storage_key`. Test parametrizado en `test_repositories.py` por campo. **Files**: `backend/app/statements/infrastructure/repositories.py`, `backend/tests/statements/test_repositories.py`. [D6.2]
- [x] 3.5 En `test_repositories.py` añadir tests para `bulk_associate_to_statement`, `find_pending_owner_approval_for`, y la inmutabilidad de D6.2; los tests existentes de schema (`test_models.py`) quedan como están. **Files**: `backend/tests/statements/test_repositories.py`. [D2, D6.1, D6.2, D13]

## 4. Application layer — casos de uso, audit writer, reconciliación <!-- panel: PASS 2026-08-27 (architect + security + qa + tenancy — fix-round closed QA #4 TOCTOU on delete) -->

- [x] 4.1 En `backend/app/statements/application/use_cases.py` añadir `_AuditWriter` que rechaza `actor is None` salvo cuando el llamante pasa `actor_optional=True` para `OWNER_STATEMENT_GENERATED` desde el job de generación mensual. Mismo dibujo que `_AuditWriter` de `pricing/application/use_cases.py:119-167`. **Files**: `backend/app/statements/application/use_cases.py`. [D7]
- [x] 4.2 `CreateExpenseUseCase`: en una transacción, valida el cuerpo (categoría, amount ≤ 10**8, descripción ≤ 500 chars, descripción no vacía ni con `U+0000`), resuelve `property_id` dentro del tenant, calcula `tenant.config.owner_approval_threshold_eur`, y si `amount > threshold` crea también `OwnerApproval(related_type=OTHER, related_id=expense.id, amount, reason)` vía `OwnerApprovalRepository`. Devuelve `Expense` con `pending_owner_approval_id` cuando aplica. Tests en `test_use_cases.py`. **Files**: `backend/app/statements/application/use_cases.py`, `backend/tests/statements/test_use_cases.py`. [R5.1, R5.2, R5.6, R5.7, D4]
- [x] 4.3 `UpdateExpenseUseCase`: valida el recurso completo, rechaza con `NamedExpenseInClosedPeriodError` si `date` cae en un período para el que existe `OwnerStatement`, rechaza campos inmutables de D6.2 si está consolidado, deja `Expense.approved_by` no escribible por API. **Files**: `backend/app/statements/application/use_cases.py`, `backend/tests/statements/test_use_cases.py`. [R5.3, D6.2, D6.3]
- [x] 4.4 `DeleteExpenseUseCase`: rechaza con `ExpenseAlreadyConsolidatedError` si `statement_id IS NOT NULL`. **Files**: `backend/app/statements/application/use_cases.py`, `backend/tests/statements/test_use_cases.py`. [R5.4, D6.2]
- [x] 4.5 `GetExpenseUseCase` y `ListExpensesUseCase` aplican el filtro `pending_owner_approval_id` consultando `owner_approvals WHERE related_type=OTHER AND related_id=expense.id AND status=PENDING LIMIT 1`. **Files**: `backend/app/statements/application/use_cases.py`, `backend/tests/statements/test_use_cases.py`. [R5, D13]
- [x] 4.6 `GenerateOwnerStatementUseCase`: en una transacción, comprueba idempotencia por la clave UNIQUE (`tenant_id`, `property_id`, `period_start`, `period_end`); si existe devuelve el existente con marca de replay; si no, valida el período (mes natural cerrado), comprueba mezcla de monedas vía `CurrencyFilter` (D3 — abortar entero si alguna fila computable tiene `currency != "EUR"`), calcula los once importes, persiste el statement en `DRAFT`, llama a `expenses.bulk_associate_to_statement` para fijar `statement_id` sobre las filas del período; emite `TimelineEvent OWNER_STATEMENT_GENERATED` con `metadata` cerrado (`actor_type=TimelineActorType.SCHEDULER`, `actor_user_id=None` — usar el enum canónico ya existente en `backend/app/timeline/domain/enums.py:8`, **no** inventar `ScheduleActor` ni ningún otro tipo). El `AuditLog` para esta acción **no** se escribe desde el job (sexta excepción de regla 9, per D5/D12); sólo se escribe desde `POST /api/v1/owner-statements/generate` cuando el actor es una persona autenticada. **Files**: `backend/app/statements/application/use_cases.py`, `backend/app/statements/application/generation.py` (nuevo: `Period`, `MonetaryAggregator`, `CurrencyFilter`), `backend/tests/statements/test_use_cases.py`, `backend/tests/statements/test_generation.py`. [R1, R2, D3, D6.1]
- [x] 4.7 `GetOwnerStatementUseCase` y `ListOwnerStatementsUseCase` con filtros `property_id`, `period_start_from`, `period_start_to`, `status`, paginación tamaño 20 fija, sobre `{items, total, page, per_page}`. **Files**: `backend/app/statements/application/use_cases.py`, `backend/tests/statements/test_use_cases.py`. [R3.1, R3.2, R3.4]
- [x] 4.8 `UpdateOwnerStatementNotesUseCase` aplica `update_notes` (D1) y revierte `updated_at` desde una instantánea si la validación rechaza. Audit: `OWNER_STATEMENT_NOTES_UPDATED` con `{"changed": true}` (`notes` no entra en `audit_logs.changes`). **Files**: `backend/app/statements/application/use_cases.py`, `backend/tests/statements/test_use_cases.py`. [R4.5, R4.6, D1]
- [x] 4.9 `TransitionOwnerStatementStatusUseCase` valida la transición contra `_TRANSITIONS`; rechazo de origen ilegal y de transicionar desde `SENT`. Audit: `OWNER_STATEMENT_STATUS_CHANGED` con diff de `status`. **Files**: `backend/app/statements/application/use_cases.py`, `backend/tests/statements/test_use_cases.py`. [R4.2, R4.3, R4.4, D1]
- [x] 4.10 `ExportOwnerStatementCsvUseCase` (csv stdlib, UTF-8 sin BOM, sin eco de valores del llamante) y `ExportOwnerStatementPdfUseCase` (delegando en `PdfStatementGenerator`). **Files**: `backend/app/statements/application/use_cases.py`, `backend/tests/statements/test_use_cases.py`. [R6.1, R6.2, D10]
- [x] 4.11 En `backend/app/statements/application/reconciliation.py` (nuevo): `ReconcileOwnerApprovalsForExpensesUseCase`. Query principal (JOIN por guardas, sin cursor externo): `SELECT oa.id, oa.status, oa.responded_by, oa.related_id FROM owner_approvals oa JOIN expenses e ON e.id = oa.related_id AND e.tenant_id = oa.tenant_id WHERE oa.related_type = 'OTHER' AND oa.responded_at IS NOT NULL AND ((oa.status = 'APPROVED' AND e.approved_by IS NULL AND e.statement_id IS NULL) OR (oa.status = 'REJECTED' AND e.statement_id IS NULL))`. Por cada fila: APPROVED → `UPDATE expenses SET approved_by = :responded_by WHERE id = :related_id AND approved_by IS NULL AND statement_id IS NULL`; REJECTED → `DELETE FROM expenses WHERE id = :related_id AND statement_id IS NULL`. Query de inconsistencias: `REJECTED + OTHER + statement_id IS NOT NULL` se reporta como `failed_reconciliation: List[Tuple[approval_id, expense_id, property_id, period_start, period_end]]`, **sin tocar filas**, logueado con `logger.error`. **Files**: `backend/app/statements/application/reconciliation.py` (nuevo), `backend/tests/statements/test_reconciliation.py`. [D4]

## 5. Scheduler — MonthlySchedule + jobs <!-- panel: PASS 2026-08-27 (architect + security + qa + tenancy + cicd + documentation + i18n — documentation finding fixed in fix-round: 'seventh' → 'sixth' in two docstrings of tasks.py to align with design.md D5/D12 numbering) -->

- [x] 5.1 En `backend/app/scheduler/schedule.py` añadir la dataclass `MonthlySchedule(day_of_month, hour, lock_ttl)`, el dict `MONTHLY_JOBS: dict[str, MonthlySchedule]` con `generate_owner_statements: MonthlySchedule(day_of_month=1, hour=2, lock_ttl=timedelta(hours=6))`, y la rama mensual en `beat_schedule()` que produce `{name}-monthly-{day:02d}{hour:02d}00-utc` con `crontab(day_of_month, hour, minute=0)`. **Files**: `backend/app/scheduler/schedule.py`. [D11]
- [x] 5.2 En `backend/app/scheduler/tasks.py` añadir `_generate_owner_statements(session, tenant_id, now)` (instancia `GenerateOwnerStatementUseCase` con sus repos + `_SqlAlchemyUnitOfWork`; **sin** `_AuditWriter` para el path del job — la sexta excepción de regla 9 nombra la acción `OWNER_STATEMENT_GENERATED` como exenta para el llamante del reloj, y `_AuditWriter` debe aceptarlo vía un flag `actor_optional=True` o no ser llamado) y `_guarded_monthly(name, work)` análogo a `_guarded_daily` (usa `MONTHLY_JOBS[name].lock_ttl`); registrar `@celery_app.task("generate_owner_statements")` que llame `_guarded_monthly`. La llamada al caso de uso pasa `actor=None` para `OWNER_STATEMENT_GENERATED` desde el job — **no** se escribe `AuditLog` para esta vía (D5/D12); el `TimelineEvent` se emite con `actor_type=TimelineActorType.SCHEDULER`. La vía manual `POST /api/v1/owner-statements/generate` sigue escribiendo `OWNER_STATEMENT_GENERATED` con su actor humano siempre. **Files**: `backend/app/scheduler/tasks.py`. [D5, D11]
- [x] 5.3 En `backend/app/scheduler/tasks.py` añadir `reconcile_owner_approvals_for_expenses` al `CADENCES` con `timedelta(minutes=5)`; añadir `_reconcile_owner_approvals_for_expenses(session, tenant_id, now)` (instancia `ReconcileOwnerApprovalsForExpensesUseCase` con sus dos repos + `SqlAlchemyUnitOfWork`) y registrar `@celery_app.task("reconcile_owner_approvals_for_expenses")` que llame `_guarded(...)`. **Files**: `backend/app/scheduler/tasks.py`. [D4]
- [x] 5.4 En `test_reconciliation.py` añadir tests parametrizados: APPROVED + Expense pendiente → setea `approved_by`; APPROVED + Expense consolidado → no-op; REJECTED + Expense pendiente → DELETE; REJECTED + Expense consolidado → entra en `failed_reconciliation` sin tocar la fila; idempotencia (dos ejecuciones seguidas del mismo barrido, la segunda no aplica cambios). **Files**: `backend/tests/statements/test_reconciliation.py`. [D4]

## 6. API surface — router, schemas, errors, dependencies <!-- panel: PASS 2026-08-27 (architect + i18n findings FIXED in fix-rounds: architect 3 — router importing infrastructure moved into use case via serializer port; per_page echoes LIST_PER_PAGE constant; dead Annotated metadata removed. i18n 4 — OwnerStatement/ExpenseNotFoundError now carry constant "Resource not found." message; PATCH description and module docstrings translated to English; "regla 11" → "rule 11" across use_cases.py. Other reviewers — CI/CD, security, QA, tenancy, documentation — all PASS in their first round) -->

- [x] 6.1 En `backend/app/statements/api/schemas.py` (nuevo) definir los DTOs Pydantic v2: `OwnerStatementResponse`, `OwnerStatementPage[T]`, `ExpenseResponse` (con `pending_owner_approval_id: UUID | None = None`), `ExpenseCreateRequest`, `ExpenseUpdateRequest`, `GenerateOwnerStatementRequest`, `OwnerStatementTransitionRequest`, `OwnerStatementNotesUpdateRequest`, `GenerationReport`. **Files**: `backend/app/statements/api/schemas.py` (nuevo). [D9, D13]
- [x] 6.2 En `backend/app/statements/api/errors.py` (nuevo) declarar `_MAPPING: tuple[tuple[type[StatementsDomainError], int, ErrorCode], ...]` exhaustivo sobre las excepciones de §1.2; función `http_error_for(exc)` y `register_statements_error_handlers(app)`. Patrón espejo de `app/pricing/api/errors.py`. **Files**: `backend/app/statements/api/errors.py` (nuevo). [D9]
- [x] 6.3 En `backend/app/statements/api/dependencies.py` (nuevo) declarar `SessionDep`, builders de los once casos de uso y de la reconciliación. Patrón espejo de `app/pricing/api/dependencies.py:62-108`. **Files**: `backend/app/statements/api/dependencies.py` (nuevo). [D9]
- [x] 6.4 En `backend/app/statements/api/router.py` (nuevo) declarar `router = APIRouter` (prefijo `API_V1_PREFIX` se añade en `main.py`) con los once endpoints de D9 (GET lista, GET detalle, POST generate, PATCH notes/status, GET export.csv, GET export.pdf para statements; GET lista, POST, PATCH, DELETE para expenses); registrar `register_statements_error_handlers(app)` en `backend/app/main.py`. **Files**: `backend/app/statements/api/router.py` (nuevo), `backend/app/main.py`. [R1-R7, D9]
- [x] 6.5 En `backend/tests/statements/test_api.py` añadir tests de integración (httpx AsyncClient) cubriendo: lista y detalle de statements (200, 404 cross-tenant), generate manual (idempotente, abort por mixed-currency, error codes), patch notes/status (D6.2 inmutabilidad, transición ilegal), expenses CRUD (con y sin umbral → `pending_owner_approval_id`). Los tests de export CSV/PDF están marcados como `pytest.importorskip` y se completarán en §7.2/§7.3. **Files**: `backend/tests/statements/test_api.py`. [R1-R7, D9, D13]

## 7. PDF y CSV infrastructure <!-- panel: PASS 2026-08-30 -->

- [x] 7.1 Añadir `fpdf2` a `backend/pyproject.toml` como dependencia nueva (pure-Python, sin nativas; cabe en `python:3.12-slim`). Regenerar `backend/uv.lock`. **Files**: `backend/pyproject.toml`, `backend/uv.lock`. [D10]
- [x] 7.2 En `backend/app/statements/infrastructure/pdf.py` (nuevo) implementar `PdfStatementGenerator` con `render(statement, reservations, expenses_by_category, tenant, property) -> bytes`: cabecera de tenant, bloque property, bloque período, tabla de reservas, tabla de gastos por categoría con subtotal, fila de totales, caja de `notes`. Sin logo; sin marca propia; importes con dos decimales y separador `,` (locale `es-ES`). **Files**: `backend/app/statements/infrastructure/pdf.py` (nuevo). [R6.3, R6.4, R6.5, D10]
- [x] 7.3 En `backend/app/statements/infrastructure/csv_export.py` (nuevo) implementar `CsvStatementExporter` que envuelve `csv.writer` con la cabecera `date,category,description,amount,currency,receipt_storage_key` y UTF-8 sin BOM. **Files**: `backend/app/statements/infrastructure/csv_export.py` (nuevo). [R6.1, R6.2, D10]
- [x] 7.4 Tests en `backend/tests/statements/test_pdf.py`: `bytes[:4] == b"%PDF"`, número de páginas esperado para una liquidación conocida, contenido textual buscable en el binario (`PyPDF2` o `pdfminer.six` ya en el lockfile). Tests en `backend/tests/statements/test_csv.py`: cabecera exacta, número de filas, codepoints no escapados. **Files**: `backend/tests/statements/test_pdf.py`, `backend/tests/statements/test_csv.py`. [R6, D10]

## 8. Steering — security.md

- [x] 8.1 En `sdd/steering/security.md` añadir la excepción de auditoría de D12 (el job mensual no escribe `AuditLog`). **Files**: `sdd/steering/security.md`. [D12] <!-- CORRECCIÓN 2026-09-01 (review H4): la redacción original de esta tarea conflaba dos listas distintas. La excepción es de **regla 9** (AuditLog), NO de regla 11 (ownership/single-source de sumideros de texto). La implementación inicial la puso por error como «Excepción 7» de la lista de regla 11 y subió su contador a «siete»; H4 la reubicó a la enumeración de regla 9 como **sexta excepción** (tras la quinta, la de `price_recommendations` del job) y devolvió el contador de regla 11 a «seis» (sus seis excepciones son formas de sumidero de texto: notification_logs, incidents guest, prosa autenticada, messages guest, price_recommendations.explanation, properties.access_notes — ninguna la aporta este change). Las dos filas de censo de §8.2 (owner_statements.notes / expenses.description) siguen usando la excepción 3 existente; el recuento de columnas/filas del censo (23/30) es correcto y no se toca. Alinea con D12 ("sexta excepción de regla 9"). -->
- [x] 8.2 En la tabla de regla 11 añadir dos filas: `owner_statements.notes` (excepción 3, escritor: statements, lo teclea un PROPERTY_MANAGER autenticado al anotar el statement, acotado a N caracteres por DDL); `expenses.description` (excepción 3, escritor: statements, lo teclea un PROPERTY_MANAGER autenticado, acotado a 500 caracteres por `String(500)`, **fuera** de `AUDITABLE_FIELDS`). **Files**: `sdd/steering/security.md`. [D12, R7.4]
- [x] 8.3 Actualizar el contador inicial de la sección ("Columnas y filas no son el mismo número…") para reflejar las dos filas nuevas. **Files**: `sdd/steering/security.md`. [D12]

## 9. OpenAPI + frontend types

- [x] 9.1 Regenerar `backend/openapi.json` con `make openapi` ( desde el worktree, no en el principal). Commiteado en el mismo PR que las rutas. **Files**: `backend/openapi.json`. [D9, documentation.md §"Endpoint nuevo"]
- [x] 9.2 Regenerar el artefacto derivado del frontend ejecutando `docker compose exec -T frontend mkdir -p /backend` + los `docker compose cp` de `sdd/project.md` §Worktree bootstrap, y luego `docker compose exec -T frontend npm run api:generate`. Commiteado junto con el openapi.json. **Files**: `frontend/lib/api/generated/openapi.d.ts`. [D9, documentation.md §"Endpoint nuevo"]

## 10. Documentación y traducciones

- [x] 10.1 En el módulo de i18n (`backend/app/core/i18n/` o donde `pricing` registre sus claves) añadir las traducciones ES/EN de los mensajes de error de las nuevas excepciones (`owner_statement_not_found`, `expense_not_found`, `expense_already_consolidated`, `named_expense_in_closed_period`, `owner_statement_invalid_transition`). **Files**: módulo i18n existente. [R7.7]
- [x] 10.2 Crear `docs/revenue-statements.md` orientado a operación: cómo se ejecuta la generación mensual, cómo se responde a aprobaciones, cómo se exporta CSV/PDF, qué hace el panel financiero del detalle de propiedad. Patrón espejo de `docs/pricing.md`. **Files**: `docs/revenue-statements.md`. [documentation.md §"`docs/`"]
- [x] 10.3 Revisar `README.md` raíz: si el change añade un módulo nuevo (sí), confirmar que la sección "Estructura" lista `backend/app/statements/{application,api,infrastructure}`; si la sección "Arrancar" menciona tests o make targets, no se ven afectados por este change (no se toca). **Files**: `README.md`. [documentation.md §"README raíz al día por change"]

## 11. Verification

> Comandos exactos del proyecto, desde `sdd/project.md`.

- [x] 11.1 Suite backend completa en el worktree: `docker compose run --rm backend uv run pytest -n 2` — todos los tests pasan, incluido el parametrizado de inmutabilidad D6.2, el de reconciliación D4, el de idempotencia R1.3/R2.3, y el cross-feature de `dashboard-api` (pendientes que se vacían al consolidar). **Files**: ninguno (verificación). [all]
- [x] 11.2 Lint estático del backend: `docker compose run --rm backend uv run ruff check backend/` y `docker compose run --rm backend uv run pyright backend/` (si pyright está instalado; `sdd/project.md` §LSPs). **Files**: ninguno (verificación). [all] <!-- 2026-09-01: tooling Pyright canónico reproducible ejecutado (uv sync --frozen + `uv run pyright .` en /app==backend), pyright 1.1.411 arranca y completa análisis (exit 1 = findings, no fallo de arranque). Criterio de la capacidad (spec backend-tooling): tooling reproducible que llega a ejecutar el análisis; Pyright NO es gate de cero findings, y su corrección es cambio aparte. `ruff` no es herramienta declarada del proyecto (ausente de pyproject/uv.lock/config); baseline previo vía uvx = 0 findings en el diff del feature. Evidencia detallada (685 global; 2 preexistentes vs 19 introducidos; sin suppressions; §11.1 9225 passed/41 skipped) en BLOCKED.md. -->

- [x] 11.3 Comprobación manual del flujo end-to-end en el stack del worktree:
  1. `make bootstrap && make seed-demo`.
  2. `make up` (con `PORT_OFFSET` si la sesión ya tiene el principal corriendo).
  3. Generar manualmente un statement para una propiedad vía `POST /api/v1/owner-statements/generate` con un período del seed; comprobar 201, `status=DRAFT`, 11 importes.
  4. PATCH al `notes`; comprobar 200 y `TimelineEvent OWNER_STATEMENT_NOTES_UPDATED` en el detalle.
  5. Crear un `Expense` con `amount > 100.00` (umbral por defecto) vía `POST /api/v1/expenses`; comprobar 201 y `pending_owner_approval_id` no nulo; aprobar vía `POST /api/v1/owner-approvals/{id}/respond`; esperar hasta 5 minutos y verificar `approved_by` actualizado.
  6. Intentar `PATCH /api/v1/expenses/{id}` con `amount` nuevo sobre el Expense consolidado; comprobar 409.
  7. Descargar `GET /api/v1/owner-statements/{id}/export.csv` y `export.pdf`; abrir ambos.
  - **Files**: ninguno (verificación). [D9, D10, D13]
- [ ] 11.4 (deferred-to-PR_OPEN) Tras `/sdd:ship` y la apertura del PR, verificar que los workflows `compose-ports`, `backend-tests` y `api-contract` siguen verdes mediante `gh pr checks`. No es una condición ejecutable durante `/sdd:run` antes de existir un PR. **Files**: ninguno (verificación). [D9, D10]
