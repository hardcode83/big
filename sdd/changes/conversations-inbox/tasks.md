# Tasks: conversations-inbox

## 1. Shell, navigation & locales

- [ ] 1.1 Registrar `conversation-detail` en `frontend/features/shell/navigation/route-registry.ts` con `pattern: "/conversations/[id]"`, `match: "exact"`, **sin** `href` y **sin** `navigationGroup`, replicando la forma de `property-detail`, `reservation-detail` e `incident-detail` — `breadcrumbs: crumbs("conversations", "conversation-detail")`. [R1.2, D6]
- [ ] 1.2 Extender `PRD_24_SURFACES` en `frontend/features/shell/navigation/route-registry.test.ts` con `/conversations/[id]`, junto a `/incidents/[id]`, `/properties/[id]` y `/reservations/[id]`. El test "covers exactly the PRD §24 surfaces" debe seguir verde. [R1.5, D6]
- [ ] 1.3 Crear `frontend/locales/{es,en}/conversations.json` con las seis secciones declaradas en D2: `status` (4), `escalationStatus` (4), `channel` (6), `senderType` (5), `fields` (etiquetas de columnas + cabeceras + copy del formulario + nota del UUID del sender + **`prevPage` y `nextPage`**), `thread` (copy específico del hilo: «Mensajes», «Sin mensajes», «Responder», «Enviar», «Cancelar», «Caracteres restantes», placeholders). **No** se reutilizan claves de `incidents.json`. [R1.3, R5.1-5.5, D2]
- [ ] 1.4 Añadir las dos claves `routes.conversation-detail.{title,description}` a `frontend/locales/{es,en}/navigation.json`, sin hardcodear la cadena en ningún componente. [R1.3]
- [ ] 1.5 Registrar el namespace `conversations` en `frontend/lib/i18n/resources.ts` (alta en `NAMESPACES`, import del JSON ES/EN, alta en `resources.es` y `resources.en`). [D2]

## 2. Data layer

- [ ] 2.1 Crear `frontend/features/conversations/data/dto.ts` con: re-exports de `ConversationStatus`, `ConversationEscalationStatus`, `ConversationChannel` y `MessageSenderType` desde `@/lib/api/generated/openapi`; `ConversationSummaryDto` (6 campos: `id`, `channel`, `status`, `escalationStatus`, `lastMessageAt`, `createdAt` — sin `propertyId`, D5); `ConversationDetailDto` (los 13 campos de `ConversationResponse` en camelCase); `MessageDto` (los 11 campos de `MessageResponse` en camelCase, **descartando `metadata` en el mapper**, DTO §Data & interfaces); `ConversationList`, `MessageList`, `ConversationFilters`, `ConversationPagination`. [R2.1, R3.1, R6.1, D3]
- [ ] 2.2 Crear `frontend/features/conversations/data/http/http-conversations-source.ts` con la clase `HttpConversationsSource` que toma un `ApiClient` en el constructor y expone: `listConversations(tenantId, filters)` — la `filters` de entrada usa **claves camelCase** (`status`, `escalationStatus`, `page`, `perPage`) según `ConversationFilters`; el método traduce esa frontera a **claves snake_case** que es lo que acepta el backend (`status`, `escalation_status`, `page`, `per_page`), omitiendo `property_id` (D4) y omitiendo cualquier clave con valor `undefined` para que el payload del wire coincida exactamente con lo que el test asserta; `getConversation(tenantId, id)`, `listMessages(tenantId, id, page)`, `replyToConversation(tenantId, id, { content })` (body **sin** `sender_type`, D9), y los tres mappers `mapConversationSummary`, `mapConversationDetail`, `mapMessage` (con field-enumeration explícito, D3). [R2.1, R3.1, R4.2, D3, D4, D9]
- [ ] 2.3 Crear `frontend/features/conversations/data/http/http-conversations-source.test.ts` con tests de los cuatro métodos: rutas y query params, mappers (sobre `components["schemas"]["*"]`), serialización del body sin `sender_type`. [R6.3, D3, D9]
- [ ] 2.4 Crear `frontend/features/conversations/data/index.ts` con el composition point: importar `createAuthenticatedClients` de `@/lib/api/authenticated-client`, instanciar el `HttpConversationsSource` con el `apiClient` autenticado, y exportar `getConversationsDataSource()` que devuelve ese singleton. **Sin** interfaz, **sin** `MockConversationsSource`. [D1]

## 3. Error mapping & query keys

- [ ] 3.1 Crear `frontend/features/conversations/lib/error-mapping.ts` con `mapConversationsError<TData>` (discriminated union `loading` / `forbidden` / `not-found` / `validation` / `error` / `ok` con `data`), replicando `features/incidents/lib/error-mapping.ts`. **Nunca** lee, mapea ni expone `message`, `details`, `code` del envelope PRD §23; solo estado para mostrar copy localizado. [R3.7, R6.4]
- [ ] 3.2 Crear `frontend/features/conversations/hooks/query-keys.ts` con `conversationsKeys` usando `tenantScopedKey` del shell: `list(tenantId, filters)` (recibe objeto de filtros normalizado, **sin** `JSON.stringify`), `detail(tenantId, id)`, `messages(tenantId, id)`, `listPrefix(tenantId)` y `messagesPrefix(tenantId, id)` para invalidación por prefijo. [R2.7, R6.6, D4, D9]

## 4. Hooks

- [ ] 4.1 Crear `frontend/features/conversations/hooks/use-conversations.ts` con `useConversations(filters)` (cachea `listConversations`, `retry: retryPolicy`), `useConversation(id)` (cachea `getConversation`, `retry: retryPolicy`) y `useConversationMessages(id, page)` (cachea `listMessages`, `retry: retryPolicy`). Cada uno extrae `tenantId` del `useAuth()` y falla si el contexto no está autenticado (mismo patrón que `use-incidents.ts:31-37`). [R2.1, R3.1, R6.1]
- [ ] 4.2 Crear `frontend/features/conversations/hooks/use-conversations.test.tsx` con tests de los tres hooks: wiring con el `getConversationsDataSource()`, paso de `tenantId`, `retry: retryPolicy`. Mockear solo el `HttpConversationsSource`, **no** el dominio. [R6.3]
- [ ] 4.3 Crear `frontend/features/conversations/hooks/use-reply-to-conversation.ts` con `useReplyToConversation(conversationId)`: `useMutation` con `mutationFn` que llama `replyToConversation` (body **sin** `sender_type`), `retry: false`, `onSettled` invalida `conversationsKeys.listPrefix(tenantId)`, `conversationsKeys.detail(tenantId, conversationId)` y `conversationsKeys.messagesPrefix(tenantId, conversationId)`. **No** almacena ni devuelve copia del draft — el draft pertenece exclusivamente al formulario (D9). [R4.2, R4.4, D9]
- [ ] 4.4 Crear `frontend/features/conversations/hooks/use-reply-to-conversation.test.tsx` con tests: `onSettled` invalida los tres keys (prefijo, no enumeración); body serializado **no** incluye `sender_type`; `retry: false` propagado. [R4.2, R4.4, D9]

## 5. List view & filters

- [ ] 5.1 Crear `frontend/features/conversations/components/list/conversations-view.tsx`: client view con `useTranslation(["conversations", "states", "navigation"])`, consume `useConversations(filters)`, renderiza tabla con cinco columnas (canal / estado / escalación / último mensaje / creada), enlaces a `/conversations/[id]`, paginación derivada `lastPage = max(1, ceil(total/perPage))`, estados loading / empty / error mapeados por `mapConversationsError`. Las etiquetas de los enums usan `t(\`conversations:status.${row.status}\`)` etc. [R2.1, R2.4, R2.5, R2.6, R2.8, D5]
- [ ] 5.2 Crear `frontend/features/conversations/components/list/conversations-view.test.tsx` con tests de render: estado de carga, render con datos, estado vacío (items vacío → muestra copy vacío localizado), estado de error (`refetch` dispara nueva consulta). [R2.4]
- [ ] 5.3 Crear `frontend/features/conversations/components/list/conversations-filters.tsx`: filtros controlled (`status` y `escalationStatus`) con control tipado; `buildNext` resetea `page = 1` al cambiar un filtro; orden estable de claves en el objeto (`status`, `escalationStatus`, `page`, `perPage`). **No** expone `propertyId` (D4). [R2.6, D4]
- [ ] 5.4 Crear `frontend/features/conversations/components/list/conversations-filters.test.tsx` con tests: cambio de filtro resetea `page` a `1`; el objeto de filtros se emite con orden estable. [R2.6, D4]

## 6. Thread view

- [ ] 6.1 Crear `frontend/features/conversations/components/thread/conversation-thread-view.tsx`: client view que consume `useConversation(id)` y `useConversationMessages(id, page)`, renderiza cabecera con canal + estado + escalación (badge replicado del de la lista), `ConversationThreadMessages`, `ConversationReplyForm`. Paginación de mensajes con `lastPage` derivada en cliente por la misma fórmula que la lista. [R3.1, R3.3, R3.8, D5, D7]
- [ ] 6.2 Crear `frontend/features/conversations/components/thread/conversation-thread-view.test.tsx` con tests: estado de carga, render con datos, `404` muestra estado localizado de «no encontrado» distinto del error genérico, `401`/`403`/`5xx` muestran error genérico. [R3.7]
- [ ] 6.3 Crear `frontend/features/conversations/components/thread/conversation-thread-messages.tsx`: bloque presentacional que itera `messages.items`, pinta cada `content` como `{value}` directo con `whitespace-pre-wrap` y `max-w-prose` (sin `dangerouslySetInnerHTML`, sin `react-markdown`, sin `parseHtml`), etiqueta de rol localizada por `sender_type` (`Tú` / `Huésped` / `IA` / `Sistema` / `Propietario`), `intent` visible cuando `sender_type = AI`. [R3.4, R3.5, R5.4, D7]
- [ ] 6.4 Crear `frontend/features/conversations/components/thread/conversation-thread-messages.test.tsx` con tests: contenido con `<script>alert(1)</script>` se renderiza como texto literal (la alerta no se ejecuta); etiqueta de rol correcta por cada `sender_type`; `intent` visible solo cuando AI. [R3.4, R3.5, D7]
- [ ] 6.5 Crear `frontend/features/conversations/components/thread/conversation-thread-sender-meta.tsx`: sección secundaria etiquetada `thread.senderUserIdSection` con `sender_user_id` (UUID pelado) y la nota localizada única `thread.senderUserIdNote` (D8). La sección se oculta cuando `sender_user_id` es `null`. **Sin** tooltip de copia, **sin** botón «copiar UUID», **sin** elemento de UI específico para su valor. [R3.5, R5.6, D8]
- [ ] 6.6 Crear `frontend/features/conversations/components/thread/conversation-thread-sender-meta.test.tsx` con tests: la nota localizada aparece una sola vez; la sección se oculta cuando `sender_user_id` es `null`. [R3.5, D8]

## 7. Reply form

- [ ] 7.1 Crear `frontend/features/conversations/components/thread/conversation-reply-form.tsx`: formulario con `useState` local para el draft, contador visible de caracteres como información al operador (basado en `MAX_MESSAGE_CONTENT_LENGTH = 4000` declarado en `messaging-ai.md` R3 y en el esquema Pydantic `CreateMessageRequest`, `openapi.json:1253-1257`), botón «Enviar» localizado, deshabilitado **únicamente** mientras la mutación vuela, error localizado si falla, **preserva el draft en error** y **lo limpia en éxito**. La validación contractual (`length(content) > 4000` ⇒ backend `422`) **no** se aplica como bloqueo previo del UI: el botón permanece habilitado para contenido válido aunque el contador esté cerca del límite — el rechazo de longitud vive en el backend y se muestra como error localizado al volver de la mutación. El draft vive exclusivamente en este `useState`; `useReplyToConversation` no lo conoce. [R4.1, R4.2, R4.3, R4.4, D9]
- [ ] 7.2 Crear `frontend/features/conversations/components/thread/conversation-reply-form.test.tsx` con tests: el contador refleja `length(draft)`; el botón **no** se deshabilita por acercarse al límite (solo por mutación en vuelo); en éxito el draft se limpia; en error el draft se preserva y se muestra el copy de error (incluye el caso `length(draft) > 4000` → backend `422` → draft preservado + copy de validación localizado). [R4.1, R4.3, R4.4, D9]

## 8. Barrel & pages

- [ ] 8.1 Crear `frontend/features/conversations/index.ts` (barrel) exportando `ConversationsInboxView`, `ConversationThreadView`, `useConversations`, `useConversation`, `useConversationMessages`, `useReplyToConversation`, `getConversationsDataSource`, los tipos `ConversationSummaryDto`, `ConversationDetailDto`, `MessageDto`, `ConversationFilters`, `ConversationPagination`, `ConversationStatus`, `ConversationEscalationStatus`, `ConversationChannel`, `MessageSenderType`. [D1, D3]
- [ ] 8.2 Sustituir `frontend/app/(workspace)/conversations/page.tsx`: el archivo renderiza `<ConversationsInboxView />` en lugar del `RoutePlaceholder routeId="conversations"`. Conservar el `routeMetadata("conversations")`. [R1.1]
- [ ] 8.3 Crear `frontend/app/(workspace)/conversations/[id]/page.tsx`: renderiza `<ConversationThreadView conversationId={id} />`, con `routeMetadata("conversation-detail")`. Lee `id` de `params` (Next.js App Router, type `Promise<{id: string}>`). [R1.4, R1.6]

## 9. Verification

- [ ] 9.1 Backend del worktree levantado y suite BE verde (no se toca backend, pero la sesión autenticada del FE la consume; precedent `incidents-web` R5.7). Desde la raíz del worktree: `make up` y `docker compose exec backend uv run pytest`. [R6.3]
- [ ] 9.2 Frontend — typecheck: `cd frontend && npm run typecheck` (falla en rojo si un mapper olvida un campo, si `status` o `escalationStatus` se pasan como `string`, si el body de `replyToConversation` se serializa con `sender_type`). [R6.6]
- [ ] 9.3 Frontend — lint: `cd frontend && npm run lint` (sin warnings nuevos en los archivos del change). [R6.3]
- [ ] 9.4 Frontend — tests: `cd frontend && npm test` (suite completa verde; cobertura obligatoria: lista con filtros + paginación, hilo con `<script>` como texto plano, formulario con draft preservado/limpiado, hook de mutación con `onSettled` invalidando los tres keys y body sin `sender_type`). [R6.3, R6.7]
- [ ] 9.5 Contrato OpenAPI sin regenerar: `cd frontend && npm run api:check` pasa en verde (los tipos `ConversationResponse`, `MessageResponse`, `ConversationPageResponse`, `CreateMessageRequest` y los enums ya están en `frontend/lib/api/generated/openapi.d.ts:1091-1155, 1889-1929` — este change **no** los modifica). [R6.3]
- [ ] 9.6 Manual en navegador (opcional, si el revisor necesita ver la UI): desde el worktree, `make up PORT_OFFSET=<n>` para evitar colisión con el principal. Crear una conversación vía API o transcribir un mensaje desde el panel, abrir `/conversations`, abrir `/conversations/{id}`, responder y verificar que el badge de escalación y `lastMessageAt` se refrescan. **No** requiere conversación previa preexistente en el seed (limitación de demo documentada en el `Why` del proposal). [D1, R2.4]