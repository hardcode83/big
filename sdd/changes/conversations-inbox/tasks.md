# Tasks: conversations-inbox

Change **solo de frontend**: no toca backend, `backend/openapi.json` ni
`frontend/lib/api/generated/openapi.d.ts`, así que no hay tareas de `make openapi`
ni de `npm run api:generate` (design, *Changes by area*). Todas las rutas de fichero
son relativas a la raíz del repo.

## 1. Frontera de datos de la feature <!-- panel: PASS 2026-08-19 -->

- [x] 1.1 Crear `frontend/features/conversations/data/dto.ts` con `ConversationPage<T>` (`items`, `page`, `perPage`, `total`, `totalPages`), `ConversationSummary`, `ConversationDetail`, `ThreadMessage`, `PropertyLabel`, `InboxFilters` y `NewMessage`; `confidenceScore` se declara `string | null` y `ThreadMessage` expone `deliveryStatus` y `escalationReason` como dos campos nombrados, nunca el `metadata` completo (D3, D8, D14) [R7.2, R3.4].
- [x] 1.2 Crear `frontend/features/conversations/data/conversations-source.ts` con la interfaz `ConversationsDataSource` de exactamente siete métodos —`listConversations`, `getConversation`, `listMessages`, `createMessage`, `escalate`, `resolve`, `listPropertyLabels`— todos con `tenantId` explícito en la firma; `POST /api/v1/conversations` (crear) no aparece (D2) [R7.1].
- [x] 1.3 Implementar `frontend/features/conversations/data/http/http-conversations-source.ts` sobre el `ApiClient` compartido, con mapeo explícito `snake_case` → `camelCase` de los cinco endpoints de mensajería y de `GET /api/v1/properties`; `method` explícito y respuesta estrechada en `/messages` (tiene `get` y `post`), como ya hace `http-guest-portal-source.ts` (D2) [R7.2].
- [x] 1.4 Cubrir con test el mapeo de la lista y del hilo: `totalPages` derivado como `Math.max(1, Math.ceil(total / perPage))` desde el sobre `{items, page, per_page, total}` de mensajería, `data`/`total_pages` leídos aparte en `listPropertyLabels`, `null` preservados (`property_id`, `guest_id`, `last_message_at`) y cadenas ISO-8601 sin reformatear (D3) [R7.2, R1.3].
- [x] 1.5 Cubrir con test que `confidence_score` se copia como cadena decimal sin `Number()` ni redondeo en la frontera, y que de `metadata` solo se extraen `delivery_status` y `escalation_reason` (D8, D14) [R3.4].
- [x] 1.6 Cubrir con test la serialización de query: `listConversations` omite `status`, `escalation_status` y `property_id` cuando no están seleccionados, envía `page`/`per_page`, y `listPropertyLabels` va con `per_page: 100` en una sola petición [R2.1, R1.7, R1.6].
- [x] 1.7 Cubrir con test los dos cuerpos de `createMessage`: responder envía `{content}` **sin** `sender_type`, y transcribir envía `{content, sender_type: "GUEST"}`; `escalate` y `resolve` van sin body [R4.1, R4.2, R5.1].
- [x] 1.8 Crear `frontend/features/conversations/data/index.ts` como único punto de composición (`createAuthenticatedClients({ apiBaseUrl: "", onSessionExpired: notifySessionExpired })` + `HttpConversationsSource`) y re-exportar la interfaz y los DTOs (D2) [R7.1].
- [x] 1.9 Crear `frontend/features/conversations/data/boundary.test.ts` al estilo del de `features/dashboard`: ningún componente, hook, store ni `lib/` de la feature importa fixtures de test ni una implementación que no sea la HTTP; sin `data/mock/` en el árbol (D4) [R7.3].

## 2. Lógica pura de la feature <!-- panel: PASS 2026-08-19 -->

- [x] 2.1 Crear `frontend/features/conversations/lib/labels.ts` con un `Record<Literal, claveI18n>` exhaustivo por cada enum cerrado del contrato generado —`ConversationStatus` (4), `ConversationEscalationStatus` (4), `ConversationChannel` (6), `MessageSenderType` (5)— sin interpolar claves; test que comprueba que las cuatro tablas cubren la unión completa, de modo que un valor nuevo en el backend deje de compilar (D7) [R2.2, R3.3].
- [x] 2.2 Crear `frontend/features/conversations/lib/transitions.ts` con la tabla de D10 (escalar: `escalationStatus === "NONE"` **y** `status === "OPEN"`; resolver: `status ∈ {OPEN, ESCALATED}`; responder/transcribir: `status !== "CLOSED"`) y test sobre el producto de los dos ejes, incluidos los casos que **no** se ofrecen —`RESOLVED` + `NONE` no es escalable— para no prometer un 409 (D10) [R5.2].
- [x] 2.3 Crear `frontend/features/conversations/lib/permissions.ts` con `canManageConversations(role)` como `Record<UserRole, boolean>` sobre el literal generado, `true` solo en `PROPERTY_MANAGER`; test que recorre todos los roles y comprueba que no hay ningún RBAC ni scoping de tenant implementado en el cliente (D12) [R6.1, R6.2].
- [x] 2.4 Crear `frontend/features/conversations/lib/format.ts` con `formatAge(iso, locale)` sobre `Intl.RelativeTimeFormat` (unidad más gruesa que aplique), `formatDateTime` y el formateo de porcentaje con `Intl.NumberFormat(locale, { style: "percent", maximumFractionDigits: 0 })` sobre `Number(value)`; test con `vi.setSystemTime` para fijar el reloj, incluyendo `confidenceScore` `null` (no se pinta cifra) y `last_message_at` `null` (texto localizado de «sin mensajes», nunca fecha inventada ni cadena vacía) (D8, D9) [R1.2, R1.3, R3.4].
- [x] 2.5 Crear `frontend/features/conversations/lib/errors.ts` que traduce `ApiError.status` (403 / 404 / 409 / 422) a claves de i18n con fallback genérico; test que comprueba que `ApiError.message` no se usa nunca como copia visible y que cada status mapea a su clave (D18) [R1.4, R3.6, R4.5, R5.2, R6.3].

## 3. Catálogos i18n <!-- panel: PASS 2026-08-19 -->

- [x] 3.1 Crear `frontend/locales/es/conversations.json` y `frontend/locales/en/conversations.json` con todas las claves de esta superficie (etiquetas de los cuatro enums, cabeceras de fila, estados de carga/vacío/error/sin-acceso/no-encontrada, nota de `CLOSED`, avisos de canal mudo, copias del compositor y de la transcripción, motivos de acción deshabilitada, textos del diálogo de confirmación, nombres accesibles y marca de «no entregado»), y registrar el namespace `conversations` en `NAMESPACES` y en `resources` de ambos locales en `frontend/lib/i18n/resources.ts`; el test de paridad de `lib/i18n/catalog-parity.test.ts` cubre la igualdad de claves al añadir el namespace [R7.4]. **Enmendado durante `/sdd:run`**: tres grupos que esta enumeración pedía se borraron al no tener consumidor —`inbox.columns.*` (la fila es una lista de botones, no una tabla), `message.escalationReason` (D14 pide el campo en el DTO y la marca de entrega, no que se pinte el motivo) y `dialog.close` (ningún diálogo lleva botón de cerrar propio)—, y se añadió `transcribe.field` para que la etiqueta del campo no colisione con el título del diálogo.

## 4. Estado de UI, claves de query y hooks <!-- panel: PASS 2026-08-19 -->

- [x] 4.1 Crear `frontend/features/conversations/state/use-inbox-filters-store.ts` (Zustand) con `{ status, escalationStatus, propertyId, page }` y nada más; cambiar cualquier filtro resetea `page` a 1. Test que comprueba el reset y que el store no guarda conversaciones ni mensajes (D6) [R2.5].
- [x] 4.2 Crear `frontend/features/conversations/hooks/query-keys.ts` sobre `tenantScopedKey` con `list(tenantId, filters, page)`, `detail(tenantId, id)`, `messages(tenantId, id, page)` y `propertyLabels(tenantId)`; test que comprueba el prefijo `['tenant', tenantId, …]` y que dos combinaciones de filtros/página producen claves distintas (D16) [R2.4].
- [x] 4.3 Crear `frontend/features/conversations/hooks/use-conversations.ts` con `useConversationList`, `useConversation`, `useThread` y `usePropertyLabels`, todas resolviendo el data source por el punto de composición y usando el `retryPolicy` compartido (sin reintento en 4xx); `usePropertyLabels` es una sola query cacheada para toda la lista. Test de los estados de carga y de la ausencia de reintento en 403/404 [R1.1, R1.4, R1.7, R3.2, R3.5, R3.6].
- [x] 4.4 Crear `frontend/features/conversations/hooks/use-conversation-actions.ts` con `useSendReply`, `useTranscribeGuestMessage`, `useEscalate` y `useResolve`, todas con `retry: false`, invalidando por prefijo la lista y los mensajes del hilo y por clave exacta el detalle tras cada éxito; sin actualización optimista. Test que comprueba las invalidaciones y que no se llama a `queryClient.clear()` (D16) [R4.4, R5.1, R5.3].

## 5. Lista y filtros de la bandeja <!-- panel: PASS 2026-08-19 -->

- [x] 5.1 Crear `frontend/features/conversations/components/inbox-row.tsx`: código interno de la propiedad, canal, `status`, `escalation_status`, idioma y antigüedad de `last_message_at` en un `<time dateTime={iso}>` con la fecha absoluta en `title`; si la `property_id` no aparece en las etiquetas cacheadas, marcador localizado sin romper la fila. Test de la fila completa, de la fila con `last_message_at` null y de la propiedad no resuelta (D9) [R1.2, R1.3, R1.7].
- [x] 5.2 Crear `frontend/features/conversations/components/inbox-list.tsx` renderizando una fila por conversación **en el orden que devuelve el backend, sin reordenar en el cliente**, con los estados compartidos de `components/states/`: carga (`aria-busy`), error (`role="alert"` con reintento que re-ejecuta la consulta y sin detalle crudo) y vacío visualmente distinto de ambos. Test que comprueba el orden preservado y los tres estados [R1.1, R1.4, R1.5].
- [x] 5.3 Crear `frontend/features/conversations/components/page-nav.tsx` usando `page`/`per_page` y los metadatos `total`/`page`/`perPage`/`totalPages`, sin acumular páginas en el cliente; reutilizable por la lista y por el hilo. Test que comprueba que cambiar de página sustituye el contenido en vez de concatenarlo [R1.6, R3.5].
- [x] 5.4 Crear `frontend/features/conversations/components/inbox-filters.tsx` con selectores de `status`, `escalationStatus` y `propertyId` alimentados por los mapas exhaustivos de 2.1 y por `usePropertyLabels`; el filtro `status = CLOSED` lleva la nota localizada de que hoy nada produce ese estado, y `escalation_status = HUMAN_HANDLING` **no** la lleva. Test que comprueba la presencia de la nota en `CLOSED`, su ausencia en `HUMAN_HANDLING` y que los filtros no seleccionados no se envían (D7) [R2.1, R2.2, R2.3].

## 6. Hilo de la conversación <!-- panel: PASS 2026-08-19 -->

- [x] 6.1 Crear `frontend/features/conversations/components/message-bubble.tsx`: `sender_type` distinguido de forma exhaustiva sobre los cinco valores con etiqueta localizada y alineación distinta para el huésped y para nosotros; `ai_generated` marca el mensaje como generado por IA con su `intent` y su `confidence_score` formateado en el componente (omitido si es `null`, nunca `null` ni `NaN`); `delivery_status = FAILED` pinta la marca localizada de «no entregado». Test de los cinco remitentes, de `confidenceScore` null y de la marca de entrega fallida (D7, D8, D14) [R3.3, R3.4].
- [x] 6.2 En el mismo `message-bubble.tsx`, renderizar `content` dentro de un `<p className="whitespace-pre-wrap">` con el escapado por defecto de React: sin `dangerouslySetInnerHTML`, sin markdown, sin autolinkado y sin recortes; `intent` e idioma se pintan igual, como dato. Test de regresión que comprueba que un contenido con HTML y con una URL se muestra literal y no genera nodos activos (D15, `steering/security.md` regla 11 excepción 4) [R3.3].
- [x] 6.3 Crear `frontend/features/conversations/components/conversation-thread.tsx` renderizando los mensajes en el orden cronológico ascendente que devuelve el backend sin reordenar, con el `page-nav.tsx` de 5.3 para cargar las páginas anteriores conservando el orden. Test del orden preservado a través de dos páginas [R3.2, R3.5].
- [x] 6.4 Crear `frontend/features/conversations/components/thread-header.tsx` con el idioma detectado y el canal, y el aviso permanente localizado cuando el canal es `AIRBNB_MSG` o `BOOKING_MSG` redactado como «se guarda, no se envía» —no como «fallará»—, más la nota de que el hilo no es tiempo real y se refresca al operar o al recargar. Test de la presencia del aviso en los dos canales mudos y de su ausencia en los demás (D13) [R3.7].
- [x] 6.5 Implementar los estados de lectura no reintentables: 404 en `GET /api/v1/conversations/{id}` → estado localizado de «no encontrada» dentro del chrome de la shell; 403 en `GET /api/v1/conversations` → estado localizado de «sin acceso» **sin** botón de reintento; el resto de fallos al `ErrorState` compartido con reintento. Test de los tres caminos (D17, D18) [R3.6, R1.4].

## 7. Responder, transcribir, escalar y resolver

- [x] 7.1 Crear `frontend/components/ui/confirm-dialog.tsx` sobre `@radix-ui/react-dialog` (ya es dependencia vía `components/ui/sheet.tsx`), como primitiva genérica con foco gestionado y textos por props; sin dependencias nuevas. Test de apertura, confirmación, cancelación y gestión de foco (D20) [R5.4].
- [x] 7.2 Crear `frontend/features/conversations/components/reply-composer.tsx`: envía por `useSendReply` sin `sender_type`, limita el contenido a 1–4000 caracteres con contador visible, bloquea el envío en vacío antes de que el backend responda 422, se deshabilita mientras el envío está en vuelo e impide un segundo envío del mismo texto. Test del límite, del vacío, del bloqueo en vuelo y de que un fallo **conserva el texto escrito** sin marcar el mensaje como enviado [R4.1, R4.3, R4.5].
- [x] 7.3 Crear `frontend/features/conversations/components/transcribe-dialog.tsx` como acción separada y etiquetada sin ambigüedad que llama al mismo endpoint con `sender_type: "GUEST"`, advirtiendo en su texto localizado de que dispara la respuesta automática de la IA y puede escalar la conversación, y —si el canal es mudo— con su aviso propio de que la transcripción puede perderse entera; su estado de error dice explícitamente que **no se ha guardado nada**. Test de que el compositor de respuesta no puede enviar un mensaje de huésped ni por defecto, y de las dos copias de aviso (D13) [R4.2].
- [x] 7.4 Crear `frontend/features/conversations/components/thread-actions.tsx` con escalar y resolver: las puertas salen de `lib/transitions.ts` (2.2), la acción no accionable se renderiza `disabled` con `aria-describedby` apuntando al motivo localizado en vez de ocultarse, resolver exige confirmación por el diálogo de 7.1, y un 409 muestra el error localizado y refresca el estado real sin dejar la UI mostrando el resultado que no ocurrió. Test de las puertas por combinación de ejes, del motivo accesible, de la confirmación obligatoria y del 409 (D10, D11, D18) [R5.1, R5.2, R5.3, R5.4].
- [x] 7.5 Aplicar la puerta de rol de 2.3 en el hilo: sin `MANAGE_CONVERSATIONS` (todos los roles salvo `PROPERTY_MANAGER`) se ocultan compositor, transcripción, escalar y resolver, y lista e hilo siguen legibles; un 403 sobre una acción que la UI mostró produce el error localizado de permisos y nunca un reintento de red. Test con rol `TENANT_OWNER` (lectura completa, cero controles de gestión) y con `PROPERTY_MANAGER` (controles presentes), más el camino del 403 (D12, D18) [R6.1, R6.2, R6.3].

## 8. Vista, ruta y responsive

- [x] 8.1 Crear `frontend/features/conversations/components/conversations-view.tsx` como Client Component maestro-detalle en una sola ruta: lee la conversación seleccionada de `?conversation=<uuid>` con `useSearchParams()` y la escribe con `router.replace(..., { scroll: false })`, de modo que el hilo sea enlazable y recargable. Test de selección desde la lista, de recarga con el parámetro presente y de parámetro ausente (D5) [R3.1].
- [x] 8.2 Implementar el colapso a una columna en `conversations-view.tsx`: dos paneles en `lg:` y arriba; por debajo se muestra la lista cuando no hay selección y el hilo cuando la hay, con un control de «volver» que borra el parámetro de la URL. La decisión depende del estado, no del viewport: solo clases de Tailwind, sin `matchMedia` ni `useMediaQuery`. Verificar además navegación por teclado con foco visible y nombres accesibles localizados en todos los controles, y ausencia de desbordamiento horizontal (D19) [R7.6].
- [x] 8.3 Crear `frontend/features/conversations/index.ts` exportando solo `ConversationsView`, y sustituir el `RoutePlaceholder` de `frontend/app/(workspace)/conversations/page.tsx` por `<Suspense fallback={<LoadingState …/>}><ConversationsView /></Suspense>` conservando `generateMetadata` con `routeMetadata("conversations")` intacto; la frontera de suspense acota al subárbol el abandono de prerender que provoca `useSearchParams()` (D1, D5) [R7.5]. **Corregido en `/sdd:review` (2026-08-21)**: esta tarea decía que la frontera «es lo que permite que `next build` pase», y es falso —`getServerT()` lee `cookies()` en cada página, así que la ruta ya es dinámica y el build compila igual sin frontera, comprobado quitándola—; quien guarda su retirada es `error-architecture.test.ts`. **Consecuencia no prevista en el plan, resuelta durante `/sdd:run`**: `frontend/app/error-architecture.test.ts` prohibía que *cualquier* `page.tsx` importara `Suspense` o `LoadingState` («no ceremonial loading/Suspense on placeholders»). La regla se escribió para placeholders y esta frontera no es ceremonia sino requisito del framework, así que el test pasa a llevar una lista de exenciones con su motivo —hoy solo esta ruta— más las aserciones que comprueban que cada página exenta usa de verdad la frontera. **Reforzadas en `/sdd:review` (2026-08-21)**: la comprobación original solo hacía `toContain("Suspense")` sobre el fichero, que pasaba incluso con la frontera inerte, mal colocada o solo mencionada en un comentario; ahora se ignoran los comentarios y se exige que el `<Suspense>` tenga `fallback` y **envuelva** al componente cliente, y que ese componente siga llamando a `useSearchParams()`, de modo que la exención no sobreviva al motivo por el que se concedió.
- [x] 8.4 Añadir `"(workspace)/conversations/page.tsx": "conversations"` a `REAL_PAGE_ROUTE_IDS` en `frontend/app/route-coverage.test.ts`, porque la página deja de tener el prop `routeId="…"` del que ese test deriva la cobertura de rutas [R7.5].

## 9. Verification

- [x] 9.1 Ejecutar la suite frontend completa: `cd frontend && npm test` — en verde, incluidos `app/route-coverage.test.ts`, `lib/i18n/catalog-parity.test.ts` y el `boundary.test.ts` de la feature [R1, R2, R3, R4, R5, R6, R7]. **2026-08-21: 635/636 tests en verde.** Los dos ficheros que fallan ya fallaban antes de este change y por el entorno, no por el código: `features/provenance/workflow-contract.test.ts` y `lib/config/build-identity-contract.test.ts` leen `../.github/workflows/deploy-dev.yml` y `../backend/app/provenance/provenance-contract.json`, y el contenedor `frontend` **solo monta `./frontend`** (`sdd/project.md`, «Worktree bootstrap»). Los dos ficheros existen en el árbol; el contenedor no los ve. Nada de este change los toca.
- [x] 9.2 Ejecutar `cd frontend && npm run typecheck`, `cd frontend && npm run lint` y `cd frontend && npm run build` — el build de producción pasa **sin backend corriendo** [R7.5]. **2026-08-21: los tres en verde**; el build compila y genera las 24 rutas, con `/conversations` entre ellas.
- [x] 9.3 Comprobar que el contrato generado no ha derivado: `cd frontend && npm run api:check` sin cambios pendientes (este change no toca `backend/openapi.json` ni `frontend/lib/api/generated/openapi.d.ts`; si el comando literal no corre en un worktree enlazado, usar la variante documentada en `sdd/project.md`) [R7.2]. **2026-08-21: «api: generated types are up to date»**, con la variante de worktree enlazado que documenta `sdd/project.md`.
- [ ] 9.4 Comprobación manual de la superficie con el stack levantado (`make up`, o `make up PORT_OFFSET=<n>` si es un worktree enlazado, porque sin puertos publicados no hay UI alcanzable): como `PROPERTY_MANAGER` recorrer lista → filtros (incluido `CLOSED` con su nota) → paginación → abrir un hilo por URL → responder → transcribir un mensaje de huésped y ver aparecer la respuesta de la IA con su `intent` y su confianza → escalar → resolver con confirmación; repetir en móvil (una columna con «volver») y como `TENANT_OWNER` verificando que lee sin ningún control de gestión [R1, R2, R3, R4, R5, R6, R7.6].
- [x] 9.5 Revisar el diff final: toca solo `frontend/features/conversations/`, `frontend/components/ui/confirm-dialog.tsx`, `frontend/app/(workspace)/conversations/page.tsx`, `frontend/app/route-coverage.test.ts`, `frontend/lib/i18n/resources.ts` y los dos catálogos `conversations.json`; **no** toca backend, contrato OpenAPI, artefacto generado del frontend, autenticación/sesión ni otras features [R7.1, R7.2]. **Verificado el 2026-08-21**, y **corregido en `/sdd:review` el mismo día**: esta nota decía «dos ficheros más de los que esta lista preveía» y eran **cuatro**, ninguno fuera de `frontend/` — `frontend/components/ui/confirm-dialog.test.tsx` (el test de la primitiva de 7.1), `frontend/app/error-architecture.test.ts` (la exención de la frontera de suspense, motivada en 8.3), `frontend/features/conversations/lib/channels.ts` (+ su test: el predicado `isMuteChannel` que D13 exige, extraído de `thread-header.tsx`) y `frontend/features/conversations/components/thread-role-gate.test.tsx`. La revisión añadió además `frontend/components/ui/dialog-shell.tsx` y `frontend/features/conversations/lib/limits.ts` al cerrar dos hallazgos menores. La tabla *Changes by area* de `design.md` queda al día con los seis.
