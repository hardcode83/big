# BLOCKED — infra-dev-hardening

Implementación **completa** (22/22 tareas): código aplicado por el pipeline (budget €1 + 2 alertas, vault + key, security list acotado, cloud-init Docker, workflow endurecido), IAM mínima aplicada y verificada, versioning del bucket activo, y la clave SSH subida al Vault (out-of-band, recuperación verificada == clave original). Queda solo el panel de revisión antes de archivar.

## panel · run · deferred — panel de revisión no ejecutado
El código infra no pasó por el panel (architect/security/qa + reviewers de proyecto); se difirió por longitud de sesión.
**Resume:** `/sdd:review infra-dev-hardening` y luego `/sdd:archive infra-dev-hardening`.
