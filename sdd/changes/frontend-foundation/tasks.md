# Tasks: frontend-foundation

> **Implementation gate:** todas las tareas posteriores a 1.1 permanecen bloqueadas hasta verificar que `infra-scaffold` está mergeado en `origin/main`. Si 1.1 falla, `/sdd:run frontend-foundation` debe detenerse sin instalar dependencias ni modificar archivos.

## 1. Gate de implementación y baseline

- [ ] 1.1 Verificar el bloqueo remoto sin escribir en el repo: ejecutar `git fetch origin main` e inspeccionar exclusivamente `origin/main` (árbol e historial remoto) para confirmar que el trabajo de `infra-scaffold` está realmente integrado y que contiene los artefactos/configuración que el repositorio vigente necesita para instalar, arrancar y verificar el frontend; adaptar la evidencia a los nombres y ubicaciones reales del repo, sin exigir como prueba única una spec o directorio concreto y sin aceptar evidencia exclusivamente local; si la integración o los prerrequisitos no pueden demostrarse desde `origin/main`, detener la ejecución antes de 1.2 — **Files:** ninguno (remote read-only) — [R1, R8]
- [ ] 1.2 Comprobar que la versión de Next.js del scaffold sigue siendo una release estable Active/Maintenance LTS `>=14` compatible con React, Tailwind y shadcn/ui; conservarla si cumple D1 y registrar cualquier ajuste mínimo posterior únicamente en lockfile/package manifest — **Files:** `frontend/package.json`, `frontend/package-lock.json` (solo si la comprobación exige ajuste) — [R1]
- [ ] 1.3 Ejecutar el baseline `cd frontend && npm test`, inspeccionar `frontend/app/**` y confirmar antes de editar que el health-check actual es el único comportamiento a sustituir y que no existe implementación parcial del shell que deba adoptarse — **Files:** ninguno (read-only) — [R1, R6, R8]

## 2. Scaffolding y tooling frontend

- [ ] 2.1 Añadir únicamente las dependencias aprobadas por D1/D11–D16 (TanStack Query v5, Zustand, i18next/react-i18next, Lucide, utilidades/Radix requeridas por las primitivas shadcn y axe-core), mantener versiones compatibles y añadir scripts `typecheck` y `lint` sin alterar Docker/monorepo — **Files:** `frontend/package.json`, `frontend/package-lock.json` — [R1, R2, R3, R4, R6]
- [ ] 2.2 Configurar ESLint flat para Next/TypeScript y reglas `no-restricted-imports` que hagan verificable `app → features → components/lib`, impidan imports de internals entre features y mantengan `strict: true`; incluir casos de lint que demuestren la regla sin crear features de negocio — **Files:** `frontend/eslint.config.mjs`, `frontend/tsconfig.json`, fixtures/tests de reglas si el setup los requiere — [R1, R6]
- [ ] 2.3 Consolidar Vitest + Testing Library + jest-dom + axe en un setup reutilizable, con cleanup automático y helpers de render sin datos de negocio — **Files:** `frontend/vitest.config.ts`, `frontend/test/setup.ts`, `frontend/test/render.tsx`, `frontend/package.json` — [R2, R6]
- [ ] 2.4 Inicializar shadcn/ui de forma mínima y añadir exclusivamente Button, Sheet, Tooltip, Separator, Skeleton y Badge con su utilidad de clases; no ampliar el catálogo ni introducir un design system completo — **Files:** `frontend/components.json`, `frontend/lib/utils.ts`, `frontend/components/ui/**` — [R2]
- [ ] 2.5 Añadir tests semánticos de las primitivas interactivas aprobadas para verificar nombre accesible, focus, teclado, Escape y retorno de foco donde aplique, sin probar detalles internos de Radix — **Files:** tests colocados junto a `frontend/components/ui/**` — [R2, R6]
- [ ] 2.6 Definir únicamente los estilos/tokens base necesarios para shell y estados: mobile-first, focus visible, reduced motion, safe-area, targets táctiles y contraste AA; no introducir branding definitivo ni un design system completo — **Files:** `frontend/app/globals.css` — [R2]

## 3. Configuración transversal, Query y cliente API

- [ ] 3.1 Implementar la frontera de configuración server/public/runtime de D15 con allowlist explícita, locale default `es`, registro vacío de feature flags y provider cliente; probar que `BACKEND_INTERNAL_URL` y valores no allowlisted nunca aparecen en el snapshot público y que el shell no exige backend para arrancar — **Files:** `frontend/lib/config/server.ts`, `frontend/lib/config/public.ts`, `frontend/lib/config/constants.ts`, `frontend/lib/config/runtime-config-provider.tsx`, `frontend/lib/config/*.test.ts[x]` — [R6, R8, R9]
- [ ] 3.2 Crear `QueryProvider`, factory estable de `QueryClient` y convención de query keys que exija `tenantId` para recursos tenant-scoped; probar estabilidad de instancia y forma de keys sin definir recursos, queries, endpoints ni prefetch reales — **Files:** `frontend/lib/query/query-client.ts`, `frontend/lib/query/query-provider.tsx`, `frontend/lib/query/query-keys.ts`, `frontend/lib/query/*.test.ts[x]` — [R3, R6]
- [ ] 3.3 Crear el transporte genérico basado en `fetch` y el error técnico compatible solo con el envelope común de PRD §23; probarlo con `fetch` stub y respuestas sintácticas, devolviendo `unknown` y sin añadir endpoints, DTOs, hooks, mocks de negocio, tokens ni llamadas reales — **Files:** `frontend/lib/api/client.ts`, `frontend/lib/api/errors.ts`, `frontend/lib/api/index.ts`, `frontend/lib/api/*.test.ts` — [R3, R6, R7]

## 4. Internacionalización y route registry

- [ ] 4.1 Implementar inicialización i18next por request y provider cliente, resolución validada de cookie `autohostai.locale` con fallback `es`, sincronización de `<html lang>` y control accesible ES/EN; probar resolución, cambio de idioma y aislamiento de instancia — **Files:** `frontend/lib/i18n/server.ts`, `frontend/lib/i18n/client-provider.tsx`, `frontend/lib/i18n/locale.ts`, `frontend/features/shell/components/locale-switcher.tsx`, tests asociados — [R4, R6, R8, R9]
- [ ] 4.2 Implementar el `ShellRouteDescriptor` y registro completo de las rutas exactas de PRD §24 con perfiles `workspace | cleaner | technician | public | guest`, manteniendo `/tech` solo como slug; probar unicidad, cobertura, matching metadata y ausencia de campos de roles/endpoints/datos — **Files:** `frontend/features/shell/navigation/route-registry.ts`, `frontend/features/shell/navigation/route-registry.test.ts` — [R1, R4, R6, R8]
- [ ] 4.3 Implementar selectores del registro que requieran un `ShellProfile` concreto y no ofrezcan modo “all” para renderizado; probar que Workspace excluye Cleaner/Technician, que cada shell recibe solo sus rutas y que Guest/Public no consumen navegación interna — **Files:** `frontend/features/shell/navigation/select-routes.ts`, `frontend/features/shell/navigation/select-routes.test.ts` — [R6, R7, R8]
- [ ] 4.4 Crear catálogos `common`, `navigation` y `states` completos en ES/EN para route titles, descriptions, metadata, shells y estados; añadir test recursivo de paridad que falle ante claves ausentes y confirmar que no hay strings visibles hardcodeadas en componentes — **Files:** `frontend/locales/es/*.json`, `frontend/locales/en/*.json`, `frontend/lib/i18n/catalog-parity.test.ts` — [R4, R6, R8]
- [ ] 4.5 Crear `AppProviders` con composición `RuntimeConfigProvider → I18nProvider → QueryProvider`, documentar el slot futuro de `AuthProvider` sin implementarlo y probar que renderiza children sin backend ni contexto de sesión — **Files:** `frontend/app/providers.tsx`, `frontend/app/providers.test.tsx` — [R3, R4, R6, R7, R8, R9]

## 5. Estados visuales reutilizables

- [ ] 5.1 Implementar `StatePanel`, `LoadingState`, `ErrorState` y `EmptyState` con APIs y semánticas distintas; probar `aria-busy`, `role="alert"`, retry solo con callback real y empty state sin semántica de error — **Files:** `frontend/components/states/state-panel.tsx`, `frontend/components/states/loading-state.tsx`, `frontend/components/states/error-state.tsx`, `frontend/components/states/empty-state.tsx`, tests colocados — [R2, R5, R6]
- [ ] 5.2 Implementar `ModulePlaceholder` sobre `StatePanel` con icono, badge planificado, título/descripción/explicación localizados y sin ETA, progreso, retry, alert, datos ni acciones de negocio; probar que es distinguible de loading/error/empty y que ignora parámetros dinámicos — **Files:** `frontend/components/states/module-placeholder.tsx`, `frontend/components/states/module-placeholder.test.tsx` — [R4, R5, R6, R8]
- [ ] 5.3 Exportar una API pública estable para los estados sin barrel imports que conviertan el árbol en Client Component; type-check/testear que `ModulePlaceholder` sigue siendo server-compatible — **Files:** `frontend/components/states/index.ts`, test de import/render server-compatible — [R1, R5, R6, R8]

## 6. Shells y navegación responsive

- [ ] 6.1 Implementar matching de active route (exacto y prefijo más largo), cadenas explícitas de breadcrumbs y metadata de navegación contextual; probar trailing slash, rutas nested/dynamic, ocultación de IDs/tokens y estado activo dentro de “Más” — **Files:** `frontend/features/shell/navigation/match-route.ts`, `frontend/features/shell/navigation/breadcrumbs.ts`, tests asociados — [R4, R6, R8]
- [ ] 6.2 Implementar el store Zustand UI con `sidebarCollapsedByProfile`, overlays efímeros y persistencia selectiva versionada; probar rehidratación, cierre en cambio de pathname y que preferencias Workspace no alteran Cleaner/Technician — **Files:** `frontend/features/shell/state/use-shell-ui-store.ts`, `frontend/features/shell/state/use-shell-ui-store.test.ts` — [R3, R6, R8]
- [ ] 6.3 Implementar primitivas compartidas `ShellFrame`, `Topbar`, `Sidebar`, `BottomNavigation`, `PageHeader`, breadcrumbs, “Más” y skip-link como islas cliente mínimas; probar landmarks, teclado, `aria-current`, `aria-expanded`, Escape, retorno de foco y axe sin convertir el layout completo en Client Component — **Files:** `frontend/features/shell/components/**`, tests colocados — [R1, R2, R6, R8]
- [ ] 6.4 Implementar `WorkspaceShell` con sidebar+topbar desktop, sidebar colapsable tablet y topbar+bottom nav mobile (Dashboard, Timeline, Cleaning, Incidents, Más); probar que todas sus superficies siguen accesibles y que Cleaner/Technician no aparecen como navegación principal ni secundaria en este change — **Files:** `frontend/features/shell/components/workspace-shell.tsx`, `frontend/features/shell/components/workspace-shell.test.tsx` — [R2, R6, R8]
- [ ] 6.5 Implementar `CleanerShell` independiente y mobile-first usando únicamente rutas perfil `cleaner`, con topbar y navegación mínima proporcional a sus destinos estáticos reales; no renderizar bottom navigation/sidebar artificial cuando `/cleaner` sea el único destino navegable ni enlazar `/cleaner/tasks/[id]` sin un ID real aportado por el futuro módulo; probar que no consume Workspace/Technician ni inventa IDs o destinos — **Files:** `frontend/features/shell/components/cleaner-shell.tsx`, `frontend/features/shell/components/cleaner-shell.test.tsx` — [R2, R6, R8]
- [ ] 6.6 Implementar `TechnicianShell` independiente y mobile-first usando internamente `technician` y públicamente `/tech`, con topbar y navegación mínima proporcional a sus destinos estáticos reales; no renderizar bottom navigation/sidebar artificial cuando `/tech` sea el único destino navegable ni enlazar `/tech/incidents/[id]` sin un ID real aportado por el futuro módulo; probar que no consume Workspace/Cleaner, no inventa IDs/destinos y no existe `MaintenanceShell` — **Files:** `frontend/features/shell/components/technician-shell.tsx`, `frontend/features/shell/components/technician-shell.test.tsx` — [R2, R6, R8]
- [ ] 6.7 Implementar `PublicShell` y `GuestShell` como shells hermanos con topbar mínima, sin navegación privada, simulación de sesión ni dependencia de otros shells; probar que Guest nunca renderiza el token y que ambos respetan teclado/axe — **Files:** `frontend/features/shell/components/public-shell.tsx`, `frontend/features/shell/components/guest-shell.tsx`, tests asociados — [R2, R6, R7, R8]
- [ ] 6.8 Integrar el locale switcher en las topbars aplicables y verificar que cambiar ES/EN actualiza textos, accessible names y `lang` sin guardar locale en Zustand — **Files:** componentes topbar/shell afectados, tests de integración i18n — [R3, R4, R6, R8]

## 7. App Router, layouts y rutas placeholder

- [ ] 7.1 Convertir `frontend/app/layout.tsx` en Root Server Layout con providers, documento/locale y slots estables; sustituir `frontend/app/page.tsx` y su health-check por el redirect `/ → /dashboard`, eliminando toda llamada backend y actualizando sus tests — **Files:** `frontend/app/layout.tsx`, `frontend/app/page.tsx`, `frontend/app/page.test.tsx` o nueva ubicación de test — [R1, R3, R4, R6, R8, R9]
- [ ] 7.2 Crear `(workspace)/layout.tsx` con `WorkspaceShell` y placeholders para `/dashboard`, `/properties`, `/properties/[id]`, `/timeline` y `/reservations`; cada page será un Server Component fino que usa route ID + `ModulePlaceholder`, sin fetch, mocks o lógica de negocio, con tests de routing/render — **Files:** `frontend/app/(workspace)/layout.tsx`, rutas indicadas y tests asociados — [R1, R4, R6, R8]
- [ ] 7.3 Añadir placeholders Workspace restantes: `/cleaning`, `/incidents`, `/conversations`, `/approvals`, `/pricing`, `/statements`, `/reviews`, `/settings` y `/settings/integrations`; probar cobertura del registro y ausencia de comportamiento funcional — **Files:** pages bajo `frontend/app/(workspace)/**`, tests asociados — [R1, R4, R6, R8]
- [ ] 7.4 Crear layout/rutas Cleaner para `/cleaner` y `/cleaner/tasks/[id]` usando `CleanerShell`; probar que el parámetro opaco no se renderiza y que no existen checklist, fotos, llamadas ni workflows — **Files:** `frontend/app/(field)/cleaner/layout.tsx`, pages/tests bajo `cleaner/**` — [R1, R4, R6, R8]
- [ ] 7.5 Crear layout/rutas Technician para `/tech` y `/tech/incidents/[id]` usando `TechnicianShell`; probar que internamente se usa `technician`, que el ID no se renderiza y que no existen acciones, costes, fotos ni workflows — **Files:** `frontend/app/(field)/tech/layout.tsx`, pages/tests bajo `tech/**` — [R1, R4, R6, R8]
- [ ] 7.6 Crear layouts/rutas Public y Guest para `/login`, `/forgot-password` y `/guest/[token]`; renderizar solo placeholders localizados, sin formularios/auth ni contenido de huésped, y probar que el token nunca aparece en DOM — **Files:** `frontend/app/(public)/**`, `frontend/app/(guest)/**`, tests asociados — [R4, R6, R7, R8]

## 8. Error boundaries y Suspense

- [ ] 8.1 Implementar `global-error.tsx` con documento propio, fallback ES/EN independiente de providers, focus inicial y recuperación real; probar que no muestra `error.message`, stacks, secretos ni URLs internas — **Files:** `frontend/app/global-error.tsx`, `frontend/app/global-error.test.tsx`, catálogo mínimo de error global — [R2, R4, R5, R6, R8]
- [ ] 8.2 Añadir boundaries de segmento para Workspace, Cleaner, Technician, Public y Guest y tests unitarios limitados a verificar que cada `error.tsx` compone `ErrorState`, no expone el error recibido y solo pasa retry cuando dispone del `reset` real de App Router; no simular en unit tests el aislamiento o la jerarquía del router — **Files:** `frontend/app/(workspace)/error.tsx`, `frontend/app/(field)/cleaner/error.tsx`, `frontend/app/(field)/tech/error.tsx`, `frontend/app/(public)/error.tsx`, `frontend/app/(guest)/guest/[token]/error.tsx`, tests unitarios asociados — [R2, R5, R6, R8]
- [ ] 8.3 Inspeccionar la jerarquía App Router y confirmar que cada `error.tsx` está bajo el layout propietario correspondiente, dentro del slot de contenido que debe aislar y sin elevarse por encima de WorkspaceShell, CleanerShell, TechnicianShell, PublicShell o GuestShell — **Files:** ninguno (read-only structural verification) — [R1, R5, R6, R8]
- [ ] 8.4 Verificar con Next.js ejecutándose y navegador/Playwright que un fallo controlado y transitorio dentro del contenido de cada perfil activa su boundary y mantiene visible el chrome del shell; retirar completamente la instrumentación de fallo antes de marcar la tarea y confirmar que no queda ruta/flag/test hook permanente — **Files:** pages placeholder afectadas solo durante la verificación; diff final sin instrumentación — [R2, R5, R6, R8]
- [ ] 8.5 Verificar por test/inspección de árbol que los placeholders estáticos no añaden `loading.tsx`, Suspense, promesas artificiales ni `LoadingState`; documentar que futuros `loading.tsx` deben componer `LoadingState` en el segmento propietario — **Files:** tests de arquitectura/rutas, `frontend/README.md` — [R1, R5, R6, R8]

## 9. Metadata App Router

- [ ] 9.1 Implementar helper tipado de metadata localizada por route ID y defaults globales: `AutoHostAI`, descripción ES/EN, template `%s | AutoHostAI`, Open Graph genérico y `noindex, nofollow`; probar ambos locales y no configurar `metadataBase` sin URL autorizada — **Files:** `frontend/lib/metadata/create-route-metadata.ts`, `frontend/lib/metadata/create-route-metadata.test.ts`, `frontend/app/layout.tsx` — [R4, R6, R8, R9]
- [ ] 9.2 Conectar todos los placeholders al helper usando claves del route registry; probar títulos/descripciones por ruta, política noindex de todos los shells y ausencia de IDs/tokens/params en title, description, Open Graph, canonical y especialmente `/guest/[token]` — **Files:** pages/layouts bajo `frontend/app/**`, tests de metadata y route registry — [R4, R6, R7, R8]

## 10. Documentación de convenciones

- [ ] 10.1 Documentar en `frontend/README.md` estructura y dependency rules, taxonomía de cinco shells, navegación por perfil, sustitución de placeholders, Server/Client Components, performance, providers, TanStack Query, Zustand, API transport, i18n, estados, error/Suspense, metadata, config, testing y puntos futuros de auth sin guards/roles; registrar literalmente la recomendación `TECHNICIAN includes external technicians, repair professionals and maintenance workers assigned to incidents.` sin modificar el PRD — **Files:** `frontend/README.md` — [R1, R3, R4, R5, R6, R7, R8, R9]
- [ ] 10.2 Actualizar el README raíz para reflejar la estructura frontend y los comandos reales `npm test`, `npm run typecheck`, `npm run lint`, `npm run build` y `npm run dev`, sin documentar módulos placeholder como funcionalidades disponibles — **Files:** `README.md` — [R1, R6, R8]

## 11. Verificación final

- [ ] 11.1 Ejecutar type-check completo: `cd frontend && npm run typecheck`; corregir cualquier error sin relajar TypeScript strict — **Files:** solo los frontend files necesarios para corregir fallos — [R1, R6]
- [ ] 11.2 Ejecutar lint completo: `cd frontend && npm run lint`; confirmar boundaries/import rules y cero bypasses añadidos para aprobar — **Files:** solo los frontend files necesarios para corregir fallos — [R1, R6]
- [ ] 11.3 Ejecutar la suite frontend exacta del proyecto: `cd frontend && npm test`; confirmar tests de navegación por perfil, estados, i18n, config, metadata, errors y accesibilidad sin backend funcional — **Files:** solo código/tests frontend necesarios para corregir fallos — [R2, R3, R4, R5, R6, R7, R8, R9]
- [ ] 11.4 Ejecutar production build: `cd frontend && npm run build`; inspeccionar chunks por ruta, registrar baseline del shell y confirmar que no hay imports de features de negocio, canary/experimental APIs ni bundle global de módulos futuros — **Files:** documentación de baseline en `frontend/README.md` si procede — [R1, R6, R8]
- [ ] 11.5 Arrancar con `cd frontend && npm run dev` sin backend disponible y verificar con navegador/Playwright a 390 px, 768 px y 1280 px: chrome correcto por shell, navegación proporcional a destinos reales (incluida ausencia de bottom nav artificial en Cleaner/Technician), sidebar/bottom nav solo donde apliquen, active route, breadcrumbs, locale, placeholders y ausencia de overflow — **Files:** ninguno salvo correcciones verificadas — [R2, R4, R6, R8]
- [ ] 11.6 Realizar verificación manual WCAG AA del shell: recorrido solo teclado, skip-link, focus visible/retorno, Escape, landmarks, accessible names, targets 44×44, contraste, reduced motion y estabilidad del chrome durante error de ruta; registrar resultados en la checklist de ejecución — **Files:** ninguno salvo correcciones verificadas — [R2, R5, R6, R8]
- [ ] 11.7 Auditar el diff final: confirmar que no se modificaron monorepo/Docker/Compose/Makefile/infra/PRD, que no existen auth/RBAC/tokens, lógica de negocio, endpoints/DTOs, llamadas backend, datos/mocks de negocio, `MaintenanceShell` ni navegación cruzada indiscriminada; confirmar que únicamente `/tech` conserva “tech” como slug público — **Files:** ninguno (read-only) — [R1, R3, R6, R7, R8, R9]
