# Blocked items

- **phase**: review
  **type**: deferred
  **what & why**: La implementación revisada está en cambios sin commit; `HEAD` (`38db322`) solo contiene el marcador del roadmap. `mark-ready` registra únicamente `HEAD`, por lo que no puede certificar estos cambios sin incluir código no committed en la evidencia.
  **exact resume command**: `/sdd:review app-version-provenance`
