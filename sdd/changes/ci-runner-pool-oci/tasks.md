# Tasks: ci-runner-pool-oci

<!-- "hard" on a section heading escalates its implementer to the stronger model;
     "panel: PASS <date>" is written by /sdd:run when a section's review panel passes. -->

## 1. Terraform: variable `runner_count` y cableado al cloud-init <!-- panel: PASS 2026-09-04 -->

- [x] 1.1 `infra/environments/dev/variables.tf` — añadir `variable "runner_count"` (default 4, validation 1..4, **amend 2026-09-04**: default subido de 2 a 4 por decisión de producto). Cubre R1 (default), R6 (rango). Sin esto D2 no se puede cablear.
- [x] 1.2 `infra/environments/dev/main.tf` — pasar `runner_count` al `templatefile()` que renderiza `cloud-init.yaml.tftpl`. Cubre R1 (cómo llega al bootstrap), R3 (reaprovisionamiento declarativo).
- [x] 1.3 `infra/environments/dev/cloud-init.yaml.tftpl` — añadir `runner_count = ${runner_count}` como variable del template y pasarlo al `runcmd` que invoca `bootstrap-runner.sh` (`RUNNER_COUNT=<n> sudo bash /opt/bootstrap-runner.sh "$RUNNER_COUNT"`). Cubre R3.

## 2. `runner-bootstrap.sh`: pool parametrizado, migración del legado y baja con guardia de liveness <!-- hard --> <!-- panel: PASS 2026-09-04 -->

<!-- hard: la baja condicionada a `systemctl is-active` + API de jobs en vuelo, y la
     migración del legado `autohostai-dev-vm`, tienen variantes reales (orden de
     retirada, qué hacer si GitHub rechaza el registration-token a mitad del bucle). -->

- [x] 2.1 `infra/environments/dev/runner-bootstrap.sh` — reescribir el bloque de configuración del runner como bucle `for i in $(seq 1 "$RUNNER_COUNT"); do … done` con `./config.sh --unattended --replace --url … --token … --labels "$ENV" --name "autohostai-${ENV}-vm-<i>"`, `./svc.sh install <user>`/`start` solo si `systemctl is-active` devuelve `inactive`/`failed`/`unknown`. Cubre R1 (N agentes), R2 (un usuario por agente), R3 (alta idempotente), R4 (toolchain compartido), R6 (default y rango).
- [x] 2.2 `infra/environments/dev/runner-bootstrap.sh` — añadir fase previa al bucle que migra el legado: si `gh api ... /repos/{owner}/{repo}/actions/runners` lista un agente con nombre `autohostai-${ENV}-vm` (sin sufijo numérico), `./config.sh remove --token ... && ./svc.sh uninstall actions.runner.<org>-<repo>.autohostai-${ENV}-vm.service`. Cubre R5 (rollback a N=1 deja un único agente).
- [x] 2.3 `infra/environments/dev/runner-bootstrap.sh` — añadir fase previa al bucle que baja agentes sobrantes cuando `RUNNER_COUNT` es menor que la lista en `/var/lib/autohostai-runner/agents.list`. Para cada sobrante: `./config.sh remove --token ... && ./svc.sh uninstall` **solo si** `systemctl is-active` devuelve `inactive`/`failed`/`unknown`; si está `active`, `set -e` aborta con un mensaje que nombre al agente y al job en vuelo (vía `gh api ... /actions/runs?status=in_progress` filtrando por `runner.name`). Cubre R3 (baja explícita), R5 (rollback).
- [x] 2.4 `infra/environments/dev/runner-bootstrap.sh` — escribir `/var/lib/autohostai-runner/agents.list` con los nombres de los agentes numerados tras cada `./config.sh --replace`, atómico (`mv` desde un temporal). Fuente de verdad para la fase de baja (2.3). Cubre R3 (service discovery).
- [x] 2.5 `infra/environments/dev/runner-bootstrap.sh` — capturar errores por iteración con nombre (`agent <i>/<N>: <acción>: <error>`) y dejar a los agentes 1..k-1 reconciliados si el agente k falla. Cubre R3 (reporte de error por agente).

## 3. Runbooks y README <!-- panel: PASS 2026-09-04 -->

- [x] 3.1 `docs/ci-runner-rollback.md` — añadir sección "Subir N" (`runner_count++` en `variables.tf` o `dev.tfvars`, `terraform apply`, `sudo bash /opt/bootstrap-runner.sh "$RUNNER_COUNT"`, verificación con `gh api ... /actions/runners`). Cubre R5.
- [x] 3.2 `docs/ci-runner-rollback.md` — añadir sección "Bajar N" (pasos inversos, con la fase de baja condicionada a `is-active == inactive`; si falla con "agente activo, jobs en vuelo: <url>", esperar a que terminen o cancelar el PR y reaplicar). Cubre R5.
- [x] 3.3 `infra/environments/dev/RUNBOOK.md` — actualizar §6.2 para mencionar el parámetro `RUNNER_COUNT` en el comando de reaprovisionamiento y la lista esperada de `N` agentes tras reaplicar. Cubre R5.
- [x] 3.4 `infra/environments/dev/README.md` — añadir nota sobre el rango razonable de `runner_count` (1–4 con la métrica que lo sostiene: 4 OCPU/24 GB AD-3 PAYG, app conviviendo; default 4) y la exigencia de nota de medición para `runner_count > 4`. Cubre R6. **Amend 2026-09-04**: default = 4, no se exige nota para el valor por defecto.

## 4. Spec `ci-runner-self-hosted.md`: pool en lugar de runner único <!-- panel: PASS 2026-09-04 -->

- [x] 4.1 `sdd/specs/ci-runner-self-hosted.md` — reescribir el Purpose (líneas 5–10) para hablar de **pool de N agentes** con label `dev` en lugar de runner único; añadir al final de cada sección de Requirements los SHALL que cubren R1 (N agentes), R2 (un usuario por agente), R3 (idempotencia y baja con guardia), R4 (toolchain compartido), R5 (runbook subir/bajar N) y R6 (rango y nota de medición). Cubre R1 (canonical), R5.

## 5. Verification <!-- panel: PASS 2026-09-04 (5.1, 5.2) | BLOCKED 2026-09-04 (5.3, 5.4) -->

- [x] 5.1 `terraform -chdir=infra/environments/dev fmt -check -recursive && terraform -chdir=infra/environments/dev validate` — formato y validación estática sin estado remoto. **PASS 2026-09-04**: `FMT_EXIT=0`, `VAL_EXIT=0`.
- [x] 5.2 `git diff origin/main -- .github/workflows/` — confirmar que los 10 workflows siguen declarando `runs-on: [self-hosted, dev]` y que ningún `runs-on:` ha cambiado. Cubre R1 (workflows intactos). **PASS 2026-09-04**: diff vacío (10 workflows × `runs-on:` intactos).
- [ ] 5.3 **(BLOCKED)** `terraform -chdir=infra/environments/dev apply` sobre la VM viva + reaplicar bootstrap + `gh api ... /actions/runners` muestra N entradas `autohostai-dev-vm-<i>` (i ∈ [1..4]) online + sin legado `autohostai-${ENV}-vm`. **Reason**: el worktree de implementación no tiene acceso a la VM OCI (`oci-cli` con instance-principal no resuelve desde aquí). **Resume command**: `ssh ubuntu@<dev-vm> 'sudo /opt/bootstrap-runner.sh "$RUNNER_COUNT"' && gh api repos/autohostai-labs/AutoHostAI/actions/runners?per_page=100`. **Gate**: antes de mergear, ejecutar este gate y adjuntar el output al PR.
- [ ] 5.4 **(BLOCKED)** matriz de verificación (D7): `workflow_dispatch` secuencial con `sleep 60` entre cada uno de `api-contract.yml` desde la rama del PR; para cada `actions_run_id`, `gh api ... /actions/runs/<id>` devuelve un `runner.name` distinto (`autohostai-${ENV}-vm-1` … `autohostai-${ENV}-vm-N`); los N jobs acaban en `success`. **Reason**: requiere runners reales ya registrados por 5.3. **Resume command**: ver `sdd/changes/ci-runner-pool-oci/BLOCKED.md`. **Gate**: la matriz de N jobs × N runners debe coincidir (cada runner corre al menos un job, ningún job se queda en cola).

## Implementation Notes

<!-- Append-only, written by the implementer of each section for the next one. -->

- `infra/environments/dev/variables.tf`: nueva `variable "runner_count"` (`type = number, default = 4`, **amend 2026-09-04** — antes 2) con `validation` `1 ≤ runner_count ≤ 4` — fuente única del número de agentes para el resto del change.
- `infra/environments/dev/main.tf` línea 161: `runner_count = var.runner_count` añadido al mapa de `templatefile()` de `cloud-init.yaml.tftpl` (entre `env` y `github_repo`; los `file()` siguen al final).
- `infra/environments/dev/cloud-init.yaml.tftpl`: la entrada final de `runcmd` (única con quoting, single-quoted YAML para preservar el `$` y `"$RUNNER_COUNT"` literales) es `'RUNNER_COUNT=${runner_count} sudo bash /opt/bootstrap-runner.sh "$RUNNER_COUNT"'`. `runner-bootstrap.sh` debe leer `$1` como `RUNNER_COUNT` (tarea 2.1).
- `infra/environments/dev/runner-bootstrap.sh` sección 2: variables por agente — `RUNNER_USER=actions-runner-<i>`, `RUNNER_HOME=/opt/actions-runner-<i>`, `agent_name=autohostai-${ENV}-vm-<i>`, `svc=actions.runner.${GITHUB_REPO//\//-}.autohostai-${ENV}-vm-<i>.service` (prefijo `SERVICE_PREFIX`).
- `infra/environments/dev/runner-bootstrap.sh` sección 2: source of truth de los agentes numerados = `AGENTS_LIST=/var/lib/autohostai-runner/agents.list` (un nombre por línea, escrito con `mv` desde un `mktemp` al final del bucle de alta — la fase de baja lo lee antes del bucle).
- `infra/environments/dev/runner-bootstrap.sh` sección 2: agente legado = `LEGACY_NAME=autohostai-${ENV}-vm` (sin sufijo numérico), `LEGACY_HOME=/opt/actions-runner`, `LEGACY_SERVICE=actions.runner.<org>-<repo>.${LEGACY_NAME}.service`. La migración se ejecuta ANTES del bucle como Fase 0; si GitHub lo lista vía `GET /repos/{owner}/{repo}/actions/runners`, se retira con `config.sh remove --token $REG_TOKEN && svc.sh uninstall $LEGACY_SERVICE` (el registration-token sirve también para remove).
- `infra/environments/dev/runner-bootstrap.sh` sección 2: API endpoints llamados — (a) `GET https://api.github.com/repos/$GITHUB_REPO/actions/runners?per_page=100` con `Authorization: Bearer $INSTALL_TOKEN` para listar corredores en Fase 0; (b) `GET /actions/runs?status=in_progress&per_page=100` + `/actions/runs/{id}/jobs?per_page=100` para buscar jobs en vuelo de un runner activo (Fase 1, condición de baja abortada). El registration-token (`POST /actions/runners/registration-token`) sigue siendo vía `curl --config` para no aparecer en `argv`.
- `infra/environments/dev/runner-bootstrap.sh` sección 2: gotchas — (1) `INSTALL_TOKEN` se mantiene vivo durante todo el script (no se `unset` tras mintear el `REG_TOKEN`) porque lo reusan los helpers GitHub; se `unset` al final junto con `REG_TOKEN`. (2) El trap ERR por agente usa FUNCIÓN (`on_err`), no string single-quoted — un `'...$step...'` capturaría `step=""` y diría "agent k/N: failed" sin contexto (necesario para depurar). (3) `local` no es válido dentro de la subshell `(...)` del bucle de alta; las variables de paso (`state`, `runner_version`) son subshell-locales sin `local`. (4) El bucle de alta es tolerante a fallos por iteración (tarea 2.5); el surplus phase ABORTA con `set -e` (tarea 2.3). (5) `agents.list` se sobrescribe al final del bucle desde el `mktemp`; en una caída mid-loop, la versión anterior sobrevive y el re-run usa `--replace` para re-registrar lo que ya estaba.
- `infra/environments/dev/runner-bootstrap.sh` (sección 2 review-panel, F1): línea 260 (`for name in "${declared_names[@]:-}"`) cambiada a `"${declared_names[@]+"${declared_names[@]}"}"` — bajo `set -euo pipefail`, `:-` iteraba UNA vez con `name=""` cuando el array estaba vacío, y el cuerpo calculaba `svc=…-vm-.service` que `systemctl is-active` no encontraba, terminando en `retire_named_agent ""` (regex fail → `set -e` abortaba antes de Fase 2). Idem línea 269 para `surplus`. Reproducido con `bash -c 'set -euo pipefail; surplus=(); for name in "${surplus[@]+"${surplus[@]}"}"; do echo "[$name]"; done; echo done'` → imprime solo `done`, sin `[ ]`.
- `infra/environments/dev/runner-bootstrap.sh` (sección 2 review-panel, F2): `gh_list_runner_names` (líneas 82-92) reescrita para enviar `INSTALL_TOKEN` por `printf … | curl --config -` (stdin), mismo patrón que `REG_TOKEN` arriba (línea 72). Antes el `-H "Authorization: Bearer $INSTALL_TOKEN"` ponía el installation-token en el `argv` de `curl`, legible por cualquier usuario de la VM vía `/proc/<pid>/cmdline` (incluido el nuevo `actions-runner-<i>`). `awk` del cuerpo de la función: 0 ocurrencias de `-H "Authorization`; el `Authorization: Bearer` solo aparece dentro del template del `printf`.
- `infra/environments/dev/runner-bootstrap.sh` (sección 2 review-panel, F3): al inicio del cuerpo del `for name in surplus` (líneas 269-273) añadida defensa en profundidad — `[[ ! "$name" =~ ^autohostai-${ENV}-vm-([0-9]+)$ ]]` salta con `WARN` y `continue`. Independiente de F1: si `agents.list` se edita a mano con un nombre que no encaja, no se produce un `…-vm-.service` inválido ni se delega la validación exclusivamente en `retire_named_agent` (que abortaría `set -e` y bloquearía el bootstrap).
- `docs/ci-runner-rollback.md`: añadidas dos secciones nuevas como §7 «Subir N» y §8 «Bajar N», al final del documento (después de §6 «Rotación / retirada del runner», no después de la tabla de §2 — la tabla queda intacta). §7 cubre `runner_count++` + `terraform apply` + `sudo bash /opt/bootstrap-runner.sh "$RUNNER_COUNT"` + `gh api /repos/.../actions/runners` para verificar N entradas `autohostai-${ENV}-vm-<i>` `online` y sin legado. §8 cubre el camino inverso, condicionando la fase de baja a `is-active == inactive`; el mensaje de aborto esperado del script es del tipo `agente activo, jobs en vuelo: <run-url>` (operador espera a que termine o cancela el PR y reaplica). Gotcha para la spec (sección 4): el rollback a `runner_count = 1` deja `autohostai-${ENV}-vm-1`, **no** el legado `autohostai-${ENV}-vm` — la migración de la sección 2 ya retiró el legado en la primera reaplicación; el comportamiento del pool es idéntico, el nombre no.
- `infra/environments/dev/RUNBOOK.md §6.2`: una sola línea cambiada en el bloque bash — `sudo bash /opt/bootstrap-runner.sh` ahora pasa `"$RUNNER_COUNT"` explícito (mismo patrón que el runcmd de `cloud-init.yaml.tftpl`). Añadida una frase tras el bloque: `$RUNNER_COUNT` debe coincidir con la variable `runner_count` del apply; sin argumento el script cae al `RUNNER_COUNT=4` interno (`runner-bootstrap.sh` línea 37, **amend 2026-09-04**: el fallback subió de 2 a 4 para alinearse con `variables.tf` `runner_count` default; el fallback solo aplica a invocaciones ad-hoc, el `runcmd` del cloud-init siempre pasa el valor del apply). Línea de verificación actualizada: «N entradas `autohostai-dev-vm-<i>` (i ∈ [1..N]) Idle» en vez del singular `autohostai-dev-vm`. Procedimiento de recuperación de un agente puntual actualizado a `sudo /opt/actions-runner-<i>/svc.sh start`.
- `infra/environments/dev/README.md`: añadida sección `## Runner self-hosted (pool N)` justo antes de `## Pendiente (no automatizable por este change)`. Tres frases: el runner es pool de N agentes (variable `runner_count`, default 4 — amend 2026-09-04, validación 1..4); rango razonable 1–4 sostenido por la métrica explícita (VM `VM.Standard.A1.Flex`, 4 OCPU/24 GB/200 GB, AD-3, PAYG — ADR 0001 addendum 2026-07-21, R6.1); `runner_count > default` exige nota de medición (suite del backend con frontend en paralelo + latencia p50 frontend público) archivada en `RUNBOOK.md §6.2` (R6.3). Sin nota, no se acepta el cambio.
- `sdd/specs/ci-runner-self-hosted.md` (sección 4): reescrita como spec del **pool de N agentes**, no del runner único. Purpose cambiado de "VM `dev`, label `[self-hosted, dev]`… un runner self-hosted" a "pool de N agentes… nombres `autohostai-${ENV}-vm-<i>` para `i ∈ [1..N]`"; añade cláusulas SHALL al final de cada sección de Requirements cubriendo R1 (N agentes con label `dev` y nombre `autohostai-${ENV}-vm-<i>`, sin label nuevo ni partición por dominio), R2 (un usuario Linux `actions-runner-<i>` por agente, grupo `docker`, `RUNNER_HOME=/opt/actions-runner-<i>`, servicio systemd `actions.runner.<org>-<repo>.autohostai-${ENV}-vm-<i>.service`, `./config.sh --unattended --replace`), R3 (reaprovisionamiento idempotente; baja explícita condicionada a `is-active != active` y aborta con URL del run si hay job en vuelo; `agents.list` atómico en `/var/lib/autohostai-runner/`; migración del legado `autohostai-${ENV}-vm` antes del bucle; reporte de error por agente; sin nuevos secrets ni nuevas policies IAM), R4 (toolchain en cloud-init una vez por VM; bootstrap solo crea usuario, instala `actions-runner-<i>`, registra y arranca), R5 (runbook `docs/ci-runner-rollback.md` con secciones «Subir N» y «Bajar N», `RUNBOOK.md §6.2` con `RUNNER_COUNT` explícito, `runner_count = 1` como rollback válido) y R6 (validación TF `1 ≤ runner_count ≤ 4`, default 4, nota de medición exigida para `runner_count > default`, sin auto-escalado). Lo que **se conservó** del texto previo (no se pierde): label `[self-hosted, dev]`, GitHub App registration vía `gh-app-install-token.py` con installation-token compartido, instance principal sin nuevas policies, IaC-first (sin configuración a mano), `runs-on: [self-hosted, dev]` intacto en los 9 workflows (10 si se cuenta `multiarch-build-check` en su excepción), `multiarch-build-check.yml` en `ubuntu-latest` con su nota "QEMU no verificado", tooling por job (`actions/setup-node`/`actions/setup-python` pineados por SHA, `backend/.python-version = 3.12`), convivencia (`umask 077`, `docker logout`, `$HOME/.autohostai-dev-runtime.env`, `127.0.0.1:5432`/`6379`), riesgo aceptado del radio de confianza (sin ampliarlo cualitativamente), gate verde runner-side antes de mergear, `concurrency`/`permissions`/`timeout-minutes`/`paths` preservados. Secciones reordenadas para que las SHALL de R1 vivan al final de «Workflows», las de R2 vivan en su propia subsección «Aislamiento por usuario Linux y servicio systemd» (extraída del cuerpo anterior, sin duplicar las SHALL de R1), las de R3 vivan en «Reaprovisionamiento declarativo e idempotente del pool» (nueva subsección), las de R4 se añadan al final de «Tooling declarado por job, no asumido del runner base», las de R5 vivan al final de «Rollback» y las de R6 vivan en «Holgura CPU/memoria y contención con la app» (nueva subsección). `Key files` ampliado con `runner-bootstrap.sh`, `{variables.tf,main.tf,cloud-init.yaml.tftpl}` (fuente del parámetro `runner_count`), `RUNBOOK.md §6.2` y `README.md`. Gotcha para el panel y la sección de Verification: el `terraform apply` sobre la VM viva del gate final (task 5.3) sigue siendo el único camino para confirmar que `gh api /actions/runners` lista **exactamente** N entradas sin legado — la spec declara la verificación pero no la implementa; el `agents.list` que R3 introduce es interno al bootstrap y no es fuente de verdad para el gate externo.
