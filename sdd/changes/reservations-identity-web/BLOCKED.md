# Blocked items

## reservations-identity-web: `mark-local-verified` gate refuses on task 4.3 (manual browser check)

- **Review panel result:** all 7 reviewers (sdd-architect, sdd-security, sdd-qa, sdd-review-tenancy, sdd-review-i18n, sdd-review-documentation, sdd-review-cicd) returned **PASS** with zero findings. `sdd-qa`'s own test run: 62 passed / 1 failed, and the failure was independently re-run in isolation and confirmed as host contention (a 5s timeout in an unrelated filters test, no R1–R6 fields involved), not a regression.
- **Gate that failed:** `sdd_lifecycle.py mark-local-verified reservations-identity-web` refuses with "Change has 1 incomplete task(s) at tasks.md line(s) 84" — task `4.3 Comprobación manual` is unchecked. There is no override flag in the script; every task box must be checked before `mark-local-verified`/`mark-ready` will proceed.
- **Why still unchecked:** the implementer documented (tasks.md:84-88) that the UI is only reachable via `make up PORT_OFFSET=<n>` (this worktree's stack runs with `docker-compose.worktree.yml`, which strips all host port publishing), and declined to do so because the host was already memory-constrained with several other live worktree sessions. I re-checked host state just now before attempting it myself: `top -l 1` shows **192 MB unused of 16 GB, 96.6% CPU busy** (53.84% user + 42.76% sys), and three peer sessions are currently `busy` (auth-session-persistence-3f, incident-triage-web-e2, approvals-web-96). Publishing new ports means starting another `frontend`/`backend` pair (each ~100-150 MB idle, more under load) on a host with no real headroom — the same risk the implementer already declined, now confirmed rather than assumed.
- **Why this needs a human, not just a retry:** this isn't an async wait (unlike sdd-qa earlier) — it's a standing resource conflict with other people's live sessions that I can't resolve by waiting a few minutes, and the lifecycle gate has no documented-exception path for it.
- **Decision needed:** see `decisions` in the structured output below.
