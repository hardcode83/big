# Tasks: domain-foundation-ops

## 1. `cleaning/` — CleaningTask, CleaningChecklistTemplate, CleaningChecklistCompletion, CleaningPhoto

- [x] 1.1 Enums `CleaningTaskStatus` (9 valores PRD §7.9), `CleaningValidationStatus` (`PENDING`,`PASSED`,`FAILED`,`WAIVED` — nombre `ASSUMPTION`, ver design.md D2) — files: `backend/app/cleaning/domain/enums.py` [R1]
- [x] 1.2 Dataclasses `CleaningTask`, `CleaningChecklistTemplate`, `CleaningChecklistCompletion`, `CleaningPhoto` (campos exactos PRD §7.9-7.12) + test unitario por entidad que la instancia sin DB — files: `backend/app/cleaning/domain/entities.py`, `backend/tests/cleaning/test_entities.py` [R2, R6]
- [x] 1.3 Modelos SQLAlchemy de las 4 entidades: `CleaningTaskModel` (mixins completos, `INDEX(property_id, status)`, `INDEX(assigned_cleaner_id, status)`, FK `reservation_id`/`checklist_template_id` `RESTRICT`, FK `assigned_cleaner_id`/`validated_by_user_id` nullable `SET NULL`); `CleaningChecklistTemplateModel` (mixins completos, `items`/`required_photos` `JSONB`, FK `property_id` nullable `RESTRICT`); `CleaningChecklistCompletionModel` (sin `TenantScopedMixin`/`TimestampMixin` — solo `id` + `completed_at` nullable, D3/D4, `UNIQUE(cleaning_task_id, item_id)`, FK `cleaning_task_id` `RESTRICT`, FK `completed_by` nullable `SET NULL`); `CleaningPhotoModel` (sin `TenantScopedMixin`, solo `created_at` manual sin `TimestampMixin`, `INDEX(cleaning_task_id)`, FK `cleaning_task_id` `RESTRICT`, FK `uploaded_by` `RESTRICT`); todas las FKs cross-module con `Uuid` explícito (D5) + registrar `app.cleaning.infrastructure.models` en `backend/alembic/env.py` y `backend/tests/conftest.py` (D9) + test de integración por modelo, incluyendo la violación de `UNIQUE(cleaning_task_id, item_id)` — files: `backend/app/cleaning/infrastructure/models.py`, `backend/alembic/env.py`, `backend/tests/conftest.py`, `backend/tests/cleaning/test_models.py` [R3, R5, R6]

## 2. `maintenance/` — Incident

- [x] 2.1 Enums `IncidentCategory` (13 valores PRD §7.13), `IncidentStatus` (9 valores PRD §7.13), `IncidentSource` (`GUEST`,`CLEANER`,`OWNER`,`SYSTEM`,`PMS`,`LOCK_ALERT` — `ASSUMPTION`), `IncidentSeverity` (`LOW`,`MEDIUM`,`HIGH`,`CRITICAL` — `ASSUMPTION`, distinto de `TimelineSeverity`, ver design.md D2) — files: `backend/app/maintenance/domain/enums.py` [R1]
- [x] 2.2 Dataclass `Incident` (PRD §7.13) + test unitario sin DB — files: `backend/app/maintenance/domain/entities.py`, `backend/tests/maintenance/test_entities.py` [R2, R6]
- [x] 2.3 Modelo SQLAlchemy `IncidentModel` (mixins completos, `INDEX(property_id, status)`, `INDEX(tenant_id, severity, status)`, FK `property_id` `RESTRICT`, FK `reservation_id` nullable `RESTRICT`, FK `reported_by_user_id`/`assigned_technician_id` nullable `SET NULL`, `ai_classification` `JSONB`, FKs cross-module con `Uuid` explícito) + registrar `app.maintenance.infrastructure.models` en `backend/alembic/env.py` y `backend/tests/conftest.py` + test de integración — files: `backend/app/maintenance/infrastructure/models.py`, `backend/alembic/env.py`, `backend/tests/conftest.py`, `backend/tests/maintenance/test_models.py` [R3, R5, R6]

## 3. `messaging/` — Conversation, Message

- [x] 3.1 Enums `ConversationChannel` (6 valores PRD §7.14), `ConversationStatus` (`OPEN`,`RESOLVED`,`ESCALATED`,`CLOSED` — `ASSUMPTION`), `ConversationEscalationStatus` (`NONE`,`PENDING_HUMAN`,`HUMAN_HANDLING`,`RESOLVED` — `ASSUMPTION`), `MessageSenderType` (`GUEST`,`OWNER`,`MANAGER`,`AI`,`SYSTEM` — `ASSUMPTION`) — files: `backend/app/messaging/domain/enums.py` [R1]
- [x] 3.2 Dataclasses `Conversation`, `Message` (PRD §7.14-7.15) + test unitario por entidad sin DB — files: `backend/app/messaging/domain/entities.py`, `backend/tests/messaging/test_entities.py` [R2, R6]
- [x] 3.3 Modelos SQLAlchemy `ConversationModel` (mixins completos, `INDEX(tenant_id, status)`, `INDEX(reservation_id)`, FK `property_id`/`reservation_id`/`guest_id` nullable `RESTRICT`) y `MessageModel` (sin `TenantScopedMixin`/`TimestampMixin` — solo `id` + `created_at` manual, D3/D4, `metadata_` mapeado a columna `"metadata"` `JSONB` por colisión con `Base.metadata`, D7, `INDEX(conversation_id, created_at ASC)`, FK `conversation_id` `RESTRICT`, FK `sender_user_id` nullable `SET NULL`) + registrar `app.messaging.infrastructure.models` en `backend/alembic/env.py` y `backend/tests/conftest.py` + test de integración por modelo — files: `backend/app/messaging/infrastructure/models.py`, `backend/alembic/env.py`, `backend/tests/conftest.py`, `backend/tests/messaging/test_models.py` [R3, R5, R6]

## 4. `access/` — AccessRecord

- [x] 4.1 Enums `AccessProvider` (`GRINPASS`,`MANUAL`,`MOCK`,`EXTERNAL_MANAGED` — `ASSUMPTION`), `AccessRecordStatus` (`PENDING`,`CREATED_EXTERNAL`,`MANUAL_ADDED`,`DELIVERED`,`EXPIRED`,`REVOKED` — `ASSUMPTION`), `AccessCreatedMode` (`EXTERNAL_PMS_AUTOMATIC`,`MANUAL`,`MOCK` — `ASSUMPTION`) — files: `backend/app/access/domain/enums.py` [R1]
- [x] 4.2 Dataclass `AccessRecord` (PRD §7.16) + test unitario sin DB — files: `backend/app/access/domain/entities.py`, `backend/tests/access/test_entities.py` [R2, R6]
- [x] 4.3 Modelo SQLAlchemy `AccessRecordModel` (mixins completos, `INDEX(reservation_id)`, `INDEX(property_id, valid_from, valid_to)`, FK `property_id` `RESTRICT`, FK `reservation_id` nullable `RESTRICT`, `code_masked` como `VARCHAR(50)` sin cifrado real todavía per `security.md`, FKs cross-module con `Uuid` explícito) + registrar `app.access.infrastructure.models` en `backend/alembic/env.py` y `backend/tests/conftest.py` + test de integración — files: `backend/app/access/infrastructure/models.py`, `backend/alembic/env.py`, `backend/tests/conftest.py`, `backend/tests/access/test_models.py` [R3, R5, R6]

## 5. Alembic — migración encadenada sobre la baseline

- [x] 5.1 Migración `alembic revision --autogenerate` con `down_revision = "4a5faad7796b"` que crea las 8 tablas nuevas (`cleaning_tasks`, `cleaning_checklist_templates`, `cleaning_checklist_completions`, `cleaning_photos`, `incidents`, `conversations`, `messages`, `access_records`) en orden de dependencia; añadir explícitamente la lista de tipos `ENUM` nuevos y su `DROP TYPE` en `downgrade()` (mismo patrón que `_ENUM_TYPE_NAMES` de la baseline, D9) — files: `backend/alembic/versions/a1a72da30f8e_domain_foundation_ops.py` [R4]

## 6. Verification

- [x] 6.1 `docker compose down -v` + `make up`: confirma que `migrate` aplica esta migración además de la baseline sin error, y que las 16 tablas existen (`docker compose exec postgres psql -U $POSTGRES_USER -d $POSTGRES_DB -c '\dt'`) [R4, R6]
- [x] 6.2 `cd backend && uv run alembic downgrade 4a5faad7796b && uv run alembic upgrade head`: revierte solo las 8 tablas de este change y las reaplica, sin afectar el esquema de `domain-foundation-core` ni dejar tablas/tipos ENUM/índices huérfanos [R4]
- [x] 6.3 Suite completa de tests backend: `cd backend && uv run pytest` — unit (`domain/`, sin DB) + integration (`infrastructure/`, contra la BD de test aislada `<db>_test`) en verde, incluyendo el caso de violación de `UNIQUE(cleaning_task_id, item_id)` [R2, R3, R6]
- [x] 6.4 Confirmar que las 8 entidades de dominio (`CleaningTask`, `CleaningChecklistTemplate`, `CleaningChecklistCompletion`, `CleaningPhoto`, `Incident`, `Conversation`, `Message`, `AccessRecord`) se instancian en tests unitarios sin necesitar la DB (R6.2) [R2, R6]
- [x] 6.5 Revisar `docs/diagrams/2026-07-13_autohost-er-entidades-core.png` contra el esquema ampliado; regenerar con `/sdd:diagram` solo si el diagrama pretende cubrir estas 8 entidades y diverge (documentation.md) — si el diagrama es explícitamente "core", confirmar que no aplica y no tocarlo
