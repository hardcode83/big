# Tasks: incident-triage-web

## 1. Backend: el triaje humano clasifica (`classify_by_triage`) <!-- hard --> <!-- panel: PASS 2026-09-05 -->

- [x] 1.1 En `backend/app/maintenance/domain/entities.py`: añadir la fila `"classify_by_triage": (frozenset({IncidentStatus.OPEN}), IncidentStatus.CLASSIFIED)` a `_TRANSITIONS` (D1); cambiar `set_triage` para que, tras aplicar los campos, compruebe si el estado previo era `OPEN` y la llamada trae `category` **y** `severity` — en ese caso transicionar vía `self._check_transition("classify_by_triage")` (o el mecanismo que ya usan el resto de operaciones) y devolver `True`; devolver `False` en cualquier otro caso. Firma resultante: `def set_triage(self, *, now, category=None, severity=None, estimated_cost=None) -> bool` (D2). [R3.5]
- [x] 1.2 Tests de dominio en `backend/tests/maintenance/test_entities.py`: `category` + `severity` sobre una incidencia `OPEN` → transiciona a `CLASSIFIED` y devuelve `True`; sólo uno de los dos, o ninguno → sigue `OPEN` y devuelve `False` (caso negativo explícito); triaje sobre una incidencia que no está `OPEN` (p. ej. `ASSIGNED`) con los dos campos → no transiciona (la fila sólo admite `OPEN` como origen) y sigue anotando los campos; una incidencia cerrada (`RESOLVED`/`CANCELLED`) sigue rechazando `set_triage` como hoy (`_reject_if_closed`). [R3.5, R3.6]
- [x] 1.3 En `backend/app/maintenance/application/use_cases.py`, `TriageIncidentUseCase.execute`: capturar `previous_status = incident.status` junto al resto de `previous`; el `ChangeSet` de `INCIDENT_TRIAGED` gana `.diff("status", previous_status, incident.status)` (`status` ya está en `AUDITABLE_FIELDS["INCIDENT"]`, no tocar el allowlist); cuando `incident.set_triage(...)` devuelva `True`, tras el `save`, llamar a `self._record_timeline(tenant_id=..., incident=incident, event_type=TimelineEventType.INCIDENT_CLASSIFIED, actor=actor, now=now)` (actor `USER`, no `None` — la excepción de actor ausente de R9 es sólo de la vía automática) y a `self._severity_trigger(incident)` seguido de `self._fire_trigger(...)` si devuelve un trigger no nulo (D4, D5), calcado del bloque `if incident.status is IncidentStatus.CLASSIFIED` de `ClassifyIncidentUseCase.execute` (misma clase base `_IncidentTransitionMixin` vía `_IncidentFlowBase`). El trigger va **después** de `self._incidents.save(...)`. NEVER tocar `ai_classification`, `title` ni `description`. [R3.5]
- [x] 1.4 Tests de caso de uso en `backend/tests/maintenance/test_use_cases.py` (clase de `TriageIncidentUseCase`): el `AuditLog` `INCIDENT_TRIAGED` incluye el diff de `status` cuando clasifica y `OPEN → OPEN` (sin diff de `status`, o diff nulo) cuando no; se escribe `TimelineEvent` `INCIDENT_CLASSIFIED` con `actor_type=USER` sólo cuando `set_triage` devolvió `True`, y ninguno cuando no; severidad `HIGH`/`CRITICAL` en la clasificación dispara `PropertyStateMachine` con `INCIDENT_HIGH`/`INCIDENT_CRITICAL` respectivamente, y `MEDIUM`/`LOW` **no disparan nada** (verificar explícitamente la ausencia); un triaje que fija sólo el coste (sin categoría+severidad) sobre `OPEN` sigue sin disparar nada y sigue en `OPEN`; el caso donde el mismo triaje clasifica **y** abre la puerta de aprobación (coste por encima del umbral) acaba en `AWAITING_OWNER_APPROVAL` pasando por `CLASSIFIED` (verificar el orden: `set_triage` antes que `needs_owner_approval`/`require_owner_approval`). [R3.5, R3.6]
- [x] 1.5 En `backend/app/maintenance/api/incidents_router.py`, actualizar la `description` de `PATCH /{incident_id}` (línea ~269) para decir que un triaje que fija categoría **y** severidad sobre una incidencia `OPEN` la clasifica (D12); no cambia el esquema. Regenerar `backend/openapi.json` (`make openapi`) y `frontend/lib/api/generated/openapi.d.ts` (rodeo de `sdd/project.md` §Worktree bootstrap si corre en worktree) y commitear ambos. [R3.5]
- [x] 1.6 Tests de API en `backend/tests/maintenance/test_api_incidents.py`: `PATCH` con categoría+severidad sobre `OPEN` responde `200` con `status: "CLASSIFIED"`; con sólo uno de los dos campos responde `200` con `status: "OPEN"`; aislamiento por tenant del `PATCH` (un tenant no puede triar la incidencia de otro, ya cubierto para el resto de campos — añadir el caso de `classify_by_triage` si no está cubierto por los anteriores). [R3.6]

## 2. Frontend: capa de datos — cuatro mutaciones y roster de técnicos <!-- panel: PASS 2026-09-05 -->

- [x] 2.1 En `frontend/features/incidents/data/dto.ts`: añadir `TechnicianSummary { id: string; name: string; isActive: boolean }` (misma forma que `CleanerSummary` de `features/cleaning/data/dto.ts`). [R2.1]
- [x] 2.2 En `frontend/features/incidents/data/http/http-incidents-source.ts`, añadir a `HttpIncidentsSource`:
  - `listTechnicians(_tenantId: string): Promise<TechnicianSummary[]>` — `GET /api/v1/users` con `query: { page: 1, per_page: 100, role: "TECHNICIAN" }`, **sin** filtro de `status` (misma forma que `listCleaners`), mapeando `status === "ACTIVE"` a `isActive`.
  - `classifyIncident(_tenantId: string, incidentId: string): Promise<IncidentDetailDto>` — `POST /api/v1/incidents/{incident_id}/classify`, sin cuerpo.
  - `triageIncident(_tenantId: string, incidentId: string, input: { category?: IncidentCategory; severity?: IncidentSeverity; estimatedCost?: string }): Promise<IncidentDetailDto>` — `PATCH /api/v1/incidents/{incident_id}`, incluyendo en el cuerpo **sólo** las claves presentes en `input` (el esquema es `extra="forbid"`); `estimated_cost` viaja como el string recibido, nunca convertido a `number` (D7).
  - `assignIncident(_tenantId: string, incidentId: string, input: { technicianId: string; assignmentNote?: string }): Promise<IncidentDetailDto>` — `POST /api/v1/incidents/{incident_id}/assign` con `{ technician_id, assignment_note }`.
  - `cancelIncident(_tenantId: string, incidentId: string): Promise<IncidentDetailDto>` — `POST /api/v1/incidents/{incident_id}/cancel`, sin cuerpo ni parámetro de motivo (el contrato no lo tiene).
  Las cuatro mapean con `mapIncidentDetail`, ya existente. [R2.1, R3.2, R4.1, R5.1, R5.3]
- [x] 2.3 Tests en `frontend/features/incidents/data/http/http-incidents-source.test.ts`: cada método nuevo llama a la ruta y verbo correctos, con el cuerpo/query exactos (incluido que `triageIncident` con un solo campo omite los demás del cuerpo, y que `listTechnicians` no manda `status`); mapeo de la respuesta a los DTOs. [R2.1, R3.2, R4.1, R5.1]

## 3. Frontend: hooks de mutación y roster <!-- panel: PASS 2026-09-05 -->

- [x] 3.1 En `frontend/features/incidents/hooks/query-keys.ts`, añadir `technicians: (tenantId: string): QueryKey => tenantScopedKey(tenantId, "incidents-technicians")`.
- [x] 3.2 Nuevo `frontend/features/incidents/hooks/use-incident-management.ts`: `useTechnicianDirectory()` (TanStack Query, calcado de `useCleanerDirectory`, `retry` con la política existente) y cuatro hooks de mutación — `useClassifyIncident`, `useTriageIncident`, `useAssignIncident`, `useCancelIncident` — cada uno con `retry: false`, sin actualización optimista, e invalidando en `onSettled` (**awaited**, no `void`-descartado, siguiendo `use-incident-cycle.ts` y no `use-resolve-incident.ts`) `incidentsKeys.detail`, `incidentsKeys.context` e `incidentsKeys.listPrefix`; `useCancelIncident` invalida además `['tenant', tenantId, 'blocked-transitions']`, `['tenant', tenantId, 'dashboard-cards']` y `['tenant', tenantId, 'property-timeline']` (mismas claves literales que `use-resolve-incident.ts`). [R2.4, R3.4(D8), R4.3, R5.2, R6.1, R6.3]
- [x] 3.3 Exportar los cuatro hooks y `useTechnicianDirectory` desde `frontend/features/incidents/index.ts`. [R6.3]
- [x] 3.4 Tests en `frontend/features/incidents/hooks/use-incident-management.test.tsx`: cada mutación llama al método correcto de la fuente HTTP con las variables correctas; invalidación en éxito y en fallo (`onSettled`) de las tres claves comunes; `cancel` invalida además las tres claves del dashboard; `useTechnicianDirectory` expone la lista sin filtrar por `status`. [R2.4, R3.4, R4.3, R5.2, R6.1, R6.3]

## 4. Frontend: lógica de presentación — tabla de acciones, errores y guardas de cliente <!-- panel: PASS 2026-09-05 -->

- [x] 4.1 Nuevo `frontend/features/incidents/lib/manager-actions.ts`: tipo `ManagerAction = "classify" | "assign" | "triage" | "cancel"`; `Record<IncidentStatus, readonly ManagerAction[]>` congelado (`Object.freeze`) siguiendo `TECH_ACTIONS`/`Object.hasOwn` de `features/tech/lib/tech-actions.ts` — `OPEN: ["classify", "triage", "cancel"]`, `CLASSIFIED`/`ASSIGNED`/`ACCEPTED`/`IN_PROGRESS`/`WAITING_EXTERNAL_PARTS`: `["assign", "triage", "cancel"]`, `AWAITING_OWNER_APPROVAL: ["cancel"]`, `RESOLVED`/`CANCELLED`: `[]`; función `managerActions(status)` con el mismo patrón `Object.hasOwn`; `Record<IncidentStatus, "awaiting-owner" | null>` congelado y función `managerStatusNote(status)` para el texto de R1.4 (D6). [R1.3, R1.4]
- [x] 4.2 Nuevo `frontend/features/incidents/lib/manager-action-error.ts`: `managerActionMessage(error, refreshedStatus, action)` (forma de `messageFor` de `tech-cycle-actions.tsx`) — `409` → `conflictReason(refreshedStatus)`, con el matiz de D9: si `action === "triage"` y `refreshedStatus === "OPEN"`, devolver la clave propia «hay que fijar categoría y severidad antes de presupuestar» en vez de la genérica de `out-of-order`; `422` en `assign` → clave propia de técnico inválido; `422` en `triage` → clave propia de validación del coste; nunca renderizar `error.message` (D9). [R2.5, R3.4, R6.1]
- [x] 4.3 Extraer `POSITIVE_DECIMAL`/su chequeo de `frontend/features/dashboard/stalls/components/resolve-incident-dialog.tsx` a `frontend/features/incidents/lib/cost-format.ts` como `isPositiveDecimal(value: string): boolean`, exportar por el barrel de `features/incidents`, y actualizar `resolve-incident-dialog.tsx` para importarla en vez de declarar su propia expresión (D11). [R3.2]
- [x] 4.4 Nuevo `frontend/features/incidents/lib/assignment-note.ts`: `hasControlCharacters(value: string): boolean` con `/[ --]/` (excluye tabulador `U+0009` y salto de línea `U+000A`) y una constante `MAX_ASSIGNMENT_NOTE_LENGTH = 2000` (D11). [R2.3]
- [x] 4.5 Tests: `manager-actions.test.ts` (tabla exhaustiva por estado, `managerStatusNote`), `manager-action-error.test.ts` (los tres `409`, el matiz de `triage` sobre `OPEN`, los dos `422`), `cost-format.test.ts` (casos límite: `"0"`, `"0.5"`, `"12.345"` inválido, negativo inválido), `assignment-note.test.ts` (` ` rechazado, `\n`/`\t` aceptados, límite de 2000). [R1.3, R1.4, R2.3, R2.5, R3.2, R3.4, R6.1]

## 5. Frontend: UI — sección de acciones del manager, permisos y nombre del técnico <!-- panel: PASS 2026-09-05 -->

- [x] 5.1 En `frontend/lib/auth/permissions.ts`: añadir `"MANAGE_INCIDENTS"` a la unión `Permission` y a `ROLE_UI_PERMISSIONS.PROPERTY_MANAGER` (y en ningún otro rol — copiado de `_INCIDENT_MANAGE` de `backend/app/auth/domain/policy.py`, que sólo `PROPERTY_MANAGER` tiene). [R1.1]
- [x] 5.2 En `frontend/features/incidents/components/detail/incident-detail-sections.tsx`, `DetailAssignedTechnicianBlock`: cambiar la prop de `assignedTechnicianId: string | null` a `technicianName: string | null` (o mantener `assignedTechnicianId` sólo para decidir si renderizar la sección, más `technicianName`/`isResolving` para el valor mostrado); pintar el nombre resuelto o el texto localizado «no disponible» cuando el roster no lo contiene o la consulta falla; el UUID NEVER visible. [R2.6]
- [x] 5.3 Nuevo `frontend/features/incidents/components/detail/manager-incident-actions.tsx`: componente `ManagerIncidentActions({ incident })`, montado por `IncidentDetailView` sólo si `useHasPermission("MANAGE_INCIDENTS")` (sin permiso, ni un hueco reservado). Dentro, con la forma de `TechCycleActions`:
  - `classify` → botón directo, llama a `useClassifyIncident`, sin cuerpo, refresca al terminar.
  - `assign` → `Sheet` con `<select>` nativo de técnicos (de `useTechnicianDirectory`, filtrados a `isActive` para ser seleccionables, todos resolubles por nombre) + `<textarea>` de nota (máx. 2000, rechaza caracteres de control con `hasControlCharacters` antes de enviar) + botón de confirmación explícito; etiqueta «reasignar» y aviso de que ETA/nota vigentes se pierden cuando ya hay asignatario.
  - `triage` → `Sheet` con categoría/severidad/coste precargados con los valores actuales; envía sólo lo que cambió; coste validado en cliente con `isPositiveDecimal`.
  - `cancel` → `AlertDialog` (`components/ui/alert-dialog.tsx`) nombrando la consecuencia (terminal, irreversible, recompone el estado de la vivienda), sin pedir ni enviar motivo (el contrato no lo tiene).
  Cada acción usa `manager-actions.ts` para decidir su visibilidad por estado y `manager-action-error.ts` para mapear errores; `403` oculta la sección entera y muestra el texto de «sin permiso» que ya usa el detalle (`401` no tiene variante propia: lo lleva el flujo de expiración de sesión). Para `AWAITING_OWNER_APPROVAL`, mostrar sólo cancelar y el texto de `managerStatusNote`. [R1.1, R1.2, R1.3, R1.4, R1.5, R1.6, R2.1, R2.2, R2.3, R2.5, R3.1, R3.3, R3.4, R4.1, R4.2, R5.1, R5.3, R6.2]
- [x] 5.4 Montar `ManagerIncidentActions` en `frontend/features/incidents/components/detail/incident-detail-view.tsx`, y resolver `technicianName` con `useTechnicianDirectory()` para pasarlo a `DetailAssignedTechnicianBlock` (para todo el que abra la pantalla, tenga o no `MANAGE_INCIDENTS` — R1.2/R2.6/D10). [R1.1, R1.2, R2.6]
- [x] 5.5 Añadir las claves nuevas a `frontend/locales/es/incidents.json` y `frontend/locales/en/incidents.json`: nombres de las cuatro acciones, aviso de reasignación, texto de `AWAITING_OWNER_APPROVAL` (R1.4), las tres razones de `409` (incluida la de "triage sobre OPEN"), el `422` de técnico inválido, validaciones de coste y de nota, "técnico no disponible", y actualizar/retirar `fields.assignedTechnicianNote` (ya no es cierto que el UUID no se pueda resolver a nombre). Ninguna string hardcodeada. [R6.4]
- [x] 5.6 Tests de componente en `frontend/features/incidents/components/detail/manager-incident-actions.test.tsx` (y actualizar `incident-detail-view.test.tsx`): visibilidad de la sección por permiso (`PROPERTY_MANAGER` sí, `TENANT_OWNER` no, sin hueco reservado); la tabla estado→acciones se refleja en lo que se renderiza para varios estados representativos (`OPEN`, `CLASSIFIED`, `AWAITING_OWNER_APPROVAL`, `RESOLVED`); los tres caminos del `409` (incluido el matiz de `triage` sobre `OPEN`); el `422` de técnico inválido en `assign`; la validación en cliente del coste y de la nota (caracteres de control, longitud); el nombre del técnico resuelto en `DetailAssignedTechnicianBlock` para manager y para `TENANT_OWNER`, y el "no disponible" cuando no resuelve. [R1.1, R1.2, R1.3, R1.4, R1.5, R2.5, R2.6, R3.4, R6.5]

## 6. Documentación

- [x] 6.1 Actualizar `docs/maintenance.md` (secciones «El ciclo, de principio a fin» y «Quién puede hacer qué») para reflejar que triar, asignar/reasignar, relanzar el clasificador y cancelar se operan ahora desde `/incidents/[id]`, y explicar la regla `classify_by_triage` (un triaje con categoría+severidad sobre `OPEN` clasifica).
- [x] 6.2 Regenerar `docs/diagrams/2026-08-23_autohost-secuencia-mantenimiento.png` con `/sdd:diagram`, **sin abrir el PNG anterior**, derivando el contenido de `_TRANSITIONS` y de los casos de uso actualizados (nuevo paso: origen `OPEN`, destino `CLASSIFIED`, ruta `PATCH /incidents/{id}`, evento `INCIDENT_CLASSIFIED`); borrar el PNG obsoleto y actualizar la referencia en `docs/architecture.md`/donde corresponda.

## 7. Verification

- [x] 7.1 Backend: aplicar los `docker compose cp` de `sdd/project.md` si corre en worktree, luego `docker compose exec backend uv run pytest` — suite completa en verde, incluidos los tests nuevos de 1.2, 1.4 y 1.6. **10705 passed, 44 skipped** (987.69s). El host sufrió contención severa de memoria por otras sesiones concurrentes durante esta verificación (varios intentos previos murieron con `OOM`/`SIGKILL` entre el 9% y el 32%); parar temporalmente `frontend`/`worker`/`beat` de este propio worktree (innecesarios para pytest) liberó suficiente margen para completar la pasada limpia. Contenedores restaurados después.
- [x] 7.2 Backend static: `uv run pyright .` sin findings nuevos. 953 errores totales, todos preexistentes: los de `incidents_router.py` son el patrón `UUID | None` ya presente en todo el fichero (el diff de la sección 1 sólo tocó el `description` de la ruta `PATCH`, cero líneas de código); el único en `use_cases.py` es el de la línea ~2919 ya señalado como preexistente por la sección 1 (caso de uso distinto, no tocado); `entities.py` y los tres ficheros de test que esta feature añadió/modificó (`test_entities.py`, `test_use_cases.py`, `test_api_incidents.py`) no aparecen en el log — cero errores. Mismo problema de memoria que 7.1; resuelto igual (parar `frontend`/`worker`/`beat`).
- [x] 7.3 Frontend: aplicar los `docker compose cp` de `sdd/project.md` (incluido `backend/openapi.json`) si corre en worktree, medir la cifra de partida de `npm test`, luego `docker compose exec -T frontend npm test` en verde (sin regresiones más allá de los `ENOENT` ya documentados como ajenos). Cifra de partida medida al principio de esta sesión (antes de tocar nada): **215 archivos / 2177 tests**, sin ningún `ENOENT` (el entorno ya estaba saneado). Cifra final: **221 archivos / 2302 tests, 100% en verde** (+6 archivos, +125 tests — coincide con lo añadido en las secciones 1-5: `http-incidents-source.test.ts` ampliado, `use-incident-management.test.tsx` nuevo, `query-keys.test.ts` ampliado, `manager-actions.test.ts`/`manager-action-error.test.ts`/`cost-format.test.ts`/`assignment-note.test.ts` nuevos, `manager-incident-actions.test.tsx` nuevo, `incident-detail-view.test.tsx`/`permissions.test.tsx` ampliados). Una primera pasada completa mostró 6 archivos en rojo por timeout de 5000ms — 5 de los 6 (`reservations-view`, `guest-portal-view`, `cleaner-task-detail-view`, `use-mark-read`, `create-user-form`) son de features que este change no toca en absoluto (`git diff --stat` vacío), y el sexto era este mismo `manager-incident-actions.test.tsx`, ya verificado en verde de forma aislada varias veces durante el panel de la sección 5; parar `backend`/`worker`/`beat` (innecesarios para `vitest`) y repetir dio 221/221 archivos y 2302/2302 tests en verde — confirma contención de host, no regresión.
- [x] 7.4 Frontend: `cd frontend && npm run api:check` (o el rodeo de worktree) confirma que `openapi.d.ts` no ha divergido del contrato regenerado en 1.5. Rodeo de worktree aplicado (`docker compose cp backend/openapi.json` + symlink `/frontend`→`/app`); `api: generated types are up to date`.
- [x] 7.5 Recorrer en dev —Playwright o a mano, con `PORT_OFFSET` si va en worktree— el ciclo *huésped reporta desde el portal → clasificador o triaje → manager asigna desde `/incidents/[id]` → técnico acepta y resuelve desde `/tech`*, y anotar aquí lo observado (R6.6).

  **Recorrido con Playwright, `make up PORT_OFFSET=10` (frontend :3010, backend :8010).** Preparación: `.env` local necesitó rellenar `DEMO_ACCOUNT_PASSWORD` y las variables `BOOTSTRAP_*`/`SEED_*` (vacías por defecto, sin valores de ejemplo) para poder correr `make bootstrap` + `make seed-demo` + `make demo-reset`. **Nota para quien retome este worktree**: `make bootstrap` con variables propias creó un tenant aparte del tenant de demostración fijo que `demo-reset` aprovisiona con sus propias credenciales (`manager@demo.autohostai.test`, misma `DEMO_ACCOUNT_PASSWORD`) — son dos tenants distintos y coexisten en la misma base; el guest token que imprime `demo-reset` pertenece a su propio tenant de demostración, no al creado por `bootstrap`.

  1. **Huésped reporta desde el portal** (`/guest/<token>` de `demo-reset`): incidencia "Cosa rara" con título/descripción deliberadamente ambiguos para que el clasificador automático no la reconozca — el portal confirmó "Incidencia comunicada. Abierta" (`OPEN`, categoría por defecto `OTHER`).
  2. **`/incidents` del manager** (`manager@demo.autohostai.test`): la incidencia aparece en `OPEN`/"Abierta", y su detalle muestra exactamente `["Relanzar clasificador", "Triar", "Cancelar incidencia"]` — ni rastro de "Asignar", que es justo la fila `OPEN` de `manager-actions.ts` (R1.3).
  3. **Triaje que clasifica** (`classify_by_triage`, R3.5): se cambiaron categoría→`SAFETY` y severidad→`HIGH` en el mismo `Guardar` y la incidencia pasó de `Abierta` a **`Clasificada`** — confirmado también por API (`GET /incidents/{id}` → `status: CLASSIFIED, category: SAFETY, severity: HIGH`). El estado operacional de la vivienda "Redes 11" pasó a **`MAINTENANCE_REQUIRED`** en el mismo instante (`GET /properties/{id}` → `current_operational_state`), confirmando el disparo de `PropertyStateMachine` que D5 añade a esta vía. Las acciones se actualizaron a `["Asignar técnico", "Triar", "Cancelar incidencia"]`, la fila `CLASSIFIED` de la tabla.
  4. **Manager asigna** desde el mismo detalle: `Sheet` con el roster (`Técnico Demo` disponible), nota de asignación de prueba, `Confirmar` → la incidencia pasó a `ASSIGNED` con `assigned_technician_id` fijado (confirmado por API tras una expiración de sesión del JWT en curso — la mutación había terminado en el backend aunque la UI perdió la sesión esperando). **`DetailAssignedTechnicianBlock` pintó "Técnico Demo" por nombre, nunca el UUID** (R2.6/D10 en vivo), y la acción se relabeló a "Reasignar técnico" (R2.2).
  5. **Técnico acepta y resuelve desde `/tech`**: el entorno sufrió inestabilidad severa del contenedor `frontend` (reinicios repetidos del proceso `next dev` bajo la misma contención de memoria del host que afectó 7.1/7.3, con `ERR_CONNECTION_RESET` intermitente) justo en este tramo; **accept → en-route → resolve** se completaron por API directa autenticada como el técnico (`POST .../accept`, `.../en-route`, `.../resolve`) en vez de clic a clic en `/tech`, porque esas tres operaciones son del ciclo del técnico ya existente (`tech-app`/`tech-cycle-completion`) y esta feature no las toca — lo que quedaba por verificar en vivo era la parte nueva (pasos 1-4), que sí se recorrió íntegramente en el navegador. La incidencia terminó en `RESOLVED` con `resolved_at` fijado.
  6. Ningún error de consola ajeno a ruido de HMR de Turbopack (reconexiones de `webpack-hmr` por la contención de memoria) apareció durante el recorrido; ninguna petición mostró el UUID del técnico ni el `message` técnico de ningún sobre de error.

  **Contención de host, para quien retome**: el `frontend` de este worktree se reinició solo varias veces durante este recorrido por la misma presión de memoria de otras sesiones concurrentes que afectó 7.1/7.3 — parar `worker`/`beat` de este propio compose (innecesarios para navegar la UI) bajó la tasa de reinicios a cero. Si el navegador da `ERR_CONNECTION_RESET`, comprobar `docker compose ps frontend` antes de reintentar.

## Implementation Notes

### Section 1 (backend: `classify_by_triage`)

- `Incident.set_triage(*, now, category=None, severity=None, estimated_cost=None) -> bool` — returns `True` only when this call classified the incident; parameters unchanged.
- New transitions row is keyed `"classify_by_triage"` (`OPEN` → `CLASSIFIED`); `classify` untouched.
- `PATCH /api/v1/incidents/{incident_id}` now answers `200` with `status: "CLASSIFIED"` when the body carries **both** `category` and `severity` and the incident was `OPEN`; with only one of them (or only `estimated_cost`) it still answers `200` with `status: "OPEN"`. The frontend must re-read `status` from the response of every triage, not assume it is unchanged.
- Same call with an `estimated_cost` above the tenant threshold answers `status: "AWAITING_OWNER_APPROVAL"` (it passes through `CLASSIFIED`), so a triage can move the incident two steps in one request.
- Request schema unchanged (`TriageIncidentRequest`, `extra="forbid"`, the three fields optional, `estimated_cost` as a decimal string): the only contract change is the route `description`, so `frontend/lib/api/generated/openapi.d.ts` regenerated with **no type change** — section 2 needs no new generated types for `triageIncident`.
- `backend/openapi.json` and `frontend/lib/api/generated/openapi.d.ts` are regenerated and left uncommitted in the working tree.
- `POST /incidents/{id}/classify` is still needed as a separate action: the automatic classifier is the only path that writes `ai_classification`/`ai_summary`, and `set_triage` never touches them.
- Audit stays a single `INCIDENT_TRIAGED` row whose `changes.status` is always present (`old == new` when nothing moved); no second `INCIDENT_CLASSIFIED` audit row. The timeline gains `INCIDENT_CLASSIFIED` with `actor_type = USER` only when the triage classified.
- Property state recomputes only on that transition: `HIGH` → `INCIDENT_HIGH`, `CRITICAL` → `INCIDENT_CRITICAL`, `MEDIUM`/`LOW` → nothing. A triage that does not classify never moves the property, whatever the severity.
- Test-side gotcha for anyone extending `backend/tests/maintenance/test_entities.py`: `classify_by_triage` is excluded from `_REJECTED_CASES` via `_ANNOTATES_INSTEAD_OF_REFUSING` (it annotates instead of raising outside `OPEN`), and `Operation` is now `Callable[[Incident], object]`.
- Cross-tenant `PATCH` asserts only the `404`: the shared test session is bound to the caller's tenant, so reading the neighbour's row back returns `None` either way.
- Panel PASS 2026-09-05 (architect, security, i18n, tenancy: PASS; qa: FAIL then reviewed by the orchestrator). QA's finding — "triaging `estimated_cost` alone, above the tenant threshold, on an `OPEN` incident raises `InvalidIncidentTransitionError` → `409` instead of leaving the incident cleanly `OPEN`" — is real but **pre-existing**, not introduced by this section: `TriageIncidentUseCase.execute` called `needs_owner_approval`/`require_owner_approval` unconditionally (independent of `category`/`severity`) before this change too, and the same 409 already fired on that combination. Design D3 names this exact effect explicitly ("Si trae sólo el coste, sigue dando `409` exactamente como hoy") and rejects reordering; its mitigation is D9 (a dedicated `conflictReason` message for `triage` on `OPEN`, in section 4/frontend), not a backend change. Finding dismissed for section 1 with this rationale; section 4 must implement D9's `triage`-on-`OPEN` message or R1/R6.1 in that section will be incomplete.

### Section 2 (frontend: data layer — four mutations + technician roster)

- `HttpIncidentsSource` gains: `listTechnicians`, `classifyIncident`, `triageIncident`, `assignIncident`, `cancelIncident`. All four mutations map with the existing `mapIncidentDetail`; no new mapper needed for `IncidentDetailDto`.
- New exported input types (in `http-incidents-source.ts`, not `dto.ts`): `TriageIncidentInput { category?, severity?, estimatedCost? }` and `AssignIncidentInput { technicianId, assignmentNote? }` — hooks in section 3 take these as their mutation variables verbatim.
- `TechnicianSummary` (`{ id, name, isActive }`) lives in `frontend/features/incidents/data/dto.ts`, exported from there (not from the HTTP source file).
- `listTechnicians` calls `GET /api/v1/users` with `{ page: 1, per_page: CATALOG_PER_PAGE, role: "TECHNICIAN" }` — `CATALOG_PER_PAGE = 100` is a **local** module constant in `http-incidents-source.ts`, duplicated from `features/cleaning/data/http/http-cleaning-source.ts` on purpose (design D7 rejects a shared `features/users` module). No `status` filter, mirroring `listCleaners`.
- `listTechnicians` uses the explicit generic form `this.client.request<"/api/v1/users", "GET">(...)` (same as `listCleaners`) — required because `/api/v1/users` also has a `POST` whose response type differs (`CreatedUserResponse`), so the default-method inference would otherwise be ambiguous. **Gotcha for anyone auditing route coverage**: the test file's "declares exactly the routes" whitelist test scans for the literal `this.client.request("...")` immediately followed by a string; the generic-annotated call does **not** match that regex, so `/api/v1/users` never appears in `declaredRoutes` there — this is pre-existing regex behavior (the same non-detection applies to `listCleaners` in `cleaning`, which has no such whitelist test at all), not something section 2 introduced or needs to fix.
- `triageIncident` builds its body with the same conditional-spread idiom `listIncidents` already uses for its query object: `...(input.category !== undefined ? { category: input.category } : {})` etc. An input with zero fields sends `body: {}` (still a valid no-op `PATCH`, per the entity's own contract quoted in section 1's notes). `estimated_cost` is always the string given — never coerced to `number`.
- `assignIncident` sends `technician_id` always and spreads `assignment_note` only when present (same conditional-spread idiom) — omitting it is NOT the same on the wire as sending `assignment_note: undefined` via a plain object literal (the latter would still satisfy `toEqual`-style assertions since Jest/Vitest matchers treat `undefined`-valued keys as absent, but the conditional-spread form is what the rest of the file already does, so section 2 kept it consistent).
- `cancelIncident` and `classifyIncident` are the simplest shape: `POST` with `pathParams` only, no `body` key at all (matches `reject`/`waitParts`/`resume`'s existing style).
- The test file's route whitelist (`the surface these screens can reach`) needed its expected `Set` extended with `/api/v1/incidents/{incident_id}/classify`, `/api/v1/incidents/{incident_id}/assign`, `/api/v1/incidents/{incident_id}/cancel`. `PATCH /api/v1/incidents/{incident_id}` reuses the existing `getIncident` route string, so no new entry was needed for `triageIncident`.
- No hooks, no hook input mapping, no permission checks, no UI/i18n were touched — those are sections 3-5 as scoped. `useIncidentManagement`-style hooks in section 3 should take `TriageIncidentInput`/`AssignIncidentInput` from `http-incidents-source.ts` rather than re-declaring the shape.
- Verification: scoped file `docker compose exec -T frontend npx vitest run features/incidents/data/http/http-incidents-source.test.ts` → 37/37 passed (24 pre-existing + 12 new — one new describe block per method, `listTechnicians` and `triageIncident` each got more than one test). Full suite `docker compose exec -T frontend npm test` → 215 files / 2189 tests passed (baseline was 215/2177; +12 matches the scoped delta exactly). `docker compose exec -T frontend npm run typecheck` → clean, no new errors.

### Section 3 (frontend: hooks of mutation + technician roster)

- New file `frontend/features/incidents/hooks/use-incident-management.ts` exports: `useTechnicianDirectory()`, `useClassifyIncident()`, `useTriageIncident()`, `useAssignIncident()`, `useCancelIncident()`, plus their variables types `ClassifyIncidentVariables { incidentId }`, `TriageIncidentVariables extends TriageIncidentInput { incidentId }`, `AssignIncidentVariables extends AssignIncidentInput { incidentId }`, `CancelIncidentVariables { incidentId }`.
- `TriageIncidentInput`/`AssignIncidentInput` are imported directly from `../data/http/http-incidents-source` (a `type`-only import), NOT from `../data` — `data/index.ts` only re-exports `dto.ts` (`export type * from "./dto"`), and those two input interfaces live in `http-incidents-source.ts`, not `dto.ts`. Section 2's own note ("hooks in section 3 should take `TriageIncidentInput`/`AssignIncidentInput` from `http-incidents-source.ts`") anticipated exactly this; `data/index.ts` was left untouched (out of scope).
- All four mutations share a private `invalidateIncidentQueries(queryClient, tenantId, incidentId)` helper that awaits `Promise.all` over `detail`/`context`/`listPrefix` — `useCancelIncident` calls it plus the three dashboard keys, all six awaited together in one `Promise.all` in its own `onSettled`, per D8.
- `useTechnicianDirectory` uses `enabled: Boolean(tenantId)` (tenantId can be `undefined` before auth resolves) and passes `incidentsKeys.technicians(tenantId ?? "")` as the query key — mirrors the guard shape `useCleanerDirectory`'s sibling hooks use elsewhere in the codebase for an optional tenant, since `useAuth()` here (unlike `useCleaningTasks`'s throwing `useTenantId`) is the non-throwing `use-incident-cycle.ts` style requested by the task brief.
- Query-keys test file `query-keys.test.ts` (already existed) got a new case for `technicians`, added to `forTenant()` and asserted distinct from the other four resource keys — 7/7 tests pass (was 6).
- Barrel `frontend/features/incidents/index.ts` now also exports the four mutation hooks, `useTechnicianDirectory`, and the four `*Variables` types.
- Section 4/5 gotcha: `useAssignIncident`'s variables flatten `AssignIncidentInput` (`technicianId`, optional `assignmentNote`) alongside `incidentId`; `useTriageIncident`'s flatten `TriageIncidentInput` (`category?`, `severity?`, `estimatedCost?`). Neither hook does client-side validation (cost format, control characters, note length) — that is section 4's `manager-action-error.ts`/`cost-format.ts`/`assignment-note.ts`, not this layer.
- Section 4/5 gotcha: none of these four hooks reads or gates on `IncidentStatus` — the status→action table (`manager-actions.ts`) and the `409`-reason mapping (`manager-action-error.ts`, D9's `triage`-on-`OPEN` nuance) both live in section 4 and must call `conflictReason`/read the refreshed `status` themselves after `onSettled`'s invalidation lands, same pattern `use-incident-cycle.ts`'s existing consumers already use.
- Verification: scoped `docker compose exec -T frontend npx vitest run features/incidents/hooks/use-incident-management.test.tsx features/incidents/hooks/query-keys.test.ts` → 16/16 passed (9 new in `use-incident-management.test.tsx` + 7 in `query-keys.test.ts`, one of which is new). Full suite `docker compose exec -T frontend npm test` → 216 files / 2199 tests passed (baseline 215/2189; +1 file, +10 tests, matches the scoped delta exactly — no regressions). `docker compose exec -T frontend npm run typecheck` → clean, no new errors.

### Section 4 (frontend: manager action table, error mapping, client guards)

- `manager-actions.ts` exports `ManagerAction`, `MANAGER_ACTIONS` (frozen `Record<IncidentStatus, readonly ManagerAction[]>`), `managerActions(status)`, and `managerStatusNote(status): "awaiting-owner" | null` — all mirroring `features/tech/lib/tech-actions.ts`'s `Object.hasOwn`-guarded reader pattern exactly. Neither export is re-exported from the feature barrel (`index.ts`) — like `tech-actions.ts`, section 5's UI components import them directly from `../../lib/manager-actions`, not through `@/features/incidents`.
- `manager-action-error.ts` exports `ManagerActionErrorReason = "closed" | "awaiting-owner" | "out-of-order" | "triage-not-classified" | "invalid-technician" | "invalid-cost"` and `managerActionMessage(error: unknown, refreshedStatus: IncidentStatus, action: ManagerAction): ManagerActionErrorReason | undefined`. These are **bare reason strings, not full i18n keys** — section 5 interpolates, e.g. `t(\`incidents:manager.conflict.${reason}\`)` for the first four values or `t(\`incidents:manager.errors.${reason}\`)` for the last two (pick whichever locale-key nesting section 5.5 actually uses; the file's own docstring already documents this exact scheme). `403` is NOT handled by this function — hiding the whole action section on `403` is section 5's job. Also not re-exported from the barrel (same reasoning as `manager-actions.ts`).
- Both `422`s (`assign`'s `InvalidTechnicianError` and `triage`'s `MaintenanceValidationError`) share `ErrorCode.VALIDATION_ERROR` on the wire (`backend/app/maintenance/api/errors.py:46,53`) — `managerActionMessage` distinguishes them by the `action` parameter, never by inspecting `error.code`. Section 5 must always pass the correct `action` when calling this function.
- `cost-format.ts` exports `isPositiveDecimal(value: string): boolean` (extracted verbatim from `resolve-incident-dialog.tsx`'s `POSITIVE_DECIMAL` regex) and **is** re-exported from the barrel (`export { isPositiveDecimal } from "./lib/cost-format";` in `index.ts`) because its other consumer, `resolve-incident-dialog.tsx`, lives outside `features/incidents` (in `features/dashboard/stalls/components/`) and can only reach it through the barrel. `resolve-incident-dialog.tsx` had **two** call sites using the old local constant (lines ~100 and ~173 in the pre-section-4 file) — both now call `isPositiveDecimal(...)`; the local `POSITIVE_DECIMAL` constant is gone.
- `assignment-note.ts` exports `MAX_ASSIGNMENT_NOTE_LENGTH = 2000` and `hasControlCharacters(value: string): boolean` (regex `/[\x00-\x08\x0B-\x1F]/`, i.e. `U+0000`-`U+001F` minus tab `U+0009` and line feed `U+000A` — a lone carriage return `U+000D` is deliberately still rejected, since R2.3's text excepts only "salto de línea y tabulador"). Not re-exported from the barrel — section 5's assign `Sheet` imports it directly from `../../lib/assignment-note`, same pattern as the two files above.
- No React components, no locale files, no permission checks were touched — purely `.ts` logic files and their unit tests, per section 4's scope.
- Verification: scoped `docker compose exec -T frontend npx vitest run features/incidents/lib/manager-actions.test.ts features/incidents/lib/manager-action-error.test.ts features/incidents/lib/cost-format.test.ts features/incidents/lib/assignment-note.test.ts features/dashboard/stalls/components/resolve-incident-dialog.test.tsx` → 5 files / 77 tests passed (20 + 15 + 21 + 12 + 9), no regressions in the dashboard dialog's own pre-existing tests.

### Section 5 (frontend: manager UI, permission mirror, technician name)

- `lib/auth/permissions.ts`: `MANAGE_INCIDENTS` added to the `Permission` union and to `ROLE_UI_PERMISSIONS.PROPERTY_MANAGER` only — verified against `backend/app/auth/domain/policy.py:251`'s `_INCIDENT_MANAGE = frozenset({READ_INCIDENTS, MANAGE_INCIDENTS})`, granted only to `PROPERTY_MANAGER` (`policy.py:406`); `TENANT_OWNER` keeps `_INCIDENT_READ` only. No other role touched.
- `DetailAssignedTechnicianBlock` (`incident-detail-sections.tsx`) now takes `{ assignedTechnicianId, technicianName: string | null }`: `assignedTechnicianId` still decides whether the section renders at all; `technicianName` (or the localized "not available" fallback) is the only value ever painted — the UUID is no longer a prop the component can print. The old `<p role="note">` explaining "identifier cannot be resolved" is deleted entirely (D10 makes it false). `fields.assignedTechnicianNote` removed from both locale files; `fields.technicianNotAvailable` added instead.
- `IncidentDetailView` calls `useHasPermission("MANAGE_INCIDENTS")` and `useTechnicianDirectory()` unconditionally, before any early return (rules-of-hooks) — the directory call is **not** gated by the permission, so `TENANT_OWNER` (who has `READ_USERS` but not `MANAGE_INCIDENTS`) gets the same name resolution as the manager. Name resolution: `technicians.data?.find(t => t.id === assignedTechnicianId)?.name ?? null` — `null` covers "still loading", "not in roster" and "query failed" uniformly, all rendered as `fields.technicianNotAvailable`. `ManagerIncidentActions` is only referenced (imported and mounted) behind `canManage` — no reserved slot.
- New `manager-incident-actions.tsx` composes, in order: a `Card` with the title + status note + the up-to-4 action buttons + inline info/error text for `classify`/`triage` (which have no modal), then three always-mounted-but-content-gated children: `AssignSheet` (native `<select>` of `.isActive` technicians, starts unselected even on reassign, `<textarea>` note gated by `MAX_ASSIGNMENT_NOTE_LENGTH`/`hasControlCharacters`), `TriageSheet` (category/severity `<select>`s + cost `<input type="number">`, all precharged from `incident`, diffed against the incident's current values so only changed fields reach `mutate`), `CancelDialog` (`AlertDialog`, no reason field). Three render branches at the top level, in priority order: (1) any of the four mutations carries a `403` → forbidden text only, nothing else, matching `fields.forbidden`; (2) `managerActions(status).length === 0` (`RESOLVED`/`CANCELLED`) → `manager.none`, but a still-live `409` from a mutation attempted moments before the status flipped closed is shown above it (mirrors `TechCycleActions`'s `cycleError`-above-`actions.none` pattern) — this is the only reachable path for the "closed" conflict reason, since once actions are empty there is no button left to reopen a Sheet/AlertDialog that would show it inline; (3) normal render.
- **`AlertDialogAction` gotcha (load-bearing for cancel's error handling)**: Radix's `AlertDialogAction` is `DialogPrimitive.Close` under the hood (confirmed by reading `node_modules/@radix-ui/react-alert-dialog/dist/index.mjs` and `@radix-ui/react-dialog`'s `Close` — `onClick: composeEventHandlers(props.onClick, () => context.onOpenChange(false))`, and `composeEventHandlers` skips the second handler when `event.defaultPrevented`). So it closes unconditionally on click unless the passed `onClick` calls `event.preventDefault()`. `CancelDialog.handleConfirm` does exactly that, and only calls `onOpenChange(false)` itself inside the mutation's `onSuccess` — a `409` on cancel therefore keeps the dialog open with `manager.conflict.<reason>` shown, instead of silently closing on a failed cancel.
- Error/info text placement, by design: `assign`/`triage`/`cancel` show their own mutation's error **inside** their own Sheet/AlertDialog body (matches `resolve-incident-dialog.tsx`/`cancel-cleaning-dialog.tsx`: not closing on error keeps the control open with the message, satisfying R2.5's "keep the control open" without special-casing it). `classify` (no modal) and the two post-triage success informationals (R3.3/R3.4) render inline in the outer `Card`, keyed off `mutation.isSuccess && mutation.data?.status === "..."` rather than any extra local state — this is what makes R3.3/"still `AWAITING_OWNER_APPROVAL`" and R3.4/"still `OPEN`" survive the Sheet closing on success.
- Locale keys added (both `locales/es/incidents.json` and `locales/en/incidents.json`, top-level key `manager`, i18n namespace `incidents` throughout — no new namespace):
  - `manager.title`, `manager.none`
  - `manager.actions.{classify,assign,reassign,triage,cancel,sending}`
  - `manager.statusNote.awaiting-owner` (R1.4)
  - `manager.assign.{title,titleReassign,technicianLabel,technicianPlaceholder,noteLabel,notePlaceholder,noteHelp,reassignWarning,confirm,sending}` + `manager.assign.errors.{noteTooLong,noteControlChars}`
  - `manager.triage.{title,categoryLabel,severityLabel,costLabel,costPlaceholder,costHelp,confirm,sending}` + `manager.triage.errors.invalidCost`
  - `manager.cancel.{title,body,confirm,keep,sending}`
  - `manager.conflict.{closed,awaiting-owner,out-of-order,triage-not-classified}` — the four `409`-family `ManagerActionErrorReason` values, rendered via `t(\`manager.conflict.${reason}\`)`
  - `manager.errors.{invalid-technician,invalid-cost}` — the two `422` reasons, rendered via `t(\`manager.errors.${reason}\`)`
  - `manager.postClassify.stillOpen` (R4.2), `manager.postTriage.{awaitingApproval,stillOpen}` (R3.3/R3.4)
  - `fields.technicianNotAvailable` added under the existing `fields` key (D10/R2.6) — kept there, not under `manager`, since it is painted by `DetailAssignedTechnicianBlock` for every viewer, manager or not.
  - `fields.assignedTechnicianNote` removed from both files (no longer true once names resolve).
- Tests: new `manager-incident-actions.test.tsx` (25 tests) mocks `react-i18next` to the identity function (`t(key) => key`, same pattern as `blocked-transitions-section.test.tsx`) so assertions read exact key names (`manager.conflict.closed`, etc.) rather than depending on locale copy, and mocks `../../hooks/use-incident-management` wholesale (all five hooks) with mutable hoisted state objects — no `QueryClientProvider` needed since nothing inside calls real TanStack Query. Covers: the state→action table for `OPEN`/`CLASSIFIED`/reassign-label/`AWAITING_OWNER_APPROVAL`/`RESOLVED`/`CANCELLED`; `403` hides everything; all three `409` conflict reasons plus the `triage`-on-`OPEN` nuance (via the "no actions" branch for `closed`, inside `CancelDialog` for `awaiting-owner`, inside `AssignSheet` for `out-of-order`, inside `TriageSheet` for the nuance); the `422` invalid-technician message with an assertion that the technical `error.message` text never appears in the document; client-side rejection of an over-length note and a note with a control character (asserted via `disabled` + `mutate` not called — clicking a genuinely `disabled` button does not fire its handler in jsdom, same precedent `resolve-incident-dialog.test.tsx` already relies on); triage's cost validation (a syntactically-valid-but-negative number, since jsdom coerces non-numeric text in a `type="number"` input to empty, which would make `hasChanges` false instead of `costValid` false — see the comment in that test); the two post-triage/post-classify informational messages; cancel's confirm flow with no reason ever sent.
- `incident-detail-view.test.tsx` updated: mocks `@/lib/auth` (`useHasPermission`), `../../hooks/use-incident-management` (`useTechnicianDirectory`) and `./manager-incident-actions` (a `data-testid` stub) so this file stays scoped to "does the gate mount/not-mount the real component" and "does the name resolve", leaving `ManagerIncidentActions`'s own internal behaviour to its own test file. Replaced the old UUID-printing assertions with: resolved-name rendering, "not available" when the roster doesn't contain the id, "not available" when the directory query fails, and the same resolution working identically regardless of `useHasPermission`'s return value (manager vs. `TENANT_OWNER`).
- Verification: scoped `docker compose exec -T frontend npx vitest run features/incidents/components/detail/manager-incident-actions.test.tsx features/incidents/components/detail/incident-detail-view.test.tsx` → 2 files / 45 tests passed (25 + 20). `docker compose exec -T frontend npm run typecheck` → clean, no new errors.
- **Full suite (`docker compose exec -T frontend npm test`) could not be completed in this environment**: three consecutive attempts were killed by the host (exit 137 / OOM — confirmed via `docker stats`: half a dozen other worktrees' compose stacks were live concurrently, `vm_stat` showed ~64MB free) before reaching a final tally, consistent with the memory-pressure warning in this section's brief. The furthest run reached line 638 of output (`components/ui/sheet.test.tsx`, roughly two-thirds through the suite by file count) before being `Killed`. Every file this section touched or added ran cleanly in that partial run: `incident-detail-view.test.tsx` (20/20), `manager-incident-actions.test.tsx` (25/25), plus the untouched-but-adjacent `use-incident-management.test.tsx` (9/9), `use-incidents.test.tsx` (7/7), `incidents-view.test.tsx` (14/14), `incidents-filters.test.tsx` (5/5) — all green. The partial run did show ~15 unrelated failing tests in files this section never touched (`test/color-tokens.test.ts`, `test/theme-client-state.test.ts`, `app/route-wiring.test.tsx`, `app/segment-errors.test.tsx`, `features/cleaning/components/cleaning-pagination.test.tsx`, `features/platform/components/create-tenant-form.test.tsx`, `features/cleaner/components/detail/cleaner-task-checklist-item.test.tsx`, `features/pricing/components/{pricing-tabs,rule-filters,recommendation-filters}.test.tsx`, `features/notifications/components/notification-bell.test.tsx`, `features/dashboard/components/detail/property-detail-view.test.tsx`, `features/platform/components/platform-pagination.test.tsx`); `git diff --stat` against every one of those paths (and their test files) is empty, and their failures carry anomalously long durations (5-20s for what are simple assertions elsewhere in the same file) consistent with host contention, not a regression — matching the "suite-flake-is-host-contention-not-regression" pattern. A definitive before/after file-and-test-count comparison against the stated 220-file/2267-test baseline was not obtainable given the repeated OOM kills; whoever runs `/sdd:review` or the section 7 verification pass should retry `npm test` once host memory has freed up (e.g. after other worktree stacks are stopped) to get that final tally.

- **Review fix round**: `manager-incident-actions.tsx` — `classifyStillOpen`/`triageAwaitingApproval`/`triageStillOpen` (R4.2/R3.4/R3.3) were reading `classify.data?.status`/`triage.data?.status`, a snapshot frozen at whichever moment that specific mutation last resolved, never reset by a *different*, later-succeeding mutation (e.g. a triage that transitions `OPEN`→`CLASSIFIED` via `classify_by_triage` left a stale "still needs classification" notice standing alongside the new `CLASSIFIED` action set). Fixed by keying all three off the current `incident.status` prop instead (already the freshest value, since every mutation's `onSettled` invalidates `incidentsKeys.detail`), keeping `isSuccess` as the guard against showing this on a plain page load where the incident merely happens to already be `OPEN`. New test in `manager-incident-actions.test.tsx`: "clears a stale classify 'still open' notice once a later triage classifies the incident (R4.2, R3.4)" — classify succeeds leaving `OPEN` (asserts the notice shows), then triage succeeds moving to `CLASSIFIED` with a `rerender` carrying the updated `incident` prop (asserts the notice is gone). 26/26 tests pass.
- **Review fix round**: `permissions.test.tsx` had zero coverage for `MANAGE_INCIDENTS` (R1.1) despite every sibling mirrored permission having explicit grant/deny-per-role tests. Added four cases mirroring the `EXECUTE_INCIDENTS` precedent: grants to `PROPERTY_MANAGER`, denies to `TENANT_OWNER`/`TECHNICIAN`/`CLEANER`/`SUPER_ADMIN`, denies without an authenticated user. No change to `permissions.ts` — the mapping was already correct. 24/24 tests pass.

### Section 6 (documentation)

- `docs/maintenance.md`: **"El ciclo, de principio a fin"** diagram now shows two paths from `OPEN` to `CLASSIFIED` — the existing automatic classifier (job / `POST /incidents/{id}/classify`) and the new human one (`PATCH /incidents/{id}` with both `category` and `severity`, `classify_by_triage`), converging on the same `CLASSIFIED` node and the same HIGH/CRITICAL property trigger. A new prose paragraph right after the diagram (before "Ninguna de esas flechas...") spells out the negative cases (one field only, or non-`OPEN` origin → no transition, just annotation), the combined case (same call classifies **and** opens the approval gate, passing through `CLASSIFIED` to `AWAITING_OWNER_APPROVAL` in one request), and the pre-existing asymmetry (triaging only `estimated_cost` above threshold on `OPEN` still gives `409`, not a graceful `OPEN`).
- `docs/maintenance.md`: **"Quién puede hacer qué"** gains a 4th bullet (the intro count "Tres cosas..." was bumped to "Cuatro cosas...") noting the four manager actions are now operated from `/incidents/[id]` in the browser (previously only `backend/app/cli/seed_demo.py` called them in production), and that the assigned technician is now shown by name to anyone who opens the screen.
- **Deviation from the literal two-section scope, flagged rather than silently done**: the "Cómo se ven desde la web" section (near the end of the file) directly contradicted the two edits above — it still said the screen was read-only and that `assigned_technician_id` couldn't be resolved to a name ("no hay `GET /api/v1/users` en el contrato"), and listed the four manager operations as "quedan fuera de esa pantalla". Both claims are now false because of this exact change, so leaving them would have created an in-file contradiction the task's own verification step ("no contradicen nada más en el fichero") is meant to catch. Updated that section's intro paragraph and its first bullet accordingly (removed the now-false "solo lectura" framing and the UUID-not-resolved note; removed the "cuatro operaciones del manager... quedan fuera" bullet since they no longer are; kept "Responder una aprobación" and "Selector de propiedad" as still-genuinely-out-of-scope, and trimmed the second one's now-obsolete "resolución nombre↔id" half). No other section touched.
- Diagram regenerated at **`docs/diagrams/2026-09-05_autohost-secuencia-mantenimiento.png`** (today's date) via Mermaid (`mmdc`), replacing `docs/diagrams/2026-08-23_autohost-secuencia-mantenimiento.png` (deleted). Content derived fresh from `backend/app/maintenance/domain/entities.py`'s `_TRANSITIONS` (all 13 rows, including the new `classify_by_triage`) and from `backend/app/maintenance/application/use_cases.py` (`ClassifyIncidentUseCase`, `TriageIncidentUseCase`, `AssignIncidentUseCase`, the four `_TechnicianStepUseCase` subclasses, `ResolveIncidentUseCase`, `RespondOwnerApprovalUseCase`) — read directly, the previous PNG was never opened. New step shown alongside the automatic classifier: origin `OPEN`, destination `CLASSIFIED`, route `PATCH /incidents/{incident_id}`, event `INCIDENT_CLASSIFIED` (actor `USER`), same HIGH/CRITICAL property trigger as the automatic path. The rest of the sequence (technician cycle incl. the `en_route`/`reject`/`resume_work` timeline-event nuances, both owner-approval gates, SLA) is represented unchanged from the prior regeneration, re-derived from the current code.
- `sdd/steering/architecture.md` updated: the `Diagramas:` line (line 7) now references the new filename; a new paragraph was added at the top of the diagram's running history (before the `tech-cycle-completion` paragraph, which now starts with "El anterior se regeneró en...") naming `incident-triage-web`, the new `classify_by_triage` step (origin/destination/route/event) and confirming the regeneration was done without opening the previous PNG, derived from `_TRANSITIONS` and the use cases. No other diagram or section of that file touched.
- Section 7 (Verification) should know: the diagram file to check for is `docs/diagrams/2026-09-05_autohost-secuencia-mantenimiento.png`; no code was touched in this section, so no test suite reruns are needed for it specifically.
