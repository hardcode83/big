# Tasks: ci-pr-gates-optimization

<!-- Section heading markers, read by /sdd:run (HTML comments appended to the
     heading, invisible when rendered): "hard" makes that section's implementer
     run on the stronger model; "panel: PASS <date>" is written by run itself
     when the section's review panel passes. -->

## 1. Backend dependency cleanup — retire `pytest-cov` (D6) <!-- panel: PASS 2026-09-03 -->

- [x] 1.1 `backend/pyproject.toml` — eliminar `"pytest-cov>=7.0.0"` del array `[dependency-groups].dev` (líneas 59-68). Verificación: `grep -n pytest-cov backend/pyproject.toml` devuelve cero resultados. [R6.3, R6.4]
- [x] 1.2 `backend/uv.lock` — regenerar el lockfile con `docker compose run --rm -T backend uv lock` (mismo entorno que usa la suite). Verificación: `grep -c pytest-cov backend/uv.lock` devuelve `0`. Si el lockfile sigue listándolo como dependencia transitiva, revisar `backend/uv.lock:129, 157, 1028` y forzar la regeneración con `uv lock --upgrade` solo si la causa es otra dependencia que ya no la arrastra. [R6.3]
- [x] 1.3 Verificación local: `docker compose exec backend uv sync --frozen && docker compose exec backend uv run pytest -q -rs -n 2 tests/` debe pasar verde y **sin warning de "coverage"** en la salida. Comando exacto del flujo SDD según `sdd/project.md`. [R6.4]
- [x] 1.4 Verificación de retirada efectiva de `pytest-cov` (dos checks independientes):
  - **(a) Cero dependencia activa:** `grep -n pytest-cov backend/pyproject.toml backend/uv.lock` devuelve cero resultados. (Las menciones documentales en `sdd/specs/`, `sdd/changes/<este>/proposal.md`, `sdd/changes/<este>/design.md`, `sdd/changes/<este>/tasks.md` no cuentan: son legítimas y reflejan que el change documenta la retirada).
  - **(b) Cero invocación:** `grep -rn "pytest.*cov\|--cov\|pytest-cov" .github/workflows/ backend/tests/ Makefile backend/pyproject.toml` (excluyendo `sdd/`) devuelve cero resultados en invocaciones reales. (No se ejecuta `pytest --cov` en ningún workflow ni en ningún target ejecutable del Makefile).
  - Si `sdd/specs/backend-ci.md` mencionaba `pytest-cov` en su forma anterior, ese punto se cubre en la sección 6 (Specs). [R6.1, R6.3]

## 2. Playwright cache — exact, sin `restore-keys` (D3) <!-- panel: PASS 2026-09-03 -->

- [x] 2.1 `.github/workflows/frontend-tests.yml` — añadir `actions/cache` para `~/.cache/ms-playwright` con key **exacta** `playwright-${{ hashFiles('frontend/package-lock.json') }}`. **NO** incluir `restore-keys:` (ni `playwright-` ni cualquier prefijo parcial). Comentario inline en el yaml explicando que la cache es exacta a propósito: un lockfile nuevo debe producir miss, no restaurar silenciosamente browsers de otro lockfile (riesgo de binario desactualizado con CVEs). **Pin por SHA obligatorio** (R8.5, coherente con el resto del repo): `actions/cache@<sha> # v4` donde `<sha>` es la **revisión autorizada de `actions/cache@v4`** — el implementador la verifica en el repo upstream antes de fijar el valor, sigue la misma convención que `actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4` y `actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4`. Está prohibido `actions/cache@v4` flotante. [R2.1, R2.2, R8.5]
- [x] 2.2 Misma workflow — el step de install (`npm exec --no -- playwright install --with-deps chromium`) **se ejecuta siempre** (`if` ausente), en cache hit y en cache miss. El step de `actions/cache` para save va con `if: steps.cache-restore.outputs.cache-hit != 'true'`, pinneado por SHA (mismo SHA que 2.1). **NO** añadir el `if: steps.cache-restore.outputs.cache-hit != 'true'` al install (esa era la versión incorrecta que rompía la verificación de system deps). [R2.2, R2.3, R2.4, R8.5]
- [x] 2.3 Verificación local del comportamiento de cache hit/miss:
  - **(a) Mismo lockfile (cache hit esperado):** dos ejecuciones consecutivas del workflow sobre el mismo SHA con el mismo `frontend/package-lock.json` sin tocar — el log muestra `cache-hit: true` en la segunda ejecución.
  - **(b) Lockfile distinto (cache miss esperado):** regenerar `frontend/package-lock.json` con `npm install <un-paquete>@<nueva-versión>` (o `rm frontend/package-lock.json && npm install` para forzar hash nuevo) y re-ejecutar — el log muestra `cache-hit: false` y el browser binary se descarga de nuevo.
  - **No** usar cambios únicamente en `frontend/package.json` para demostrar cache miss: la key es `hashFiles('frontend/package-lock.json')`, no `package.json`. Cambiar `package.json` sin regenerar el lockfile **no** cambia la key.
  - Documentar ambos resultados en `sdd/changes/ci-pr-gates-optimization/metrics.md` como entrada de baseline de cache hit rate. [R2.4]

## 3. Frontend three-job restructure — `detect` + `suite` condicional + consolidador (D1+D2) <!-- hard --> <!-- panel: PASS 2026-09-03 -->

> El cambio más complejo del SDD: tres jobs nuevos, fusión de las 9 señales en `frontend-tests-suite`, consolidador con tabla markdown, preservation de R1.1. Está marcado como `<!-- hard -->` por la varianza real del shape de outputs por señal y por el cuidado en no romper los SHAs pinneados ni la regla `npm ci` limpio. [R1, R3, R8.5]

- [x] 3.1 `.github/workflows/frontend-tests.yml` — crear el job `frontend-tests-detect`. Réplica literal de la lógica de `backend-tests-detect` (`backend-tests.yml:106-260`): `git diff -z --no-renames`, `case "$f" in frontend/* | frontend/devops/Dockerfile | frontend/package.json | frontend/package-lock.json | .github/workflows/frontend-tests.yml)`, arranca en `true` y baja solo con afirmación positiva. Outputs: `frontend: ${{ steps.decide.outputs.frontend }}` y `reason: ${{ steps.decide.outputs.reason }}`. SHAs pinneados: `actions/checkout@11d5960a326750d5838078e36cf38b85af677262`. Mantener `persist-credentials: false` y `fetch-depth: 0` (necesario para el diff). [R1.2, R1.5, R1.6, R1.7, R5.3, R8.5]
- [x] 3.2 Misma workflow — crear el job `frontend-tests-suite` con `if: needs.frontend-tests-detect.outputs.frontend == 'true'`. Estructura interna: setup Node 22 (mismo SHA que ya hay: `actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020`) con `cache: npm` keyed por `frontend/package-lock.json`; **un único `npm ci`**; tres precondiciones (check-version-parity, extract-pr.sh --self-test, validate-provenance-contract.py --self-test); cinco señales de provenance (api:check + features/provenance, build con APP_PROVENANCE_*, test:public-artifacts); cuatro señales de suite (npm test, npm run test:layout con Playwright cache de la sección 2, lint, typecheck). `continue-on-error: true` en cada verificación individual (preserva la semántica del workflow actual). Outputs por señal vía `outputs.<signal>_outcome: ${{ steps.<id>.outputs.outcome }}`. [R3.1, R3.2, R3.3]
- [x] 3.3 Misma workflow — crear el job `frontend-tests` consolidador con `if: always()`. Shape de veredicto (mismo árbol de casos que `backend-tests.yml:449-577`, replicado palabra por palabra): si `DETECT_RESULT != success` → fail; si `SUITE_RESULT == success` → pass; si `SUITE_RESULT == skipped` y `DETECT_FRONTEND == false` → pass con línea `**OMITIDA** (no ejecutada)` y el motivo; si `SUITE_RESULT == skipped` y `DETECT_FRONTEND == true` → fail (incoherencia); si cualquier outcome por señal != `success` → fail nombrando la señal. Tabla markdown en `GITHUB_STEP_SUMMARY`. `timeout-minutes: 5`. [R1.1, R1.7, R5.3]
- [x] 3.4 Misma workflow — eliminar el job `provenance-contract` y el job `frontend-tests` originales (todos sus pasos se redistribuyen en 3.2 y 3.3). Preservar los SHAs pinneados de `actions/checkout` y `actions/setup-node` (R8.5). No introducir `actions/upload-artifact`/`download-artifact` ni cache de `node_modules`. Mantener `concurrency.cancel-in-progress: true` y `timeout-minutes: 10/15/5` por job (no se mueven timeouts — son del workflow actual). [R3.1, R8.5, R8.6]
- [x] 3.5 Verificación estática del árbol `detect → suite → consolidator` en `frontend-tests.yml`: leer el workflow en disco (no crear commits/PRs/PRs dummy/docs/foo.md ni frontend/lib/foo.ts) y comprobar:
  - Existen exactamente los jobs `frontend-tests-detect`, `frontend-tests-suite`, `frontend-tests`.
  - `frontend-tests-suite` declara `if: needs.frontend-tests-detect.outputs.frontend == 'true'`.
  - `frontend-tests` declara `if: always()` y `needs: [frontend-tests-detect, frontend-tests-suite]`.
  - El paso `decide` del job detect publica `outputs.frontend` y `outputs.reason` con el conjunto cerrado de literales documentado en `design.md` D1.
  - El árbol de veredicto del consolidador replica `backend-tests.yml:449-577` (cinco casos enumerados: `DETECT_RESULT != success`, `SUITE_RESULT == success`, `SUITE_RESULT == skipped && DETECT_FRONTEND == false` → pass con `**OMITIDA**`, `SUITE_RESULT == skipped && DETECT_FRONTEND == true` → fail, cualquier outcome por señal != `success` → fail).
  - No existen `actions/upload-artifact` ni `actions/download-artifact` para `node_modules` o `.next/`; el `npm ci` aparece exactamente una vez en el workflow. [R1.1, R1.3]

## 4. Compose-ports + Rule11-ownership three-job restructure (D1+D5) <!-- panel: PASS 2026-09-03 -->

- [x] 4.1 `.github/workflows/compose-ports.yml` — crear el job `compose-ports-detect` con la misma forma que `frontend-tests-detect` (3.1). Anclas: `docker-compose*.yml`, `docker-compose*.yaml`, `docker-compose.worktree.yml`, `.github/workflows/compose-ports.yml`. Outputs: `compose: ${{ steps.decide.outputs.compose }}` y `reason`. [R5.1, R5.2]
- [x] 4.2 Misma workflow — crear `compose-ports-suite` con `if: needs.compose-ports-detect.outputs.compose == 'true'`. Contenido: `make check-compose-ports` + `uv run --no-project --with 'pytest==9.1.1' python -m pytest scripts/ -q` (preservar la invocación exacta actual). Setup: `astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9`. [R5.1]
- [x] 4.3 Misma workflow — crear el job `compose-ports` consolidador con `if: always()`. Misma forma que 3.3: tabla markdown, veredicto explícito, fallo si `DETECT_RESULT != success` o `SUITE_RESULT == skipped && DETECT_COMPOSE == true`, pass con motivo de skip si área no tocada. [R1.1, R5.3]
- [x] 4.4 `.github/workflows/rule11-ownership.yml` — crear `rule11-ownership-detect` con anclas: `sdd/steering/*`, `sdd/specs/*`, `backend/app/*`, `backend/tests/*`, `docs/*`, `.github/workflows/rule11-ownership.yml`. Outputs: `rule11: ...` y `reason`. [R5.2]
- [x] 4.5 Misma workflow — crear `rule11-ownership-suite` con `if: needs.rule11-ownership-detect.outputs.rule11 == 'true'`. Contenido: `make check-rule11-ownership` + `uv run --no-project --with 'pytest==9.1.1' python -m pytest scripts/test_rule11_ownership.py -q` (preservar la invocación exacta). [R5.2]
- [x] 4.6 Misma workflow — crear `rule11-ownership` consolidador con `if: always()`, análogo a 3.3. [R1.1, R5.3]
- [x] 4.7 Verificación estática del árbol `detect → suite → consolidator` en `compose-ports.yml` y `rule11-ownership.yml`: leer ambos workflows en disco (no crear PRs dummy/PRs sintéticos/docs/foo.md/sdd/steering/security.md) y comprobar:
  - Cada workflow tiene exactamente tres jobs: `compose-ports-detect` + `compose-ports-suite` + `compose-ports`; `rule11-ownership-detect` + `rule11-ownership-suite` + `rule11-ownership`.
  - Cada `*-suite` declara `if: needs.<detect>.outputs.<area> == 'true'` (donde `<area>` es `compose` o `rule11` respectivamente).
  - Cada consolidador declara `if: always()` y los `needs` correctos.
  - Las anclas del `case` en cada `*-detect` son las declaradas en `design.md` D1 (verificables por inspección directa del yaml).
  - El árbol de veredicto del consolidador replica la forma del backend-tests: pass con motivo legible ante skip legítimo, fail ante incoherencia. [R5.1, R5.2, R5.3]

## 5. Buildx cache (D4) <!-- panel: PASS 2026-09-03 -->

- [x] 5.1 `.github/workflows/multiarch-build-check.yml` — añadir `cache-from: type=gha,scope=multiarch-backend` y `cache-to: type=gha,mode=max,scope=multiarch-backend` al `docker/build-push-action@v6` del job `build-backend`. Mismo cambio con `scope=multiarch-frontend` para `build-frontend`. **NO** añadir `cache-to` con scopes que ya usa `deploy-dev` (`deploy-backend`, `deploy-frontend`). [R4.1, R4.2, R4.5]
- [x] 5.2 `.github/workflows/deploy-dev.yml` — añadir `cache-from: type=gha,scope=deploy-backend` y `cache-to: type=gha,mode=max,scope=deploy-backend` al build-backend. Mismo con `scope=deploy-frontend` para build-frontend. **NO** modificar los labels OCI, los build-args, ni el job `provenance` ni el job `deploy` (R8.1, R8.2, R4.4). [R4.3, R4.4, R8.1, R8.2]
- [x] 5.3 **Verificación durante `/sdd:run` (pre-merge, bloqueante):** inspección de configuración. Comprobar que:
  - `cache-from` y `cache-to` están presentes en los 4 builds (`multiarch-{backend,frontend}` y `deploy-{backend,frontend}`).
  - Los scopes son disjuntos entre `multiarch-*` y `deploy-*` (no se solapan).
  - `mode=max` está en los 4.
  - Los labels OCI en los `docker/build-push-action@v6` son los originales (no se han alterado `tags` ni `labels`).
  - El job `provenance` y el job `deploy` de `deploy-dev.yml` están intactos. [R4.4, R8.1]

## 5a. Post-merge observability — non-gating

> **Esta sección NO es una tarea de `/sdd:run`.** Documentada para `/sdd:archive` y seguimiento posterior. **No** hay checkbox aquí. El seguimiento de los 30 días y los targets de métricas **no forman parte** de la completitud de `/sdd:run`, `/sdd:review`, `/sdd:ship` ni `/sdd:archive`.

Observación durante los 30 días posteriores al merge: comportamiento del cache hit rate y del cache miss en builds reales, y validación de las 6 invariantes de D5 sobre imágenes construidas con cache. Documentar en `metrics.md`. Si alguna invariante falla, **rollback de R4** con criterio *"algún elemento de D5 falla"*, no *"digest difiere"*. [R4.4, R8.1]

## 6. Archive prep + deferred-to-archive specs (R7)

> **Regla del proyecto:** `/sdd:archive` es la única fase que crea/actualiza specs vivas en `sdd/specs/`. Durante `/sdd:run` **NO se escribe** en `sdd/specs/*`. Esta sección prepara el material que `/sdd:archive` consumirá.

- [x] 6.1 **Archive prep — material para `/sdd:archive`:** durante `/sdd:run`, el implementador prepara los borradores de actualización para los cinco specs afectados, **sin escribirlos en `sdd/specs/*`**. Cada borrador se guarda en `sdd/changes/ci-pr-gates-optimization/spec-changes/<nombre-spec>.md` con el siguiente contenido:
  - **`<nombre-spec>.md`:** (a) resumen del estado actual del spec en `sdd/specs/<nombre-spec>.md`; (b) lista de los cambios que el archive debe aplicar; (c) texto propuesto, listo para pegar (sin haber tocado el spec vivo).
  - **Specs afectados y cambios propuestos:**
    - `backend-ci.md` — nota sobre el patrón `*-detect` / `*-suite` / `*-tests` replicado a los otros tres workflows; nota explícita de que `pytest-cov` no se invoca en CI y este change no introduce cobertura real.
    - `frontend-ci.md` — patrón de tres jobs (`detect` + `suite` condicional + consolidador); R sobre la caché exacta de Playwright (sin `restore-keys`); `npm ci` único en `frontend-tests-suite`; preservación del resto del contrato.
    - `multiarch-build-check.md` — crear (no existe aún); documentar scopes disjuntos (`multiarch-backend`, `multiarch-frontend`), `mode=max`, invariante de identidad vía referencia a `app-deploy-dev.md` R1.5 y `design.md` D5 (los 6 elementos).
    - `compose-ports.md` — patrón de tres jobs; preservar la postura de red (`specs/compose-ports.md` o crear si no existe).
    - `rule11-ownership-guard.md` — patrón de tres jobs; preservar censo y regla de scope. [R5, R8]

- [x] 6.2 **Spec writes deferred-to-archive:** la escritura real sobre `sdd/specs/*` queda diferida a `/sdd:archive`. El implementador de esta sección registra la lista en `sdd/changes/ci-pr-gates-optimization/BLOCKED.md` como tipo `deferred` (regla 5 del SDD), con la línea exacta a ejecutar en archive: *"`/sdd:archive ci-pr-gates-optimization` consume los borradores de `sdd/changes/ci-pr-gates-optimization/spec-changes/` y los aplica a `sdd/specs/`"*. Si `/sdd:run` finaliza sin ese `BLOCKED.md`, el panel de review lo señalará. **No** se acepta como completa esta sección sin el `BLOCKED.md` registrado.

- [x] 6.3 `sdd/changes/ci-pr-gates-optimization/metrics.md` — **crear**. Dos secciones: **Baseline** (copiar de runs visibles antes del merge) y **Targets** (R7.2 del proposal, métricas a 30 días). Métricas concretas: jobs-seg/PR solo-prosa ≤ 120 s; jobs-seg/PR backend+frontend ≤ 800 s; wall-clock del job más lento ≤ 9 min; suites por detect condicional ≥ 4; Descarga del browser binary de Playwright/PR → 0 cuando cache hit; cache hit rate Buildx ≥ 80 % en lockfile estable. NO incluir cobertura como métrica. **Importante:** alcanzar los targets de 30 días es una métrica posterior de efectividad. **NO bloquea `/sdd:run`, `/sdd:review`, `/sdd:ship` ni `/sdd:archive`**; el archivo existe para registrar la baseline al cierre del change y comparar después. [R7]

## 7. Static verification during /sdd:run <!-- panel: PASS 2026-09-04 -->

> **No se abren PRs durante `/sdd:run`.** Las tareas de esta sección son verificaciones locales/estáticas (lectura de los workflows en disco, simulación de branches en `case`, inspección de configuración). La verificación **end-to-end sobre GitHub Actions reales** queda en la sección **8** y se ejecuta durante `/sdd:ship` sobre el PR que esa fase abre — no se crean PRs sintéticos ni commits dummy desde `/sdd:run`.

- [x] 7.1 Suite local del backend: `docker compose exec backend uv run pytest -q -rs -n 2` debe pasar verde (sigue siendo el comando del proyecto, `sdd/project.md`). [R6.4, R8]
- [x] 7.2 Frontend suite local completa (Vitest node + Vitest browser + ESLint + typecheck, en una sola tarea; las cuatro señales F–I del consolidador):
  - `docker compose exec -T frontend npm test` (proyecto `node` de Vitest) → verde.
  - `docker compose exec -T frontend npm run lint` → verde.
  - `docker compose exec -T frontend npm run typecheck` → verde.
  - `docker compose exec -T frontend npm run test:layout` (proyecto `browser`) — **limitación local documentada**: requiere el binario de Chromium + system libs. En CI lo maneja el workflow (Playwright cache de la sección 2 + `--with-deps` idempotente de la sección 2). En local, sigue las instrucciones de `README.md`: instalar las system libs vía `apt-get` una vez en el host antes del primer `npm run test:layout`. **No** es bloqueante para `/sdd:run` si el host local no tiene Chromium — la cobertura CI es la vara. [R3.2, R8]
- [x] 7.3 **Verificación estática de detectores** (no abrir PRs; lectura de los workflows): para cada `*-detect` job, comprobar que:
  - `git -c core.quotePath=false diff -z --no-renames --name-only` se invoca con el rango correcto del PR / push.
  - El `case "$f" in` lista las anclas declaradas en `design.md` D1 (literal, no regex).
  - La decisión arranca en `backend=true` (o `frontend=true`, `compose=true`, `rule11=true` según el workflow) y solo una comprobación afirmativa la baja.
  - `outputs.<area>` y `outputs.reason` se publican.
  - **Simulación local:** ejecutar el bloque `read_diff` + `evaluate` (vía un script de shell ad-hoc, sin push) con tres conjuntos de paths: (i) solo paths que **no** tocan el área → esperado `<area>=false`; (ii) un path del área → esperado `<area>=true`; (iii) paths mixtos → esperado `<area>=true`. Documentar la salida. [R1.5, R1.6, R1.7, R5.1, R5.2]
- [x] 7.4 **Verificación estática del árbol `detect → suite → consolidator`** en los tres workflows: leer `frontend-tests.yml`, `compose-ports.yml`, `rule11-ownership.yml` en disco y comprobar:
  - Existen exactamente los tres jobs por workflow con los nombres declarados en `design.md` D1 y en `tasks.md` 3.1/4.1/4.4.
  - Cada `*-suite` declara `if: needs.<detect>.outputs.<area> == 'true'` con `<area>` correcto.
  - Cada consolidador declara `if: always()` y los `needs` correctos.
  - **No se exige ningún PR.** [R1.1, R1.7, R5.3]
- [x] 7.5 **Verificación estática del comportamiento fail-safe del consolidador:** por cada uno de los tres consolidadores, leer el árbol de veredicto y comprobar (sin ejecutar):
  - `DETECT_RESULT != success` → `fail` con mensaje propio.
  - `SUITE_RESULT == success` → `pass`.
  - `SUITE_RESULT == skipped && DETECT_<AREA> == false` → `pass` con la línea `**OMITIDA** (no ejecutada)` y el motivo del detect.
  - `SUITE_RESULT == skipped && DETECT_<AREA> == true` → `fail` (incoherencia del gate).
  - Cualquier outcome por señal != `success` → `fail` nombrando la señal.
  - Forma verificada contra `backend-tests.yml:449-577`. **No** se abre PR para probar esto. [R1.1, R5.3, R5.4]
- [x] 7.6 **Verificación estática de configuración Playwright y Buildx (D3, D4):**
  - **Playwright:** `frontend-tests.yml` declara `actions/cache@<sha> # v4` (pin por SHA obligatorio, R8.5); key `playwright-${{ hashFiles('frontend/package-lock.json') }}` **sin** `restore-keys`; el step de install (`npm exec --no -- playwright install --with-deps chromium`) **no** tiene `if: condicional` (corre siempre); el step de save lleva `if: steps.cache-restore.outputs.cache-hit != 'true'`.
  - **Buildx:** los 4 builds (`multiarch-{backend,frontend}` y `deploy-{backend,frontend}`) tienen `cache-from: type=gha,scope=<workflow>` y `cache-to: type=gha,mode=max,scope=<workflow>`. Los scopes son disjuntos. `mode=max` está en los 4. Los labels OCI no se han alterado (leer los `docker/build-push-action@v6` y comparar contra `origin/main`).
  - El job `provenance` y el job `deploy` de `deploy-dev.yml` están intactos.
  - **No** se abre PR para probar esto. [R2, R4, R8.5]
- [x] 7.7 **Verificación estática de preservación de las 6 invariantes D5 (D5):** confirmar por inspección de los workflows que, sobre el SHA construido, se pueden leer las seis invariantes sin ambigüedad:
  1. `org.opencontainers.image.revision` igual al SHA del commit (lo fija `deploy-dev.yml`).
  2. `org.opencontainers.image.version` cumple `X.Y.Z+YYYY-MM-DD.<7 hex>` (lo fija el job `provenance`).
  3. Labels OCI completas (cuatro para backend; tres para frontend) — verificar que los `labels:` en `deploy-dev.yml:92-96` y `137-140` están intactos.
  4. Provenance end-to-end verde — el job `provenance` produce los outputs esperados; el extractor y validador siguen corriendo en `frontend-tests-suite`.
  5. Build inputs sin cambios (Dockerfile, contexto, build-args, target, platforma son los originales).
  6. Imagen funcionalmente correcta — el job `deploy` la levanta, los healthchecks pasan, la sonda de ingress responde. Verificable en 8 (PR real).
  - **No** se exige byte-identical con builds anteriores. **No** se abre PR. [R4, R8.1]
- [x] 7.8 Verificación final del grafo (no exigir cero uso de `upload-artifact`/`download-artifact` en todo el repo; solo verificar lo introducido por este change):
  - `grep -rn "pytest.*cov\|--cov" .github/workflows/` devuelve cero (R6.2).
  - `grep -n pytest-cov backend/pyproject.toml backend/uv.lock` devuelve cero (R6.3).
  - `grep -rn "restore-keys: |" .github/workflows/frontend-tests.yml` no aparece (la cache de Playwright es exacta, sin `restore-keys`; R2.1).
  - En `frontend-tests.yml`: **no** aparece `actions/upload-artifact` ni `actions/download-artifact` para `node_modules` o `.next/`; el `npm ci` corre exactamente una vez por ejecución (R3.1, decisión del usuario sobre node_modules cache).
  - Las nuevas actions (`actions/cache`) están pinneadas por SHA (R8.5). [R2, R3, R6, R8.5]

## 8. E2E GitHub Actions — deferred-to-PR_OPEN

> **Esta sección NO es una tarea de `/sdd:run`.** Documentada como work-item que se ejecuta durante `/sdd:ship` sobre el PR real que esa fase abre. **`/sdd:run` no abre PRs sintéticos, ni crea `docs/foo.md`, ni `frontend/lib/foo.ts`, ni `sdd/steering/security.md` ficticios.** El registro de este bloque sigue la regla 5 del SDD: si una verificación no puede ejecutarse en este momento, se persiste en `BLOCKED.md` para reanudar después.

Comprobación end-to-end durante `/sdd:ship` (sobre el PR real, no sobre PRs sintéticos):
- En el PR abierto por `/sdd:ship`, abrir un push con un commit vacío que toque **solo** un path del área que **no** dispara ningún detect (p. ej. `docs/foo.md`). Esperar a que `frontend-tests`, `compose-ports`, `rule11-ownership` aparezcan como **success** (no skipped), y leer el `GITHUB_STEP_SUMMARY` de cada consolidador para confirmar la línea `**OMITIDA** (no ejecutada)`.
- En el mismo PR, empujar un commit que toque **solo** un path dentro del área del workflow (p. ej. `frontend/lib/foo.ts` para `frontend-tests`). Confirmar que el check ejecuta la suite y termina en success.
- En el mismo PR, empujar un commit que toque `backend/devops/Dockerfile`. Confirmar que `multiarch-build-check` corre, que los labels OCI y la version canónica se preservan, y que el job `deploy` de `deploy-dev.yml` levanta la imagen con healthchecks OK y sonda de ingress OK.
- Si alguna de las verificaciones falla, **rollback** del bloque correspondiente (no se fuerza merge con verificaciones en rojo).

**`/sdd:run` no necesita ejecutar nada de lo que aparece arriba para marcar la sección 7 como completa.** La regla es: la sección 7 termina cuando la verificación estática confirma que el workflow, en disco, ejecutaría el comportamiento correcto sobre el PR real. [R1.1, R5.3]<!-- Future-change candidate (fuera de scope de este change): `.github/workflows/multiarch-build-check.yml:27,44` usa `docker/build-push-action@v6` (tag flotante) — preexistente, NO introducido por §5 (cuyo diff solo añade `cache-from`/`cache-to`). El SHA correcto de v6 ya existe en el repo (`deploy-dev.yml`: `docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6`). Pinnearlo excede R8.5 (que solo prohíbe *cambiar* pins existentes) y las tareas de §5; se recomienda un change de higiene de supply-chain aparte. Registrado 2026-09-03. -->

## 9. Remediación del fail-open de superficie de detección (panel FAIL 2026-09-04) <!-- panel: PASS 2026-09-04 -->

> **Origen:** el panel de `/sdd:review` (architect + security, convergentes) encontró que los tres `*-detect` anclaban un **subconjunto** de la superficie de dependencias de su suite: un PR que toca solo un guard ejecutado por la suite (o un census root que la guardia recorre) recibía `**OMITIDA**` verde sin ejecutar la suite. La superficie correcta está derivada en **design.md D1.1** y codificada en **proposal.md R1.2 / R5.1 / R5.2**. Esta sección **supersede las listas de anclas** de las tareas 3.1 / 4.1 / 4.4 y **re-verifica** 3.5 / 4.7 / 7.3 / 7.4 contra la tabla D1 corregida. No cambia el contenido de ninguna `*-suite` (comandos byte-idénticos), ni la consolidación, ni `on:`, ni los timeouts, ni los SHAs — **solo el `case "$f" in` de cada detect**.

- [x] 9.1 `.github/workflows/rule11-ownership.yml` — sustituir el patrón `case "$f" in` del job `rule11-ownership-detect` por (una sola línea de patrones separados por ` | `, cerrada con `)`):
  `sdd/steering/* | sdd/specs/* | sdd/project.md | sdd/README.md | sdd/metrics.md | docs/* | backend/app/* | backend/alembic/versions/* | backend/tests/* | scripts/* | Makefile | .github/workflows/rule11-ownership.yml)`.
  Derivación (design D1.1): census prose caminado por `SCOPE` (`scripts/rule11-ownership.py:136-230`) = `sdd` menos exclusiones (`sdd/steering`, `sdd/specs`, y los tres `.md` top-level `project`/`README`/`metrics`; **NO** `sdd/changes` ni `sdd/roadmap*`) + `docs`; census code = `backend/app`, `backend/alembic/versions`, `backend/tests`, `scripts`; código ejecutado por la suite = `scripts/rule11-ownership.py` + `scripts/test_rule11_ownership.py` (⊂ `scripts/*`) + `Makefile` (target `check-rule11-ownership`). Actualizar cualquier comentario del workflow que **enumere** las anclas para que coincida; no tocar el resto del job. [R5.2]
- [x] 9.2 `.github/workflows/compose-ports.yml` — sustituir el `case "$f" in` de `compose-ports-detect` por:
  `docker-compose*.yml | docker-compose*.yaml | docker-compose.worktree.yml | scripts/* | Makefile | .github/workflows/compose-ports.yml)`.
  Derivación: la suite corre `pytest scripts/ -q` (**árbol de tests de scripts entero**) + `make check-compose-ports` → `scripts/compose-ports.py`; `Makefile` define el target. [R5.1]
- [x] 9.3 `.github/workflows/frontend-tests.yml` — sustituir el `case "$f" in` de `frontend-tests-detect` por:
  `frontend/* | frontend/devops/Dockerfile | frontend/package.json | frontend/package-lock.json | scripts/validate-provenance-contract.py | scripts/check-version-parity.py | .github/scripts/* | Makefile | .github/workflows/frontend-tests.yml)`.
  Derivación: guards ejecutados fuera de `frontend/**` = `check-version-parity` (Make target → `scripts/check-version-parity.py`), `extract-pr.sh --self-test` (⊂ `.github/scripts/*`), `validate-provenance-contract.py --self-test`. **Frontera deliberada (design D1.1): NO anclar `backend/pyproject.toml`, `VERSION` ni `backend/app/provenance/contract.py`** — inputs de datos cross-area; el contrato ya lo gatea `backend-tests` vía `backend/tests/provenance/`, y version-parity queda como residual documentado (candidato de future-change). [R1.2]
- [x] 9.4 **Verificación estática de la superficie corregida** (lectura de los tres workflows en disco + simulación local del bloque `read_diff`+`evaluate` en scratchpad, sin abrir PRs ni crear ficheros sintéticos en el árbol real). Comprobar, para cada `*-detect`:
  - **Casos negativos (anti-fail-open):** cada ancla NUEVA, presentada como **único** fichero cambiado, produce `<area>=true`. Como mínimo: rule11 → `scripts/rule11-ownership.py`, `scripts/compose-offset.py`, `backend/alembic/versions/000x_dummy.py`, `Makefile`, `sdd/project.md` → todos `rule11=true`; compose → `scripts/compose-ports.py`, `scripts/test_compose_stacks.py`, `Makefile` → `compose=true`; frontend → `scripts/validate-provenance-contract.py`, `scripts/check-version-parity.py`, `.github/scripts/extract-pr.sh`, `Makefile` → `frontend=true`.
  - **No sobre-disparo (ahorro preservado, R7):** un PR que NO toca la superficie sigue dando `<area>=false` y `**OMITIDA**`: p. ej. un PR de solo `sdd/changes/foo/proposal.md` → `rule11=false` (la guardia excluye `sdd/changes`); un PR de solo `backend/app/models/x.py` → `compose=false` y `frontend=false`; un PR de solo `frontend/lib/x.ts` → `compose=false` y `rule11=false`. Confirmar que las anclas añadidas no arrastran áreas sin relación.
  - **Frontera documentada:** un PR de solo `backend/pyproject.toml` da `frontend=false` (esperado; residual de version-parity registrado en design D1.1, no un defecto de esta tarea).
  - Documentar la salida de la simulación en Implementation Notes. [R1.2, R5.1, R5.2, R7]
- [x] 9.5 **Re-verificación de que la suite sigue verde** con los detectores corregidos (el contenido de las suites no cambia, pero se confirma que los comandos referenciados siguen pasando): correr en local `make check-rule11-ownership`, `make check-compose-ports`, `make check-version-parity`, `uv run --no-project --with 'pytest==9.1.1' python -m pytest scripts/ -q`, y `bash .github/scripts/extract-pr.sh --self-test` + `python3 scripts/validate-provenance-contract.py --self-test`. Todos deben pasar. Registrar resultado. [R5.1, R5.2, R1.2]
- [x] 9.6 **Coherencia spec ↔ impl:** confirmar que el `case` de cada detect en disco == tabla D1 de `design.md` (12 anclas rule11, 6 compose, 9 frontend) == EARS R5.2/R5.1/R1.2 de `proposal.md`, y que la frontera de `frontend-tests-detect` (sin `backend/**`) coincide con la "Frontera deliberada" de D1.1. Re-anota como superadas las verificaciones de anclas de 3.5/4.7/7.3/7.4. [R1.2, R5.1, R5.2]

## 10. Remediación del review FAIL a escala de feature (panel 2026-09-05) <!-- panel: PASS 2026-09-05 -->

> **Origen:** el panel de `/sdd:review` quedó FAIL con findings **de exactitud de artefactos de documentación** (el YAML de los workflows pasó todos los criterios EARS R1–R8 — confirmado por qa y cicd) más un latente de bajo riesgo en el detector de rule11. Todos verificados en disco por el orquestador. Esta sección los remedia. Decisión de usuario sobre F4: **mantener las anclas explícitas del detector** (NO generalizar a `sdd/*` recursivo, NO cambiar el `SCOPE`) y **añadir un test** que haga machine-checked la invariante `relevant-SCOPE-walked-surface(rule11) ⊆ detect-anchor-surface(rule11)` (design D1.1.a / proposal R5.2).

- [x] 10.1 **(F1)** `sdd/changes/ci-pr-gates-optimization/metrics.md` — corregir la afirmación falsa de que `multiarch-build-check.yml` **ya** tenía scopes disjuntos de Buildx `mode=max` "desde antes de este change". `git show HEAD:.github/workflows/multiarch-build-check.yml | grep -c cache` = 0: la caché la **añade** este change (§5/D4). En la línea ~45, separar claramente: `backend-tests.yml` **sí** traía ya el patrón de tres jobs (precedente real que este change replica), pero `multiarch-build-check.yml` **no** tenía caché Buildx — pertenece al grupo "pre-change, este change introduce la caché" junto a frontend/compose/rule11. Corregir el encabezado de sección (~línea 82) y la fila de target (~línea 107) para NO decir "ya usa … desde antes de este change". **Mantener los tiempos crudos** (job-seconds/wall-clock) como baseline pre-change **sin caché** válido; solo cambia la atribución narrativa. Si los run IDs de muestra de multiarch provienen de `origin/main` avanzado más allá del baseline `888edfa4` (staleness ya registrada en BLOCKED.md), re-etiquetarlos como tal o anotar la limitación. [R7.1]
- [x] 10.2 **(F2)** `sdd/changes/ci-pr-gates-optimization/spec-changes/{frontend-ci,compose-ports,rule11-ownership-guard}.md` — resincronizar las listas de anclas de detección con el estado **post-§9** (las que están en disco en los `case` de los tres workflows y en la tabla D1 / D1.1 de `design.md`). Los drafts se escribieron en §6 (2026-09-03), **antes** de la remediación §9 (2026-09-04), y listan las anclas antiguas y estrechas; `/sdd:archive` los pega literales a `sdd/specs/*` → reintroduciría a nivel de spec el fail-open que §9 cerró en el código. Cada draft debe: (a) listar las anclas completas post-§9 de su workflow, (b) citar el principio D1.1 (`detect surface ⊇ suite dependency surface`), y para `rule11-ownership-guard.md` además (c) mencionar la invariante machine-checked D1.1.a y su test. NO tocar `sdd/specs/*` (eso es de `/sdd:archive`). [R5.1, R5.2, R1.2]
- [x] 10.3 **(F3)** `sdd/changes/ci-pr-gates-optimization/spec-changes/multiarch-build-check.md` — en la sección (a), corregir la cita del patrón de scopes disjuntos `mode=max`: es **D4** de `design.md` ("Caché de Buildx con scopes separados"), no **D5** ("Qué significa preservar la identidad de imagen"). La cita a D5 para la invariante de identidad (sección de identidad, ~línea 108) es correcta y se conserva. [D4/D5]
- [x] 10.4 **(D-F2)** `sdd/changes/ci-pr-gates-optimization/tasks.md` — línea ~49 (texto de la tarea 4.4, superada por §9): usa `sdd/steering/**`/`sdd/specs/**` (doble `*`) mientras `design.md` D1 y el `case` on-disk usan `*` simple (en un `case` de shell `*` ya casa cualquier profundidad, incluidos los `/`). Alinear ese texto a `*` simple para que no contradiga al YAML/design. Cambio puramente de consistencia documental; no toca ningún workflow. [consistencia]
- [x] 10.5 **(F4)** `scripts/test_rule11_ownership.py` — añadir el test que hace machine-checked la invariante D1.1.a **sin** ampliar el detector ni cambiar el `SCOPE`. Reusar la lógica de recorrido de la propia guardia (ya importada en el test como `module`, `SCOPE`, `ROOT`): la superficie recorrida = `module.prose_files(SCOPE, ROOT) + module.code_files(SCOPE, ROOT)` (devuelven `(relative, path)`; ya aplican las exclusiones `OUT_OF_CENSUS` de `sdd/changes`, `sdd/roadmap.md`, `sdd/roadmap` y la autoridad). Las anclas del detector se parsean del `case "$f" in … )` del job `rule11-ownership-detect` en `ROOT/".github/workflows/rule11-ownership.yml"` (única sentencia `case` del fichero; split por ` | `, quitar espacios y el `)` final). El emparejamiento ancla↔ruta debe reproducir la semántica de `case` de shell (`*` casa cualquier profundidad incluidos `/`): usar `fnmatch.fnmatchcase(relative, anchor)`. Dos tests, y el segundo **prueba el rojo** (obligación del fichero: "un verde debe significar «lo miró»"):
  1. `test_rule11_detect_surface_covers_walked_scope`: **todo** fichero de la superficie recorrida casa con **alguna** ancla del detector → el conjunto de no-cubiertos es vacío. (Pasa hoy.)
  2. `test_rule11_detect_surface_invariant_catches_a_new_sdd_subtree`: prueba que la invariante NO es vacía — una ruta sintética `sdd/adr/example.md` (que la guardia **recorrería**: bajo `sdd/`, no es `OUT_OF_CENSUS` — verificar con `module._is_excluded('sdd/adr/example.md', SCOPE) is False`) **no** casa con ninguna ancla actual → el helper de cobertura la marca como descubierta, demostrando que si apareciera, el test 1 FALLARÍA. Documentar en el docstring que la resolución correcta ante ese rojo es añadir la ancla explícita (o declarar la ruta `OUT_OF_CENSUS` en `SCOPE` si pertenece a las exclusiones), nunca ampliar a `sdd/*` ni recortar el `SCOPE` para pasar. [R5.2, design D1.1.a]
- [x] 10.6 **Verificación** (sin abrir PRs): (a) correr `uv run --no-project --with 'pytest==9.1.1' python -m pytest scripts/test_rule11_ownership.py -q` (o el `python3 -m pytest` del host si `uv` no está — documentar la limitación como en §9.5) → los dos tests nuevos pasan y el resto de la suite del guard sigue verde; (b) `make check-rule11-ownership` sigue verde (el `SCOPE` no cambió); (c) coherencia: los tres drafts de §10.2 == anclas on-disk; `metrics.md` ya no afirma caché pre-change en multiarch; `grep -n "D5" spec-changes/multiarch-build-check.md` solo en la sección de identidad; `tasks.md:~49` con `*` simple. Registrar resultados en Implementation Notes. [R5.2, R7.1]

## 11. Cierre de la clase de regresión de conditional-gates (review FAIL #2, 2026-09-05) <!-- hard --> <!-- panel: PASS 2026-09-05 -->

> **Origen:** el re-`/sdd:review` tras §10 quedó FAIL con residuales más profundos de la misma invariante de superficie de detección. Auditoría de superficies (antes always-run → ahora condicional) en `design.md` D1.1.b/D1.1.c; findings SEC-1/SEC-2/SEC-3 + QA-R2.4. **Fuera de scope (decisión de usuario):** los findings pre-existentes de `multiarch-build-check.yml` (tags flotantes, `permissions:`) — NO se tocan aquí. `proposal.md` (R1.8, R5.5, R5.1, R2.4) y `design.md` (D1.1.a/b/c, frontera version-parity) ya actualizados por el orquestador; esta sección implementa el código y los tests.

- [x] 11.1 **(SEC-1)** `.github/workflows/compose-ports.yml` — en `compose-ports-detect`, ampliar el `case "$f" in` con la mitad `compose.*` de la familia de descubrimiento por defecto de Docker Compose (la mitad `docker-compose.*` ya la cubre `docker-compose*`). Nueva línea:
  `compose.yaml | compose.yml | compose.override.yaml | compose.override.yml | docker-compose*.yml | docker-compose*.yaml | docker-compose.worktree.yml | scripts/* | Makefile | .github/workflows/compose-ports.yml)`.
  Motivo (design D1.1.b): `scripts/compose-ports.py` corre `docker compose config` **sin `-f`**; `compose.yaml`/`compose.yml` tienen precedencia sobre `docker-compose.yml`. Actualizar cualquier comentario del workflow que enumere las anclas. [R5.1]
- [x] 11.2 **(SEC-2)** crear `.github/workflows/version-parity.yml` — workflow **always-run** mínimo (patrón de `frontend-api-contract.yml`): `on: {pull_request: {}, push: {branches: [main]}, workflow_dispatch: {}}`, sin `paths:`; `concurrency` con `cancel-in-progress: true`; `permissions: contents: read`; un job `version-parity` (nombre estable = check requerido) con `actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4` (`persist-credentials: false`) y un paso `run: make check-version-parity`. **NO** `uv`/`setup-uv`: `check-version-parity.py` es stdlib puro (json/tomllib/pathlib), corre con el `python3` del runner. `timeout-minutes: 5`. NO quitar la señal `version_parity` de `frontend-tests-suite` (belt-and-suspenders). [R1.8]
- [x] 11.3 **(SEC-3, invariante temporal)** crear `scripts/check-detect-surface.py` (CLI stdlib, sin deps) y cablearlo always-run:
  - El CLI toma un nombre de workflow (`rule11` | `compose` | `frontend`) y computa el conjunto de rutas de la **superficie de entrada de la suite** NO cubiertas por las anclas on-disk de ese `*-detect`; imprime los no-cubiertos y `sys.exit(1)` si hay alguno, `exit(0)` si vacío. Fuentes del lado izquierdo: **rule11** = `module.prose_files(SCOPE, ROOT) + module.code_files(SCOPE, ROOT)` (importa `scripts/rule11-ownership.py` vía `importlib`, como hace el test); **compose** = la familia de descubrimiento de Compose (`compose.yaml`,`compose.yml`,`compose.override.yaml`,`compose.override.yml`,`docker-compose.yaml`,`docker-compose.yml`,`docker-compose.override.yaml`,`docker-compose.override.yml`) que **existan** en el árbol, más `scripts/**` y `Makefile`; **frontend** = las rutas fuera de `frontend/**` que la suite ejecuta, extraídas parseando los `run:` de `frontend-tests-suite` (referencias a `scripts/*.py`, `.github/scripts/*`, y `make <target>` → `Makefile`). Lado derecho: parsear el `case "$f" in` del `*-detect` correspondiente (misma lógica que `_rule11_detect_anchors`, generalizada) y emparejar con `fnmatch.fnmatchcase`. Extraer los helpers compartidos aquí; `scripts/test_rule11_ownership.py` importa de este módulo (una sola fuente de verdad).
  - Añadir un paso **always-run** `run: python3 scripts/check-detect-surface.py <wf>` a **cada** job `*-detect` (`frontend-tests-detect`, `compose-ports-detect`, `rule11-ownership-detect`), **antes** del paso `decide` (fuera de su `set +e`, con `bash -e` por defecto para que su fallo aborte el job). Comentario inline: una superficie incompleta es error de config del gate → fail-closed vía consolidador (`DETECT_RESULT != success`), distinto del fail-open ante diff ambiguo.
  - Target de Makefile `check-detect-surface` (corre los tres) para uso local. [R5.5, design D1.1.c]
- [x] 11.4 **(QA-R2.4)** verificar coherencia: `proposal.md` R2.4 (ya enmendado por el orquestador) describe el comportamiento implementado (install siempre, no-op del binario en cache hit); confirmar que `frontend-tests.yml` y `tasks.md` 2.2 siguen coincidiendo. Sin cambios de código esperados. [R2.4]
- [x] 11.5 **Tests anti-regresión** (los cuatro escenarios de R5.5) en `scripts/test_detect_surface.py` (nuevo; corre vía `pytest scripts/` de compose-suite + local) importando de `check-detect-surface.py`:
  - (a) **suite crece sin ampliar detector:** para los tres workflows, la superficie de entrada ⊆ anclas (no-cubiertos == ∅). Reusa el CLI. rule11 ya tiene la versión F4 en `test_rule11_ownership.py`; añadir compose y frontend.
  - (b) **ruta Compose nueva no anclada:** cada miembro de la familia de descubrimiento de Compose casa una ancla de `compose-ports-detect`; y un caso que **prueba el rojo** (p. ej. si `compose.yaml` no estuviera anclado, el helper lo marca descubierto-no-anclado).
  - (c) **invariante movida detrás de una suite condicional (estructural/temporal):** parsear los tres workflows y afirmar que **cada** `*-detect` invoca `python3 scripts/check-detect-surface.py` en un paso **sin `if:`** (always-run), y que ese paso está en el job `*-detect` (no solo en la suite). Falla si alguien quita el paso o lo mueve a la suite.
  - (d) **detector devuelve skip ante input que altera el gate:** reproducir el `evaluate()`/`case` de cada `*-detect` (leído de disco) y afirmar: `compose.yaml`→`compose=true`; `sdd/adr/x.md`→`rule11=true`; `VERSION`→ (documentar: `frontend=false` **pero** `version-parity.yml` lo cubre always-run — afirmar que `VERSION` es input de `check-version-parity` y que existe el gate always-run). Casos negativos de no-sobredisparo también (p. ej. `README.md` raíz → los tres `false`). [R5.5]
- [x] 11.6 Resincronizar drafts `spec-changes/`: `compose-ports.md` (familia de descubrimiento completa en las anclas), `rule11-ownership-guard.md` y `frontend-ci.md` (mención del paso always-run `check-detect-surface` en el detect y de que la invariante de superficie es fail-closed); crear `spec-changes/version-parity.md` (draft para la spec viva del nuevo gate always-run, a aplicar en `/sdd:archive`). NO tocar `sdd/specs/*`. [R5.1, R5.5, R1.8]
- [x] 11.7 **Verificación** (sin PRs): (a) `python3 scripts/check-detect-surface.py rule11|compose|frontend` → exit 0 los tres; (b) simular el rojo: comprobar que el CLI sale 1 con un caso construido (documentar, sin tocar el árbol real); (c) `python3 -m pytest scripts/ -q` (o `uv run …` si está) → todos verdes incl. los nuevos; (d) `make check-rule11-ownership`, `make check-compose-ports`, `make check-version-parity`, `make check-detect-surface` verdes; (e) parse YAML de `compose-ports.yml`, `rule11-ownership.yml`, `frontend-tests.yml`, `version-parity.yml`; (f) confirmar el paso always-run en los tres `*-detect` (grep `check-detect-surface` sin `if:` en el job detect). Registrar resultados. [R5.5, R1.8, R5.1]

## 13. Cierre del residual de `_SCRIPTS_REF` y del hueco de README (review round 7, 2026-09-07) <!-- hard --> <!-- panel: PASS 2026-09-07 -->

> **Origen:** el `/sdd:review` a escala de feature (round 7, 2026-09-07) quedó FAIL con 2 findings reales, ambos verificados en disco por el orquestador (los otros 5 lentes PASS: architect/qa/cicd/i18n/tenancy + qa):
> - **security (medium, en-scope, R5.5(a)/D1.1.c):** el fix de round 5 dejó `_SCRIPTS_REF` como un **allow-list de prefijos virtuales** (`./`, `../`, `$VAR/`, `${VAR}/`). Ese allow-list **sigue soltando en silencio** varias grafías bash mainstream de una referencia a script de raíz — reproducido con ejecución directa del regex: `bash "$ROOT"/scripts/x.sh` (comilla cerrada antes de la barra), `bash "$(git rev-parse --show-toplevel)/scripts/x.sh"` / forma con backtick, y `bash "${VAR:-.}/scripts/x.sh"` (expansión con modificador) → las cuatro devuelven `[]`. Como `frontend-tests-detect` ancla solo dos scripts `.py` **nombrados** (no `scripts/*` en bloque), un guard cross-area futuro añadido a `frontend-tests-suite` en cualquiera de esas grafías dejaría el always-run `check-detect-surface.py frontend` en verde mientras un PR que tocara solo ese guard recibiría `**OMITIDA**` verde — la misma clase fail-open §9/§11/§12, reabierta. Latente hoy (`check('frontend') == []`), pero el docstring (líneas 176-198) presenta el manejo de prefijos como la respuesta *cerrada*, y por eso el hueco es silencioso.
> - **documentation (R, `steering/documentation.md:17`):** el target `check-detect-surface` (creado en §11.3, `Makefile:389`) **falta** en el bloque de comandos `make` documentados del README raíz (`README.md:26-39`), que sí lista sus tres hermanos (`check-version-parity`, `check-compose-ports`, `check-rule11-ownership`).
>
> Esta sección **no cambia el contrato** (la invariante R5.5(a)/D1.1.c es la misma): solo hace que el cómputo de superficie de `frontend_surface()` la honre de forma robusta, y pone el README al día. No toca ningún workflow (`.github/workflows/**`), ni `on:`, ni los consolidadores, ni el `SCOPE` de ninguna guardia.

- [x] 13.1 **(SEC-medium)** `scripts/check-detect-surface.py` — reforzar el cómputo de la superficie frontend para que reconozca una referencia a script de **raíz** escrita en cualquier grafía de la ruta al repo, **fallando cerrado** ante grafías no reconocidas en vez de soltarlas en silencio. Principio (reemplaza el marco «allow-list de prefijos virtuales» del docstring): un token `scripts/<path>` es una referencia de raíz **salvo** que sea un subdirectorio real de otro directorio ya anclado (`frontend/scripts/…` es input `frontend/**` anclado por `frontend/*`; `.github/scripts/…` lo captura `_GH_SCRIPT` y lo ancla `.github/scripts/*`). Toda otra grafía —bare, `./`, `../`, `$VAR/`, `${VAR}/`, `${VAR:-.}/`, `"$VAR"/`, `"$X"/`, `$(...)/`, `` `...`/ ``— es una referencia de raíz y **debe** capturarse. **Criterios de aceptación exactos** (verificar con ejecución directa del regex/función):
  - **Deben capturar `scripts/x.sh`** (o el tail `scripts/…` correspondiente): `bash scripts/x.sh`, `bash ./scripts/x.sh`, `bash ../scripts/x.sh`, `bash "$GITHUB_WORKSPACE/scripts/x.sh"`, `bash "${GITHUB_WORKSPACE}/scripts/x.sh"`, `bash "$ROOT"/scripts/x.sh`, `bash "$(git rev-parse --show-toplevel)/scripts/x.sh"`, `` bash `git rev-parse --show-toplevel`/scripts/x.sh ``, `bash "${VAR:-.}/scripts/x.sh"`, `pytest scripts/ -q` (→ `scripts/`).
  - **NO deben capturar por `_SCRIPTS_REF`**: `node frontend/scripts/build.js` (→ `[]`; input `frontend/**`), `bash .github/scripts/extract-pr.sh` (→ `[]` por `_SCRIPTS_REF`; lo captura `_GH_SCRIPT`).
  - **Fail-closed ante lo desconocido:** si aparece una grafía que la lógica no sabe normalizar a un `scripts/…` de raíz pero que contiene la subcadena literal `scripts/` fuera de `frontend/`/`.github/`, **debe** entrar en la superficie (over-match) para que `check()` la reporte descubierta, en vez de descartarla. Documentar en el docstring del módulo el enfoque nuevo y una nota **Known limitations** con lo que quede fuera (si algo).
  - `check("frontend")` **debe seguir devolviendo `[]`** (la superficie de hoy sigue totalmente cubierta; esto es un endurecimiento, no un cambio de anclas). No tocar `rule11_surface`/`compose_surface` ni las anclas de ningún detect. [R5.5, design D1.1.c]
- [x] 13.2 **(SEC-medium, tests prove-the-red)** `scripts/test_detect_surface.py` — extender `test_frontend_surface_regex_is_path_prefix_agnostic` (o añadir un test hermano) con las cuatro grafías nuevas del finding como aserciones de que **ahora sí** capturan `scripts/x.sh`: `"$ROOT"/scripts/x.sh`, `"$(git rev-parse --show-toplevel)/scripts/x.sh"`, `` `git rev-parse --show-toplevel`/scripts/x.sh ``, `"${VAR:-.}/scripts/x.sh"`. Conservar/verificar la aserción de que `node frontend/scripts/build.js` → `[]` (no confundir un subdir real con raíz). Los tests deben **fallar contra el regex previo** (prueba-del-rojo) y pasar con el fix. Mantener verde el resto de `pytest scripts/`. [R5.5, design D1.1.c]
- [x] 13.3 **(documentation)** `README.md` — añadir la línea `make check-detect-surface` al bloque de comandos `make` documentados (líneas 26-39), inmediatamente **después** de `make check-rule11-ownership` (línea 34), con una glosa breve y coherente en estilo con las hermanas (p. ej. «comprueba que la superficie de detección de cada gate condicional cubre su superficie de dependencias»). No tocar otras secciones del README. [`steering/documentation.md:17`]
- [x] 13.4 **Verificación** (sin PRs): (a) ejecución directa de las 10 grafías «deben capturar» + las 2 «no capturar» del 13.1 contra `_SCRIPTS_REF`/`frontend_surface`, todas con el resultado esperado; (b) `python3 scripts/check-detect-surface.py frontend|compose|rule11` → exit 0 los tres, y `check("frontend") == []`; (c) `python3 -m pytest scripts/ -q` → todos verdes, incluidos los nuevos (contar el delta vs 263); (d) confirmar que los tests nuevos **fallan** contra el regex previo (revertir mentalmente / commit temporal en scratchpad, o aserción de no-vacuidad); (e) `make check-detect-surface` verde; (f) `grep -n "make check-detect-surface" README.md` devuelve la línea nueva bajo el bloque de comandos. Registrar resultados en Implementation Notes. [R5.5, `steering/documentation.md:17`]

- [x] 13.5 **(SEC-medium, resolución de raíz — decisión de usuario 2026-09-07, opción A)** — tras 2 fix-rounds del parser textual (cada una cerró una variante posicional de la MISMA clase under-match: grafías virtuales → dir real `lib/` → sufijo-hermano `notfrontend/` → anidado `a/frontend/`), el usuario elige **disolver la clase anclando `scripts/*` en bloque** en `frontend-tests-detect`, como ya hacen `compose-ports-detect`/`rule11-ownership-detect`. Cambios (design D1.1.d, supersede la «frontera named-scripts» de D1.1 y las tareas 13.1/13.2):
  - `.github/workflows/frontend-tests.yml` — en el `case` de `frontend-tests-detect`, sustituir `scripts/validate-provenance-contract.py | scripts/check-version-parity.py` por `scripts/*`; actualizar el comentario de cabecera que enumera anclas. Ningún otro cambio al workflow (`on:`, suite, consolidador, timeouts, SHAs intactos).
  - `scripts/check-detect-surface.py` — `frontend_surface()` reporta cada referencia `scripts/…` por su **ruta real**: `_root_script_refs` quita solo prefijos VIRTUALES reconocidos de ruta-a-raíz (`./`, `../`, `$VAR/`, `${…}/`, `$(…)/`, `` `…`/ ``, `"…"/`, `'…'/`) y **conserva** cualquier segmento de directorio real → `scripts/x`→`scripts/x` (cubierto por `scripts/*`), `lib/scripts/x`→`lib/scripts/x` (descubierto → rojo), `frontend/scripts/x`→`frontend/scripts/x` (cubierto por `frontend/*`), `.github/scripts/x`→`.github/scripts/x` (cubierto por `.github/scripts/*`). Elimina el `_SCRIPTS_REF`/deny-list frágil y `_GH_SCRIPT` (el matcher unificado cubre `.github/scripts/`). Sin frontend/.github special-casing; una grafía de prefijo no reconocida se conserva literal → descubierta → rojo (fail-closed).
  - `scripts/test_detect_surface.py` — tests actualizados a la semántica de ruta real: `test_frontend_surface_keeps_real_directory_segments` (lib/, tools/, notfrontend/, sub.github/, a/frontend/, vendor/.github/ reportados por su ruta real; frontend//.github/ por su ruta cubiertos), `test_frontend_detect_anchors_the_whole_scripts_tree` (`scripts/*` ∈ anclas; `tools/scripts/x` descubierto), e2e `..._resolves_prefixes_and_fails_closed` (prefijos virtuales → root; `tools/scripts/…` no anclado → uncovered).
  - `design.md` D1.1 (fila de tabla) + **D1.1.d** nuevo; `proposal.md` R1.2 → `scripts/**`. Coste R7 aceptado (un PR solo-`scripts/**` dispara también la suite de frontend). [R1.2, design D1.1.d]
- [x] 13.6 **Verificación §13-A** (sin PRs): (a) 22 grafías por ejecución directa de `_root_script_refs` (root-prefix→`scripts/…`; real-dir→ruta real; `myscripts/`→`[]`); (b) `scripts/*` ∈ `detect_anchors(frontend)`; (c) `check(frontend|compose|rule11)`→`[]`/exit 0; `frontend_surface()` sin cambios (3 elementos); (d) `python3 -m pytest scripts/ -q` → **265 passed**; (e) `make check-detect-surface` exit 0; (f) YAML de `frontend-tests.yml` parsea. [R1.2, R5.5, design D1.1.d]

## Implementation Notes

<!-- Append-only, written by the implementer of each section for the next one:
     decisions taken, names chosen, gotchas found. One bullet each, no prose. -->
- 10.1 (F1): verified `git show HEAD:.github/workflows/multiarch-build-check.yml | grep -c cache` = `0` on baseline `888edfa4` — confirms the caché is new, not pre-existing. Rewrote `metrics.md`'s "punto de partida asimétrico" paragraph so only `backend-tests.yml` is credited as the real pre-change 3-job precedent; `multiarch-build-check.yml` moved into the "pre-change, this change introduces the cache" group alongside frontend/compose/rule11. Retitled the `### multiarch-build-check (...)` section header and fixed the Targets-table cache-hit-rate row to stop claiming pre-change cache. Raw job-seconds/wall-clock numbers kept unchanged (still valid uncached baseline). Added a note referencing `BLOCKED.md`'s already-registered `origin/main` staleness for the sample run IDs rather than re-deriving them.
- 10.2 (F2): resynced the anchor lists in `spec-changes/frontend-ci.md` (9 anchors), `spec-changes/compose-ports.md` (6 anchors), `spec-changes/rule11-ownership-guard.md` (12 anchors) to the post-§9 on-disk `case` blocks (verified programmatically identical, see 10.6 output). Each draft now cites design D1.1 (`detect surface ⊇ suite dependency surface`); `rule11-ownership-guard.md` additionally cites D1.1.a and names the two new tests from §10.5 as the machine-checked invariant. Renumbered list items 3-6 → 4-7 in `rule11-ownership-guard.md` §(b) after inserting the new D1.1.a bullet. Did not touch `sdd/specs/*`.
- 10.3 (F3): fixed the section-(a) citation in `spec-changes/multiarch-build-check.md:12` from "D5 de design.md" to "D4 de design.md, 'Caché de Buildx con scopes separados'". Left the two identity-section D5 citations (lines 42, 109) unchanged — those are correct.
- 10.4 (D-F2): changed `tasks.md` task 4.4's anchor list from `sdd/steering/**`/`sdd/specs/**`/`backend/app/**`/`backend/tests/**`/`docs/**` (double `*`) to single `*`, matching design.md D1 and the on-disk `case` blocks. No workflow touched.
- 10.5 (F4): added `import fnmatch`, `_rule11_detect_anchors()` (parses the single `case "$f" in … )` of `rule11-ownership-detect` from `.github/workflows/rule11-ownership.yml`), and `_uncovered(relatives, anchors)` (shared `fnmatch.fnmatchcase` matching helper) to `scripts/test_rule11_ownership.py`. Two new tests: `test_rule11_detect_surface_covers_walked_scope` (asserts the walked `SCOPE` surface minus anchor coverage is empty — passes today) and `test_rule11_detect_surface_invariant_catches_a_new_sdd_subtree` (proves non-vacuousness via the synthetic string literal `"sdd/adr/example.md"`, asserting `module._is_excluded(...) is False` and that it matches no current anchor — no file created on disk). Both reuse `module.prose_files`/`module.code_files`/`module._is_excluded` per the design D1.1.a referent; SCOPE and the detector's anchors were not changed.
- 10.6: `uv` is **not installed** on this host (`command -v uv` empty) — same env limitation as §9.5. Ran `python3 -m pytest scripts/test_rule11_ownership.py -q` instead: **24 passed** (22 pre-existing + 2 new), including `-k detect_surface` isolating the 2 new tests (2 passed). `make check-rule11-ownership` still green (SCOPE unchanged, census counts unchanged: 102 md / 857 py). Coherence greps: `grep -n "D5" spec-changes/multiarch-build-check.md` → only lines 42 and 109 (both in identity sections); `grep -n "ya usa\|ya con\|ya implementaban\|ya tenía" metrics.md` → only the legitimate `backend-tests` precedent line; `tasks.md:49` (task 4.4) uses single `*`; the 3 drafts' anchor lists (9/6/12) verified programmatically identical to the on-disk `case` blocks of `frontend-tests.yml`/`compose-ports.yml`/`rule11-ownership.yml`.

- D6: `pytest-cov` quitada de `backend/pyproject.toml:66` y `uv.lock` regenerado limpio (`grep -c pytest-cov backend/uv.lock` = 0). En una captura intermedia `coverage==7.15.2` seguía apareciendo en el lockfile (arrastre transitivo de `pytest-cov`); una regeneración posterior del lockfile lo eliminó por completo al no quedar nada que lo requiera. **Estado final en disco: `grep -c coverage backend/uv.lock` = 0** — ni `pytest-cov` ni `coverage` quedan en el lockfile, estrictamente más limpio que lo que exige R6.4.
- D6: `docker compose run --rm -T backend uv lock` resolvió en 8 ms sin necesidad de `--upgrade`; las referencias 132/163/1219 del design ya no aplican porque el lockfile se reescribió completo.
- Gotcha de entorno: el primer `pytest` post-cambio falló 1 test (`test_env_example_declares_the_demo_password_by_name_and_without_a_value`) por bind mount obsoleto de `./.env.example` en el contenedor (`/workspace/.env.example` mostraba 392 líneas, el host 408; el contenedor pre-fechaba 2026-08-26). `docker compose restart backend` refrescó la montura y la suite pasó 9880/41 sin warnings de coverage. **El fallo no era del change** — quedó documentado para que la sección 7 no se confunda si reproduce.
- Verificación 1.4(b) literal del regex `pytest.*cov\|--cov\|pytest-cov` produce matches espurios por substrings no cobertura (palabras como `discover` o `Recovery`). Comprobación efectiva con `\-\-cov|pytest-cov|addopts.*cov` devuelve cero. Las menciones documentales en `sdd/specs/`, `proposal.md`, `design.md`, `tasks.md` son legítimas (registran la retirada) y no cuentan para el check.
- R6.4 preservado: `pytest>=9.1.1`, `pytest-asyncio>=1.4.0`, `pytest-xdist>=3.6.1` siguen en `pyproject.toml:64-66`; la invocación `uv run pytest -q -rs -n 2` en `backend-tests.yml:404` intacta.
- 2.1: SHA de `actions/cache@v4` resuelto vía `gh api repos/actions/cache/git/ref/tags/v4 --jq '.object'` y confirmado con `gh api repos/actions/cache/tags --jq '.[] | select(.name=="v4") | .commit.sha'` (ambos coinciden) = `0057852bfaa89a56745cba8c7296529d2fc39830`.
- 2.1: step `Restore Playwright browser cache` (`id: cache-restore`) insertado en `frontend-tests.yml` inmediatamente antes del step `Guarda de desbordamiento a 360px (Vitest browser)` (`id: layout`), porque el install de Playwright vive dentro del `run:` de ese step, no como step separado.
- Gotcha para 2.2: el `grep -n "restore-keys"` literal SÍ matchea (falso positivo) porque el comentario inline mandado por D3 contiene la subcadena `` `restore-keys:` `` en prosa explicativa; el check real de "sin restore-keys" es por la clave YAML (`grep -rn "restore-keys: |"`, ver tasks.md:137), no por substring — no lo confundas con una violación real.
- 2.2: step `Save Playwright browser cache` (sin `id`, no lo necesita otro step) insertado en `frontend-tests.yml` inmediatamente después del step `Guarda de desbordamiento a 360px (Vitest browser)` (`id: layout`, línea ~150) y antes de `Lint (ESLint)`. Usa el mismo SHA pinneado que la restore (`0057852bfaa89a56745cba8c7296529d2fc39830 # v4`), mismo `path` y misma `key`; `if: steps.cache-restore.outputs.cache-hit != 'true'`. El install (línea 151, dentro del `run:` de `layout`) sigue sin condicional de cache — solo hereda el `if` del step `layout` ligado a `steps.install.outcome` (el `npm ci`, id `install`, línea 84), no al cache. Para 2.3: verificar cache hit/miss comprobando que en un run con cache-hit el job de `Save` se salta (`skipped`) y en cache-miss se ejecuta (`success`).
- 2.3: la observación empírica de `cache-hit: true`/`false` requiere dos ejecuciones reales de GitHub Actions, fuera del alcance de `/sdd:run` (secciones 7/8). Se creó `sdd/changes/ci-pr-gates-optimization/metrics.md` con el plan de verificación de ambos escenarios (sección "Playwright cache verification (R2.4)") y `sdd/changes/ci-pr-gates-optimization/BLOCKED.md` con el diferimiento a `/sdd:ship` (sección 8). La sección 3 y la tarea 6.3 (que también toca `metrics.md`) deben añadir/ampliar, no reemplazar, este archivo — ya existe.
- 3: `frontend-tests.yml` reestructurado a tres jobs — `frontend-tests-detect` (siempre, ~seg), `frontend-tests-suite` (`if: needs.frontend-tests-detect.outputs.frontend == 'true'`), `frontend-tests` (consolidador, `if: always()`, `needs: [detect, suite]`). Timeouts: detect=10, suite=15, consolidador=5 (10/15/5 preservados de los dos jobs originales). SHAs pinneados intactos (checkout, setup-node, cache). Cero `upload-artifact`/`download-artifact`; `npm ci` una sola vez (línea ~269).
- 3.1: cabecera del workflow reescrita (líneas 3-28): antes decía que el patrón de backend-tests se dejaba SIN copiar a propósito; ahora dice que `ci-pr-gates-optimization` lo adopta citando `ci-backend-tests-conditional-gate` como precedente literal. Reason set de detect = conjunto cerrado de D1 con `workflow-dispatch-runs-full-suite` (guion, forma de D1, no `workflow_dispatch-runs-full-suite` con guion bajo de backend) — coherente con lo que verifica 3.5.
- 3.2: nueve señales como outputs con sufijo `_outcome` leídos vía `steps.<id>.outcome`. Las TRES precondiciones (version_parity, extract_pr, validate_provenance) que en `provenance-contract` corrían SIN `continue-on-error` (fallo duro) ahora llevan `continue-on-error: true` + id + output, para que el consolidador las nombre en vez de dejar el check sin reportar (preserva R1.1 + las 5 señales de provenance de R3.3). Playwright restore/install(always)/save de la sección 2 reubicados dentro de `frontend-tests-suite` sin cambios de SHA/key/path. `tests` y `layout` publican además `outputs.seconds` para las duraciones de R1.7.
- 3.3: el árbol de veredicto añade una diferencia real sobre backend: como las 9 señales llevan `continue-on-error`, el `result` del job suite puede ser `success` aunque una señal falle, así que la rama "la suite se ejecutó" es `SUITE_RESULT == success || failure` y el veredicto se decide señal a señal (bucle sobre `signals[]`), no por el resultado agregado. Sin `pytest budget/ceiling` (no aplica al frontend).
- 3.5: verificado en disco por lectura completa + PyYAML (jobs, if, needs, timeouts, concurrency, SHAs, npm ci único, cero artifacts). Limitación de tooling: `actionlint` no está instalado en el host; no bloqueante — la estructura se validó con `yaml.safe_load` y lectura manual del árbol de veredicto contra `backend-tests.yml:449-577`.
- 3 (panel fix, 2026-09-03): el panel de §3 (architect/security/qa/cicd, gate closed-world exit 0) halló un falso verde real en el consolidador. La rama "suite ejecutada" era `[ "$SUITE_RESULT" = "success" ] || [ "$SUITE_RESULT" = "failure" ]`; como `install` (npm ci) SÍ se trackea en `signals[]` pero `cache-restore`/`cache-save` (`actions/cache`, sin `continue-on-error`) NO, un fallo de esos pasos dejaba el job `suite` en `failure` con las 10 señales en verde (los steps posteriores corren por `if: … && !cancelled()`), y la rama ancha reportaba PASS. Fix (línea ~462): restringida a `[ "$SUITE_RESULT" = "success" ]`; un `failure` de step no trackeado cae al `else` genérico y falla fail-closed, alineado con D2 y `backend-tests.yml`. El bucle señal-a-señal se conserva (captura los `continue-on-error` que dejan el job en `success`). Probado con 6 casos del árbol (A=0,B=1,C=1,D=0,E=1,F=1) y demostrado que el caso B daba exit 0 antes del fix.
- 4: `compose-ports.yml` y `rule11-ownership.yml` reestructurados a tres jobs cada uno, mismo patrón que `frontend-tests.yml`/`backend-tests.yml`. Timeouts por job: detect=5, suite=10 (preserva el `timeout-minutes: 10` único del job original), consolidador=5. `concurrency.cancel-in-progress: true` preservado (mismo `group` que antes). Ningún `paths:` en `on:` (sigue prohibido); `on:` intacto en ambos (pull_request/push main/workflow_dispatch).
- 4.2/4.5: `compose-ports-suite` y `rule11-ownership-suite` NO usan `continue-on-error` en sus dos steps (a diferencia de `frontend-tests-suite`): son "hard steps" secuenciales, igual que `backend-tests-suite` — si `make check-*` o `pytest` fallan, el job entero pasa a `failure` y el consolidador lo detecta por `SUITE_RESULT != success` (rama `else` fail-closed), sin necesidad de un bucle señal-a-señal. Coincide con la guía de la tarea ("si un único hard step, `SUITE_RESULT` alone suffices"). Comandos `make check-compose-ports`, `pytest scripts/ -q`, `make check-rule11-ownership`, `pytest scripts/test_rule11_ownership.py -q` — byte-idénticos a los originales (verificado con `git show HEAD:<archivo>` vs disco).
- 4.1/4.4: anclas de `case "$f" in ...)` para `compose` = `docker-compose*.yml | docker-compose*.yaml | docker-compose.worktree.yml | .github/workflows/compose-ports.yml`; para `rule11` = `sdd/steering/* | sdd/specs/* | backend/app/* | backend/tests/* | docs/* | .github/workflows/rule11-ownership.yml`. Nota: en un `case` de shell (no filename globbing) `docs/*` YA casa cualquier profundidad (`docs/a/b/c.md`) porque `*` no trata `/` de forma especial fuera de pathname expansion — probado con script ad-hoc (`docs/diagrams/foo.svg` → `rule11=true`), así que no hace falta ninguna sintaxis `**` literal. Reason set = mismo conjunto cerrado de D1 que frontend/backend (`diff-touches-<area>`, `no-<area>-changes`, `pr-shas-missing`, `pr-base-unreachable`, `git-diff-failed`, `push-before-is-zero`, `push-before-unreachable`, `workflow-dispatch-runs-full-suite`, `unknown-event-defaults-to-full-suite`).
- 4.3/4.6: árbol de veredicto del consolidador replica exactamente el fix de §3 (rama "suite ejecutada" = `SUITE_RESULT == success` estricto, sin `|| failure`); probado con script ad-hoc los 6 casos (detect-failure→fail, success/success→pass, skip+area=false→pass-OMITIDA, skip+area=true→fail-incoherencia, suite=failure→fail, suite=cancelled→fail) y los 6 dieron el resultado esperado.
- 4.7: verificado en disco — `python3 -c "import yaml; ..."` parseó ambos ficheros (`YAML OK`); jobs/if/needs/timeouts/concurrency confirmados por inspección estructurada (PyYAML); SHAs (`actions/checkout@11d5960a326750d5838078e36cf38b85af677262`, `astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9`) intactos en ambos workflows, sin tags flotantes. No se creó ningún PR/commit dummy ni fichero sintético (`docs/foo.md`, `sdd/steering/security.md`) — toda la verificación fue lectura + simulación local de los bloques `evaluate`/veredicto en `/private/tmp/.../scratchpad/` (borrados al terminar).
- 5.1/5.2: `docker/build-push-action@v6` (multiarch-build-check.yml, tag flotante ya existente antes de este change — no es un pin propio de la sección 5, no se toca) y `docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6` (deploy-dev.yml, pinneado, no tocado) reciben solo dos claves nuevas cada uno dentro de `with:`. Scopes usados (D4, 4 disjuntos): `multiarch-backend`, `multiarch-frontend`, `deploy-backend`, `deploy-frontend`. `mode=max` en los 4. `type=gha` en los 4 (no `type=registry`).
- 5.3: `git diff main -- .github/workflows/multiarch-build-check.yml .github/workflows/deploy-dev.yml` muestra ÚNICAMENTE las 8 líneas `cache-from`/`cache-to` añadidas (4 pares) — cero cambios a `tags`, `labels`, `build-args`, `context`, `file`, `target`, `platforms`, el SHA pinneado de `build-push-action`, el job `provenance` ni el job `deploy`. `python3 -c "import yaml; ..."` → `YAML OK` en ambos ficheros. Scopes confirmados disjuntos por grep (`multiarch-*` vs `deploy-*`, cero solapamiento).
- 6.1: `multiarch-build-check.yml` NO adopta el patrón de 3 jobs (a diferencia de lo que hicieron las secciones 3/4 con frontend-tests/compose-ports/rule11-ownership): sigue con `paths:` en `on:` y dos jobs disjuntos `build-backend`/`build-frontend`, sin consolidador. El draft de `multiarch-build-check.md` documenta esa forma tal cual está en disco — no una forma inventada. Confirmado leyendo el workflow en disco antes de escribir el draft.
- 6.1: `sdd/specs/compose-ports.md` NO existe como fichero — el contrato vigente de `compose-ports.yml` vive dentro de `sdd/specs/local-environment.md` § «Guardia de la postura de red» (líneas 347-451). El draft (`spec-changes/compose-ports.md`) da texto para las dos rutas (actualizar esa sección in situ, o extraer a un `compose-ports.md` dedicado) y deja la decisión explícitamente a `/sdd:archive`, en vez de asumir una de las dos.
- 6.1: `app-deploy-dev.md` no usa numeración `R1.5` explícita en el texto vivo — la cita "(R1.5 de `app-deploy-dev.md`)" viene literal de `design.md:215` y `design.md:242`, referida al job `provenance` que compone SHA/revision, versión canónica y labels OCI antes de los builds. El draft de `multiarch-build-check.md` reproduce esa misma cita tal como la usa `design.md`, sin inventar un número de requisito que no está en el spec vivo.
- 6.3 (R7.1): `gh auth status` confirmó sesión autenticada (`mreyesojeda`, scopes `repo`/`workflow`) — se usó `gh run list`/`gh run view` real, NO el placeholder de "sin acceso a runs". Baseline capturada 2026-09-03 sobre `autohostai-labs/AutoHostAI`, muestras de 5 runs `pull_request` por workflow (o últimos 15 para el ratio de fallos). Punto asimétrico documentado explícitamente en `metrics.md`: **solo `backend-tests.yml`** tenía ya en disco, en el momento de la captura, el patrón que este change generaliza (3 jobs). `multiarch-build-check.yml` **no** tenía caché de Buildx en el baseline (`git show HEAD:.github/workflows/multiarch-build-check.yml | grep -c cache` = 0 sobre `888edfa4`) — los scopes disjuntos `mode=max` los **introduce** este change (§5/D4), así que `multiarch-build-check.yml` está en forma pre-change igual que `frontend-tests.yml`/`compose-ports.yml`/`rule11-ownership.yml`; la baseline de los cuatro mide lo que este change reemplaza o extiende. **(Corregido en §10.1/QA-F1: la versión anterior de esta nota afirmaba erróneamente que multiarch ya traía los scopes disjuntos pre-change.)**
- 6.3: wall-clock del job más lento medido en la baseline (`backend-tests-suite`, media 637s/~10,6 min) YA EXCEDE el target de ≤9 min declarado en la misma sección — se dejó constancia explícita en la tabla de Targets en vez de ocultarlo, como la brecha más probable a revisar tras el merge.
- Verificación final: `git status --porcelain sdd/specs/` vacío tras completar 6.1-6.3 (confirmado dos veces, antes y después de escribir los cinco drafts) — ningún spec vivo tocado por esta sección.
- 7.1: backend `docker compose exec backend uv run pytest -q -rs -n 2` → **9880 passed, 41 skipped, 0 failed** en 1365s, **cero menciones de "coverage"** en la salida (grep -ic coverage = 0). Un primer pase dio 1 fallo espurio en `tests/messaging/test_repositories.py:754` ("the loser never blocked on the winner's row: the two did not race") — test de carrera de row-lock, timing-sensitive, **no tocado por este change** (que solo altera workflows + pyproject/uv.lock); pasó verde al re-ejecutar la suite entera. Flaky, no defecto.
- 7.2: `npm run lint` → verde (exit 0); `npm run typecheck` → verde (exit 0). `npm test` (Vitest node) → **2095 passed**; 4 ficheros en rojo, todos NO del change: dos son los artefactos de entorno documentados en `project.md` (contenedor monta solo `./frontend`) — `features/provenance/workflow-contract.test.ts` (ENOENT `/.github/workflows/deploy-dev.yml`) y `lib/config/build-identity-contract.test.ts` (ENOENT `/backend/app/provenance/provenance-contract.json`); los otros dos (`features/cleaning/components/cleaning-view.test.tsx` con timeout de `waitFor`, y `test/topbar-overflow.test.ts`) fueron **flaky por contención de CPU** (la suite corrió en paralelo con el pytest del backend; environment setup 949s) y **pasan verde en aislamiento** (`npm test -- --run <los-dos>` → 2 files, 48 tests, exit 0). `npm run test:layout` (Vitest browser) NO ejecutado en local: requiere Chromium + system libs; limitación de host documentada y no bloqueante (la cobre CI vía la cache de §2 + `--with-deps`). Gotcha de entorno resuelto por el implementer de §7: el contenedor `frontend` quedó con `node_modules/.bin` vacío (install a medias) → `next: not found` → restart loop; un `npm ci` limpio (one-off `docker compose run --rm --no-deps`) lo dejó bien.
- 7.3: simulación local de los tres `case` de detect (script ad-hoc, sin push) sobre 13 conjuntos de paths — none→false, area→true, mixed→true por área, más path acentuado (`frontend/café.ts`→true) y profundo (`docs/diagrams/foo.svg`→true, `backend/app/domain/x.py`→true). Todos coincidieron con lo esperado. Anclas leídas en disco = D1 literal en los tres workflows; arranque en `true`; `outputs.<area>`+`reason` publicados; invocación `git -c core.quotePath=false diff -z --no-renames --name-only` con rango `...` (PR) y `before after` (push).
- 7.4/7.5: los tres workflows (`frontend-tests.yml`, `compose-ports.yml`, `rule11-ownership.yml`) tienen exactamente `*-detect`/`*-suite`/`<consolidador>` con los nombres de D1; `*-suite` con `if: needs.<detect>.outputs.<area> == 'true'`; consolidador `if: always()` + `needs: [detect, suite]`. Árbol de veredicto fail-closed verificado en los tres: `DETECT!=success`→fail, `SUITE==success`→pass (rama estricta, sin `|| failure` — el fix de §3), `skipped && area==false`→pass `**OMITIDA**`, `skipped && area==true`→fail, señal!=success→fail. Frontend usa bucle señal-a-señal (9 señales con `continue-on-error`); compose/rule11 usan hard-steps (`SUITE_RESULT` basta).
- 7.6/7.7: Playwright en `frontend-tests.yml` = `actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4` (restore línea 333 + save línea 384), key `playwright-${{ hashFiles('frontend/package-lock.json') }}` sin `restore-keys` (grep `restore-keys: |` vacío), install `npm exec --no -- playwright install --with-deps chromium` sin condicional de cache (corre siempre), save `if: steps.cache-restore.outputs.cache-hit != 'true'`. Buildx: los 4 builds con `cache-from`/`cache-to: type=gha,mode=max` y scopes disjuntos (`multiarch-{backend,frontend}`, `deploy-{backend,frontend}`). Las 5 señales de provenance de R3.3 conservadas en `frontend-tests-suite`. Las 6 invariantes D5 legibles e intactas. **Baseline correcto = punto de rama (`888edfa4`)**, NO `origin/main`: ver blocker de base obsoleta en `BLOCKED.md`. El diff propio del change (`git diff` vs `888edfa4`) sobre `deploy-dev.yml`/`multiarch-build-check.yml` es EXACTAMENTE los 8 renglones de cache (4 pares), labels OCI y jobs `provenance`/`deploy` sin tocar.
- 7.8: `grep -rnE "\-\-cov|pytest-cov|addopts.*cov" .github/workflows/` → cero; `grep -n pytest-cov backend/pyproject.toml backend/uv.lock` → cero; `grep -rn "restore-keys: |" frontend-tests.yml` → cero; sin `upload-artifact`/`download-artifact` en `frontend-tests.yml`; `npm ci` como invocación real exactamente 1 vez (línea 269; las otras 8 coincidencias son comentarios/echo); `actions/cache` pinneada por SHA.
- **§7 blocker de base obsoleta (registrado en `BLOCKED.md`, tipo `decision`):** el tip de rama `888edfa4` es el merge-base y está ~40 commits por detrás de `origin/main` (`3e0aa4bb`); nunca se commiteó nada en la rama (todo el trabajo está sin commitear en el árbol). Desde el punto de rama, `origin/main` fusionó y archivó `ci-runner-oci` (runners self-hosted, `.env` fuera del workspace compartido, `Logout GHCR`) y el trabajo de SMTP (secrets SMTP, `PUBLIC_HOSTNAME`/`FRONTEND_BASE_URL`), todo lo cual toca `deploy-dev.yml`. Un `git diff origin/main` de `deploy-dev.yml` PARECE revertir ese trabajo fusionado — es ruido de staleness, no del change. Al sincronizar la base en `/sdd:ship`, `deploy-dev.yml` **conflictuará** (ambos lados editan los bloques `with:` de `build-push-action`) y la resolución debe conservar **tanto** los renglones de cache **como** el trabajo de runner/SMTP/seguridad de main. Requiere decisión humana en ship.
- 9.1: `rule11-ownership-detect` `case` reemplazado por las 12 anclas de D1 (`sdd/steering/*`, `sdd/specs/*`, `sdd/project.md`, `sdd/README.md`, `sdd/metrics.md`, `docs/*`, `backend/app/*`, `backend/alembic/versions/*`, `backend/tests/*`, `scripts/*`, `Makefile`, `.github/workflows/rule11-ownership.yml`). El header del workflow no enumeraba las rutas propias (solo hablaba de "rutas vigiladas" en genérico, o citaba el `case` de `backend-tests.yml` como incidente ajeno) — no requería edición de prosa, solo la línea del `case`.
- 9.2: `compose-ports-detect` `case` reemplazado por las 6 anclas (`docker-compose*.yml`, `docker-compose*.yaml`, `docker-compose.worktree.yml`, `scripts/*`, `Makefile`, `.github/workflows/compose-ports.yml`). Mismo caso: el header no enumeraba rutas propias, nada más que editar.
- 9.3: `frontend-tests-detect` `case` reemplazado por las 9 anclas (`frontend/*`, `frontend/devops/Dockerfile`, `frontend/package.json`, `frontend/package-lock.json`, `scripts/validate-provenance-contract.py`, `scripts/check-version-parity.py`, `.github/scripts/*`, `Makefile`, `.github/workflows/frontend-tests.yml`); `backend/pyproject.toml`, `VERSION` y `backend/app/provenance/contract.py` deliberadamente NO anclados (frontera D1.1). A diferencia de rule11/compose, el header de `frontend-tests.yml` (líneas 11-13) SÍ enumeraba las anclas en prosa ("el manifiesto, el lockfile, el Dockerfile del frontend o este propio workflow") — actualizado para nombrar los guards fuera de `frontend/**` (`scripts/validate-provenance-contract.py`, `scripts/check-version-parity.py`, `.github/scripts/**`, `Makefile`).
- 9.4: harness bash en scratchpad (`verify_detect.sh`) que copia literalmente los 3 bloques `case` on-disk y los evalúa contra listas de ficheros sintéticas (sin PRs, sin ficheros reales). 18/18 casos pasan:

  | Caso | Área | Fichero único | Esperado | Obtenido |
  |---|---|---|---|---|
  | anti-fail-open | rule11 | `scripts/rule11-ownership.py` | true | true |
  | anti-fail-open | rule11 | `scripts/compose-offset.py` | true | true |
  | anti-fail-open | rule11 | `backend/alembic/versions/0001_x.py` | true | true |
  | anti-fail-open | rule11 | `Makefile` | true | true |
  | anti-fail-open | rule11 | `sdd/project.md` | true | true |
  | anti-fail-open | compose | `scripts/compose-ports.py` | true | true |
  | anti-fail-open | compose | `scripts/test_compose_stacks.py` | true | true |
  | anti-fail-open | compose | `Makefile` | true | true |
  | anti-fail-open | frontend | `scripts/validate-provenance-contract.py` | true | true |
  | anti-fail-open | frontend | `scripts/check-version-parity.py` | true | true |
  | anti-fail-open | frontend | `.github/scripts/extract-pr.sh` | true | true |
  | anti-fail-open | frontend | `Makefile` | true | true |
  | no-over-trigger | rule11 | `sdd/changes/foo/proposal.md` | false | false |
  | no-over-trigger | compose | `backend/app/models/x.py` | false | false |
  | no-over-trigger | frontend | `backend/app/models/x.py` | false | false |
  | no-over-trigger | compose | `frontend/lib/x.ts` | false | false |
  | no-over-trigger | rule11 | `frontend/lib/x.ts` | false | false |
  | frontera documentada | frontend | `backend/pyproject.toml` | false | false |

- 9.5: `make check-rule11-ownership` (857 .py + 102 .md recorridos, veredicto ok), `make check-compose-ports` (7 servicios, 4 mapeos, veredicto ok) y `make check-version-parity` (0.1.0) pasan sin cambios. `bash .github/scripts/extract-pr.sh --self-test` → ok. `python3 scripts/validate-provenance-contract.py --self-test` → ok (importa `backend/app/provenance/contract.py` sin problema en este host). **Limitación de entorno (no defecto):** `uv` no está instalado en este host (`/bin/bash: uv: command not found`), así que la invocación pinneada `uv run --no-project --with 'pytest==9.1.1' python -m pytest scripts/ -q` no pudo ejecutarse literal; se corrió el equivalente con el `pytest` del host (9.0.2, no 9.1.1) — `python3 -m pytest scripts/ -q` → **247 passed**. El contenido de la suite es intacto por esta sección; la limitación es de disponibilidad de `uv` en este host de verificación, no de la remediación.
- 9.6: recuento de anclas por `grep`+`tr '|' '\n' | wc -l` confirma 12 (rule11) / 6 (compose) / 9 (frontend), coincidente con D1 y con R5.2/R5.1/R1.2 de `proposal.md` (texto EARS leído literal, coincide con el `case` on-disk palabra por palabra). `grep -n "backend/" .github/workflows/frontend-tests.yml` → cero resultados, confirma ausencia de ancla `backend/**` en `frontend-tests-detect` (frontera deliberada D1.1).
- Panel §9 fix-round 1 (cicd FAIL → refutada): el reviewer cicd marcó `backend/openapi.json` como ancla ausente de `frontend-tests-detect` (lo lee `npm run api:check` en `provenance_suite`), afirmando que ningún otro required check cubre la deriva `openapi.json → tipos generados`. **Falso positivo:** existe `.github/workflows/frontend-api-contract.yml` que corre `npm run api:check` **incondicionalmente en cada PR** (sin `paths:`, sin detect gate; líneas 6-10,39-41) — es el gate designado de esa deriva; el reviewer solo auditó `api-contract.yml` (backend side) y no lo vio. Anclar `backend/openapi.json` en `frontend-tests-detect` sería redundante con un check que ya corre siempre y erosionaría R7. Resolución: NO se ancla; se documenta `backend/openapi.json` como tercer ítem de la "Frontera deliberada" de D1.1 citando `frontend-api-contract.yml` como su gate, y se re-corre cicd con esa evidencia.
- Panel §10 **PASS 2026-09-05** (architect/security/qa/cicd/documentation; i18n/tenancy no aplican — §10 solo toca `scripts/test_rule11_ownership.py` + artefactos de `sdd/changes/`). Fix-rounds: **round 1** los 5 reviewers verificaron sus fixes → architect/security/cicd/documentation PASS, qa FAIL con 2 residuales; **round 2** (qa) PASS. QA-F1: la nota de impl de 6.3 repetía la afirmación falsa de F1 sobre la caché pre-change de multiarch → corregida y marcada. QA-F2: la fila suelta al final de `metrics.md` **no era telemetría filtrada** sino el *ledger* que `usage-phase.sh` del toolkit añade a `sdd/changes/<feature>/metrics.md` (línea 22 `ledger=…/metrics.md`, línea 55 `>>`); resuelto envolviéndola en una sección `## Registro de uso del toolkit (auto-generado)` con cabecera de tabla, para que las filas del toolkit sean coherentes y separadas de la evidencia R7 (borrarla era inútil: se re-añade en cada `usage-phase`). Verificación de cierre: `python3 -m pytest scripts/test_rule11_ownership.py -q` → **24 passed** (uv ausente en host, limitación §9.5); `make check-rule11-ownership` OK (SCOPE intacto); `sdd/specs/` sin tocar; `usage-phase.sh` ejecutado (fila nueva coherente bajo la sección de ledger).
- §11 (review FAIL #2, cierre de la clase de regresión de conditional-gates). Auditoría antes-vs-después: los 3 workflows eran always-run (frontend 2 jobs, compose/rule11 1); todo quedó tras `*-detect`. Residuales cerrados: **SEC-1** anclas compose = familia de descubrimiento por defecto de Docker Compose (`compose.yaml|yml|override.*` + `docker-compose*`), porque `compose-ports.py` corre `docker compose config` sin `-f` (`config_command()` = COMPOSE+CONFIG_BASE, verificado). **SEC-2** `version-parity.yml` nuevo, always-run, stdlib (sin uv), `make check-version-parity`; señal `version_parity` conservada en la suite (belt-and-suspenders); `VERSION`/`backend/pyproject.toml` NO se anclan en frontend-detect (evita arrastrar la suite; su gate es version-parity.yml). **SEC-3** `scripts/check-detect-surface.py` (stdlib, 293 líneas; expone `detect_anchors`/`uncovered`/`rule11_surface`/`compose_surface`/`frontend_surface`/`COMPOSE_DISCOVERY_FAMILY`/`check`) cableado como paso **always-run sin `if:`** en los 3 `*-detect`, ANTES del `decide`, fuera del `set +e` → superficie incompleta = `DETECT_RESULT != success` = fail-closed (no fail-open). `test_rule11_ownership.py` refactorizado para importar los helpers de ahí (una fuente de verdad). **QA-R2.4** proposal enmendado (install siempre / no-op del binario en cache hit, D3). Tests anti-regresión: `scripts/test_detect_surface.py` (10 tests, 4 escenarios de R5.5 — cobertura de superficie compose/frontend, familia Compose completa + rojo, wiring always-run estructural, skip-ante-input-que-altera-el-gate + no-sobredisparo + cobertura de version-parity). Gotcha: primer intento del test de `paths:` daba falso positivo por el substring en el comentario de cabecera de `version-parity.yml` — corregido a parsear solo líneas no-comentario (claves YAML reales). Drafts resincronizados (compose 10 anclas, rule11/frontend mención always-run) + `spec-changes/version-parity.md` creado. **Verificación §11.7:** `check-detect-surface.py rule11|compose|frontend` exit 0; `python3 -m pytest scripts/ -q` → **259 passed** (249+10); `make check-{rule11-ownership,compose-ports,version-parity,detect-surface}` OK; los 4 workflows parsean; paso always-run sin `if:` confirmado en los 3 detect. NO se tocó `multiarch-build-check.yml` (findings pre-existentes fuera de scope por decisión de usuario). Implementer opus cayó por 429 tras 11.1-11.3; 11.5-11.7 + marcado completados por el orquestador.
- **/sdd:review feature-scale round 5 → panel PASS 2026-09-07** (tras remediación). Panel de 7 lentes: i18n, tenancy, cicd, documentation, qa, architect → **PASS**; security → **FAIL** con 1 bloqueante (F1). **F1 (MEDIUM):** `scripts/check-detect-surface.py:_SCRIPTS_REF` usaba el lookbehind `(?<![\w./-])scripts/…` que excluía `/`, cegando toda referencia a script de raíz con prefijo de ruta en la suite frontend (`bash ./scripts/x.sh`, `../scripts/x.sh`, `$VAR/scripts/x.sh`) y la forma bare `pytest scripts/` — reabría la clase fail-open §9/§11/§12 (un guard `bash ./scripts/x.sh` no lo vería la superficie, y un PR que tocara solo ese script recibiría `**OMITIDA**` verde). **Fix:** regex `(?:^|(?<=[\s=("']))(?:\.{1,2}/|$\{?\w+\}?/)*(scripts/[A-Za-z0-9_./-]*)` — captura las formas con prefijo `./`,`../`,`$VAR/`,`${VAR}/` (cierra el fail-open), captura la forma bare `scripts/` (fail-closed para el detector frontend, que ancla solo 2 `.py` nombrados), sigue delegando `.github/scripts/…` en `_GH_SCRIPT`, y NO confunde `frontend/scripts/…` (input `frontend/**`, evita falso positivo que pondría el guard always-run en rojo sobre un PR legítimo — hueco que el one-liner `(?<!\.github/)` propuesto sí habría abierto). **F2 (tests):** `test_frontend_surface_regex_is_path_prefix_agnostic` (regresión de F1), renombrado del test mal etiquetado a `test_frontend_detect_anchors_only_named_scripts_not_the_whole_tree` (solo fija estrechez de anclas), y `test_frontend_surface_end_to_end_reports_a_prefixed_suite_script` (ejercita `frontend_surface()` sobre un workflow sintético en tmp_path — no-vacuo, falla contra el regex previo). No bloqueantes también corregidos: **F3** nota "Known limitations" (indirección `make`/`uses:`) en el docstring del módulo; **F4** premisa falsa de scoping de cache GHA en `design.md` (un PR no consume la cache de otro; se aísla por `ref`); conteos de architect (`metrics.md` cinco→cuatro `*-detect`; `spec-changes/backend-ci.md` cuatro→tres specs; `sdd/roadmap/…` cuatro→seis workflows +version-parity); nota obsoleta de `coverage` en tasks.md alineada al estado real (0 refs en uv.lock); `version-parity.md` añadido a "Affected specs" del proposal. `multiarch-build-check.yml` NO tocado (permissions/tags flotantes fuera de scope por decisión de usuario, tasks.md:189). **Verificación:** `check-detect-surface.py {rule11,compose,frontend}` exit 0; `python3 -m pytest scripts/ -q` → **263 passed** (261+2); `make check-{detect-surface,version-parity}` OK. **Re-review dirigido de security (round 6) → PASS, 0 findings**: F1 confirmado cerrado en los 4 ejes por ejecución directa del regex, e2e test no-vacuo (retorna `[]` con el regex previo), invariantes SEC-1/2/3 + consolidadores fail-closed intactos. **local_review: READY_FOR_PR.**
- 13.1 (SEC-medium): `_SCRIPTS_REF` deja de ser **allow-list de prefijos virtuales** y pasa a regla **fail-closed**: un token `scripts/<path>` es referencia de raíz salvo que sea subdirectorio real de otro directorio ya anclado (`frontend/scripts/…` → `frontend/*`; `.github/scripts/…` → `_GH_SCRIPT` + `.github/scripts/*`). Regex nuevo = frontera exterior `(?:^|(?<=[\s=("']))` + grupo VIRTUAL-PREFIX repetible que **nunca** puede casar un `word/` bare — `\.\.?/|\$\{[^}]*\}/|\$\w+/|\$\([^)]*\)/|` + backtick + `|"[^"]*"/|'[^']*'/` — + captura `(scripts/[A-Za-z0-9_./-]*)`. Claves: la alternativa `"[^"]*"/` es la que caza `"$ROOT"/scripts/x.sh` (comilla cerrada ANTES de la barra, la grafía que el allow-list soltaba); `\$\{[^}]*\}/` sustituye a `\$\{?\w+\}?/` para admitir modificadores (`${VAR:-.}`); `\$\([^)]*\)/` y la forma con backticks cubren la sustitución de comando (el motor entra por la posición posterior a la comilla de apertura, donde el lookbehind `"` se satisface). Exclusiones intactas sin `(?<!…)` alguno: `frontend/` no casa ninguna alternativa virtual y `scripts` va precedido de `/` (no es char de frontera). Docstring del módulo reescrito (marco fail-closed + Known limitations nueva: ruta ensamblada en runtime a partir de piezas que no aparecen literales en la línea, p. ej. `d=scripts; bash "$d/x.sh"`). NO se tocó `rule11_surface`/`compose_surface` ni ancla alguna.
- 13.2: test hermano nuevo `test_frontend_surface_regex_is_fail_closed_on_every_root_prefix_spelling` (se conserva intacto `test_frontend_surface_regex_is_path_prefix_agnostic`, que es la regresión de F1/round-5): 4 grafías nuevas → `scripts/x.sh`, las 2 exclusiones → `[]`, y `check("frontend") == []` como pin de que el ensanche es endurecimiento y no cambio de anclas.
- 13.3: `README.md:35` — `make check-detect-surface` insertado inmediatamente tras `make check-rule11-ownership` en el bloque de comandos, glosa en el estilo de las hermanas.
- 13.4 Verificación: (a) las 10 grafías «deben capturar» + 2 «no capturar» ejecutadas directamente contra `_SCRIPTS_REF` → 12/12 esperado (antes del fix 4/12 fallaban devolviendo `[]`: `"$ROOT"/`, `"$(…)/`, backticks, `${VAR:-.}/`); (b) `python3 scripts/check-detect-surface.py {frontend,compose,rule11}` → exit 0 los tres, `check("frontend") == []`; (c) `python3 -m pytest scripts/ -q` → **264 passed** (263 + 1 test nuevo); (d) prueba-del-rojo por inyección del regex previo en `cds._SCRIPTS_REF` antes de invocar el test: el nuevo **falla** (AssertionError) contra el regex pre-fix y pasa con el fix — no vacuo; (e) `make check-detect-surface` exit 0; (f) `grep -n "make check-detect-surface" README.md` → línea 35. Limitación de entorno heredada de §9.5: `uv` ausente en este host, se usa el `pytest` del host.
- §13 panel fix-round 1 (security FAIL → resuelto): el panel de run de §13 dio architect/qa/cicd/i18n/tenancy PASS y **security FAIL** con 1 medium — el regex de 13.1 (allow-list de prefijos VIRTUALES) seguía soltando en silencio cualquier `scripts/<path>` precedido por un directorio real NO anclado (`bash lib/scripts/x.sh`, `vendor/scripts/x.sh`, `tools/scripts/x.sh`, `~/repo/scripts/x.sh` → `[]`), contradiciendo el contrato del propio fichero («OVER-match, never under-match»): la misma clase de silent-drop, ahora por un subdirectorio real en vez de una grafía virtual imprevista. **Fix (deny-list de exactamente 2 prefijos):** `_SCRIPTS_REF = (?<![A-Za-z0-9_])(?<!frontend/)(?<!\.github/)(scripts/[A-Za-z0-9_./-]*)` — dos lookbehind negativos de ancho fijo para los ÚNICOS prefijos excluidos (`frontend/`, `.github/`); toda otra grafía (bare, `./`, `../`, `$VAR/`, `${VAR:-.}/`, `"$ROOT"/`, `$(…)/`, backticks, y cualquier `<dir>/scripts/…` no anclado) cae a la captura → over-match fail-closed en vez de drop silencioso. El boundary de palabra evita el falso positivo en `myscripts/`. Docstring del módulo (punto 2 + Direction of error) reescrito de «allow-list de prefijos virtuales» a «deny-list de exactamente dos prefijos». Test nuevo `test_frontend_surface_regex_denies_only_the_two_anchored_prefixes` (prueba-del-rojo: el regex intermedio devolvía `[]` sobre `lib/scripts/x.sh`). **Verificación:** 14/14 «capturar» (incl. las 4 nuevas `<dir>/scripts/`) + 3/3 «no capturar» (frontend/, .github/, myscripts) por ejecución directa; `check(frontend|compose|rule11)` → `[]`/exit 0; `python3 -m pytest scripts/ -q` → **265 passed** (264 + 1); `make check-detect-surface` exit 0. Re-review dirigido de security pendiente para cerrar el panel §13.
- §13 panel fix-round 2 (security FAIL → resuelto): el re-review de security halló que los lookbehind de ancho fijo `(?<!frontend/)`/`(?<!\.github/)` no estaban anclados a frontera de componente de ruta → cualquier directorio que **terminara** en `frontend`/`.github` (`notfrontend/`, `old-frontend/`, `myfrontend/`, `sub.github/`, `myapp.github/`) caía en silencio en la exclusión: la misma clase de under-match, reubicada del enumerado de prefijos a la frontera del lookbehind. **Fix (comparación de segmento completo, no substring/lookbehind):** `_SCRIPTS_REF` (regex) sustituido por la función `_root_script_refs(line)` — un candidato `_SCRIPTS_CANDIDATE = (?<![A-Za-z0-9_.-])scripts/[A-Za-z0-9_./-]*` localiza cada token `scripts/…` en frontera de componente, y `_PRECEDING_SEGMENT = ([A-Za-z0-9_.-]+)/\Z` extrae el **segmento de ruta inmediatamente anterior completo**; solo se excluye si ese segmento es exactamente `frontend` o `.github` (`_ANCHORED_SIBLINGS`). Toda otra grafía (incluidas `notfrontend/`, `lib/`, `$(…)/` con espacios, `"$ROOT"/`, `${VAR:-.}/`) cae a la captura → over-match fail-closed. `frontend_surface()` y los tests llaman ahora `_root_script_refs`; `_SCRIPTS_REF` eliminado (0 referencias colgando). Test nuevo `test_frontend_surface_excludes_by_whole_segment_not_by_suffix` (prueba-del-rojo de los 5 casos sufijo-hermano). Docstring del módulo (cabecera + el propio `_root_script_refs`) reescrito a «deny-list por segmento completo». **Verificación:** 21/21 «capturar» (incl. los 5 sufijo-hermano + lib/vendor/tools/~repo) + 4 «no capturar» (frontend/, .github/, myscripts, y check-version-parity anclado) por ejecución directa; `check(frontend|compose|rule11)` → `[]`/exit 0; `frontend_surface()` sin cambios (3 elementos); `python3 -m pytest scripts/ -q` → **266 passed** (265 + 1); `make check-detect-surface` exit 0. Re-review dirigido de security pendiente (2.ª y última ronda de fix permitida por la skill).
- §13 re-review fix-round 2 → **security FAIL de nuevo** (3.er finding posicional de la MISMA clase): `_root_script_refs` compara el segmento inmediato pero no exige que `frontend`/`.github` esté en la RAÍZ de la ruta → un directorio **anidado** cuyo último segmento sea exactamente `frontend`/`.github` (`a/frontend/scripts/x.sh`, `sub/frontend/scripts/x.sh`, `vendor/.github/scripts/x.sh`, `deep/nested/frontend/scripts/x.sh`) se descarta en silencio (verificado: `_root_script_refs(...)==[]` en los cuatro). Misma clase under-match ya hallada en round-5 (allow-list), fix-round-1 (dir real no anclado), fix-round-1b (sufijo-hermano), ahora reubicada a la **profundidad de ruta**. **Límite de la skill alcanzado: máximo 2 fix-rounds por sección → STOP y escalar la DECISIÓN al usuario.** Todos los findings de esta clase han sido LATENTES en cada ronda (`check("frontend")==[]`, `frontend_surface()` estable en 3 elementos); ninguno lo dispara la suite frontend de hoy. La decisión de raíz (dissolver la clase anclando `scripts/*` en `frontend-tests-detect` como hacen compose/rule11, vs. seguir parcheando el parser textual, vs. aceptar y documentar como known-limitation) toca la frontera «anclas estrechas» de design D1.1.a (decisión deliberada de usuario por el ahorro de R7) → requiere al usuario. **§13 panel NO marcado PASS.** Ver `BLOCKED.md`.
- §13-A (resolución, decisión de usuario 2026-09-07 = **opción A: anclar `scripts/*`**): implementado como tareas 13.5/13.6. Cambio sustantivo = `.github/workflows/frontend-tests.yml` ancla `scripts/*` en bloque (antes: 2 scripts `.py` nombrados). `check-detect-surface.py` reescrito: `_root_script_refs` reporta cada referencia `scripts/…` por su **ruta real** (quita solo prefijos virtuales reconocidos, conserva segmentos reales) — se eliminan `_SCRIPTS_REF` (deny-list frágil) y `_GH_SCRIPT`; `_SCRIPTS_TAIL`+`_VIRTUAL_PREFIX`+`_REAL_SEGMENTS` nuevos. Esto disuelve la clase: `scripts/*` cubre cualquier root-ref por el `case` sobre rutas cambiadas, con independencia del parser; un dir real anidado no anclado (`lib/scripts/`, `a/frontend/scripts/`) se reporta por su ruta real → uncovered → rojo (fail-closed, ya no silent-drop). Tests migrados a semántica de ruta real (el linaje round5→fix1→fix1b→fix2 colapsa en `test_frontend_surface_keeps_real_directory_segments`). Docs sincronizadas: `design.md` D1.1 fila + **D1.1.d**; `proposal.md` R1.2 → `scripts/**`. **Verificación:** 22 grafías por ejecución directa OK; `scripts/*` ∈ anclas frontend; `check(frontend|compose|rule11)`→`[]`; `frontend_surface()` sin cambios (`['.github/scripts/extract-pr.sh','Makefile','scripts/validate-provenance-contract.py']`); `python3 -m pytest scripts/ -q` → **265 passed**; `make check-detect-surface` exit 0; YAML de `frontend-tests.yml` parsea. Coste R7 aceptado por el usuario. Re-run del panel §13 pendiente (ahora toca un workflow → cicd aplica).
- §13-A panel (re-run): qa/cicd/i18n/tenancy PASS; **architect FAIL** (1) + **security FAIL** (1), ambos triviales y remediados:
  - **architect (doc-consistency):** la fila de la tabla de *derivación* de dependencias de D1.1 (`design.md:42`, columna «Anclas derivadas» de `frontend-tests`) seguía listando los dos scripts `.py` nombrados en vez de `scripts/*`, contradiciendo D1.1.d y la fila-resumen (línea 21) ya sincronizada. **Fix:** `design.md:42` → `scripts/*` (supersede los dos nombrados, ya subsumidos).
  - **security (parser robustness, SEC-medium):** `_REAL_SEGMENTS = (?:[A-Za-z0-9_.-]+/)+\Z` no casaba un prefijo con separador **doblado** (`notfrontend//scripts/x.sh`, plausible de una var que ya acaba en `/` o un typo de path-join) → `real=None`, se soltaba el prefijo real entero y se reportaba el `scripts/x.sh` pelado (cubierto por `scripts/*`) = silent fail-open. Distinto disparador de los 4 históricos (que security confirma **disueltos**). **Fix:** `_REAL_SEGMENTS` usa `/+` (tolera separadores repetidos) + `_REPEATED_SLASH` colapsa `/{2,}`→`/` en el ref construido, de modo que el directorio real se conserva y se reporta con ruta limpia (`notfrontend/scripts/x.sh` → descubierto → rojo, fail-closed). Test nuevo `test_frontend_surface_tolerates_doubled_path_separators`. **Verificación:** `notfrontend//`, `a/frontend//`, `lib///`, `scripts//`, `"$DIR/"/` resueltos correctamente por ejecución directa; `check(frontend|compose|rule11)`→`[]`; `frontend_surface()` sin cambios; `python3 -m pytest scripts/ -q` → **266 passed**; `make check-detect-surface` exit 0.
- §13-A re-review (fixes) → architect **PASS**; **security FAIL** (1) de nuevo — un componente `..` pegado a un directorio anclado (`frontend/../notfrontend/scripts/x.sh`) se conservaba verbatim y `fnmatch(...,'frontend/*')` casaba (el `*` cruza `/`) → cubierto, aunque la ruta real resuelve a `notfrontend/scripts/x.sh` no anclado = fail-open. **Fix (normalización canónica, subsume el parche de doble-barra):** `_root_script_refs` normaliza el ref con `posixpath.normpath` (resuelve `.`/`..` y colapsa `/` repetidas) antes de comparar contra las anclas, preservando la barra final de una referencia de directorio pelado (`scripts/`); `_REPEATED_SLASH` retirado (subsumido). `_REAL_SEGMENTS` mantiene `/+` para capturar el prefijo real completo (incl. segmentos `.`/`..`), que normpath luego resuelve. Test nuevo `test_frontend_surface_resolves_dot_dot_segments_before_anchoring`. **Verificación:** `frontend/../notfrontend/scripts/x.sh`→`notfrontend/scripts/x.sh` (descubierto→rojo, el caso de security); `scripts/../lib/scripts/x.sh`→`lib/scripts/x.sh`; `a/./scripts/x.sh`→`a/scripts/x.sh`; sin fail-open nuevo en la interacción `..` (el peor caso es un falso-rojo fail-closed, dirección segura); `check(frontend|compose|rule11)`→`[]`; `frontend_surface()` sin cambios; `python3 -m pytest scripts/ -q` → **267 passed**; `make check-detect-surface` exit 0. Re-review dirigido de security pendiente. Postura: normpath es la normalización canónica y comprehensiva del sub-clase de rutas; si security halla otro fail-open **real** (no un falso-rojo) tras esto, es el punto de escalar al usuario — el ancla `scripts/*` es el guardián real y el parser textual está en su límite.
