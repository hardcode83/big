# Tasks: auth-tenancy

Orden pensado para que el sistema siga arrancando y con la suite verde al final de cada
sección. Las secciones 1-2 son fontanería que no cambia comportamiento observable; los
endpoints no aparecen hasta la 7, cuando ya existe todo lo que necesitan.

Comandos del proyecto (`sdd/project.md`): tests `docker compose exec backend uv run pytest`
(o `docker compose run --rm backend uv run pytest` con el stack parado); arranque `make up`.

## 1. Dependencias y configuración <!-- panel: N/A (sección de config/dependencias, sin código de producción) -->

- [x] 1.1 Añadir `pyjwt` y `bcrypt` a `[project].dependencies` de `backend/pyproject.toml` y regenerar `uv.lock`; confirmar que la imagen dev las instala (`docker compose build backend` y un `import jwt, bcrypt` dentro del contenedor). Sin `passlib` (design D4). [R1, R6]
- [x] 1.2 Extender `backend/app/core/config.py` con `jwt_secret_key`, `jwt_access_token_minutes` (15), `jwt_refresh_token_days` (7), `bcrypt_rounds` (12), `login_rate_limit_per_minute` (10), `login_max_failed_attempts` (10), `login_lockout_minutes` (15), `trusted_client_ip_header` (vacía) y los `bootstrap_*`. Test: `Settings` sin `JWT_SECRET_KEY` falla al construirse con mensaje explícito, y el algoritmo **no** es configurable (constante en el código, design D3). [R1.6, R1.7, R6.7]
- [x] 1.3 Declarar las variables nuevas en `.env.example` **por nombre y sin valor** (incluida `JWT_SECRET_KEY`) y en `docker-compose.yml` (servicios `backend` y `worker`) con `${JWT_SECRET_KEY:?...}`. Para no romper el arranque sin pasos manuales (DoD §28.20), el target `up` del `Makefile` **genera** la clave con `openssl rand -hex 32` y la escribe en el `.env` local si falta o está vacía — idempotente, también sobre un `.env` preexistente. Así se cumple a la vez la regla 8 de `steering/security.md` y el comentario que `local-environment` ya dejó en `.env.example` ("la clave de firma JWT nunca lleva default aquí"): el valor se genera en la máquina, nunca vive en el repositorio, y `.env` está en `.gitignore`. Los `BOOTSTRAP_*_PASSWORD` van vacíos y **no** se generan, porque una contraseña de usuario la elige la persona (R7.4). [R6.7, R7.4]

## 2. Fontanería HTTP compartida <!-- panel: PASS 2026-07-30 -->

- [x] 2.1 Crear `backend/app/core/errors.py`: `AppError(code, message, http_status, details)` y las subclases `ValidationError`, `InvalidCredentialsError`, `InvalidTokenError`, `ForbiddenError`, `NotFoundError`, `RateLimitedError`; `register_error_handlers(app)` con handlers para `AppError`, `RequestValidationError` y `HTTPException`. Tests en `backend/tests/test_error_envelope.py`: cada código produce `{"error":{"code","message","details"}}` con el status correcto, y **el 422 de FastAPI sale reformado** (por defecto es `{"detail":[...]}`, que `frontend/lib/api/errors.ts:isApiErrorEnvelope` rechazaría degradando a `UNKNOWN_ERROR`). Mensajes en inglés. [R6.2]
- [x] 2.2 Convertir `backend/app/main.py` en `create_app()` que registre handlers y routers, conservando `app = create_app()` a nivel de módulo. `GET /health` se queda en la raíz, fuera de `/api/v1/`. Verificar que `backend/tests/test_health.py` sigue pasando sin tocarlo y que el healthcheck de `docker-compose.yml:69` sigue respondiendo. [R6.1]
- [x] 2.3 Añadir a `backend/app/core/db.py` la dependencia `get_db_session()` que cede una `AsyncSession`, hace `rollback()` si la request termina en excepción y `close()` siempre. Tests: una excepción dentro del handler no deja la escritura persistida, y la sesión queda cerrada en ambos caminos. El `commit` NO vive aquí (design D10). [R6.3]
- [x] 2.4 Crear `backend/app/core/redis.py` con cliente `redis.asyncio` perezoso y su dependencia, leyendo `REDIS_URL`. Test de que la construcción es perezosa y no abre conexión al importar. [R5.4]

## 3. Dominio de auth (TDD — `domain/` es Python puro) <!-- panel: PASS 2026-07-30 (tras una ronda de arreglos) -->

- [x] 3.1 `backend/app/auth/domain/exceptions.py`: familia `AuthDomainError` con `InvalidCredentials`, `InvalidToken`, `TokenTypeMismatch`, `SessionReuseDetected`, `AccountLocked`, `PasswordTooLong`, siguiendo el patrón de `app/timeline/domain/exceptions.py`. Test primero de que cada una es subclase de la base. [R1.4, R2.2, R2.5]
- [x] 3.2 `backend/app/auth/domain/context.py`: `RequestContext` frozen dataclass con `user_id`, `tenant_id`, `role`. Test primero: es inmutable y valida que los UUID son UUID y el rol un `UserRole`. [R4.1]
- [x] 3.3 `backend/app/auth/domain/policy.py`: enum `Permission` con **solo** los permisos que este change usa, `ROLE_PERMISSIONS: Mapping[UserRole, frozenset[Permission]]` que materializa PRD §6, y `is_allowed(role, permission) -> bool`. Test primero parametrizado sobre los cinco valores de `UserRole` (incluido que ninguno tiene permisos no declarados). No pre-declarar permisos de módulos futuros (design D8). [R3.1]
- [x] 3.4 `backend/app/auth/domain/entities.py`: añadir la entidad `UserSession` (`id`, `tenant_id`, `user_id`, `family_id`, `parent_id`, `expires_at`, `used_at`, `revoked_at`, `revoked_reason`) con enum `SessionRevokedReason` (`ROTATED`, `LOGOUT`, `REUSE_DETECTED`) en `enums.py`, y el invariante `is_usable(now)` (no usada, no revocada, no expirada). Test primero de las cuatro combinaciones. [R2.1, R2.2]
- [x] 3.5 `backend/app/auth/domain/ports.py`: Protocols `UserRepository` (con `get_by_id(tenant_id, user_id)` y `find_by_email_globally(email)` — el único método sin `tenant_id`, nombrado así a propósito, design D16; async desde 11.10 igual que `PasswordHasher`), `SessionRepository`, `PasswordHasher`, `TokenCodec` y `LoginThrottle`. Todos hablan de entidades de dominio, nunca de modelos ORM. [R6.5]
- [x] 3.6 Crear `backend/tests/test_layering.py`: recorre los ficheros de todo `backend/app/*/domain/` y afirma que ninguno importa `fastapi`, `sqlalchemy` ni `pydantic` (AST, no grep de texto). Debe pasar también sobre los dominios ya existentes. [R6.4]

## 4. Adaptadores de infraestructura <!-- panel: PASS 2026-07-30 (tras dos rondas de arreglos) -->

- [x] 4.1 `backend/app/auth/infrastructure/password_hasher.py`: `BcryptPasswordHasher` implementando el puerto, con `rounds` desde settings. Tests: hash y verificación correctos, contraseña equivocada falla, y **una contraseña de más de 72 bytes UTF-8 lanza `PasswordTooLong` en vez de truncarse** (design D4). [R1.3]
- [x] 4.2 `backend/app/auth/infrastructure/token_codec.py`: `JwtTokenCodec` con HS256 fijado en código, emitiendo `sub`, `tenant_id`, `role`, `type`, `jti`, `iat`, `exp` y `fam` en el refresh. Tests: claims completos, vidas de 15 min y 7 días, firma inválida rechazada, token expirado rechazado, **un refresh presentado como access rechazado y al revés**, y un token con `alg: none` rechazado. [R1.5, R1.6, R2.5]
- [x] 4.3 Añadir `UserSessionModel` a `backend/app/auth/infrastructure/models.py` (con `UUIDPrimaryKeyMixin`, `TenantScopedMixin`, `TimestampMixin`, índices `(tenant_id, user_id)`, `(family_id)`, `(expires_at)`) y crear la migración en `backend/alembic/versions/`. Test: ciclo `alembic upgrade head` + `downgrade -1` sobre la base de test, que hoy ningún test cubre (design, Risks). [R2.1]
- [x] 4.4 `backend/app/auth/infrastructure/repositories.py`: `SqlAlchemyUserRepository` y `SqlAlchemySessionRepository` implementando los puertos, mapeando modelo↔entidad, con `tenant_id` en toda firma salvo la excepción del login. Los repos no hacen `commit` (design D10). Tests de integración sobre `db_session`. [R4.2, R6.5]
- [x] 4.5 `backend/app/auth/infrastructure/throttle.py`: `RedisLoginThrottle` con `login:ip:{ip}` (`INCR` + `EXPIRE 60`), `login:fail:{user_id}` y `login:lock:{user_id}`, más un doble en memoria en `backend/tests/auth/doubles.py` para no exigir Redis en la suite. Tests: 10 intentos/min por IP disparan el límite, 10 fallos consecutivos bloquean 15 min, un login correcto pone el contador a cero, y ventana fija asumida (design D13). [R5.1, R5.2, R5.3, R5.4]

- [x] 4.6 Corregir los hallazgos del panel de las secciones 3-4 (todos con referente): **(a)** `SessionRepository.add` recibe `tenant_id` explícito como el resto de métodos del puerto (D6); **(b)** normalizar el email con `normalize_email` en escritura y lectura, igualdad simple en SQL, más índice único funcional sobre `(tenant_id, lower(email))` y su migración — sin esto dos variantes de mayúsculas conviven en un tenant y dejan **las dos cuentas con 401 permanente** (design D19); **(c)** `token_codec` devuelve `InvalidTokenError` y no un 500 ante un claim de tipo raro (`tenant_id: 123` daba `AttributeError`) o un `exp` desbordado (`OverflowError`), que R2.5 nombra explícitamente como camino de 401; **(d)** el TTL del contador por IP se aplica en cada intento (`EXPIRE ... NX`), porque un `EXPIRE` perdido dejaba la IP bloqueada para siempre y con D12 esa IP es todo el despliegue (design D20); **(e)** `test_layering.py` cubre también los imports relativos que suben de paquete (`from ..infrastructure import x`) y las llamadas a `importlib.import_module` dentro de `domain/`, ambos agujeros demostrados por el panel. [R1.1, R2.5, R4.2, R5.1, R6.4]

## 5. Filtro global de tenant (defensa en profundidad) <!-- panel: PASS 2026-07-30 (tras dos rondas de arreglos) -->

- [x] 5.1 Añadir a `backend/app/core/db.py` el listener del evento `do_orm_execute` que, **solo si `session.info` lleva `tenant_id`**, añade `with_loader_criteria(<modelo>, cls.tenant_id == tenant_id, include_aliases=True)` por cada modelo mapeado con columna `tenant_id`, recorriendo `Base.registry.mappers` (un mixin sin mapear no sirve como entidad de `with_loader_criteria`). Tests: (a) con la sesión marcada, un `select(UserModel)` **sin** filtro explícito no devuelve filas del otro tenant; (b) sin marcar, la sesión no se ve afectada, que es lo que necesita el login; (c) el filtro también alcanza `session.get()` por PK y un UPDATE ORM sin filtro de tenant. **Ajustado durante `/sdd:run`**: la tarea pedía originalmente probar la carga de una relación, pero un `grep` confirma que el proyecto **no define ni una sola `relationship()`** — el test no era escribible. Se sustituye por `session.get()` y el UPDATE ORM, que sí son caminos reales de `repositories.py` (`save()` usa `session.get`, `revoke_family()` usa un UPDATE). Documentar en el docstring los dos límites: no cubre `text()` ni Core, ni sesiones sin contexto (design D16). [R4.2]

## 6. Casos de uso <!-- panel: PASS 2026-07-30 (tras una ronda de arreglos) -->

- [x] 6.1 `backend/app/auth/application/use_cases.py` → `LoginUseCase`: consulta el throttle, busca por email de forma global (una fila como máximo desde ADR 0005, tanda 11.10), verifica el hash, comprueba `UserStatus.ACTIVE` y `TenantStatus.ACTIVE`, actualiza `last_login_at`, resetea el contador de fallos, crea la `UserSession` raíz y emite el par de tokens, con un único `commit` al final. Tests: éxito; email inexistente, contraseña mala, usuario `INACTIVE`, usuario `SUSPENDED` devuelven todos el mismo `InvalidCredentials` indistinguible (el caso de email duplicado en dos tenants desapareció en 11.10: lo rechaza el índice único global); `last_login_at` se actualiza; cuenta bloqueada responde igual que credencial inválida. **Restricción del panel de seguridad**: un intento rechazado por bloqueo **no** debe llamar a `record_failure`, o el bloqueo se auto-renueva y deja de estar acotado a 15 minutos — con test explícito. Consume `TenantStatusReader`, que hasta esta tarea no tiene consumidor. [R1.1, R1.2, R1.3, R1.4, R5.2, R5.3]
- [x] 6.2 `RefreshTokenUseCase`: valida firma y `type`, carga la fila por `jti`, y si es usable marca `used_at` e inserta la hija con el mismo `family_id`, todo en una transacción. Si la fila **ya tiene `used_at`**, revoca la familia completa (`UPDATE ... WHERE family_id = :fam AND revoked_at IS NULL`, `revoked_reason = REUSE_DETECTED`) y lanza `SessionReuseDetected`. Tests: rotación correcta invalida el presentado; reuso invalida la familia entera incluida la hija recién emitida; token expirado, revocado o de `type` access rechazados. [R2.1, R2.2, R2.5]
- [x] 6.3 `LogoutUseCase`: revoca la sesión presentada con `revoked_reason = LOGOUT`. Tests: el refresh deja de servir tras el logout; y un test que documenta R2.4 explícitamente — **un access token emitido antes del logout sigue siendo válido hasta su expiración**, porque no hay lista de revocación de access tokens. [R2.3, R2.4]
- [x] 6.4 `GetCurrentUserUseCase`: devuelve la entidad `User` del contexto. Test de que el caso de uso no filtra `password_hash` hacia arriba. [R2.6]
- [x] 6.5 Añadir el logging de R5.5 en el camino de login: cada intento fallido y cada bloqueo se registran en inglés con `user_id`/IP y **sin la contraseña presentada**. Test que captura los logs y afirma que la contraseña no aparece en ningún registro. [R5.5]

## 7. Capa API <!-- panel: PASS 2026-07-30 (tras una ronda de arreglos) -->

- [x] 7.1 `backend/app/auth/api/schemas.py`: `LoginRequest` (`email`, `password`), `RefreshRequest` (`refresh_token`), `TokenPairResponse` (`access_token`, `refresh_token`, `token_type`, `expires_in`) y `CurrentUserResponse` (`id`, `tenant_id`, `name`, `email`, `role`, `preferred_language`). Tests: ningún schema de request tiene campo `tenant_id`, y `CurrentUserResponse` **no** expone `password_hash` ni `status`. [R2.6, R4.1]
- [x] 7.2 `backend/app/auth/api/dependencies.py`: `get_authenticated_request()` extrae y verifica el Bearer, **recarga usuario y tenant de la base de datos** comprobando que ambos están `ACTIVE`, marca `session.info["tenant_id"]` para el filtro de 5.1, y devuelve `RequestContext` con el rol **de la base de datos**, no del token; más la fábrica `require(permission)`. Tests: sin cabecera → 401; esquema distinto de `Bearer` → 401; usuario suspendido después de emitir el token → 401; rol degradado en BD surte efecto de inmediato; tenant inexistente o no `ACTIVE` → 401. **Dos restricciones del panel de seguridad**: (a) la IP del cliente se valida con `ipaddress.ip_address` tomando el salto de más a la derecha de la última aparición de la cabecera —**corregido en el review**: escribí "primer salto" y con un proxy que añade el primero es el que envía el cliente— con caída al peer del socket si no parsea — si no, con la cabecera activada un atacante se reparte el presupuesto de R5.1 enviando un valor distinto por request; (b) el mapeo de excepciones de dominio a HTTP vive en `app/auth/api/`, no en `app/core/errors.py`, porque `app.auth.domain.exceptions.InvalidTokenError` y `app.core.errors.InvalidTokenError` comparten nombre y un import equivocado daría 500 en lugar de 401. [R3.4, R3.5, R4.5, R5.1]
- [x] 7.3 `backend/app/auth/api/router.py` con los cuatro endpoints bajo `/api/v1/auth`, registrado en `create_app()`: `login` y `refresh` anónimos, `logout` (204) y `me` con Bearer. Declarar el esquema de seguridad Bearer en OpenAPI y anotar `summary`/`description`/`response_model` de cada uno (`steering/documentation.md`). Test de que `/openapi.json` declara el securityScheme y que los endpoints protegidos lo referencian. [R1.1, R2.1, R2.3, R2.6, R6.6]
- [x] 7.4 Crear `backend/tests/test_route_authorization.py`: recorre `app.routes` y, para cada ruta fuera de la lista explícita de anónimas (`/health`, `POST /api/v1/auth/login`, `POST /api/v1/auth/refresh`, `/docs`, `/redoc`, `/openapi.json`), afirma inspeccionando `route.dependant` que declara una dependencia de autenticación/autorización. Añadir en el mismo test una ruta protegida de prueba para comprobar que el mecanismo detecta de verdad la ausencia (que el test puede fallar). [R3.2, R3.3, R3.6]
- [x] 7.5 Tests de integración de los endpoints en `backend/tests/auth/test_api.py` con `httpx.AsyncClient` + `ASGITransport` y overrides de dependencias, cubriendo el flujo completo login → me → refresh → logout y los códigos de error con su sobre. [R1.1, R2.1, R2.3, R2.6, R6.2]

## 8. Aislamiento por tenant <!-- panel: PASS 2026-07-30 -->

- [x] 8.1 `backend/tests/auth/test_isolation.py`: fixtures con **dos** tenants poblados y un usuario de cada uno de los cinco roles en cada tenant; parametrizado por rol, afirma que `UserRepository.get_by_id(tenant_a, user_de_b)` no devuelve nada y que ninguna operación de escritura cruza. [R4.4]
- [x] 8.2 En el mismo fichero: un token válidamente firmado cuyo `tenant_id` es el del tenant B para un `user_id` real de A → 401 en `GET /api/v1/auth/me`; y token cuyo tenant no existe o está no-`ACTIVE` → 401. [R4.1, R4.5]
- [x] 8.3 Test de que un `tenant_id` enviado en el cuerpo, la query o una cabecera de `POST /api/v1/auth/login` y `GET /api/v1/auth/me` es ignorado y no altera el tenant efectivo. [R4.1]
- [x] 8.4 Anotar en el propio fichero de tests, como comentario de cabecera, el alcance real de R4.4 según design D15: la matriz por endpoint de negocio no existe todavía porque no hay endpoints de negocio, y la hereda `reservations`. Sirve para que el panel de `/sdd:review` no lo lea como requisito a medias. [R4.4]

## 9. Bootstrap del acceso inicial <!-- panel: PASS 2026-07-30 (tras una ronda de arreglos) -->

- [x] 9.1 Crear `backend/app/cli/bootstrap.py` ejecutable con `python -m app.cli.bootstrap`: valida que están todas las `BOOTSTRAP_*` **antes** de abrir transacción, crea `Tenant` + `TenantConfig` + un `TENANT_OWNER` y un `PROPERTY_MANAGER` con hash bcrypt, y es idempotente buscando por nombre de tenant y por `(tenant_id, email)`. **Restricción del panel de seguridad**: aplicar `normalize_email` tanto al insertar como en la búsqueda de idempotencia (design D19) — con `BOOTSTRAP_OWNER_EMAIL=Jose@Example.com` sin normalizar, la fila queda en mayúsculas, el login nunca la encuentra y la única cuenta administrativa de un despliegue nuevo no puede entrar, sin ningún error en el bootstrap; y una segunda ejecución con otra caja rompería la idempotencia con `IntegrityError`. Test con una variable en mayúsculas seguido de un login correcto. Tests: primera ejecución crea; segunda no duplica ni modifica; falta una variable → sale con error y la base queda intacta; ninguna contraseña por defecto en el código. [R7.1, R7.2, R7.3, R7.4]
- [x] 9.2 Añadir el target `bootstrap` al `Makefile` (`docker compose exec backend uv run python -m app.cli.bootstrap`), **sin** engancharlo a `up` para no romper el arranque sin pasos manuales. [R7.1]

## 10. Documentación

- [x] 10.1 Escribir `docs/adr/0004-backend-layering-pattern.md` con el formato de los ADR 0001-0003: las cuatro capas y la regla de dependencia, DI por `Depends`, frontera transaccional en el caso de uso, sobre de error por handlers globales, `RequestContext` explícito más el filtro global y sus dos límites, y la cobertura de autorización por test de rutas. Es la referencia que citarán los designs siguientes (design D17). [R6]
- [x] 10.2 Escribir `docs/auth-tenancy.md` (`steering/documentation.md`): cómo se opera — el flujo de login/refresh/logout, el bootstrap y sus variables, la configuración del throttle, y **el aviso de `TRUSTED_CLIENT_IP_HEADER`**: mientras la API no se enrute por el túnel la IP de cliente no es fiable, y cuando se enrute hay que activarla o el límite por IP contará todo el despliegue en un solo contador (design D12). Enlazar a la spec, no duplicarla. [R5.1, R6]
- [x] 10.4 Regenerar el diagrama ER de `docs/diagrams/` para incluir `user_sessions` y su lineaje (`family_id`, `parent_id`), y reemplazar el obsoleto sin acumularlo. **Tarea añadida durante `/sdd:run`**: la levantó el reviewer de documentación citando `steering/documentation.md` ("si el change altera el modelo de datos de forma que un diagrama existente queda obsoleto → regenerarlo"), y mi `tasks.md` no la contemplaba aunque la sección 4 añade una tabla nueva. [R2.1]
- [x] 10.3 Actualizar el `README.md` de la raíz: los cuatro endpoints nuevos, el comando `make bootstrap` con sus variables, y la estructura del backend con las capas `application/`, `api/` y `app/cli/` que antes no existían. [R6, R7.1]

## 11. Verification

- [x] 11.1 Suite completa verde: `docker compose exec backend uv run pytest` (con el stack parado, `docker compose run --rm backend uv run pytest`). [todos]
- [x] 11.2 Ciclo de migración: `alembic upgrade head` y `alembic downgrade -1` sobre la base de test, confirmando que `user_sessions` se crea y se elimina limpiamente. [R2.1]
- [x] 11.3 Arranque real: `make up` desde un `.env` recién copiado de `.env.example` levanta el stack con todos los servicios `healthy` — es la comprobación de que 1.3 no rompió el arranque sin pasos manuales. [R6.7]
- [x] 11.4 Flujo manual end-to-end contra el stack local con `curl`: `make bootstrap`, después `POST /api/v1/auth/login` → `GET /api/v1/auth/me` con el access → `POST /api/v1/auth/refresh` → reusar el refresh viejo y ver que la familia queda revocada → `POST /api/v1/auth/logout`. Comprobar de paso que el sobre de error sale con la forma que espera `frontend/lib/api/errors.ts`. [R1, R2, R6.2]
- [x] 11.5 Confirmar que `GET /health` responde en la raíz y que `docker compose ps` da `healthy` en los servicios que declaran healthcheck en el compose local — `backend`, `postgres` y `redis`; `frontend` y `worker` no declaran uno ahí (sí en el de deploy), así que 'healthy' no les aplica — el healthcheck depende de esa ruta y de él cuelgan los `depends_on` (design D2). [R6.1]
- [x] 11.7 Corregir los 6+5 hallazgos del panel de las secciones 6-9: **(a)** todo camino de fallo del login gasta el mismo trabajo de bcrypt (`PasswordHasher.burn`), porque los cuerpos idénticos no bastaban: 2 ms frente a 240 ms era un oráculo de enumeración de usuarios y de estado de bloqueo, justo lo que la ASSUMPTION de R1.4 quiere impedir; **(b)** `require()` etiqueta el permiso que aplica y el test de rutas busca **esa etiqueta**, no la dependencia de autenticación — con `AuthenticatedDep` siendo un export público, un endpoint autenticado sin permiso pasaba el test; **(c)** el bootstrap se niega si el email ya existe bajo otro tenant, porque la idempotencia va por nombre de tenant y `tenants` no tiene unicidad en `name`: un typo en `BOOTSTRAP_TENANT_NAME` dejaba las dos cuentas administrativas sin poder entrar, para siempre; **(d)** `get_client_ip` toma el salto de más a la **derecha** y la última aparición de la cabecera — el primero es el que envía el cliente con un proxy que añade, así que mi versión leía entrada del atacante; **(e)** tests nuevos para la denegación 403 (era código muerto para los 1115 tests), para R2.4 en la capa donde viviría una lista de revocación, y para que el rol venga de la BD y no del claim; **(f)** dos tests míos dependían del entorno del contenedor en vez de monkeypatchear, y se rompieron al rellenar las `BOOTSTRAP_*` en 11.4. `/auth/refresh` como cuarto camino sin marcar quedó documentado en D16. [R1.4, R3.4, R5.1, R7.2]
- [x] 11.8 Corregir los 20 hallazgos del panel de `/sdd:review` a escala de feature (siete reviewers, tres FAIL de fondo). **Código:** (a) la rotación de refresh era un read-then-write sin guarda —dos presentaciones concurrentes del mismo token veían ambas `used_at IS NULL`, las dos rotaban y la detección de reuso de R2.2 no saltaba— ahora hay un `consume()` con `UPDATE ... WHERE used_at IS NULL` y comprobación de `rowcount`; (b) mi propio arreglo del oráculo por tiempo tenía dos agujeros: con la caché fría `burn()` costaba **dos** operaciones bcrypt, así que el primer login con email inexistente de cada worker delataba la no-existencia, y el doble de test contaba 1 afirmando una propiedad que no se cumplía — ahora el hash señuelo se precalienta al importar, con test que cuenta las llamadas reales; (c) quitada la asignación directa de `revoked_reason` sobre la entidad, que dejaba filas con razón de revocación y `revoked_at` en `NULL`; (d) eliminada `AccountLockedError`, definida y mapeada a HTTP pero sin ningún caller. **Mecanismo:** (e) el test de cobertura de autorización solo recorría `APIRoute`, así que un `websocket`/`mount`/`Route` añadía superficie sin autenticar que nunca inspeccionaba, y leía la etiqueta desde `route.dependant`, cuyo `.call` es el propio endpoint — ahora falla ante cualquier tipo de ruta no reconocido fuera de la lista de anónimas y solo lee la etiqueta de nodos de dependencia. **Requisitos:** (f) R4.3 queda declarado unmet como se hizo con R4.4 (pide 404 frente a 403 y ningún endpoint de este change recibe un identificador de recurso); (g) R7 se cierra para el entorno desplegado — `make bootstrap` usaba `uv run`, ausente de la imagen `prod`, y apuntaba al compose local: ahora es `python -m`, que vale en las dos imágenes, con el procedimiento completo en RUNBOOK §6.5 y la nota en `.env.deploy.example`. **Propagación:** (h) la obligación cross-tenant del alta de usuarios anotada en la entrada `user-management` del roadmap, porque un admin del tenant B podía dejar fuera indefinidamente al propietario del tenant A. **Documentación:** (i) nueve correcciones de coherencia, entre ellas que `docs/auth-tenancy.md` documentaba el bug del primer salto que 11.7 había arreglado, que tres ficheros citaban `get_current_context()` —una función que nunca existió— y que el ADR y el docstring de `db.py` no coincidían en el número de límites del filtro. [R1.4, R2.1, R2.2, R3.2, R4.3, R7.1]
- [x] 11.9 Corregir los 17 hallazgos de la segunda vuelta de `/sdd:review` sobre el diff commiteado (seis reviewers; dos PASS, cuatro FAIL). **Bloqueantes:** (a) `consume()` cerraba la carrera de `used_at` y dejaba abierta la de revocación — sin `revoked_at IS NULL` en el `WHERE`, un `revoke_family` concurrente (logout, o reuso en un hermano) perdía el desempate y se insertaba una hija nueva de 7 días que sobrevivía a la revocación; añadidas `revoked_at` y `expires_at` a la sentencia; (b) la lista de rutas anónimas iba por path y no por método, así que un `GET /login` o un `POST /health` heredaba la exención — ahora es un conjunto de `(método, path)`. **Tests que no probaban lo que decían**, ambos demostrados vacuos por QA con mutación: (c) el test del rol desde la BD pasaba aunque se usara `claims.role`, porque `/me` vuelve a leer la fila por su cuenta — sustituido por una aserción directa sobre el `RequestContext` que produce `get_authenticated_request`; (d) los tests de "concurrencia" de la rotación eran secuenciales en una sola sesión, y un read-then-write ingenuo los pasaba — añadido uno con dos sesiones separadas compitiendo bajo `asyncio.gather`, más los de sesión revocada y expirada. **DESIGN-CONFLICT:** (e) `SessionRevokedReason.ROTATED` se quedó sin call site al dejar de marcar la rotación como revocación; eliminado del enum, de la migración y de D5, porque una rotación no es una revocación y el valor contradecía su propio nombre. **Superficie especulativa:** (f) eliminados `SessionRepository.save` y `UserRepository.save`, sin caller de producción y footguns al lado de las primitivas seguras — el segundo además copiaba la fila entera y podía **revertir una suspensión concurrente**, sustituido por un `touch_last_login` dirigido; (g) el check cross-tenant del bootstrap pasa por el puerto, de modo que la única query sin scope vuelve a ser de verdad una sola y el grep de D16 sigue siendo exhaustivo. **Seguridad operativa:** (h) el RUNBOOK pasaba las contraseñas con `-e`, afirmando que así no quedaban en ningún fichero: acaban en el `bash_history` y en `/proc/<pid>/cmdline` (CWE-214) — ahora por env-file `600` borrado al terminar. **Documentado, no resuelto:** (i) igualar el trabajo de bcrypt subió la amplificación del atacante de ~1 a ~100 en el camino del email inexistente, con ~0,25 s de CPU síncrona en el event loop; hoy no es alcanzable y queda como requisito de `api-ingress-routing`, que es el change que lo hace alcanzable. Más seis correcciones de coherencia documental. [R1.2, R1.4, R2.1, R2.2, R3.3, R7.4]
- [x] 11.10 Aplicar los seis bloques de la **revisión humana de Marta sobre el PR #25**, tres de ellos convirtiendo en trabajo de este change lo que yo había dejado como deuda futura. **(1) Requisitos y alcance:** R4.3 y la matriz por endpoint de R4.4 salen de aquí y entran como criterios **bloqueantes** de `user-management` —no de `reservations`, como decía D15: `user-management` va antes en el roadmap y ya recibe `users/{id}` y `tenants/{id}`—; reescritos D15, `proposal.md` (TRASLADADO, conservando la numeración para no romper las referencias a R4.5 repartidas por código y tests), la tabla de cobertura y la entrada del roadmap. Sin override de archive. **(2) bcrypt fuera del event loop (D21 nueva):** el puerto `PasswordHasher` pasa a async y sus cinco llamantes con él; `hash`/`verify`/`burn` corren con `anyio.to_thread.run_sync` bajo un `CapacityLimiter` **de módulo** —`get_password_hasher()` crea una instancia por request, así que uno por instancia no acotaría nada— indexado por event loop para que la suite, que usa un loop por test, no choque con primitivas de anyio de otro loop. Medido antes de decidirlo: un `verify` en el loop congela el proceso 250 ms y ocho se serializan en 1,87 s, frente a 271 ms en hilos con el loop libre; bcrypt 5.0.0 libera el GIL, así que el paralelismo es real. La cota es lo que impide que el pool por defecto de anyio (40 hilos) ponga 40 bcrypt en una VM de 4 OCPU. Tests por identidad de hilo, no por latencia, y de la cota con un bcrypt falso bloqueado; ambos verificados en mutación. Se conserva la simetría del atajo de >72 bytes entre `verify` y `burn`, con test propio, porque romperla es el mismo oráculo con el signo cambiado. **(3) Email como identidad global (ADR 0005 nuevo):** índice único funcional `uq_users_lower_email` sobre `lower(email)`, retirada `UNIQUE(tenant_id, email)`, garantía en base de datos y no en Python; `find_by_email_across_tenants` pasa a `find_by_email_globally` devolviendo `User | None` y desaparece la regla de "exactamente una coincidencia" y su rama en el caso de uso. Reescrita la migración `e1eed2e039ee` en vez de apilar una que deshaga la anterior. Es desviación deliberada de PRD §7.3 y **no se edita el PRD**: la desviación se registra en el ADR, en D16/D19 y en `docs/auth-tenancy.md`. Efecto lateral en el roadmap: la obligación cross-tenant que 11.8 dejó anotada en `user-management` deja de ser una regla que recordar y pasa a ser invariante del esquema — lo que le queda es traducir el `IntegrityError` a 409. **(4) Gate de CI:** `.github/workflows/backend-tests.yml` con Postgres **y Redis** como services —Redis no es opcional: `test_throttle.py` no tiene skip a propósito, así que sin él esos tests van a rojo en vez de desaparecer del recuento—, migraciones sobre base limpia, `alembic check`, suite completa y `downgrade base`; sin filtro `paths`, porque un required check con filtro no se ejecuta en los PR que no tocan esas rutas y GitHub deja el PR esperando para siempre. Clave JWT generada por ejecución con `openssl rand -hex 32`, para no versionar nada con pinta de secreto. **(5) Nombre de la base de tests:** `tests/db_names.py` da un sufijo por proceso a la base de la suite y a la de `test_migrations.py` —esta última abría con `DROP DATABASE`, así que una segunda ejecución concurrente borraba la base de la primera— más borrado al cerrar la sesión de pytest y `make db-clean-test` para las huérfanas. Verificado ejecutando dos suites en paralelo: 1172 verdes cada una y cero bases residuales. **(6) Coherencia:** un único contrato para el secreto JWT — se mantiene `JWT_SECRET_KEY`, que ya es consistente en los doce ficheros que lo usan, y se documenta por qué frente al `SECRET_KEY` de PRD §25 (ya convive `ENCRYPTION_KEY`, y la variable vive en OCI Vault y en el `.env` de la VM, así que renombrar es un paso operativo, no una edición); anotadas en el roadmap la prioridad acotada de `auth-account-recovery`/`saas-cross-tenant`, `api-ingress-routing` como requisito previo a exposición pública y `guest-portal` como capability omitida por el roadmap original; RUNBOOK §6.4 con la query de duplicados agrupada solo por dirección. [R1.3, R1.4, R1.8, R1.9, R4.3, R4.4, R5.1]
- [x] 11.11 Cubrir las **siete verificaciones del bloque 5 de la revisión formal de Marta** (`pullrequestreview-4819522597`). Su revisión es de 13:57Z y el primer commit de la tanda 11.10 de 14:30Z, así que sus bloques 1-4 estaban ya resueltos cuando la escribió —describe el hasher como síncrono y el índice como `(tenant_id, lower(email))`—, pero el bloque 5 pide siete propiedades concretas **confirmadas por test** y solo tres lo estaban. Auditadas una por una: ya cubiertas la rotación concurrente única (`test_only_one_of_two_concurrent_consumers_wins`) y la imposibilidad de sustituir el tenant por cuerpo, query o cabecera (tres tests en `test_isolation.py`). **Cuatro no lo estaban y son nuevas:** (a) el perdedor de la carrera revoca **también la hija creada por el ganador** — el test que existía consumía la fila directamente, así que no había hija y la propiedad no era observable; (b) un logout concurrente no deja insertar después una hija sin revocar; (c) `touch_last_login` **no** se ejecuta en un login fallido (solo estaba el caso positivo, y el negativo es el que importa: una columna que se mueve con una contraseña equivocada deja de ser rastro de auditoría y delata que el intento alcanzó una cuenta real); (d) una suspensión, un cambio de rol o de contraseña confirmados a media petición **no se pierden** — es la propiedad por la que 11.9 sustituyó `save(user)` por un `UPDATE` de una columna, ahora fijada como guarda de regresión. Y (e) para el séptimo punto, que solo estaba afirmado por grep: un test de superficie que falla si `save` reaparece en cualquiera de los dos repositorios, con la salvedad escrita de que la ausencia de consumidores no se puede demostrar con un test — eso es lo que hace el grep y la revisión — pero la ausencia del método sí, y es la parte que se degrada. **Un test mío volvió a ser vacuo y lo detecté con mutación**: el del logout concurrente confirmaba el logout *antes* de que el refresh leyera, así que lo rechazaba `is_usable` en la entidad y `consume()` no llegaba a ejecutarse — pasaba con la guarda quitada. Reescrito inyectando el logout **entre** la lectura y la escritura del caso de uso con un envoltorio del repositorio, que es el único instante en que las dos operaciones pueden competir. Los cuatro verificados en mutación, cada uno contra el cambio que debería detectar: quitar la guarda de revocación de `consume()`, acotar `revoke_family` a una sola fila, mover `touch_last_login` antes de autenticar y devolverle la escritura de la fila entera. [R1.2, R2.1, R2.2, R2.3, R4.1]
- [x] 11.6 **No hay comando de lint/typecheck en el proyecto**: `sdd/project.md` no lo declara y los tres workflows de `.github/workflows/` (deploy-dev, infra-dev, multiarch-build-check) no ejecutan ni `ruff` ni `pytest`. No se inventa uno aquí; queda como observación para el change que decida montar CI de backend. **Segunda observación para ese mismo change**: el nombre de la base de test es fijo (`<db>_test`), así que dos `pytest` concurrentes en la misma máquina se pisan en el `create_all`/`drop_all` por test — lo encontró el arquitecto al no poder verificar el commit 1 de forma independiente. No lo causa este change (el `conftest` viene de antes) pero tampoco lo arregla; un sufijo por proceso lo resolvería. **Actualización (11.10):** las dos observaciones dejan de estar abiertas — la revisión de Marta pidió el gate de CI y el arreglo del nombre de la base, y ambos entran en este change. Lo que sigue sin existir es lint/typecheck: `sdd/project.md` no declara ningún comando, y el workflow nuevo no inventa uno.

## Cobertura de requisitos

R1 → 1.1, 1.2, 4.1, 4.2, 6.1, 7.3, 7.5, 11.4 · R2 → 3.4, 4.2, 4.3, 6.2, 6.3, 6.4, 7.1, 7.3, 7.5, 11.2, 11.4 ·
R3 → 3.3, 7.2, 7.4 · R4 → 3.2, 4.4, 5.1, 7.1, 7.2, 8.1, 8.2, 8.3, 8.4 · R5 → 4.5, 6.1, 6.5, 10.2 ·
R6 → 2.1, 2.2, 2.3, 3.5, 3.6, 7.3, 10.1, 10.2, 10.3, 11.3, 11.5 · R7 → 1.3, 9.1, 9.2.

Ningún criterio de aceptación del alcance de este change queda sin tarea, y **ninguno queda
sin cumplir**.

Lo que antes figuraba aquí como "dos criterios incumplidos pero declarados" **ya no
pertenece a este change**: R4.3 (404 frente a 403 al referenciar un recurso de otro tenant)
y la matriz por endpoint de negocio y por rol de R4.4 están **trasladados a
`user-management`** como criterios de aceptación bloqueantes de su entrada del roadmap
(revisión del PR #25, design D15, y `proposal.md` los marca TRASLADADO conservando la
numeración). El alcance de `auth-tenancy` es la infraestructura de autenticación,
autorización, `RequestContext` y tenant scoping; R4.3 y esa matriz necesitan endpoints que
reciban identificadores de recurso, y `user-management` es la primera entrada que los tiene
—y va antes que `reservations`, que es donde los dejaba la versión anterior de esta nota—.

Esto es lo que hace coherente el estado del change: ya no hay un `local_review: APPROVED`
que convive con requisitos declarados incumplidos, y no se cierra ninguno con un override al
archivar.
