# Tasks: pricing-web

Todas las rutas son relativas a `frontend/`. Ninguna tarea toca `backend/`: el contrato
está congelado y `lib/api/generated/openapi.d.ts` ya declara las cinco operaciones
(design, «Changes by area», última fila).

Cada tarea trae su test (regla de `steering/testing.md`). El orden mantiene el árbol
compilando y la suite verde después de cada sección: la sección 1 es una refactorización
sin cambio de comportamiento, las secciones 2-5 añaden módulos aislados que nadie importa
todavía, y `/pricing` sólo deja de ser placeholder en la sección 8.

## 1. Paleta de tonos compartida (prerrequisito, sin cambio de comportamiento) <!-- panel: PASS 2026-08-23 -->

- [x] 1.1 Crear `lib/ui/status-tone.ts` con `export type Tone = "green" | "blue" | "amber" | "red" | "gray"` y `export const TONE_BADGE_CLASS: Record<Tone, string>`, copiando **literalmente** las cinco cadenas Tailwind que hoy están duplicadas en `components/property-state-badge.tsx` (`STATE_BADGE_CLASS`, privado al módulo) y `features/cleaning/lib/task-status.ts` (`STATUS_BADGE_CLASS`). Antes de escribir, enfrentar las dos declaraciones y confirmar que siguen idénticas cadena a cadena; si divergieron, parar y reportarlo en vez de elegir una. [R6.7]
- [x] 1.2 `components/property-state-badge.tsx`: borrar su `STATE_BADGE_CLASS` privado e importar `TONE_BADGE_CLASS`; dejar `StateColorGroup` como alias de `Tone` (lo consumen `property-state-badge.test.tsx` y `features/properties`). **No editar `property-state-badge.test.tsx`.** [R6.7]
- [x] 1.3 `features/cleaning/lib/task-status.ts`: borrar el valor y conservar el nombre (`export const STATUS_BADGE_CLASS = TONE_BADGE_CLASS`), con `StatusColorGroup` como alias de `Tone`; corregir su comentario obsoleto, que apunta a `features/dashboard/components/property-card.tsx` cuando la segunda copia vive en `components/property-state-badge.tsx` desde `properties-web`. **No editar `task-status.test.ts`.** [R6.7]
- [x] 1.4 Verificar la equivalencia: `npm run typecheck` y `npx vitest run components/property-state-badge.test.tsx features/cleaning features/properties` pasan **sin haber editado ningún test**. Si algún test hubo que tocarlo, la extracción no fue equivalente: revertir la sección 1 y dejar la paleta duplicada (condición explícita de D22). [R6.7]

## 2. Costura de datos de la feature <!-- panel: PASS 2026-08-23 -->

- [x] 2.1 `features/pricing/data/dto.ts` (nuevo): `PricingPage<T>` (`items`, `total`, `page`, `perPage`, `totalPages`), `PriceRecommendationStatus` desde `components["schemas"]`, `DecisionStatus` como `Extract<…, "APPROVED" | "REJECTED" | "APPLIED_EXTERNAL">`, `PriceRecommendation` **sin** `current_price`, `confidence` ni `created_at`, `ModifierCounts`, `PricingRule` con `modifierCounts` y **sin** las cinco columnas JSONB, `GenerationReport`, `PropertySummary`, `RecommendationFilters`, `PricingRuleFilters`. El nombre `PricingPage` es deliberadamente distinto de `PaginatedResponse`. [R2.2, R2.5, R2.6, R5.2, R5.4, R6.1]
- [x] 2.2 `features/pricing/data/pricing-source.ts` (nuevo): interfaz `PricingDataSource` con `listRecommendations`, `listRules`, `listProperties`, `decideRecommendation`, `generateRecommendations`. **No declarar** ningún método de detalle de regla. [R5.5]
- [x] 2.3 Comprobar D20 **antes** de escribir el adaptador: pasar el filtro `active` (booleano) a `GET /api/v1/pricing-rules` y correr `npm run typecheck`. Si no compila, ensanchar `RequestOptions.query` en `lib/api/client.ts` a `Record<string, string | number | boolean | null | undefined>` y añadir a `lib/api/client.test.ts` el caso de que un booleano se serializa como `active=true`. Si compila tal cual, **no** tocar `client.ts` y anotarlo aquí. [R5.1] — **Resultado: no compilaba.** `TS2322: Type '{ page: number; per_page: number; active: true; }' is not assignable to type 'Record<string, string | number | null | undefined> & { … active?: boolean | null | undefined; }'`. Ensanchado hecho en `RequestOptions.query` y en `appendQuery`, con el caso `active=true` en `lib/api/client.test.ts`.
- [x] 2.4 `features/pricing/data/http/http-pricing-source.ts` (nuevo) + `.test.ts`: mapeo `{items,total,page,per_page}` → `PricingPage<T>` con `totalPages = perPage > 0 ? Math.ceil(total / perPage) : 0`; filtro de estado enviado con el nombre de query **`status`**; `date_from`/`date_to`/`property_id`/`active` como query; `countEntries(value)` = `Object.keys(value).length` para `weekday_modifiers` (objeto), `value.length` para los cuatro arrays, `0` para cualquier otra cosa. Tests: el campo se llama `items` y no `data`, `total = 0 → totalPages = 0`, `per_page = 0 → 0`, el `status` viaja con ese nombre, y `countEntries` nunca lee un valor del JSONB. [R2.1, R2.2, R2.5, R5.1, R5.2, R5.4]
- [x] 2.5 `features/pricing/data/http/http-pricing-source.ts`: `listProperties` pide `GET /api/v1/properties` con `page: 1, per_page: 100`, con el `ASSUMPTION` anotado en el código (a partir de la vivienda 100 la identidad degrada). `decideRecommendation` hace `PATCH /api/v1/price-recommendations/{id}` con cuerpo `{status}` y nada más (`extra="forbid"`); `generateRecommendations` hace `POST /api/v1/price-recommendations/generate` con `{property_id}`. Tests del cuerpo exacto de las dos escrituras. [R2.8, R3.1, R4.1]
- [x] 2.6 `features/pricing/data/index.ts` (nuevo): `getPricingDataSource()` con `createAuthenticatedClients`, copiando la forma de `features/cleaning/data/index.ts` para que los tests de componente puedan inyectar un doble. Sin `MockPricingSource`. [R2.1]

## 3. Módulos puros de la feature <!-- panel: PASS 2026-08-23 -->

- [x] 3.1 `features/pricing/lib/format.ts` (nuevo) + `.test.ts`: `fmtDecimal(value: string, locale: string)` con `Number(value)` **sólo para formatear**, dos decimales fijos y la cadena original si el número no es finito, **sin símbolo ni código de moneda**; el `locale` es un parámetro y no `undefined` (enmienda de D14 acordada el 2026-08-23: `undefined` es el locale del runtime y no el de i18next, contra R6.2, y dejaba el test de ES vs EN sin poder escribirse) — los llamantes pasan `i18n.language`; `fmtDay(isoDay, locale)` con `Intl.DateTimeFormat(locale, { dateStyle: "medium", timeZone: "UTC" })`. Tests: separador decimal de ES vs EN, cadena no numérica devuelta tal cual, y `"2026-01-01"` formateado con `TZ` simulada al oeste de UTC sigue dando el día 1. No se escribe `fmtUtc`: ningún `created_at` se muestra. [R6.1, R6.2, R6.3]
- [x] 3.2 `features/pricing/lib/recommendation-status.ts` (nuevo): `RECOMMENDATION_STATUS_ORDER` derivado de las claves de un `Record<PriceRecommendationStatus, …>` exhaustivo en el orden de PRD §7.18 (`DRAFT`, `RECOMMENDED`, `APPROVED`, `APPLIED_EXTERNAL`, `REJECTED`), y `RECOMMENDATION_STATUS_TONE: Record<PriceRecommendationStatus, Tone>` (`DRAFT → gray`, `RECOMMENDED → amber`, `APPROVED → blue`, `APPLIED_EXTERNAL → green`, `REJECTED → red`) importando `Tone` de `lib/ui/status-tone.ts`. Anotar como `ASSUMPTION` que reutiliza la paleta y **no** son los colores de estado operacional de PRD §9.1. [R6.4, R6.7]
- [x] 3.3 `features/pricing/lib/decision-moves.ts` (nuevo) + `.test.ts`: `legalMoves(status): readonly DecisionStatus[]` sobre un `Record<PriceRecommendationStatus, readonly DecisionStatus[]>` exhaustivo — `RECOMMENDED → ["APPROVED","REJECTED"]`, `APPROVED → ["APPLIED_EXTERNAL"]`, `DRAFT`/`APPLIED_EXTERNAL`/`REJECTED → []`. Tests: los cinco estados, y un estado desconocido (deploy skew) devuelve `[]`. Comentar que es affordance y no autoridad: el backend valida y contesta `409`. [R3.1, R3.2]
- [x] 3.4 `features/pricing/lib/pricing-error.ts` (nuevo) + `.test.ts`: tres tablas `Record<number, string>` con `?? GENERIC`, eligiendo por `ApiError.status` — decidir (`403`, `404`, **`409`**, `422`, genérico), regenerar (`403`, `422`, genérico), leer (`403`, genérico). Sin rama `401` (lo resuelve el cliente HTTP). Tests: el `409` da una clave distinta del genérico, y **ninguna** de las tres funciones lee `message`, `code` ni `details`. [R3.6, R3.7, R3.8]
- [x] 3.5 `features/pricing/lib/property-directory.ts` (nuevo) + `.test.ts`: unión de cuatro formas `portfolio | pending | unavailable | resolved`, adaptada de `features/cleaning/lib/directory.ts`. `property_id === null` (sólo alcanzable desde reglas) resuelve a `portfolio`, no a «sin asignar»; el fallo del catálogo resuelve a `unavailable` y **no** propaga a error de vista. Tests de las cuatro ramas. [R2.8, R5.3]

## 4. Store de UI <!-- panel: PASS 2026-08-23 -->

- [x] 4.1 `features/pricing/state/use-pricing-ui-store.ts` (nuevo) + `.test.ts`: Zustand con `tenantId`, `activeTab: "recommendations" | "rules"` (inicial `"recommendations"`) y **dos rebanadas independientes** — `recommendations {propertyId, dateFrom, dateTo, status, page}` y `rules {propertyId, active, page}`. El reset a página 1 vive **dentro de cada setter**; `adoptTenant` devuelve el store entero al estado inicial al cambiar de tenant; `setActiveTab` no toca ninguna rebanada. Tests: reset a página 1 por setter, las dos rebanadas no comparten `propertyId` ni `page`, cambiar de pestaña conserva filtros y página de cada una, y `adoptTenant` con otro tenant borra el `propertyId` anterior. [R1.1, R1.3, R4.1]

## 5. Hooks de query y mutación <!-- panel: PASS 2026-08-23 -->

- [x] 5.1 `features/pricing/hooks/query-keys.ts` (nuevo) + `.test.ts`: sobre `tenantScopedKey`, recursos `"pricing-recommendations"`, `"pricing-rules"`, `"pricing-properties"`, y las entradas `recommendations(tenantId, filters, page)`, `recommendationsPrefix(tenantId)`, `rules(tenantId, filters, page)`, `properties(tenantId)`. `normalizeRecommendationFilters` / `normalizeRuleFilters` emiten en **orden fijo**, omiten las ausentes y canonizan `page`. Tests: dos objetos de filtro equivalentes con otro orden dan la misma clave, y `recommendationsPrefix` es prefijo de `recommendations` para cualquier filtro y página. [R2.1, R3.4, R5.1]
- [x] 5.2 `features/pricing/hooks/use-pricing-data.ts` (nuevo) + `.test.tsx`: `useRecommendations`, `usePricingRules`, `usePropertyDirectory` con la `retryPolicy` del árbol. El fallo del catálogo no marca error en las otras dos. [R2.1, R2.8, R5.1]
- [x] 5.3 `features/pricing/hooks/use-decide-recommendation.ts` (nuevo) + `.test.tsx`: `retry: false`, **sin** parcheo optimista, con `invalidateQueries({ queryKey: pricingKeys.recommendationsPrefix(tenantId) })` en **`onSettled`**. Tests: la invalidación ocurre también cuando el `PATCH` falla, y la clave de reglas **no** se invalida nunca. [R3.4, R3.5]
- [x] 5.4 `features/pricing/hooks/use-generate-recommendations.ts` (nuevo) + `.test.tsx`: `retry: false`, cuerpo `{property_id: <filtro activo de recomendaciones> | null}`, misma invalidación de prefijo en `onSettled`, y devuelve los cuatro contadores. Tests: el `property_id` sale de la rebanada de **recomendaciones** y nunca de la de reglas; la invalidación ocurre también al fallar. [R3.5, R4.1, R4.2, R4.4] — **Nota:** el hook lee el filtro del store en vez de recibirlo, y por eso lleva su propio guardia de tenant obsoleto; el `staleFilters` de la vista (7.9) cubre las lecturas y no esta escritura. Enmienda de D7 acordada el 2026-08-23 tras un `DESIGN-CONFLICT` del arquitecto.

## 6. i18n <!-- panel: PASS 2026-08-23 -->

- [x] 6.1 `locales/es/pricing.json` y `locales/en/pricing.json` (nuevos) con la estructura de claves de D19: `tabs.*`, `separator`, `recommendations.{list,columns,empty,error}.*`, `status.{DRAFT,RECOMMENDED,APPROVED,APPLIED_EXTERNAL,REJECTED}`, `identity.*`, `filters.*`, `pagination.*`, `decide.{approve,reject,markPublished,confirm,cancel,sending,success}`, `decide.error.{forbidden,notFound,conflict,invalid,generic}`, `generate.{button,sending,report}`, `generate.error.*`, `rules.{list,columns,scope,empty,error}.*`, `read.error.{forbidden,generic}`. La copia de `generate.report` enseña los cuatro contadores y **ni una palabra** de éxito, completitud o corrección; `rules.scope` incluye «Toda la cartera» / «Whole portfolio»; la etiqueta de `max_daily_change_pct` lleva el `%`. [R2.3, R3.6, R4.3, R5.3, R6.4, R6.6]
- [x] 6.2 `lib/i18n/resources.ts`: registrar `pricing` en los **cuatro** puntos — el `import` por locale, la lista `NAMESPACES`, y la entrada dentro de `resources.es` y `resources.en`. Verificar que `catalog-parity.test.ts` recorre ya el namespace nuevo. [R6.5, R6.6] — **Verificado por mutación**: añadiendo una clave sólo en ES, el test falla con «has identical key sets in both locales for "pricing"»; restaurada, verde. Pero esa prueba **no cubría el cuarto punto**: quitar `"pricing"` de `NAMESPACES` dejaba `typecheck` limpio y hacía que `catalog-parity` pasara de 12 tests a 11, **todos verdes** (panel de QA). Cerrado con `namespace registration (R6.5)` en `catalog-parity.test.ts`, que compara los ficheros de `locales/{es,en}/` contra `NAMESPACES` y cubre así a cualquier namespace futuro.
- [x] 6.3 `features/pricing/locales/pricing-locale.test.ts` (nuevo): con el patrón de `features/properties/locales/properties-locale.test.ts` (lista derivada del `Record` tipado, no transcrita a mano), comprobar que los **cinco** valores de `PriceRecommendationStatus` —incluido `DRAFT`— tienen etiqueta en ES y EN. [R6.4, R6.6]

## 7. Componentes <!-- panel: PASS 2026-08-23 -->

- [x] 7.1 `features/pricing/components/pricing-pagination.tsx` (nuevo) + `.test.tsx`: presentacional puro (`page`, `totalPages`, `total`, `onPageChange`), contra `useTranslation("pricing")`, sin tocar la red y sin selector de tamaño de página. Test de que con `totalPages = 0` no se renderiza. [R2.3]
- [x] 7.2 `features/pricing/components/pricing-tabs.tsx` (nuevo) + `.test.tsx`: `role="tablist"` con dos `<button role="tab">` (`aria-selected`, `aria-controls`, roving `tabIndex`, flechas izquierda/derecha + Home/End) y un único `role="tabpanel"` con `aria-labelledby`. Se monta **sólo el panel activo** (render condicional, no `hidden` por CSS). Tests: teclado completo, y el panel inactivo no está en el DOM. [R1.1]
- [x] 7.3 `features/pricing/components/recommendation-filters.tsx` (nuevo) + `.test.tsx`: vivienda (`<select>` nativo desde el catálogo), rango de fechas con `<input type="date">`, y estado desde `RECOMMENDATION_STATUS_ORDER`. Nunca se deshabilitan mientras vuela una escritura. [R2.1, R6.4]
- [x] 7.4 `features/pricing/components/decision-controls.tsx` (nuevo) + `.test.tsx`: los movimientos de `legalMoves(status)` con confirmación **en dos pasos dentro de la fila** («¿Aprobar este precio? [Confirmar] [Cancelar]», `pendingMove` local a la fila, texto distinto por movimiento); la confirmación es lo que dispara la mutación. Deshabilitado mientras `isBusy`, con texto «Enviando…» en la fila cuya decisión vuela. Oculto tras `useHasPermission("MANAGE_PRICE_RECOMMENDATIONS")`. Tests: `RECOMMENDED` ofrece dos botones, `APPROVED` ofrece **sólo** «Marcar como publicada», los otros tres estados ninguno, y sin confirmar no se llama a la mutación. [R3.1, R3.2, R3.3]
- [x] 7.5 `features/pricing/components/recommendation-row.tsx` (nuevo) + `.test.tsx`: tarjeta con vivienda (las tres formas de identidad de R2.8), `date` con `fmtDay`, `recommendedPrice` con `fmtDecimal`, insignia de estado con su tono, `explanation` **plegada en `<details>`** con `<summary>` localizado y cerrado por defecto, y `<DecisionControls />`. Tests: no aparece `current_price`, ni `confidence`, ni marca temporal alguna; una `explanation` con marcado dentro (`Season (<b>x</b>) +10.00%`) se ve **literal** y no crea ningún elemento; ningún `dangerouslySetInnerHTML` en toda la feature. [R2.4, R2.5, R2.6, R2.7, R2.8, R6.1, R6.3, R6.4]
- [x] 7.6 `features/pricing/components/recommendations-panel.tsx` (nuevo) + `.test.tsx`: filtros, botón **Regenerar ahora** (deshabilitado mientras vuela, con texto de progreso), **una sola** región viva `role="status" aria-live="polite"` para el informe de generación y los errores, lista de filas, y paginación sólo cuando `total > 0`. Con `total = 0` renderiza el estado vacío localizado y **nunca** «página 1 de 0». Tests: el estado vacío, el aviso con los cuatro contadores, y el `409` mostrando su copia propia distinta del genérico. [R2.1, R2.3, R3.6, R3.8, R4.1, R4.2, R4.3, R4.4] — **Los tres viven en `pricing-view.test.tsx`, no en un `recommendations-panel.test.tsx`.** El panel monta `useRecommendations` por dentro, así que probarlo suelto exige el mismo `QueryClient` y los mismos dobles que la vista y además reconstruir a mano su superficie de props: sería el mismo test con más andamio y una integración menos. Los nombres exactos: «renders the empty state and no pagination when total is 0 (R2.3)», «announces the four counters after a generation», «shows the 409 copy of its own, distinct from the generic one (R3.6)».
- [x] 7.7 `features/pricing/components/rule-filters.tsx` y `rule-row.tsx` (nuevos) + tests: filtros `property_id` y `active`; por regla `name`, ámbito (con «Toda la cartera» cuando `propertyId === null`), `active`, la banda `min/base/max` con `fmtDecimal`, `maxDailyChangePct` y los cinco recuentos de `modifierCounts`. Test de que **no** se renderiza ningún contenido interior de los cinco JSONB. [R5.2, R5.3, R5.4, R6.1]
- [x] 7.8 `features/pricing/components/rules-panel.tsx` (nuevo) + `.test.tsx`: listado de reglas en sólo lectura con filtros, estado vacío, error de lectura y paginación; sin ninguna llamada a `GET /api/v1/pricing-rules/{rule_id}`. [R5.1, R5.5] — Cubierto desde `pricing-view.test.tsx` y desde `rule-row.test.tsx` / `rule-filters.test.tsx`; R5.5 lo garantiza la interfaz `PricingDataSource`, que no declara el método. **Corrección**: la nota original decía «igual que 7.6» y era falsa. El panel de QA enumeró la suite y demostró que el estado vacío y el error de lectura **de la pestaña de reglas** no los ejercía nada: son ramas propias de `rules-panel.tsx`, con sus propias claves (`rules.list.empty.*`, `rules.list.error.title`), y ni el test feliz de la vista ni los de `rule-row`/`rule-filters` —que nunca montan `RulesPanel`— llegaban a ellas. Cerrado con tres tests nuevos en `pricing-view.test.tsx`, verificados por mutación: quitar el guardia `total === 0` y cambiar `readErrorKey` por `decideErrorKey` hacen fallar al test que los nombra.
- [x] 7.9 `features/pricing/components/pricing-view.tsx` (nuevo) + `.test.tsx`: orquesta las pestañas, posee las dos mutaciones y expone `isBusy = decide.isPending || generate.isPending`, y replica el guardia `staleFilters` de `cleaning-view.tsx` para que la primera petición no salga con el filtro de la sesión anterior. **Ese guardia cubre las lecturas; no retirar el guardia propio de `use-generate-recommendations`**, que protege la escritura y no pasa por aquí (D7 enmendado). Tests: la pestaña activa inicial es Recomendaciones; abrir Reglas dispara su query **por primera vez** en ese momento; mientras una escritura vuela, los botones de decisión de **todas** las filas y el de regenerar están deshabilitados y los `<select>` de filtro no. [R1.1, R1.3, R3.3, R4.4]
- [x] 7.10 `features/pricing/index.ts` (nuevo): exporta **sólo** `PricingView`. [R1.1]

## 8. Permisos y ruta <!-- panel: PASS 2026-08-23 -->

- [x] 8.1 `lib/auth/permissions.ts`: ampliar la unión a `Permission = "MANAGE_CLEANING_TASKS" | "MANAGE_PRICE_RECOMMENDATIONS"` y conceder la nueva en `ROLE_UI_PERMISSIONS` a **`TENANT_OWNER` y `PROPERTY_MANAGER`**; `SUPER_ADMIN`, `CLEANER` y `TECHNICIAN` siguen con `[]`. Mantener el comentario de que el mapa es parcial y sólo sirve para ocultar. `lib/auth/permissions.test.tsx` gana los casos simétricos a los de cleaning: concedida a los dos roles —**incluida `TENANT_OWNER`**, que es lo que R7.2 prohíbe copiar mal—, denegada a los otros tres y denegada sin usuario. [R7.1, R7.2, R7.3] — **Adelantada a la sección 7** (2026-08-23): la tarea 7.4 oculta los controles tras `useHasPermission("MANAGE_PRICE_RECOMMENDATIONS")`, que **no existe** hasta esta tarea, así que el orden de `tasks.md` dejaba el árbol sin compilar entre 7.4 y 8.1. Inversión de dependencia real del plan, no un atajo.
- [x] 8.2 `app/(workspace)/pricing/page.tsx`: sustituir `RoutePlaceholder` por `<PricingView />` desde `@/features/pricing`, conservando `generateMetadata` desde `routeMetadata("pricing")`. **No tocar** `features/shell/navigation/route-registry.ts` ni `locales/{es,en}/navigation.json`, no dar de alta descriptores y no cambiar `match` a `"prefix"`. Verificar con `git diff --name-only` que ninguno de esos ficheros aparece. [R1.2, R1.4]

## 9. Verification <!-- panel: PASS 2026-08-23 -->

Antes de leer cualquier cifra de `npm test`, correr los `docker compose cp` que
`sdd/project.md` documenta para un worktree enlazado: sin ellos
`features/provenance/workflow-contract.test.ts` y `lib/config/build-identity-contract.test.ts`
fallan con `ENOENT` por causas del entorno y **no** son regresión de este change.

- [x] 9.1 Suite completa verde: `docker compose exec -T frontend npm test`. Comparar el recuento con la línea base de `sdd/project.md` (63 ficheros, 415 tests antes de este change) y no aceptar un «PASS (0) FAIL (0)» como verde. — **Medido: 123 ficheros, 1101 tests, todo en verde** (20 ficheros y 227 tests son de `features/pricing`). **La línea base de `sdd/project.md` está obsoleta**: dice «63 ficheros, 415 tests» y el árbol *antes* de este change ya tenía **103** ficheros de test — la cifra es anterior a varios changes ya mergeados y no describe el repo de hoy, así que la comparación que pedía esta tarea no se puede hacer contra ella. Se reporta la medición real en vez de forzarla a encajar. Ojo además: `make up` recrea el contenedor y con él se pierden los `docker compose cp`, así que los dos ENOENT reaparecen — hay que repetirlos.
- [x] 9.2 Typecheck y lint: `docker compose exec -T frontend npm run typecheck` y `docker compose exec -T frontend npm run lint`. — Ambos limpios.
- [x] 9.3 **No** correr `npm run api:check`: este diff no regenera el contrato y el comando no funciona tal cual en un worktree enlazado. Confirmar con `git diff --name-only` que ni `backend/openapi.json` ni `frontend/lib/api/generated/openapi.d.ts` aparecen. — **Confirmado**: no aparecen, ni tampoco `route-registry.ts` ni `navigation.json` (R1.2).
- [x] 9.4 Comprobación manual con navegador: `make up PORT_OFFSET=<n>` y `make bootstrap`, entrar como manager y como propietaria, y recorrer el flujo en `http://localhost:30<n>0/pricing` — regenerar, aprobar, rechazar, marcar como publicada, y abrir la pestaña Reglas. Verificar en los dos locales que no queda ninguna string sin traducir, y en móvil (o viewport estrecho) que la fila se lee de un vistazo con la `explanation` plegada. [R6.6] — **Hecho con navegador headless real** (`make up PORT_OFFSET=14`, `bootstrap` + `seed-demo` + dos reglas y `POST /generate` por API; 60 recomendaciones). Resultados: pestañas «Recomendaciones»/«Reglas» con la primera activa; 20 filas; **regenerar** anuncia «Generación ejecutada: 0 creadas, 60 actualizadas, 0 conservadas, 1 omitidas.»; **aprobar** («¿Aprobar este precio?» → Confirmar) deja la insignia «Aprobada» y saca «Marcar como publicada»; **rechazar** deja «Rechazada»; **marcar como publicada** deja «Publicada» — el Modo 1 se cierra entero. Pestaña Reglas: 2 reglas, «Toda la cartera» en la de `property_id` nulo, los cinco recuentos y el `%` en la etiqueta. Sin `1,00`/`1.00` de `confidence` ni marca temporal en pantalla, y cero claves crudas. En **inglés**: «Recommendations»/«Rules», «Whole portfolio», precio `120.00` con punto frente a `120,00` con coma en ES —la enmienda de D14 en funcionamiento— y ni una cadena en español. En **320 px**: `scrollWidth == clientWidth == 320`, sin scroll horizontal, y todos los `<details>` cerrados.
  - **Observación para el archivado, no defecto**: la `explanation` del backend dice «Base price 120.00 EUR.», así que en pantalla conviven un importe nuestro sin moneda (`120,00`) y un «EUR» dentro de la frase del backend. Es exactamente lo que mandan R2.7 (literal, sin parsear) y R6.2 (no inventar moneda); se anota porque se ve raro y alguien lo tomará por un bug.
- [x] 9.5 Comprobación manual de RBAC: entrar como `CLEANER`, abrir `/pricing` desde el sidebar (que no filtra por rol) y confirmar que sale la copia localizada de `403` del camino de lectura y **no** el error genérico ni una pantalla en blanco. [R3.8, R7.3] — **Hecho con navegador real.** El sidebar sí le enseña «Precios» (la carencia conocida de D17); al entrar, el listado responde `403` y la pantalla muestra «No se pudieron cargar las recomendaciones» + **«No tienes permiso para ver los precios de este tenant.»**, *no* «Vuelve a intentarlo en unos segundos», no una pantalla en blanco y **sin ningún botón de decisión ni de regenerar**. Verificado además el otro lado de R7.1/R7.2: la **propietaria** (`TENANT_OWNER`) entra y ve 18 botones «Aprobar» y el de «Regenerar ahora» — que es justo lo que R7.2 dice que no se puede copiar mal.
- [x] 9.6 Grep de cierre: `dangerouslySetInnerHTML` no aparece en `features/pricing/`; `ApiError` no se lee por `message`/`code`/`details` en `features/pricing/`; `total_pages` no aparece en la feature; y el filtro de estado viaja como `status` y no como `status_filter`. [R2.7, R3.7] — **Los cuatro limpios.** El único `dangerouslySetInnerHTML` del árbol es el comentario que lo prohíbe; los `total_pages` son comentarios sobre la asimetría del sobre más el fixture de `/api/v1/properties`, que sí lo trae; `status_filter` sólo aparece en comentarios y en el nombre de su test; y `message`/`code`/`details` no se leen en ningún sitio fuera de comentarios.
