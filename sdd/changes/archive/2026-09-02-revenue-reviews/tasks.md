# Tasks: revenue-reviews

## 1. Domain — enums, entities y value objects

- [x] 1.1 Añadir `RecurringIssueTag` con los nueve miembros (`WIFI`, `NOISE`, `CLEANLINESS`, `ACCESS`, `COMMUNICATION`, `LOCATION`, `VALUE`, `AMENITIES`, `OTHER`) en `backend/app/reviews/domain/enums.py` — `[R2.2]`
- [x] 1.2 Reforzar el dataclass `Review` en `backend/app/reviews/domain/entities.py`: validar `property_id` no nulo en `__post_init__`, validar `rating` ∈ [1.0, 5.0] con un decimal, `recurring_issues` ∈ lista de `RecurringIssueTag` (degradar a `[OTHER]` y `logger.warning` si entra un valor desconocido); añadir `classification_attempts: int = 0` y los métodos `mark_drafted()`, `approve()`, `ignore()`, `mark_posted_manually()`, `mark_classified_low_confidence()`, `increment_attempts()`, `assign_analysis(sentiment, summary, recurring_issues, vocabulary)` con `_assert_transition` que lanza `InvalidReviewTransitionError` — `[R1.4, R1.5, R2.2, R4.1, D4]`
- [x] 1.3 Reforzar `ReviewResponseDraft` en `backend/app/reviews/domain/entities.py`: añadir `edits_count: int = 0`, método `edit(new_content, actor_id)` que incrementa `edits_count` y persiste audit `REVIEW_DRAFT_EDITED` vía el repositorio de auditoría, y método `approve(actor_id)` que rellena `approved_by`/`approved_at` (R3.5, R3.6, D5) — `[R3.5, R3.6]`
- [x] 1.4 Crear `backend/app/reviews/domain/value_objects.py` con `ReviewAnalysis` (confidence, sentiment, summary, recurring_issues, vocabulary) y `GeneratedDraft` (content, confidence, language, vocabulary, template_version); ambos con `__post_init__` que rechaza `confidence` fuera de `0..1`, `language` fuera de `SUPPORTED_LANGUAGES`, y `summary`/`content` fuera del vocabulario declarado (D1, R3.3) — `[R2.1, R3.3]`
- [x] 1.5 Crear `backend/app/reviews/domain/exceptions.py` con `InvalidReviewTransitionError`, `ReviewValidationError`, `ReviewNotFoundError`, `DraftLanguageUnsupportedError`, `ReviewLanguageInferenceError` — `[R1.4, R4.1, R5.1]`
- [x] 1.6 Crear `backend/app/reviews/domain/language.py` con `detect(content: str | None) -> str | None` basado en stop-words ES/EN (`_ES_STOPS`, `_EN_STOPS`), usando el normalizador `fold`; devuelve `"es"`/`"en"` cuando un conjunto gana por ≥1.5×, `None` si ambiguo o entrada vacía — `[R5.1, D14]`
- [x] 1.7 Crear `backend/app/reviews/domain/templates.py` con `REVIEW_DRAFT_TEMPLATES: Mapping[tuple[ReviewSentiment, str], str]` (6 entradas: 3 sentimentos × 2 idiomas), `REVIEW_DRAFT_TEMPLATES_VERSION = "2026-09-01.1"`, `REVIEW_DRAFT_VOCABULARY: frozenset[str]`, `INTENTS_WITHOUT_DRAFT: frozenset[ReviewStatus] = {IGNORED, POSTED_MANUALLY}`, y `assert_in_catalogue(content)` — `[R3.2, R3.4, D6, D12]`
- [x] 1.8 Crear `backend/app/reviews/domain/ports.py` con las cuatro `Protocol`: `AIReviewAnalyzer.analyze_review(*, content, language) -> ReviewAnalysis`, `AIReviewDraftGenerator.generate_draft(*, review, language) -> GeneratedDraft`, `ReviewRepository` (8 métodos), `ReviewResponseDraftRepository` (4 métodos). Usar `Review`/`ReviewResponseDraft`/`ReviewAnalysis`/`GeneratedDraft`/`RecurringIssueTag` como tipos — `[R1.2, R2.1, R3.1, D1]`
- [x] 1.9 Crear `backend/app/reviews/domain/notifications.py` con `build_review_response_approved_log(...)` que produce la fila de `notification_logs` (D9); seguir la disciplina de `app/maintenance/domain/notifications.py` — `[R6.2]`

## 2. Domain — tests de invariantes

- [x] 2.1 Tests en `backend/tests/reviews/test_entities.py`: `Review.__post_init__` rechaza `property_id=None`, `rating=5.5` y `rating=4.55`; las cinco transiciones legales pasan; las cuatro transiciones ilegales (`NEW → APPROVED`, `APPROVED → NEW`, `IGNORED → APPROVED`, `POSTED_MANUALLY → DRAFTED`) lanzan `InvalidReviewTransitionError`. Preexistente cubre defaults; verificar y extender sin destruir lo marcado — `[R1.4, R1.5, R4.1]`
- [x] 2.2 Tests en `backend/tests/reviews/test_value_objects.py`: `ReviewAnalysis.__post_init__` rechaza `confidence=1.5`, `summary` fuera del vocabulario y `recurring_issues` con miembro que no está en `RecurringIssueTag` (degrada a `OTHER`); `GeneratedDraft.__post_init__` rechaza `confidence=-0.1`, `language="fr"`, `content` que no está en `REVIEW_DRAFT_VOCABULARY` — `[R2.1, R3.3]`
- [x] 2.3 Tests en `backend/tests/reviews/test_recurring_issues_vocabulary.py`: recorrer `backend/app/reviews/infrastructure/` y verificar que cada valor escrito en `recurring_issues` pertenece al enum; etiquetar la degradación a `OTHER` con `caplog` y assertar `logger.warning` emitido — `[R2.2]`
- [x] 2.4 Tests en `backend/tests/reviews/test_language.py`: seis casos —solo ES, solo EN, ES mayoritario, EN mayoritario, ambiguo → `None`, entrada `None` → `None`— siguiendo el patrón de `tests/messaging/test_language.py` — `[R5.1, D14]`
- [x] 2.5 Tests en `backend/tests/reviews/test_draft_templates.py`: el catálogo tiene exactamente 6 entradas; `assert_in_catalogue` rechaza cualquier string con `{...}`, `%s` o `f-string` patrón; barrido de `REVIEW_DRAFT_TEMPLATES` y `REVIEW_DRAFT_VOCABULARY` con regex de marcadores de interpolación — `[R3.2]`
- [x] 2.6 Test `test_no_draft_can_promise_what_rule_10_forbids` en `backend/tests/reviews/test_draft_templates.py`: recorrer `REVIEW_DRAFT_VOCABULARY` y rechazar cualquier frase con `reembolso`, `compensación`, `indemniz`, `responsabilidad`, `garantizamos`, `te devolvemos`, etc. (calcado del test de `messaging`) — `[R3.4, R10.security.md]`

## 3. Migración Alembic

- [x] 3.1 Crear `backend/alembic/versions/<rev>_revenue_reviews.py` con `op.create_table("reviews", ...)` y `op.create_table("review_response_drafts", ...)` siguiendo D15/§Data; índices `ix_reviews_tenant_id`, `ix_reviews_property_id_status`, `ix_reviews_tenant_id_published_at` y unique constraint `uq_review_response_drafts_review_id`; columnas `classification_attempts INT NOT NULL DEFAULT 0` en `reviews` y `edits_count INT NOT NULL DEFAULT 0` en `review_response_drafts` (D5/OQ1) — `[R1.1, R1.2, R2.4, R3.1]`
- [x] 3.2 Crear `backend/alembic/versions/<rev>_revenue_reviews_tenant_config.py` con `op.add_column("tenant_configs", sa.Column("review_recurring_issues_top_n", sa.Integer, nullable=False, server_default="5"))` y CHECK constraint `BETWEEN 1 AND 50` — `[R5.5]`
- [x] 3.3 Crear `backend/alembic/versions/<rev>_revenue_reviews_timeline_events.py` con cinco `op.execute("ALTER TYPE timeline_event_type ADD VALUE ...")` idempotentes para `REVIEW_CREATED`, `REVIEW_DRAFT_EDITED`, `REVIEW_CLASSIFIED_LOW_CONFIDENCE`, `REVIEW_IGNORED`, `REVIEW_POSTED_MANUALLY`; usar `try/except` para tolerar re-ejecución, documentar el matiz de Postgres ≥12.5 — `[R6.1, R6.2, D8]`

## 4. Infraestructura — repositorios y adaptadores de IA

- [x] 4.1 Crear `backend/app/reviews/infrastructure/repositories.py` con `SqlAlchemyReviewRepository` (8 métodos) y `SqlAlchemyReviewResponseDraftRepository` (4 métodos); los métodos de lectura de drafts deben hacer JOIN explícito a `reviews` para activar el filtro de tenant (R1.1, D10) — `[R1.1, R1.2]`
- [x] 4.2 Crear `backend/app/reviews/infrastructure/ai.py` con `MockReviewAnalyzer` (sentiment por keywords, `confidence ∈ [0,1]`, summary desde el catálogo, `recurring_issues` ∈ enum; aplica degradación a `OTHER` + warning); y `MockReviewDraftGenerator` que elige una entrada de `REVIEW_DRAFT_TEMPLATES` por `(sentiment, language)` y devuelve `GeneratedDraft` con `template_version=REVIEW_DRAFT_TEMPLATES_VERSION`; los mocks **no deben** inventar contenido fuera del catálogo — `[R2.1, R2.2, R3.2, R3.4]`
- [x] 4.3 Añadir índice `ix_reviews_property_id_status` en `backend/app/reviews/infrastructure/models.py` (D11) — `[R5.3]`

## 5. Auth, tenants, timeline y notifications — cambios cross-module

- [x] 5.1 Añadir cinco permisos al enum `Permission` en `backend/app/auth/domain/policy.py`: `READ_REVIEWS`, `CREATE_REVIEW`, `APPROVE_REVIEW`, `IGNORE_REVIEW`, `MARK_REVIEW_POSTED`. Mapearlos en `ROLE_PERMISSIONS`: `TENANT_OWNER` recibe `READ_REVIEWS`, `APPROVE_REVIEW`, `IGNORE_REVIEW`, `MARK_REVIEW_POSTED`; `PROPERTY_MANAGER` recibe los cinco. `CLEANER` y `TECHNICIAN` sin cambios — `[R1.2, R4.2, D3]`
- [x] 5.2 Añadir `review_recurring_issues_top_n: int = 5` con `_require_positive_int` en `TenantConfig` (`backend/app/tenants/domain/entities.py`); añadir `review_recurring_issues_top_n: int | None = None` con `ge=1, le=50` en `TenantConfigPatch` (`backend/app/tenants/api/schemas.py`) — `[R5.5]`
- [x] 5.3 Añadir `REVIEW_RESPONSE_APPROVED` al `NotificationType` en `backend/app/notifications/domain/enums.py`; añadir entrada en `_POLICY`/`escalation_for` en `backend/app/notifications/domain/escalation.py` con `sla_minutes=None` y `recipient_resolver="managers_or_owners"` — `[R6.2, D9]`
- [x] 5.4 Añadir cinco miembros al `TimelineEventType` en `backend/app/timeline/domain/enums.py`: `REVIEW_CREATED`, `REVIEW_DRAFT_EDITED`, `REVIEW_CLASSIFIED_LOW_CONFIDENCE`, `REVIEW_IGNORED`, `REVIEW_POSTED_MANUALLY`. Añadir sus traducciones ES/EN a `TIMELINE_TITLE_TEMPLATES` en `backend/app/timeline/domain/rendering.py` — `[R6.1, D8]`

## 6. Scheduler — job de clasificación

- [x] 6.1 Añadir `classify_reviews: timedelta(minutes=5)` a `CADENCES` y la entrada derivada en `beat_schedule()` en `backend/app/scheduler/schedule.py` — `[R2.4, D2]`
- [x] 6.2 Añadir la task `classify_reviews` en `backend/app/scheduler/tasks.py`: `run_for_every_tenant` → `ClassifyPendingReviewsUseCase` con `MockReviewAnalyzer` y `MockReviewDraftGenerator`; usar `SELECT ... FOR UPDATE SKIP LOCKED` sobre `WHERE ai_summary IS NULL AND classification_attempts < 3` (mismo patrón que `process_webhook_events`); excepciones del adaptador → `logger.exception(...)` + `attempts += 1`; al tercer fallo la fila queda pendiente de triaje manual. Informe final `{scanned, classified, low_confidence, failed, manual_triage}` — `[R2.3, R2.4, D2, D16]`

## 7. Application — use cases

- [x] 7.1 Crear `backend/app/reviews/application/use_cases.py` con `CreateReviewUseCase` (valida `property_id` y `channel`; persiste `Review` con `sentiment=NEUTRAL`, `ai_summary=NULL`, `recurring_issues=[]`; emite `TimelineEvent REVIEW_CREATED`; `language` se infiere vía `detect` si falta; `422` si no se puede inferir); `ListReviewsUseCase` (filtros de R5.3, orden `published_at DESC NULLS LAST`, envelope §23); `GetReviewUseCase` y `GetReviewDraftUseCase` (404 indistinguible) — `[R5.1, R5.2, R5.3, R5.4, R6.1]`
- [x] 7.2 En el mismo fichero, añadir `ApproveReviewUseCase`, `IgnoreReviewUseCase`, `MarkPostedManuallyUseCase` (cada uno verifica RBAC y transición de estado, escribe `TimelineEvent` correspondiente, `IgnoreReviewUseCase` no genera borrador, `ApproveReviewUseCase` rellena `approved_by`/`approved_at` y bloquea edición posterior) — `[R3.6, R4.1, R4.2, R4.3]`
- [x] 7.3 En el mismo fichero, añadir `RegenerateReviewDraftUseCase` (UPDATE por `review_id`, no DELETE+INSERT; `ai_generated` queda `TRUE`) y `EditReviewDraftUseCase` (incrementa `edits_count`, persiste `TimelineEvent REVIEW_DRAFT_EDITED`); añadir `ListReviewsSummaryForPropertyUseCase` (window 90 días, top N por `TenantConfig.review_recurring_issues_top_n`, conteo por sentiment) — `[R3.5, R5.5, D5]`
- [x] 7.4 En el mismo fichero, añadir `ClassifyPendingReviewsUseCase` (invocado por el job del §6): por reseña pendiente, llama al adaptador; si `confidence < TenantConfig.ai_confidence_threshold` → `mark_classified_low_confidence()` + `TimelineEvent REVIEW_CLASSIFIED_LOW_CONFIDENCE` + `incidents.classification_failed`; en éxito → `assign_analysis()` + crear/actualizar `ReviewResponseDraft` + `TimelineEvent REVIEW_RESPONSE_DRAFTED`. En `ApproveReviewUseCase`, emitir `NotificationType.REVIEW_RESPONSE_APPROVED` con `RoleRecipients.managers_or_owners` — `[R2.1, R2.3, R2.4, R3.1, R6.2, R6.3, D9, D16]`

## 8. API — schemas, router y errores

- [x] 8.1 Crear `backend/app/reviews/api/schemas.py` con `CreateReviewRequest` (R5.1), `ReviewResponse`, `ReviewDraftResponse`, `ReviewSummaryResponse`, `ReviewResponseActionRequest` (`Literal["APPROVE", "IGNORE", "MARK_POSTED", "EDIT"]` + `draft_content?`), `ReviewPageResponse` (envelope §23); todos con `model_config = ConfigDict(from_attributes=True)` — `[R5.1, R5.2, R5.3, R5.4, R5.5, R3.5, R4.2]`
- [x] 8.2 Crear `backend/app/reviews/api/errors.py` mapeando excepciones a `{error: {code, message, details}}` — `InvalidReviewTransitionError → 409`, `ReviewValidationError → 422`, `ReviewNotFoundError → 404`, `DraftLanguageUnsupportedError → 422`, `ReviewLanguageInferenceError → 422` — `[R4.1, R5.1]`
- [x] 8.3 Crear `backend/app/reviews/api/dependencies.py` con `get_review_use_cases(...)` que cablea `ReviewRepository`, `ReviewResponseDraftRepository`, `AIReviewAnalyzer=MockReviewAnalyzer()`, `AIReviewDraftGenerator=MockReviewDraftGenerator()`, `UnitOfWork`, y devuelve un `ReviewUseCases` con los once casos de uso — `[R5, R6]`
- [x] 8.4 Crear `backend/app/reviews/api/router.py` con los siete endpoints (R5 + R3 + R4): `POST /api/v1/reviews` (CREATE_REVIEW), `GET /api/v1/reviews` (READ_REVIEWS), `GET /api/v1/reviews/{id}` (READ_REVIEWS), `GET /api/v1/reviews/{id}/response` (READ_REVIEWS, 404 indistinguible), `POST /api/v1/reviews/{id}/response` (APPROVE_REVIEW, regenerar), `PATCH /api/v1/reviews/{id}/response` (APPROVE_REVIEW/IGNORE_REVIEW/MARK_REVIEW_POSTED), `GET /api/v1/properties/{id}/reviews/summary` (READ_REVIEWS). Todos con `require(...)` y el `404` indistinguible entre "no existe", "de otro tenant" y "no accesible por tu rol" — `[R5, R3.5, R4.2, D11]`
- [x] 8.5 Registrar el router en `backend/app/main.py` con `app.include_router(reviews_router, prefix=API_V1_PREFIX)` siguiendo el patrón de `conversations_router` — `[R5]`

## 9. Tests de integración

- [x] 9.1 Crear `backend/tests/reviews/test_tenant_isolation.py` (R1.3): cada vía de acceso —alta, detalle, listado por propiedad, listado por tenant, aprobación, ignorado, `mark_posted_manually`, regenerar y editar borrador, summary por propiedad— se prueba con dos tenants (`A` y `B`) y un usuario de cada rol; verificar que `B` recibe 404 indistinguible en todas las vías. Usar el patrón de `tests/messaging/test_tenant_isolation.py` (regla 1 de `steering/security.md`) — `[R1.3, R4.3]`
- [x] 9.2 Crear `backend/tests/reviews/test_state_machine.py` (R4.1): cinco transiciones legales pasan; cuatro ilegales lanzan `InvalidReviewTransitionError` y el router responde `409`; `POSTED_MANUALLY` y `IGNORED` son terminales — `[R4.1]`
- [x] 9.3 Crear `backend/tests/reviews/test_pipeline_classification.py` (R2): reseña recién creada → estado `NEW` con campos IA vacíos; job `classify_reviews` con `MockReviewAnalyzer` la clasifica con sentiment/summary/recurring_issues; adaptación con `confidence < threshold` → `mark_classified_low_confidence()` + evento; tres fallos consecutivos → fila pendiente de triaje manual; aislamiento: aggregate de `recurring_issues` y `count_by_sentiment_for_property` por tenant — `[R2.1, R2.3, R2.4, R2.5]`
- [x] 9.4 Crear `backend/tests/reviews/test_review_endpoints.py` (R5): alta manual con body válido → 201; alta sin `property_id` → 422; alta con `channel` no enumerado → 422; listado paginado con filtros; `404` indistinguible para id de otro tenant; borrador en reseña `IGNORED` → 404 indistinguible — `[R5]`
- [x] 9.5 Tests existentes (`test_entities.py`, `test_models.py`): verificar que siguen pasando con los nuevos campos y métodos en las entidades y modelos; añadir casos para `classification_attempts` y `edits_count` si no los cubren ya — `[R1.1, R2.4, R3.5]`

## 10. Documentación y contrato abierto

- [x] 10.1 Modificar `sdd/specs/auth-tenancy.md`: añadir los cinco permisos al `Permission` enum y los mapeos `ROLE_PERMISSIONS` para `TENANT_OWNER`/`PROPERTY_MANAGER` (R1.7) — `[R1.7]`
- [x] 10.2 Modificar `sdd/specs/notifications-inbox-web.md`: añadir `REVIEW_RESPONSE_APPROVED` a la enumeración `NotificationType` — `[R6.2]`
- [x] 10.3 Modificar `sdd/steering/security.md` regla 11 (sumideros de texto en claro): añadir las cuatro entradas —`reviews.content` (excepción 4), `reviews.ai_summary` (forma cerrada por sumidero), `review_response_drafts.draft_content` (forma cerrada por plantilla), `reviews.recurring_issues` (forma estructurada por enum cerrado, degradación a OTHER con warning). Reseñar `reviews.recurring_issues` como nueva forma estructurada del censo (D7) — `[R1.6, R1.7, R2.2, R3.2, D7]`
- [x] 10.4 Regenerar el contrato: desde el worktree enlazado, ejecutar el workaround documentado en `sdd/project.md` §Worktree bootstrap — `docker compose exec -T frontend mkdir -p /backend`, `docker compose cp backend/openapi.json frontend:/backend/openapi.json`, `docker compose exec -T frontend npm run api:generate`; confirmar con `cd frontend && npm run api:check` que el `.d.ts` coincide — `[R5, R3.5]`
- [x] 10.5 Documentar la capability en `docs/reviews.md` (mismo nivel que `docs/messaging-ai.md`): qué hace, qué no hace (no posting automático, no UI), cómo se opera el flujo end-to-end, y cómo se lee el timeline — `[R6]`

## 11. Medium panel findings (round 1)

- [x] 11.1 `_to_review` en `backend/app/reviews/infrastructure/repositories.py` re-corre `_coerce_recurring_issues` del dominio (mismo path que la entidad usa al construir `Review`), de modo que un valor fuera del enum degrade a `OTHER` con `logger.warning` en lugar de lanzar `ValueError` (D7) — `[R2.2, D7]`
- [x] 11.2 Test en `backend/tests/reviews/test_recurring_issues_vocabulary.py` (o nuevo `test_recurring_issues_read_coercion.py`): sembrar `ReviewModel` con `recurring_issues=["NOT_A_TAG", RecurringIssueTag.WIFI.value]`, invocar el repositorio (`get` o `_to_review` a través de un listado), y assertar que la reseña se devuelve con `(OTHER, WIFI)` y que `caplog.records` contiene el warning `reviews.recurring_issues.dropped_unknown_tag`
- [x] 11.3 `list_pending_classification` en `backend/app/reviews/infrastructure/repositories.py` añade `.with_for_update(skip_locked=True)` al `SELECT` y reescribe el docstring explicando por qué la doble garantía (tenant lock del scheduler + SKIP LOCKED por fila) cierra la ventana entre el SELECT y el write — `[D2, D16]`
- [x] 11.4 Test en `backend/tests/reviews/test_pipeline_classification.py`: ejecutar dos corutinas `ClassifyPendingReviewsUseCase.execute()` sobre el mismo tenant, en transacciones independientes; la segunda tick no debe clasificar las filas que la primera tiene claimadas (verificar por conteo: `report.classified == n` y la segunda tick ve `0` pendientes o un subconjunto)
- [x] 11.5 `RoleRecipients.active_holders` en `backend/app/auth/domain/recipients.py` pagina el resultado completo (loop `while True` con `per_page=MAX_RECIPIENTS`) en lugar de cortar en una sola página; el `dropped` pasa a `max(page.total - len(accumulated), 0)` para preservar el contador — `[R6.2]`
- [x] 11.6 Test en `backend/tests/auth/test_role_recipients.py` (nuevo): sembrar 150 usuarios activos `PROPERTY_MANAGER` en un tenant, invocar `managers_or_owners`, y assertar que `len(Recipients.users) == 150` y `Recipients.dropped == 0`
- [x] 11.7 Test defensivo en `backend/tests/reviews/test_pipeline_classification.py`: importar `ClassifyPendingReviewsUseCase` y verificar que las kwargs exactas que `_classify_reviews` pasa en `backend/app/scheduler/tasks.py:217` son aceptadas por el constructor (lock-in para que un refactor no reintroduzca el desfase de `audit_factory=` vs `audit=`) — `[N1]`

## N. Verification

- [x] N.1 Suite backend completa verde: `docker compose exec backend uv run pytest` — incluye `tests/reviews/` y los modulos cruzados (auth, tenants, timeline, notifications, scheduler, rule 11)
- [x] N.2 `uv run pyright .` (con `uv sync --frozen` hecho dentro de `backend`) sin findings nuevos en `app/reviews/`
- [x] N.3 Suite frontend verde: levantar con el workaround de ENOENT documentado en `sdd/project.md` y correr `npm test`; comparar el total contra la cifra de partida del worktree, no contra la del proyecto principal
- [x] N.4 Test E2E manual del job: crear una reseña vía `POST /api/v1/reviews`, esperar el ciclo de `classify_reviews` (≤5 min), verificar que `ai_summary` y `sentiment` se rellenan y que `REVIEW_RESPONSE_DRAFTED` aparece en el timeline; aprobar vía `PATCH .../response` con `action=APPROVE`, verificar `REVIEW_RESPONSE_APPROVED` en timeline + `NotificationType.REVIEW_RESPONSE_APPROVED` en `notification_logs`
- [x] N.5 Aislamiento de tenant: ejecutar `tests/reviews/test_tenant_isolation.py` con la suite global y verificar que todos los caminos devuelven 404 indistinguible desde el tenant equivocado
- [x] N.6 Confirmar `tests/test_route_authorization.py` cubre los siete endpoints (añadir casos si falta)
- [x] N.7 Confirmar `tests/test_rule11_ownership.py` reconoce las cuatro nuevas columnas con su forma y escritor