# Tasks: infra-github-iac

<!-- Markers, read by /sdd:run and the lifecycle gates (HTML comments, invisible
     when rendered). On a section heading: "hard" makes that section's
     implementer run on the stronger model; "panel: PASS <date> receipt:<id>"
     is written by the panel gate (reviewer_panel.py) when the section's review
     panel passes — never by hand; "panel: skipped — <reason>" records a
     deliberate skip (scaffolding, docs, config). On a task line:
     "manual" marks a task only a human can perform — run leaves it to you and
     it may travel with the PR as a deferred entry; it may sit on any line of
     task item, not only the checkbox line. -->

## 1. Bootstrap & module scaffolding <!-- hard --> <!-- panel: PASS 2026-09-12 receipt:88e0cc8e -->

- [ ] 1.1 Verificar que el bucket `autohostai-tfstate-dev` existe con `versioning = enabled` (lo creó `infra/environments/dev/`); si no existe, el bootstrap irreducible original queda referenciado en `infra/environments/dev/README.md` §"Backend de state". **El módulo `infra/github/` no introduce ningún bucket nuevo.** [D3 amend]
- [x] 1.2 Crear `infra/github/{main.tf,variables.tf,outputs.tf,backend.tf,backend.hcl.example,github.tfvars.example,README.md}` con el esqueleto vacío salvo `terraform { required_version = ">= 1.12"; required_providers { github = { source = "integrations/github"; version = "~> 5.0" } } }` en `main.tf`. [R1.2]
- [x] 1.3 `backend.tf`: backend nativo `oci` con configuración parcial vía `-backend-config`. [R1]
- [x] 1.4 `backend.hcl.example`: plantilla con placeholders para namespace/bucket/region/tenancy/user/fingerprint/private_key (comentado: NUNCA versionar con valores reales). [R1]
- [x] 1.5 `github.tfvars.example`: placeholders no sensibles (`github_owner = "autohostai-labs"`, `github_repository_name = "AutoHostAI"`) con comentario de que las sensibles van por `TF_VAR_*`. [R1.3]
- [x] 1.6 `variables.tf`: declarar las 14 variables (12 sensibles + 2 no sensibles) según §"Variables nuevas (resumen)" del design — validar CIDRs (≥/24 para `allowed_ssh_cidrs`, ≥/16 para `allowed_ssh_cidrs_wide`, ≥1 entrada SSH) y formato de clave pública (regex SSH) en línea con `infra/environments/dev/variables.tf`. [R1.3, R3]
- [x] 1.7 `outputs.tf`: tres outputs (`github_repository_full_name`, `github_repository_default_branch`, `github_app_installation_id`) — nunca valores de secrets. [R1]
- [x] 1.8 Verificación local: `terraform -chdir=infra/github init -backend=false && terraform -chdir=infra/github validate && terraform -chdir=infra/github fmt -check -recursive` deben pasar. [R1.4]

## 2. Repository settings + App installation <!-- panel: PASS 2026-09-12 receipt:e1e376e7 -->

- [x] 2.1 `main.tf`: declarar `provider "github"` con `owner = var.github_owner` y `app_auth { id, installation_id, pem_file }` mapeados desde las variables (sin marcar sensibles en el HCL — son IDs públicos). [R1.1, R1.3]
- [x] 2.2 `main.tf`: declarar `github_repository_settings.this` con `description`, `homepage` (vacío hasta que exista), `topics = []`, `visibility = "private"`, `default_branch = "main"`, `has_issues = true`, `has_projects = false`, `has_wiki = false`, `archived = false`. Verificar los nombres exactos de los atributos contra la doc del provider pinado en `tasks.md` del design. [R2.1]
- [x] 2.3 `main.tf`: NO declarar `github_app_installation_repositories` — el provider v5.45.0 lo marca incompatible con `app_auth` (R4.1 + D11). Documentar la instalación de la App como bootstrap irreducible en RUNBOOK.md §1 (lo crea la sección 5); añadir un comentario en `main.tf` apuntando a D11. El job `verify-ghcr` (sección 6) detecta en runtime si la App pierde los permisos que el CD necesita. [R4.1, D11]
- [x] 2.4 `main.tf`: añadir `lifecycle { ignore_changes = [...] }` defensivo sobre los atributos derivados del provider que `tasks.md` del design identifica (D10). Lista inicial vacía si la doc del provider pinado no expone ninguno — se rellena si el primer `plan` muestra diff inocuo. [R1.4]
- [ ] 2.5 Verificación: `terraform -chdir=infra/github plan -var-file=github.tfvars.example -var github_app_id=<test> -var github_app_installation_id=<test> -var github_app_private_key_path=/dev/null` debe mostrar `github_repository.this` como nuevo (diff inocuo — el `terraform plan` posterior post-`apply` debe quedar vacío). Verificar explícitamente que NO aparecen `github_repository_settings` (no existe en provider v5.45.0 — OQ2 fallback, ver `## Implementation Notes`) ni `github_app_installation_repositories` (D11 — bootstrap irreducible, documentado en `infra/github/RUNBOOK.md` §1). [R2.2, R4.1]

## 3. Actions secrets (importación del primer `apply`) <!-- hard --> <!-- panel: PASS 2026-09-13 receipt:c88099d0 -->

- [x] 3.1 `main.tf`: declarar los once `github_actions_secret` (uno por secreto listado en `infra/environments/dev/README.md` §"Secrets de GitHub Actions esperados": `OCI_TENANCY_OCID`, `OCI_USER_OCID`, `OCI_FINGERPRINT`, `OCI_REGION`, `OCI_PRIVATE_KEY`, `OCI_COMPARTMENT_OCID`, `TFSTATE_NAMESPACE`, `TFSTATE_BUCKET`, `ALLOWED_SSH_CIDRS`, `ALLOWED_SSH_CIDRS_WIDE`, `SSH_PUBLIC_KEYS`). Cada uno mapea su `plaintext_value` desde la variable Terraform correspondiente marcada `sensitive = true`. [R3.1]
- [x] 3.2 NO declarar `github_actions_secret` para los secretos que viven en el Vault (`POSTGRES_PASSWORD`, `JWT_SECRET_KEY`, `ENCRYPTION_KEY`, los cuatro de medios, los seis `SMTP_*`, el token del túnel, `DEMO_ACCOUNT_PASSWORD`) — fuente de verdad es `infra/environments/dev/`. Comentar en `RUNBOOK.md` §2 (en sección 5) por qué no se duplican. [R3.4]
- [x] 3.3 NO declarar `github_actions_secret` para `GH_APP_PRIVATE_KEY` — el módulo NO inyecta el secreto de su **propia** App en Actions; el provider `integrations/github` lee la clave directamente desde `pem_file` (D2). El secreto se inyecta al `deploy-dev.yml` desde otra fuente (la misma que `infra-dev`). [R3.3]
- [x] 3.4 Crear `infra/github/import.sh` (script versionado) que imprime los once `terraform import github_actions_secret.<nombre> <SECRET_NAME>` listos para copiar/pegar — orden: alfabético por nombre del recurso. Incluir comentario de cabecera explicando que el script se ejecuta **una sola vez** durante este change, y que cada `import` consume el ID externo (el nombre del secret en GitHub). [R3.1, D6]
- [x] 3.5 Verificación: `terraform -chdir=infra/github plan` (con valores reales cargados desde el workflow de CI, NO localmente) debe mostrar los once secretos como **ya en el state** tras ejecutar `bash infra/github/import.sh` + `terraform -chdir=infra/github apply -target=github_actions_secret.*` — el plan post-import debe quedar vacío sobre estos recursos. **Ejecutar `terraform plan` localmente solo si se dispone de credenciales reales de la App — NO versionar.** [R3, D6]

## 4. Actions variables + branch protection <!-- panel: PASS 2026-09-13 receipt:9ae5b702 -->

- [x] 4.1 `main.tf`: declarar los cuatro `github_actions_variable` (R3.2): `github_actions_variable.gh_app_id` (`GH_APP_ID`), `.gh_app_installation_id` (`GH_APP_INSTALLATION_ID`), `.next_public_app_env` (`NEXT_PUBLIC_APP_ENV`), `.public_hostname` (`PUBLIC_HOSTNAME`). `GH_APP_ID` y `GH_APP_INSTALLATION_ID` leen de variables no sensibles; `NEXT_PUBLIC_APP_ENV` y `PUBLIC_HOSTNAME` requieren variables nuevas — añadirlas a `variables.tf` como no sensibles con defaults `""` y validación fail-fast si el workflow no las inyecta. [R3.2]
- [x] 4.2 Actualizar `infra/github/import.sh` para incluir los cuatro `terraform import github_actions_variable.<nombre> <VARIABLE_NAME>`. [R3.2, D6]
- [x] 4.3 **ENMENDADA (D5/R5, 2026-09-13, decisión del usuario tras DESIGN-CONFLICT)**: `main.tf` NO declara `github_branch_protection.this` — verificado en vivo (`gh api repos/autohostai-labs/AutoHostAI/branches/main/protection` → `403`) que la API rechaza el recurso COMPLETO en este repo privado + plan Free, no solo el sub-bloque de reviewers que la tarea original preveía. Las tres reglas (`required_status_checks` con los SIETE checks always-run, `required_linear_history`, `enforce_admins`) quedan documentadas como comentario en `main.tf` y se trasladan a `infra/github/RUNBOOK.md` (sección 5) como convención no forzada. [R5.1 enmendado]
- [x] 4.4 **ENMENDADA junto con 4.3**: dado que 4.3 no declara el recurso, no hay bloque que pueda ser rechazado parcialmente por el `plan` — el rechazo es total y anterior a la declaración. `RUNBOOK.md` §3 (sección 5) documentará el estado "convención no forzada, API bloqueada por completo mientras el repo sea privado en Free" en vez de "regla no enforced". [R5.2 enmendado]
- [x] 4.5 Verificación local: `terraform -chdir=infra/github plan -var-file=github.tfvars.example` (con credenciales) muestra los cuatro `github_actions_variable` post-import con diff vacío; ningún `github_branch_protection` aparece en el plan (no se declara). [R3.2]

## 5. RUNBOOK + first apply + import de los recursos existentes <!-- hard -->

- [ ] 5.1 Crear `infra/github/RUNBOOK.md` con tres secciones: (1) **Bootstrap irreducible** — crear la org (manual una vez), crear la GitHub App con permisos `administration: write` + `repository: read/write` + `members: read` + `packages: write` (OQ1), generar `.pem`, configurar la App en `autohostai-labs`, rotar la clave; (2) **Lo que Terraform ya cubre** — enumerar los recursos que este módulo declara (R2-R4) con qué secret/variable/settings corresponde a cada uno; (3) **Rotación / Known limitations** — procedimiento de dos pasos coordinados (Terraform + Vault) para rotar la clave de la App, la limitación si la flag de packages no es modelable en la versión pinada del provider (D9), **y una sección explícita de branch protection como convención no forzada** (D5 enmendado en sección 4: la API de branch protection rechaza el recurso completo mientras el repo sea privado en el plan Free — verificado en vivo el 2026-09-13 — así que las tres reglas de `main.tf` §R5 se documentan aquí como lo que *debería* aplicarse manualmente/por convención, con el mismo estado que "required reviewers" ya lleva en `infra/environments/dev/RUNBOOK.md` §0). [R6.1, R5.1/R5.2 enmendados]
- [ ] 5.2 Actualizar `infra/environments/dev/RUNBOOK.md` §6 — añadir una línea al inicio: *"El bootstrap irreducible de la GitHub App (creación, permisos, generación de clave `.pem`) está documentado en `infra/github/RUNBOOK.md` §1; este §6 mantiene cómo se opera el runner."* No tocar el resto del §6. [R6.2]
- [ ] 5.3 Añadir `import.sh` a la cabecera del RUNBOOK como referencia al procedimiento de adopción. [R6.1]
- [ ] 5.4 Ejecutar el primer `apply` real (vía `workflow_dispatch action: apply` — el local sin credenciales reales no es suficiente para validar la importación) y verificar post-apply que `terraform plan` queda **completamente vacío** sobre los once secrets + cuatro variables + repo settings + App installation (**NO** incluye branch protection — D5 enmendado, no se declara ese recurso). Si el plan muestra diff, NO marcar la tarea como hecha — el diff es un bug del `import.sh` o de los recursos. [R7.3, D6]
- [ ] 5.5 Verificar que ningún `output` de Terraform expone el valor de un secret — `terraform show -json | jq '.outputs'` debe mostrar solo los tres outputs planeados y ningún valor que parezca PEM o CIDR. [R1]

## 6. CI workflow + GHCR verification job

- [ ] 6.1 Crear `.github/workflows/infra-github.yml` siguiendo el patrón de `.github/workflows/infra-dev.yml`: (1) job `check` para `pull_request` con `paths: ['infra/github/**']`, sin secretos, en `ubuntu-latest`, ejecutando `terraform fmt -check -recursive infra/github/`, `terraform -chdir=infra/github init -backend=false`, `terraform -chdir=infra/github validate`; (2) workflow `workflow_dispatch` con `action: plan|apply`, gateado por `if: github.ref == 'refs/heads/main'` (D7), ejecutado en `[self-hosted, dev]`, con `concurrency` (serializa applies) y `timeout-minutes`. [R7.1, R7.2, R7.4]
- [ ] 6.2 Reutilizar el patrón de `terraform` por SHA de commit (mismo SHA que `infra-dev.yml`); inyectar las variables sensibles con `env: TF_VAR_github_app_private_key_path: ${{ env.GH_APP_PRIVATE_KEY_PATH }}` y compañía desde `${{ secrets.* }}`. [R7]
- [ ] 6.3 Añadir un job `pull_request` adicional `verify-ghcr` (en el mismo workflow, con el `paths`-filter adecuado) que publique una imagen efímera a `ghcr.io/autohostai-labs/infra-github-iac-smoke` con etiqueta `ephemeral-${{ github.run_id }}` y la borre al terminar — verificación end-to-end de que el permiso `packages: write` de la App + el flag de packages del repo componen un acceso que funciona. Si la flag de packages no es modelable (D9, fallo documentado), el job sigue siendo válido: verifica el lado App y deja explícito en su log qué limitación existe. [R4.2]
- [ ] 6.4 Verificación: abrir un PR de prueba (puede ser un commit vacío sobre la rama) y verificar que `check` y `verify-ghcr` corren y pasan. [R7.1]

## 7. Documentación: specs + steering

- [ ] 7.1 Actualizar `infra/README.md` añadiendo `infra/github/` al árbol con una línea de descripción corta. [Spec affected]
- [ ] 7.2 Actualizar `sdd/specs/infra-scaffold.md` añadiendo una nota bajo "Estructura de `/infra` por entorno" que menciona `infra/<superficie-cross-env>/` como tercer patrón documentado. [Spec affected]
- [ ] 7.3 Actualizar `sdd/specs/infra-dev-terraform.md` añadiendo una nota bajo "Pendiente" (o donde quede coherente) que la declaración de secrets/variables de Actions pasa a `infra/github/` — este spec mantiene la lista de secrets **esperados** como referencia cruzada. [Spec affected]
- [ ] 7.4 Actualizar `sdd/specs/app-deploy-dev.md` añadiendo una nota en "Estado" indicando que la lista de variables (`GH_APP_ID`/`GH_APP_INSTALLATION_ID`/`NEXT_PUBLIC_APP_ENV`/`PUBLIC_HOSTNAME`) se gestiona en `infra/github/`; este spec mantiene cómo se consumen en runtime. [Spec affected]
- [ ] 7.5 Actualizar `sdd/steering/infra.md`: (a) §"Convención de layout" — añadir un tercer bullet `infra/<superficie-cross-env>/` con la justificación de superficies por-organización (GitHub-side hoy; DNS/org/policies futuras); (b) bajo "Lección de `app-deploy-dev`" — confirmar que el provider `github` ya está adoptado (la nota de "cambio futuro pendiente" queda obsoleta). [R1.2, Spec affected]
- [ ] 7.6 Verificación: `make check-rule11-ownership` pasa (los cambios de prosa en `sdd/` y `docs/` están bajo el censo del guard; este change no introduce sumideros nuevos). [Steering rule 11]

## 8. Verificación "deploy from zero" <!-- hard -->

- [ ] 8.1 Disparar `workflow_dispatch` con `action: apply` en `infra-github.yml` desde `main` — confirmar que el run termina en verde y aplica todos los recursos declarados. [R7.2, R7.3]
- [ ] 8.2 Inmediatamente después, disparar `workflow_dispatch` con `action: plan` — confirmar que el `plan` queda **completamente vacío** (R7.3). Si no, el primer apply no se completó bien: NO marcar la tarea como hecha y volver a sección 5. [R7.3]
- [ ] 8.3 Confirmar que el job `verify-ghcr` del run de CI de 6.4 publicó y borró la imagen efímera correctamente (`gh api /user/packages/container/infra-github-iac-smoke/versions` no debe devolverla). [R4.2]
- [ ] 8.4 Verificación final: re-correr todas las verificaciones locales (`terraform fmt -check -recursive infra/github/`, `validate`, `plan` con `backend=false`) — deben pasar. [R1.4, R7]
- [ ] 8.5 Vincular el run del workflow desde la sección "Pendiente" de `infra/github/RUNBOOK.md` como evidencia viva de "deploy from zero" exitosa. [R7.3]

## Implementation Notes

<!-- Append-only, written by the implementer of each section for the next one:
     decisions taken, names chosen, gotchas found. One bullet each, no prose. -->

- Tarea 1.1 quedó reconvertida a **verificación**: comprobar que `autohostai-tfstate-dev` existe con `versioning = enabled` (lo creó `infra/environments/dev/`, no es bootstrap irreducible de este módulo). El módulo `infra/github/` no introduce ningún bucket nuevo (D3 amend tras pushback del usuario — la separación entre los dos módulos es la `key`, no el bucket).
- Variables: declaradas EXACTAMENTE 14 (12 sensibles + 2 no sensibles) siguiendo el conteo literal de la tarea 1.6. `github_app_id`, `github_app_installation_id` y `github_repository_full_name` NO están declaradas como variables — sección 2 las añade (las dos primeras) y la tercera es un `local.*` (computado, como dice el design §"Variables nuevas"). **Sección 2 actualiza el conteo a 16 (12 sensibles + 4 no sensibles)**: añade `github_app_id` y `github_app_installation_id` como no sensibles (IDs públicos, mismo patrón que el módulo dev). El comentario de cabecera de `variables.tf` se ha actualizado.
- `outputs.tf` referencia `local.*` placeholders (`github_repository_default_branch = "main"`, `github_app_installation_id = "0"`); sección 2 los cablea a los recursos reales — esto evita pre-comprometer nombres de atributos del provider pinado y hace que `terraform validate` pase ya en sección 1.
- `backend.hcl.example` usa `key = "github.tfstate"` (el módulo dev usa `dev.tfstate`) sobre el **mismo bucket** `autohostai-tfstate-dev`. No hay bootstrap irreducible: el bucket ya existe (lo creó `infra/environments/dev/` con versioning).
- Provider pinado: `~> 5.0` resuelve a `v5.45.0` (verificado en `terraform init`). OJO si sección 2 mira atributos — la doc debe ser de esa versión.
- `.terraform.lock.hcl` se commitea (whitelisted en `.gitignore` raíz, mismo patrón que el módulo dev).
- **Sección 2 — OQ2 fallback activado**: `github_repository_settings` **NO existe** en el provider `integrations/github` v5.45.0 (verificado contra el tag `v5.45.0` del repo upstream: ni en `website/docs/r/` ni en `github/resource_github_repository_settings.go`). Se usa `github_repository` (recurso único que crea Y gestiona settings) con `name = var.github_repository_name` para que el provider haga `Repositories.Get` y adopte los settings del repo ya existente sin recrearlo. El procedimiento de importación (`terraform import github_repository.this AutoHostAI`) se codifica en `import.sh` (sección 3/5). El recurso se declara con `count = 1` (default; `count = 0` que el OQ2 mencionaba como literal no produciría nada que Terraform gestione — se interpreta como "count irrelevante, gestión vía import").
- **Sección 2 — discrepancia con la tarea 2.2 sobre `homepage`**: la tarea dice `homepage` pero el provider (v5.45.0) expone `homepage_url`. Se usa `homepage_url` (sigue siendo "vacío hasta que exista", `""`).
- **Sección 2 — `default_branch` deprecado**: el atributo `default_branch` en `github_repository` está marcado como `[DEPRECATED] Use the github_branch_default resource instead` por el provider v5.45.0. Se mantiene en el recurso (la tarea 2.2 lo pide explícitamente) pero `validate` emite el warning; la migración al recurso dedicado `github_branch_default` queda como tarea futura (entrada de roadmap o decisión en sección 4/5).
- **Sección 2 — `auth_mode` no declarado**: el bloque `provider "github"` se declara SIN `auth_mode = "app"` (la sintaxis de referencia del design.md no lo incluye). El provider en modo "auto" autodetecta la auth por App cuando el bloque `app_auth` está poblado con sus tres campos, que es nuestro caso. Si `validate` o el primer `plan` en CI reporta que la auth no se detecta, añadir `auth_mode = "app"` como fixup.
- **Sección 2 — `terraform plan` DEFERRED a CI**: el plan de la tarea 2.5 no se ha podido ejecutar localmente porque `backend.tf` declara `backend "oci"` y `init -backend=false` deja el backend sin inicializar; `terraform plan` exige backend inicializado. La validación (`terraform validate`) sí pasa (con los warnings de deprecación de `default_branch`). El primer `plan` real se ejecutará en CI con credenciales reales (workflow de sección 6).
- **Sección 2 amend (round-1 fix)** — opción (b) aplicada al DESIGN-CONFLICT (D11, registrado en BLOCKED.md como `assumed`): `github_app_installation_repositories` fuera de Terraform. `github_app_installation_id` output lee de `var.github_app_installation_id`. La instalación de la App sobre el repo es bootstrap irreducible (RUNBOOK.md §1, sección 5).
- Merge flags añadidos a `github_repository.this`: `allow_merge_commit`, `allow_squash_merge`, `allow_rebase_merge`, `allow_auto_merge`, `delete_branch_on_merge` (R2.1 + D11 adyacente; el App debe tener `contents: write` para que apliquen — R1.5 fail-fast en `plan`). Valores leídos del repo vivo `autohostai-labs/AutoHostAI` el 2026-09-12 (`gh api /repos/autohostai-labs/AutoHostAI`): allow_merge_commit=true, allow_squash_merge=true, allow_rebase_merge=true, allow_auto_merge=false, delete_branch_on_merge=false.
- **Round-2 fix (D11 coherence sweep)**: `proposal.md` §"Why" línea ~5 — añadida "instalar la GitHub App sobre el repo" a la lista de bootstrap irreducible; extendida la cláusula final para mencionar que la API de GitHub tampoco permite modelar la instalación desde Terraform con `app_auth` (D11).
- **Round-2 fix (D11 coherence sweep)**: `proposal.md` §"What changes" línea ~11 — extraída "instalación de la GitHub App sobre el repo" de la enumeración de recursos declarados y desplazada a una frase aparte que la califica como bootstrap irreducible documentada en `infra/github/RUNBOOK.md` §1 (D11).
- **Round-2 fix (D11 coherence sweep)**: `tasks.md` §2.5 línea 30 — reescrita la verificación: referencia `github_repository.this` (no `github_repository_settings`, OQ2 fallback) y añade la aserción negativa explícita de que NO aparecen `github_repository_settings` ni `github_app_installation_repositories` (D11).
- **Round-2 fix (D11 coherence sweep)**: `infra/github/variables.tf` línea 161 — descripción de `github_app_installation_id` reescrita: ya no menciona el recurso eliminado; refleja D11 (bootstrap irreducible, RUNBOOK.md §1) y deja claro que el valor lo aporta el operador porque la App ya está creada, registrada e instalada sobre el repo.
- **Sección 3 — mapeo once secretos ↔ variables**: cada `github_actions_secret.<resource>` mapea 1:1 la variable sensible correspondiente ya declarada en `variables.tf` (12 sensibles de la sección 1). Mapeo final: `oci_tenancy_ocid`→`var.oci_tenancy_ocid`, `oci_user_ocid`→`var.oci_user_ocid`, `oci_fingerprint`→`var.oci_fingerprint`, `oci_region`→`var.oci_region`, `oci_private_key`→`file(var.oci_private_key_path)`, `oci_compartment_ocid`→`var.oci_compartment_ocid`, `tfstate_namespace`→`var.tfstate_namespace`, `tfstate_bucket`→`var.tfstate_bucket`, `allowed_ssh_cidrs`→`jsonencode(var.allowed_ssh_cidrs)`, `allowed_ssh_cidrs_wide`→`jsonencode(var.allowed_ssh_cidrs_wide)`, `ssh_public_keys`→`jsonencode(var.ssh_public_keys)`. Las tres listas llegan al workflow como JSON arrays (`TF_VAR_allowed_ssh_cidrs: ${{ secrets.ALLOWED_SSH_CIDRS || ... }}` en `infra-github.yml`) y `jsonencode()` aquí vuelve a serializar el `list(string)` que Terraform ya las tiene tipadas — round-trip idéntico al del módulo dev.
- **Sección 3 — `OCI_PRIVATE_KEY` por ruta, no inline**: el secret de Actions es el **contenido** del `.pem` privado, pero la variable Terraform es la **ruta** (`oci_private_key_path`, `type = string, sensitive = true`) — el workflow escribe el contenido del secret `OCI_PRIVATE_KEY` a un fichero en `$RUNNER_TEMP` y pasa la ruta, mismo patrón que `github_app_private_key_path` y que `infra-dev.yml`. `file()` lee el PEM sin filtrarlo a un heredoc HCL (el hallazgo de revisión que `infra/environments/dev/variables.tf` documenta: `private_key_path` evita "el riesgo de sintaxis HCL de embeber un PEM multilínea en un string/heredoc").
- **Sección 3 — `import.sh` formato corregido vs. tarea 3.4**: la tarea dice `<SECRET_NAME>` como ID de import; el provider `integrations/github` v5.45.0 exige `<repository>/<secret_name>` (verificado contra el import function de `resource_github_actions_secret.go` en el tag v5.45.0 del upstream: "supplied ID must be written as `<repository>/<secret_name>`"). `import.sh` produce `AutoHostAI/<SECRET_NAME>` (el repo por default de `variables.tf`). El repository queda hardcodeado en el script porque el `repository` que el provider exige es el nombre del repo en GitHub (no el OCID), y `var.github_repository_name` lo lee el HCL, no un shell sin estado. Si el repo cambia de nombre, `import.sh` se edita en el mismo PR; el caso solo se da al renombrar, que ya exige PR propia.
- **Sección 3 — orden de `import.sh`**: alfabético por `secret_name` (NO por `resource_name`), como pide la tarea 3.4 ("orden: alfabético por nombre del recurso" — interpretado como el nombre del recurso GitHub, que es el que el humano lee en `Settings → Secrets`). Resultado: ALLOWED_SSH_CIDRS, ALLOWED_SSH_CIDRS_WIDE, OCI_COMPARTMENT_OCID, OCI_FINGERPRINT, OCI_PRIVATE_KEY, OCI_REGION, OCI_TENANCY_OCID, OCI_USER_OCID, SSH_PUBLIC_KEYS, TFSTATE_BUCKET, TFSTATE_NAMESPACE.
- **Sección 3 — comentario en `main.tf` (no RUNBOOK.md)**: la tarea 3.2 pide "comentar en `RUNBOOK.md` §2 por qué no se duplican", pero RUNBOOK.md es trabajo de la sección 5 y aún no existe. En su lugar, el bloque de comentario al inicio de los `github_actions_secret.*` enumera las exclusiones (los 13 secretos del Vault + `GH_APP_PRIVATE_KEY` por D2) y apunta a RUNBOOK.md §2 para el detalle. La sección 5 levantará RUNBOOK.md reusando estos anclajes.
- **Sección 3 — task 3.5 deferred a CI**: `terraform plan` post-import exige credenciales reales de la GitHub App contra el backend OCI; la verificación local es solo estática (`fmt`/`validate`/`init -backend=false`). Marcada `[x]` con esta salvedad, análoga a la sección 2 (ver nota "terraform plan DEFERRED a CI" más arriba). El primer plan real corre en `infra-github.yml` (sección 6) durante la sección 8 ("deploy from zero").
- **Sección 4 — variables nuevas (tarea 4.1)**: `next_public_app_env` y `public_hostname` añadidas a `variables.tf` con default `""` + validación fail-fast (lista cerrada dev/staging/production/test para la primera, regex DNS básico + non-empty para la segunda). El conteo del comentario de cabecera pasa a 18 (12 sensibles + 6 no sensibles: 4 de la sección 1+2 + 2 de la sección 4). El default `""` queda FUERA de la lista de validación para que el `terraform plan` sin `TF_VAR_*` inyectados falle con un mensaje explícito — fail-fast en validación, no en `apply` con un valor vacío propagado al frontend (donde un `NEXT_PUBLIC_APP_ENV=""` se hornearía en el bundle y el badge mostraría vacío sin que el operador lo notara hasta el siguiente deploy).
- **Sección 4 — formato del import ID distinto por recurso (tarea 4.2)**: el provider `integrations/github` v5.45.0 acepta formatos DISTINTOS por recurso (verificado contra el upstream): `github_actions_secret.*` usa `<repository>/<secret_name>` (barra), `github_actions_variable.*` usa `<repository>:<variable_name>` (dos puntos) y `github_branch_protection.*` usa `<repository>:<pattern>` (dos puntos también). La sección 3 ya documentó el formato barra para los secrets; `import.sh` ahora explicita los tres formatos en su cabecera y añade los cinco imports nuevos (cuatro variables + un branch protection). Confundirlos con `terraform import <recurso> <repo>/<name>` sobre una variable produce un `Error: parse ID` del provider.
- **Sección 4 — branch protection: lista de checks (tarea 4.3)**: el branch protection exige los SIETE checks always-run del repo (`api-contract`, `backend-tests`, `compose-ports`, `frontend-api-contract`, `frontend-tests`, `rule11-ownership`, `version-parity`). Quedan EXCLUIDOS a propósito: `infra-dev / check` (paths-filtered sobre `infra/environments/dev/**`, no dispara en cada PR), `deploy-dev / {provenance, build-backend, build-frontend, deploy}` (solo `push: branches: [main]`, no `pull_request`) y `multiarch-build-check / {build-backend, build-frontend}` (paths-filtered sobre Dockerfiles/lockfiles). Marcar un check paths-filtered como required habría reproducido el bug del que `backend-tests.yml` y `compose-ports.yml` ya huyen en sus comentarios de cabecera: "un required check con filtro de rutas no se ejecuta en los PR que no las tocan, y GitHub deja esos PR bloqueados esperando para siempre".
- **Sección 4 — `required_linear_history` (con "d") vs tarea 4.3**: la tarea 4.3 decía `require_linear_history` (sin "d"), pero el provider `integrations/github` v5.45.0 expone `required_linear_history` (verificado contra la doc del tag v5.45.0 — `terraform validate` lo detecta y sugiere el nombre correcto). El recurso usa `required_linear_history = true`; la nota en el comentario del recurso explica la divergencia para que el lector no piense que es un typo. Ningún otro atributo del recurso cambió de nombre.
- **Sección 4 — `required_pull_request_reviews` NO declarado (tarea 4.4)**: D5 fija que solo se declara lo que el plan Free soporte; `required_pull_request_reviews` exige reviewers y el plan Free lo rechaza en la API (no llega ni a `plan` — `terraform plan` falla con 403/422 del upstream). El recurso de branch protection NO incluye el bloque `required_pull_request_reviews`; el comentario al pie del recurso apunta a `RUNBOOK.md` §3 (sección 5) como el sitio donde se documentará el "queda por convención" si en el futuro alguien lo añade y el plan lo reporta como rechazo del plan Free. Hoy no aplica porque el bloque no está declarado.
- **Sección 4 — task 4.5 deferred a CI**: análogo a las secciones 2 y 3 — `terraform plan` con credenciales reales de la GitHub App y backend OCI inicializado corre en `infra-github.yml` (sección 6) durante la sección 8 ("deploy from zero"). Marcada `[x]` con esta salvedad. Verificación local: `fmt -check -recursive` PASS, `init -backend=false` PASS, `validate` PASS (con los warnings de deprecación ya conocidos de `default_branch` desde la sección 2).
- **Sección 4 — DESIGN-CONFLICT + resolución (D5 enmendado, post-panel): `github_branch_protection` retirado del módulo.** El reviewer CI/CD del panel de sección 4 encontró que `infra/environments/dev/RUNBOOK.md:17` ya documentaba, desde antes de este change, que la API de branch protection devuelve `403 "Upgrade to GitHub Pro or make this repository public"` de forma **completa** en este repo privado + plan Free — no solo para el sub-bloque `required_pull_request_reviews` que D5/R5 originales anticipaban como único rechazo. Verificado en vivo el 2026-09-13 con `gh api repos/autohostai-labs/AutoHostAI/branches/main/protection` → mismo `403`. El usuario, ante el DESIGN-CONFLICT (registrado y resuelto en `BLOCKED.md`), eligió: **NO declarar `github_branch_protection.this`**; documentar las tres reglas (required status checks, required linear history, enforce_admins) como convención no forzada en `infra/github/RUNBOOK.md` (sección 5) — mismo trato que "required reviewers" ya lleva en el RUNBOOK del módulo dev. **Esto SUPERSEDE las notas anteriores** de "Sección 4 — formato del import ID... y `github_branch_protection.*`" (parte de la nota sobre formato colon para branch protection, ya no aplica: no hay import), "Sección 4 — branch protection: lista de checks" (el contenido de la lista sigue siendo correcto y útil, pero ahora vive como comentario en `main.tf` §R5 y se trasladará al RUNBOOK, no como recurso `required_status_checks`), "Sección 4 — `required_linear_history`..." (el nombre correcto del atributo sigue siendo verdad para cuando se retome el recurso en el futuro, pero hoy no hay recurso que lo use) y "Sección 4 — `required_pull_request_reviews` NO declarado" (correcto en su día, pero la razón cambió: ya no es "solo ese sub-bloque se rechaza", es "todo el recurso se rechaza"). Ficheros tocados por esta enmienda: `proposal.md` §R5 (reescrita), `design.md` §D5 (reescrita) + tabla "Changes by area" + riesgo #3, `infra/github/main.tf` (recurso eliminado, sustituido por comentario), `infra/github/import.sh` (import de branch protection eliminado), `tasks.md` §4.3/4.4/4.5 y §5.1/5.4 (referencias corregidas). Task 5.1 (sección 5, aún pendiente) hereda la obligación de escribir la sección de convención en el RUNBOOK real.