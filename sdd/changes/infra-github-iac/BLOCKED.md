# BLOCKED — infra-github-iac

One entry per pending item (shared rule 5): `decision` needs a human before the change can move; `deferred` and `assumed` travel with the PR and are acknowledged by deleting them; `/sdd:archive` refuses to close while any entry remains.

## DESIGN-CONFLICT resolution (D11): App installation como bootstrap irreducible

- **phase**: run
- **type**: assumed
- **what & why**: El usuario dio autorización genérica ('no sabría decirte a o b, aplica el que consideres mejor opción') sobre el DESIGN-CONFLICT registrado antes. Opción elegida: (b) mover github_app_installation_repositories a bootstrap irreducible. Rationale: (a) introduce un PAT como excepción a D2 (D2 ya rechazó PAT por radio de daño + ADR 0002) — un fine-grained PAT con repo:write sobre un solo repo es menos dañino pero igualmente requiere rotación, secret store y monitorización. (b) no introduce credenciales nuevas, mantiene D2 limpia, y la instalación de la App es una acción one-time (no es un recurso sujeto a drift significativo); el job verify-ghcr de la sección 6 detecta en runtime si la App pierde los permisos que el CD necesita. Materializado como D11 en design.md. Documentos a actualizar por el round-1 fix: proposal.md §R4.1, design.md §D11 + tabla 'Changes by area', tasks.md §2.3, main.tf (remove github_app_installation_repositories; add merge flags). El merge flags finding (qa+architect) se aborda en el mismo round-1 fix: añadir allow_merge_commit, allow_squash_merge, allow_rebase_merge, allow_auto_merge, delete_branch_on_merge a github_repository.this.
- **exact resume command**: /sdd:run infra-github-iac

## DESIGN-CONFLICT resolution: github_branch_protection dropped from the module (D5 amended)

- **phase**: run
- **type**: assumed
- **what & why**: The user picked option (a) of the DESIGN-CONFLICT registered before (branch protection API rejects the whole resource, not just `required_pull_request_reviews`, verified live 2026-09-13 via `gh api repos/autohostai-labs/AutoHostAI/branches/main/protection` → `403`): drop `github_branch_protection.this` entirely from this change, document the three rules (required status checks, required linear history, enforce_admins) as unforced convention in `infra/github/RUNBOOK.md` (same posture "required reviewers" already has in the dev module's RUNBOOK), and amend D5/R5 to reflect that the rejection is total (API/plan-tier limitation), not a partial gap between sub-blocks. Materialized: `proposal.md` §R5 rewritten, `design.md` §D5 rewritten + "Changes by area" table + risk #3, `infra/github/main.tf` (resource removed, replaced by a documentation comment), `infra/github/import.sh` (branch protection import line removed), `tasks.md` §4.3/4.4/4.5 and §5.1/5.4 references corrected. Task 5.1 (section 5, still pending) inherits the obligation to actually write the convention section in the real RUNBOOK.
- **exact resume command**: /sdd:run infra-github-iac

## Sections 5 (5.4/5.5) and 8 (8.1-8.5) require this branch merged to `main` first — they cannot run from `sdd/infra-github-iac`

- **phase**: run
- **type**: deferred
- **tasks**: 5.4, 5.5, 8.1, 8.2, 8.3, 8.4, 8.5
- **what & why**: Sections 1-4, 6, 7 and the documentation part of section 5 (5.1-5.3) are implemented, verified, and panel-certified (PASS receipts on sections 1/2/3/4/6; section 5 partial and section 7 are pure-docs, skipped by design). What remains — task 5.4 (first real `apply` + empty post-import plan), task 5.5 (verify no secret leaks in outputs), and all of section 8 ("deploy from zero": trigger real `apply`, confirm empty subsequent `plan`, confirm `verify-ghcr`'s image was cleaned up, re-run local static checks, link the live run from the RUNBOOK) — all require triggering `workflow_dispatch` on `infra-github.yml`'s `apply` job. That job's own gate (D7, R7.4) is `if: github.event_name == 'workflow_dispatch' && github.event.inputs.action == 'apply' && github.ref == 'refs/heads/main'` — it is **structurally unable to run from any branch other than `main`**, and this code is still on `sdd/infra-github-iac`. This is not a credentials gap (the repo secrets/variables this module imports already exist, created during the original manual bootstrap) — it is the deliberate main-only trust-model gate that `steering/infra.md`'s "Decisión estable" section documents (2-person trust, no GitHub Environment, `apply` restricted to reviewed code on `main`). Triggering a real `apply` against live infrastructure is also an outward-facing, hard-to-reverse action in its own right (mutates the real `autohostai-labs/AutoHostAI` repo settings/secrets/variables) and should not be attempted without the user's explicit go-ahead regardless of branch. **The natural sequence**: this PR merges to `main` (via `/sdd:ship` after `/sdd:review`) → `workflow_dispatch action: apply` is triggered from `main` (by the user or by an operator following `infra/github/RUNBOOK.md` §0) → the resulting real run satisfies 5.4/5.5/8.1-8.4 together (they're the same live apply, not separate runs) → 8.5 links that run from the RUNBOOK's "Pendiente" note. This travels with the PR per shared rule 5; `/sdd:review` can still certify local readiness with these entries open, since they describe post-merge verification, not undone local work.
- **exact resume command**: workflow_dispatch action=apply on infra-github.yml from main (post-merge), then /sdd:run infra-github-iac 8

## Tasks 1.1, 2.5, 6.4 need live credentials this sandbox doesn't have

- **phase**: run
- **type**: deferred
- **tasks**: 1.1, 2.5, 6.4
- **what & why**: Task 1.1 (verify the autohostai-tfstate-dev bucket has versioning=enabled) needs a live OCI read; this sandbox has an OCI CLI profile (autohostai-dev) but its session token is expired and refreshing it requires an interactive browser login I cannot perform. Task 2.5 (terraform plan with placeholder App values against the real OCI backend) was already investigated during section 2: 'init -backend=false deja el backend sin inicializar; terraform plan exige backend inicializado' — it needs the real OCI backend, same credentials gap as 1.1. Task 6.4 (open a test PR, confirm check/verify-ghcr run and pass) needs a live PR against the real repo, which this session declined to trigger without checking with the user first (per section 6's implementer report) and which section 8's live run will exercise anyway (check/verify-ghcr fire on every PR touching infra/github/**, including the PR this change itself will open). All three are read-only or low-risk verifications, not decisions — they just need either OCI SSO refreshed in this session, or the eventual PR/live-apply to happen naturally, whichever comes first.
- **exact resume command**: /sdd:run infra-github-iac 1.1
