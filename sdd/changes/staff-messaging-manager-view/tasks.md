# Tasks: staff-messaging-manager-view

<!-- Markers, read by /sdd:run and the lifecycle gates (HTML comments, invisible
     when rendered). On a section heading: "hard" makes that section's
     implementer run on the stronger model; "panel: PASS <date> receipt:<id>"
     is written by the panel gate (reviewer_panel.py) when the section's review
     panel passes — never by hand; "panel: skipped — <reason>" records a
     deliberate skip (scaffolding, docs, config). On a task line:
     "manual" marks a task only a human can perform — run leaves it to you and
     it may travel with the PR as a deferred entry; it may sit on any line of
     the task item, not only the checkbox line. -->

## 1. i18n: messages.* + tabs.* en incidents.json y cleaning.json <!-- panel: PASS 2026-09-18 receipt:5a7992f9 -->

- [x] 1.1 `frontend/locales/es/incidents.json` + `frontend/locales/en/incidents.json` — añadir claves `tabs.label`, `tabs.content` y la sección `messages.*` completa (`messages.tab`, `messages.title`, `messages.loading`, `messages.error.{title,description}`, `messages.empty.{title,description}`, `messages.composer.{label,placeholder,counter,send,sending}`, `messages.roles.{TECHNICIAN,PROPERTY_MANAGER,TENANT_OWNER}`, `messages.errors.{required,tooLong,forbidden,notFound,generic}`, `messages.loadNewer`) con la misma forma y semántica que ya tienen `frontend/locales/{es,en}/tech.json`. Las claves deben ser cadenas literales (sin interpolación de JS) y pasar el panel i18n de `sdd-review-i18n` (cada clave con su traducción ES/EN). [R4.1, R4.2]
- [x] 1.2 `frontend/locales/es/cleaning.json` + `frontend/locales/en/cleaning.json` — añadir el mismo árbol de claves que en 1.1 pero con los roles `messages.roles.{CLEANER,PROPERTY_MANAGER,TENANT_OWNER}` (la limpiadora en vez del técnico), copiando la forma y semántica de `frontend/locales/{es,en}/cleaner.json`. [R4.1, R4.2]

## 2. Manager incident surface: tabs + panel + wrapper + page <!-- panel: PASS 2026-09-18 receipt:f1dc79f8 -->

- [x] 2.1 `frontend/features/incidents/components/detail/manager-incident-tabs.tsx` (nuevo) — copiar el patrón de `frontend/features/tech/components/detail/tech-incident-tabs.tsx`: dos botones `role="tab"` con prefijo DOM `manager-incident-tab-{key}`, dos paneles `role="tabpanel"` con prefijo `manager-incident-panel-{key}`, `hidden` en el inactivo, `useTranslation("incidents")`, `aria-label={t("tabs.label")}`, `aria-selected`/`aria-controls`/`aria-labelledby`, roving `tabIndex`, ←/→/Home/End, `activeTab` arranca en `"content"`, `hasOpenedMessagesTab` sticky que pasa a `true` al primer toque de `"messages"` y nunca vuelve. La pestaña `"messages"` muestra `t("messages.tab")`; la `"content"` muestra `t("tabs.content")`. Tipo `ManagerIncidentTabKey = "content" | "messages"`, exportado. [R1.1]
- [x] 2.2 `frontend/features/incidents/components/detail/manager-incident-tabs.test.tsx` (nuevo) — cubrir el contrato ARIA (roles, atributos, roving tabIndex), el manejo de teclado ←/→/Home/End, el comportamiento sticky de `hasOpenedMessagesTab` (el panel `messages` se monta una sola vez y no se desmonta al volver), y que el panel inactivo lleva `hidden`. Patrón de `frontend/features/cleaner/components/detail/cleaner-task-tabs.test.tsx`. [R1.1]
- [x] 2.3 `frontend/features/incidents/components/detail/manager-incident-messages-panel.tsx` (nuevo) — copia adaptada de `frontend/features/tech/components/detail/tech-incident-messages-panel.tsx` con prefijo DOM `manager-incident-` y `useTranslation("incidents")`. Constante local `MAX_CONTENT = 2000`. Hooks `useIncidentMessages(incidentId, page, enabled)` y `useSendIncidentMessage(incidentId)` (exportados desde `frontend/features/incidents/index.ts`). Estado: `page` arranca en 1, `olderRows: IncidentMessage[]`, `content: string`, `touched: boolean`. Carga perezosa: `enabled` se pasa desde la pestaña. Paginación: 20 por página, ascendente, botón `t("messages.loadNewer")` pide `page + 1` y añade al final. Composer: `<textarea maxLength={2000}>` con `id="manager-incident-message-content"`, `aria-describedby="manager-incident-message-counter"`, validación local `validate(content)` (`trim().length` 1..2000) que devuelve clave `messages.errors.required` o `messages.errors.tooLong`, botón de envío con `className="tap-target"` y `disabled={validationKey !== null || mutation.isPending}`. Envío: `mutation.mutate({ content: content.trim() }, { onSuccess: ... })` que vacía `content`/`touched` solo en éxito (R1.3/R4.3) y, si estaba en la última página y el refetch añade una página, hace `advancePast`. Errores de envío mapeados con `sendErrorKeyFor(kind)` (las mismas ramas `validation`/`not-found`/`forbidden`/genérico que `tech-incident-messages-panel.tsx:347-352`). 404 del hilo: `useLayoutEffect` que llama `onNotFound?.()` cuando `mapIncidentsError(query).kind === "not-found"`; si el callback está presente, devuelve `null` (sin panel-local `EmptyState`) para que el wrapper pinte la pantalla completa de "no disponible" en el mismo frame. [R1.2, R1.3, R1.4, R1.5]
- [x] 2.4 `frontend/features/incidents/components/detail/manager-incident-messages-panel.test.tsx` (nuevo) — cubrir, en este orden: carga inicial con `enabled=true` y respuesta vacía muestra `EmptyState` (`messages.empty.{title,description}`); carga inicial con respuesta poblada muestra la lista con la barra de rol por mensaje (`t(messages.roles.${authorRole})`); click en `t("messages.loadNewer")` con `totalPages > page` añade filas; envío válido llama `POST /api/v1/incidents/{id}/messages`, invalida `incidentsKeys.messagesPrefix`, limpia el textarea, persiste el mensaje al final; envío con texto `""` o `">2000"` no llama al endpoint y muestra `messages.errors.required` / `messages.errors.tooLong`; `mutation.isPending` deshabilita el botón y muestra `t("messages.composer.sending")`; `query.isError` con cero mensajes muestra `messages.error.{title,description}`; 404 dispara `onNotFound` en `useLayoutEffect` y devuelve `null` cuando el callback está presente. Patrón de `frontend/features/tech/components/detail/tech-incident-messages-panel.test.tsx`. [R1.2, R1.3, R1.4, R1.5]
- [x] 2.5 `frontend/features/incidents/components/detail/manager-incident-detail-view.tsx` (nuevo) — wrapper de D1/D7: invoca `useIncident(incidentId)` y `useTechnicianDirectory()`, llama a `mapIncidentsError` para resolver `state.kind`, mantiene sticky `messagesNotFound: boolean` que `setMessagesNotFound(true)` desde un `onNotFound` callback estable (`useCallback`). State machine: `loading` → `LoadingState`; `forbidden` → `<p>{t("incidents:fields.forbidden")}</p>`; `not-found` o `messagesNotFound` → `EmptyState` con enlace a `/incidents`; `validation` → `<p>{t("incidents:fields.validation")}</p>`; `error` → `ErrorState` con botón "Reintentar" que llama `void query.refetch()`. En éxito renderiza `ManagerIncidentTabs` con `content` siendo los seis bloques de `incident-detail-sections.tsx` (`DetailHeader`, `DetailIdentifyingBlock`, `DetailAssignedTechnicianBlock`, `DetailDescriptionBlock`, `DetailCostsBlock`, `DetailMetadataBlock`) en el mismo orden que `IncidentDetailView` y `ManagerIncidentActions` detrás de `useHasPermission("MANAGE_INCIDENTS")`, y `renderMessages={(enabled) => <ManagerIncidentMessagesPanel incidentId={...} enabled={enabled} onNotFound={onMessagesNotFound} />}`. Conserva el patrón de D7: NO importa `IncidentDetailView`, solo los bloques. `useTranslation(["incidents","states","navigation"])`. [R1.1, R1.2, R1.3, R1.4, R1.5, R3.1, R3.3]
- [x] 2.6 `frontend/features/incidents/index.ts` — añadir `export { ManagerIncidentDetailView } from "./components/detail/manager-incident-detail-view"`. [R1.1]
- [x] 2.7 `frontend/app/(workspace)/incidents/[id]/page.tsx` — cambiar el componente montado de `IncidentDetailView` a `ManagerIncidentDetailView` (import desde `@/features/incidents`). `IncidentDetailView` sigue exportado y disponible. [R1.1, R3.2]

## 3. Manager cleaning surface: tabs + panel + refactor del detail view + barrel <!-- panel: PASS 2026-09-18 receipt:7743949e -->

- [x] 3.1 `frontend/features/cleaning/components/detail/manager-cleaning-task-tabs.tsx` (nuevo) — copia de `frontend/features/cleaner/components/detail/cleaner-task-tabs.tsx` con prefijo DOM `manager-cleaning-` y `useTranslation("cleaning")`. Tipo `ManagerCleaningTaskTabKey = "content" | "messages"`, exportado. Mismo contrato ARIA, mismo sticky `hasOpenedMessagesTab`, mismo manejo de teclado. La pestaña `"messages"` muestra `t("messages.tab")`; la `"content"` muestra `t("tabs.content")`. [R2.1]
- [x] 3.2 `frontend/features/cleaning/components/detail/manager-cleaning-task-tabs.test.tsx` (nuevo) — equivalente al test de 2.2 con los selectores `manager-cleaning-*` y el namespace `cleaning`. [R2.1]
- [x] 3.3 `frontend/features/cleaning/components/detail/manager-cleaning-task-messages-panel.tsx` (nuevo) — copia de `frontend/features/cleaner/components/detail/cleaner-task-messages-panel.tsx` con prefijo DOM `manager-cleaning-`, `useTranslation("cleaning")`, hooks `useCleanerTaskMessages(taskId, page, enabled)` y `useSendCleanerTaskMessage(taskId)`. `mapCleaningError(query)` en lugar de `mapIncidentsError` (limpia la asimetría con el panel de manager de `incidents`). Constante `MAX_CONTENT = 2000`. Mismas reglas que 2.3 (composer, paginación, validación, `onNotFound` en `useLayoutEffect`). [R2.2, R2.3, R2.4, R2.5]
- [x] 3.4 `frontend/features/cleaning/components/detail/manager-cleaning-task-messages-panel.test.tsx` (nuevo) — equivalente al test de 2.4 con los selectores `manager-cleaning-*` y la mutación `useSendCleanerTaskMessage`. [R2.2, R2.3, R2.4, R2.5]
- [x] 3.5 `frontend/features/cleaning/components/detail/cleaning-task-detail-view.tsx` — refactor estructural D6: la rama `state.kind === "ok"` pasa a renderizar `ManagerCleaningTaskTabs` con `content` siendo el mismo conjunto de bloques que hoy (`DetailHeaderBlock`, `DetailIdentifyingBlock`, `DetailAssignedCleanerBlock`, `DetailContextLinksBlock`, `DetailManagerActionsBlock`, más la cancelación), en el mismo orden y con las mismas props. La región viva `role="status"` con `pickAnnouncementSource` y el `setCancelOpen` se quedan en el wrapper (NO se mueven a las pestañas ni al panel de mensajes) — D6 lo fija y un panel de revisión de arquitectura del 2026-09-18 ya midió esa separación en `cleaning-manager-task-detail` D7. Añadir sticky `messagesNotFound: boolean` + `onMessagesNotFound` callback estable (`useCallback`) para colapsar la pantalla completa a `EmptyState` cuando el panel de mensajes recibe 404 (mismo convenio que `ManagerIncidentDetailView` 2.5). [R2.1, R2.2, R2.3, R2.4, R2.5, R3.2]
- [x] 3.6 `frontend/features/cleaning/index.ts` — añadir `export { ManagerCleaningTaskTabs } from "./components/detail/manager-cleaning-task-tabs"` y `export { ManagerCleaningTaskMessagesPanel } from "./components/detail/manager-cleaning-task-messages-panel"` (simetría con 2.6). [R2.1]

## 4. Verification

- [ ] 4.1 Full test suite verde: `docker compose exec backend uv run pytest` (backend) y `cd frontend && npm test` (frontend), con el stack del worktree levantado. La cifra del suite de frontend se compara contra la salida del propio comando de esta pasada, no contra un número escrito en la prosa de `sdd/project.md` (gate `rebuild-prose-counts-dont-increment`). [R1, R2, R3, R4, R5]
- [ ] 4.2 Pyright verde: `cd backend && uv sync --frozen && uv run pyright .` (project.md, "Backend static tooling"). [R1, R2, R3, R4, R5]
- [x] 4.3 Lint frontend: `cd frontend && npm run lint` (si existe; si no, comprobar `package.json` por el script equivalente y adaptarlo aquí). [R1, R2, R3, R4, R5]
- [x] 4.4 `make check-rule11-ownership` verde desde el host (regla 11 del proyecto, prose de `sdd/` y docstrings de `backend/app/**`). [R1, R2, R3, R4, R5]
- [x] 4.5 `make check-compose-ports` y `make compose-stacks` para confirmar que la suite de contenedores no ha dejado huérfanos durante la verificación (project.md, "Stacks huérfanos"). [R1, R2, R3, R4, R5]
- [ ] 4.6 Inspección manual en navegador de los dos flujos: `/incidents/[id]` con un id existente del tenant, abrir la pestaña "Mensajes", enviar un mensaje, ver que aparece al final; `/cleaning/[id]` con un id existente, mismo flujo. Si la libre disponibilidad de puertos lo impide (worktree enlazado no publica ninguno), `make up PORT_OFFSET=<n>` y registrar el desplazamiento usado en el commit del PR. <!-- manual -->

## Implementation Notes

<!-- Append-only, written by the implementer of each section for the next one:
     decisions taken, names chosen, gotchas found. One bullet each, no prose. -->
- incidents.json: new `tabs` + `messages` placed after `manager` section (manager view had no tabs/messages section before).
- cleaning.json: new `tabs` + `messages` placed after `detail` section (cleaning view had no tabs/messages section before).
- manager `messages.roles` order follows tech.json convention (TENANT_OWNER, PROPERTY_MANAGER, TECHNICIAN/CLEANER) without SUPER_ADMIN.
- `messages.error` includes `title` (tech.json has it; cleaner.json only has description) — task spec requires both, manager view has both.
- empty.description and composer.placeholder are written from the manager's perspective (manager reads the thread, writes to technician/cleaner).
- section 2 (incidents wrapper): all new DOM IDs use the `manager-incident-` prefix; tab order `["content","messages"]` and the sticky `hasOpenedMessagesTab` are a direct copy of the tech tabs pattern.
- section 2: `ManagerIncidentMessagesPanel` keeps `MAX_CONTENT = 2000` local and uses `mapIncidentsError(query).kind` for the 404 propagation (panel-local EmptyState only as standalone fallback when no `onNotFound` is wired).
- section 2: wrapper (`ManagerIncidentDetailView`) composes the same six blocks from `incident-detail-sections.tsx` and does NOT import `IncidentDetailView` (D7); state machine lives in the wrapper.
- section 2: `messagesNotFound` collapses the whole screen to the `/incidents` EmptyState via a stable `useCallback` `onMessagesNotFound`; `useLayoutEffect` in the panel calls it before paint, no frame flash.
- section 2: wrapper test (`manager-incident-detail-view.test.tsx`) added alongside the panel/tabs tests since the verification command listed it; not a strict 2.5 deliverable but it locks the wiring (state machine + tab activation + 404 propagation).
- section 2: panel-level `ErrorState` mounts now wire `onRetry={() => { void query.refetch(); }}` + `retryLabel` from `states:error.retry` (sdd-review-ui-ux round 1); textarea's `aria-describedby` adds `manager-incident-message-error` when a visible error is on, and toggles `aria-invalid` to match (same finding, single shared id on the mutually-exclusive error span).
- section 3: new `frontend/features/cleaning/lib/messages-error.ts` exports `mapCleaningError(query)` (status → kind, mirrors `mapIncidentsError`) and `sendErrorKeyForCleaning(kind)` (the `validation`/`not-found`/`forbidden`/generic branches the composer copy reads from). Exposed from `frontend/features/cleaning/index.ts` for symmetry with the incidents mapper.
- section 3: `ManagerCleaningTaskMessagesPanel` reuses `useCleanerTaskMessages` / `useSendCleanerTaskMessage` from `@/features/cleaner/hooks/use-cleaner-task-messages` (the only frontend export of the cleaning-thread hooks — design D5 already rejected cloning them into the cleaning feature); the import path crosses feature boundaries but matches what `CleanerTaskMessagesPanel` already does.
- section 3: `CleaningTaskDetailView` keeps the `role="status"` live region and the `setCancelOpen` state outside the tabs (D6 separation measured in `cleaning-manager-task-detail` D7); `messagesNotFound: boolean` + `onMessagesNotFound` `useCallback` collapse the whole screen to the `/cleaning` EmptyState when the panel's `useLayoutEffect` fires — same convention `ManagerIncidentDetailView` uses.
- section 3: invalidation gate deferred — `npx vitest run` from the host needs `frontend_node_modules` (not installed) and `docker compose exec -T frontend npx vitest run …` needs `.env` (missing in the worktree); recorded as `deferred` per the run skill, not a failure.
- review fixes round 1 (section 3, R3.2): `cleaning-task-detail-view.test.tsx`'s `@/lib/auth` mock gained `useAuth` (tenant `"tenant-1"`) plus a `getCleanerDataSource` spy (empty resolved page), matching `manager-incident-detail-view.test.tsx`'s pattern, so the now-always-mounted `ManagerCleaningTaskMessagesPanel`'s `useCleanerTaskMessages` → `useAuth` call no longer throws in the wrapper's own 12 pre-existing tests.
- verification 4.3 (`npm run lint`): `manager-cleaning-task-messages-panel.tsx` imported `useCleanerTaskMessages`/`useSendCleanerTaskMessage`/`CleaningTaskMessage`/`PaginatedResponse` from `@/features/cleaner/hooks/use-cleaner-task-messages` and `@/features/cleaner/data` — a cross-feature internal-path import, which the project's `no-restricted-imports` eslint rule forbids for production code (design D2; test files are exempt, which is why the panel's own test never tripped it). Fixed by adding those four exports to `frontend/features/cleaner/index.ts` (the feature's public entry point, mirrors why `CleanerTaskDetailView` already lives there) and importing from `@/features/cleaner` instead. `useIncidentMessages`/`useSendIncidentMessage` in section 2 never had this problem because `ManagerIncidentDetailView` lives inside `features/incidents/` itself — same-feature import, not cross-feature. Re-ran `npm run lint` (clean) and `features/cleaning/ features/cleaner/` (701/701 pass) after the fix.
- review fixes round 1 (sdd-review-ui-ux, R2.5): `cleaning-task-detail-view.test.tsx` gained a nested `describe("CleaningTaskDetailView — tabs wiring")` block (mirroring `manager-incident-detail-view.test.tsx:234-300`) covering tab defaults + no eager fetch, page-1 fetch + thread render on tab open, hidden-not-unmounted for the content panel, and the messages-404-replaces-whole-screen case via `getCleanerDataSource`'s `getTaskMessagesMock`; 12→16 tests, all green.
- verification 4.1/4.2: pyright reports 963 pre-existing errors in `backend/tests/timeline/*` (test-fixture typing, e.g. `test_event_factory.py`/`test_filters.py` passing a `datetime` where `TimelineEventType`/`TimelineSeverity` is expected) — this change touches zero backend files, so these predate it and are out of scope (scope discipline: implement only what tasks describe). Backend pytest (`uv run pytest`) is green: 11180 passed, 44 skipped. Frontend `npm test` full suite: 3748/3749 pass; the 1 failing suite (`features/provenance/workflow-contract.test.ts`) and 1 failing test (`lib/config/build-identity-contract.test.ts`) are both the pre-existing worktree ENOENT gap `sdd/project.md` documents (the frontend container only mounts `./frontend`, not `/backend`/`/.github`) — `docker compose cp` workaround denied by the auto-mode permission classifier ("Modify Shared Resources"); recorded as `deferred` in `BLOCKED.md`, not a regression of this change.
- review fixes round 2 (sdd-qa, R3.3): both detail-view tests gained a `TENANT_OWNER parity` describe — the mocked `useAuth` role became a `vi.hoisted` knob (`session.role`, reset to `PROPERTY_MANAGER` in `beforeEach`) and `useHasPermission` is now driven by the real `ROLE_UI_PERMISSIONS` table imported from `@/lib/auth/permissions`, so the owner reaches the screen with `MANAGE_INCIDENTS`/`MANAGE_CLEANING_TASKS` genuinely denied; the messages tabpanel's `innerHTML` is captured for both roles and compared byte for byte (identical), plus an explicit owner-side test (tab selected, page-1 fetch, thread row, composer enabled) while the actions block stays hidden. No behavioural divergence found: neither wrapper nor panel reads `user.role`, so R3.3 holds as written.
- review fixes round 2 (sdd-qa, R1.3/R2.3): both messages-panel tests gained the `validate()` boundary at exactly 2000 (trimmed, padded with spaces → send enabled, no `tooLong`, backend called with the trimmed 2000-char body) and 2001 (send disabled, `messages.errors.tooLong` shown, backend never called), asserting the textarea really holds the over-cap value first — jsdom's `fireEvent.change` does not apply `maxlength`, so the assertion exercises the component's own guard rather than the DOM's. Mutation-checked: flipping `>` to `>=` in `manager-incident-messages-panel.tsx`'s `validate()` fails the 2000 case (reverted).
