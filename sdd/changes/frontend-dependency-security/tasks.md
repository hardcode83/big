# Tasks: frontend-dependency-security

## 1. Baseline y selección mínima

- [x] 1.1 Reejecutar desde `frontend/` `npm audit --json` y `npm audit --omit=dev --json`; actualizar la tabla de `sdd/changes/frontend-dependency-security/design.md` con fecha, advisories, severidad, rango vulnerable, ruta directa/transitiva y exposición de producción/desarrollo observados antes de modificar el árbol. [R1, R3]
- [x] 1.2 Inspeccionar `frontend/package.json`, `frontend/package-lock.json` y los manifests publicados de cada padre para identificar la primera versión corregida compatible con Node 22; registrar en `sdd/changes/frontend-dependency-security/design.md` la versión inicial, candidata final y motivo de Next, PostCSS, Sharp y las dos ramas de brace-expansion. [R1, R2]
- [x] 1.3 Simular la actualización mínima de Next sin overrides y documentar en `sdd/changes/frontend-dependency-security/design.md` qué transitivas siguen vulnerables; decidir cada override por la resolución natural observada, sin usar `npm audit fix --force` ni actualizar paquetes no afectados. [R1, R2, R3]

## 2. Dependencia directa: Next.js

- [x] 2.1 Actualizar en `frontend/package.json` únicamente el mínimo de `next` a la primera versión corregida compatible y regenerar `frontend/package-lock.json` mediante un comando npm explícito; mantener `eslint-config-next` y el resto de dependencias sin cambios salvo incompatibilidad demostrada. Registrar versión inicial, final y motivo en `sdd/changes/frontend-dependency-security/design.md`. [R1, R2]
- [x] 2.2 Verificar el checkpoint de Next bajo Node 22 con instalación limpia, `npm test`, `npm run lint`, `npm run typecheck` y `npm run build`, más arranque de producción y smoke HTTP de `/dashboard`; si aparece una regresión observable de SSR, routing, build o renderizado, revertir solo la actualización de Next y documentar la incidencia en `sdd/changes/frontend-dependency-security/design.md` antes de continuar. [R2, R3, R4, R5]

## 3. Dependencias transitivas

- [x] 3.1 Si el árbol posterior a Next aún resuelve PostCSS vulnerable, añadir en `frontend/package.json` un override acotado a su padre con la primera versión corregida compatible, regenerar `frontend/package-lock.json`, registrar versión inicial/final/motivo y ejecutar bajo Node 22 `npm ci`, tests, lint, typecheck y build; si el árbol ya es seguro, no añadir el override y registrar la evidencia. Ante regresión, revertir solo este checkpoint y documentarla en el Design. Antes del siguiente checkpoint, ejecutar `npm ls postcss` como evidencia documental de la versión resuelta; esta inspección no sustituye a `npm audit`. [R1, R2, R3, R4]
- [x] 3.2 Si el árbol posterior a Next aún resuelve Sharp vulnerable, añadir en `frontend/package.json` un override acotado a su padre con la primera versión corregida compatible, regenerar `frontend/package-lock.json` y registrar versión inicial/final/motivo y cambios de binarios/libvips en `sdd/changes/frontend-dependency-security/design.md`. Validar bajo Node 22 `npm ci`, tests, lint, typecheck, build, salida standalone y smoke HTTP; ante regresión, revertir solo este checkpoint y documentarla antes de continuar. Antes del siguiente checkpoint, ejecutar `npm ls sharp` como evidencia documental de la versión resuelta; esta inspección no sustituye a `npm audit`. [R1, R2, R3, R4, R5]
- [x] 3.3 Para cada rama vulnerable `minimatch` → `brace-expansion`, añadir en `frontend/package.json` solo el override acotado que siga siendo necesario con la primera versión corregida admitida por ese padre, regenerar `frontend/package-lock.json` y registrar versión inicial/final/motivo. Ejecutar bajo Node 22 `npm ci`, tests, lint, typecheck y build; ante regresión, revertir únicamente la rama causante y documentarla en el Design. Antes del siguiente checkpoint, ejecutar `npm ls minimatch brace-expansion` como evidencia documental de las ramas y versiones resueltas; esta inspección no sustituye a `npm audit`. [R1, R2, R3, R4]
- [x] 3.4 Revisar el diff de `frontend/package.json` y `frontend/package-lock.json` tras cada checkpoint y eliminar cualquier churn no atribuible a Next o a una ruta vulnerable; confirmar que PostCSS, Sharp y brace-expansion siguen siendo transitivas y que no se añadieron como dependencias directas. Si algún override deja de ser necesario porque el árbol ya resuelve versiones seguras de forma natural, eliminarlo antes de la verificación final y documentar su retirada en `sdd/changes/frontend-dependency-security/design.md`. [R1, R2, R4]

## 4. Verificación final y evidencia

- [x] 4.1 Ejecutar la suite completa desde una copia limpia bajo Node 22: `npm ci`, `npm test`, `npm run lint`, `npm run typecheck` y `npm run build`; todos los comandos deben terminar en verde sin modificar el lockfile. [R2, R4, R5]
- [x] 4.2 Tras `npm ci`, ejecutar `npm ls next postcss sharp brace-expansion` y registrar en `sdd/changes/frontend-dependency-security/design.md` las versiones y rutas realmente resueltas como evidencia documental; no tratar esta inspección como gate funcional ni como sustituto de los audits. [R1, R3]
- [x] 4.3 Ejecutar desde `frontend/` los audits posteriores `npm audit --json` y `npm audit --omit=dev --json`; demostrar la eliminación de todas las vulnerabilidades corregibles del baseline y documentar en `sdd/changes/frontend-dependency-security/design.md` cualquier advisory nuevo o exposición residual con severidad, ruta, motivo y condición de revisión. [R1, R3]
- [x] 4.4 Construir y arrancar la imagen de producción definida por `frontend/devops/Dockerfile`, comprobar por HTTP que `/dashboard` conserva SSR, routing y renderizado básico, y detener la verificación si se observa un cambio funcional atribuible a Next o Sharp. [R2, R4, R5]
- [x] 4.5 Confirmar mediante el diff final que no cambiaron `frontend/app/**`, `frontend/features/**`, `frontend/components/**`, `frontend/lib/**`, tests, mocks, fixtures, `backend/openapi.json` ni `.github/workflows/**`; el cambio de implementación debe quedar limitado a `frontend/package.json`, `frontend/package-lock.json` y la evidencia SDD. [R5]
