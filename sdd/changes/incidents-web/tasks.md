# Tasks: incidents-web

> Implementación de la pantalla `/incidents` (lista + detalle, sólo lectura), según `proposal.md` y `design.md`. Tamaño `S`; las tareas se ordenan para que el sistema siga compilando y pasando tests tras cada sección.
>
> **Nota sobre `retryPolicy` (mismo movimiento que `reservations-web`):** `frontend/lib/api/retry-policy.ts` ya está en `main` (archivado en `guest-portal-web`, precedent: `frontend/features/dashboard/hooks/use-dashboard-data.ts:6` y `frontend/features/guest-portal/hooks/use-checkin.ts:5`). Esta entrada **reutiliza** la política compartida y **no** crea copia local en `frontend/features/incidents/hooks/`. La cobertura detallada de la política (404, 422, 500, red) vive en `frontend/features/dashboard/hooks/use-dashboard-data.test.tsx`; aquí sólo se verifica el wiring.

## 1. Shell: ruta de detalle en el registro de navegación

- [ ] 1.1 Añadir el descriptor `incident-detail` en `frontend/features/shell/navigation/route-registry.ts`, replicando `property-detail` (línea 114-122) y `reservation-detail` (línea 137-145): `pattern: "/incidents/[id]"`, `match: "exact"`, **sin** `href` ni `navigationGroup` (D6), `breadcrumbKeys: crumbs("incidents", "incident-detail")`. El icono y el `profile: "workspace"` son opcionales —copiar el precedent— y la asimetría a evitar es no pintar la ruta de detalle en la barra lateral. [R1.2, D6]
- [ ] 1.2 Añadir `routes.incident-detail.title` y `routes.incident-detail.description` en `frontend/locales/es/navigation.json` y `frontend/locales/en/navigation.json`, junto a `routes.incidents`. [R1.3]
- [ ] 1.3 Extender la lista `PRD_24_SURFACES` de `frontend/features/shell/navigation/route-registry.test.ts` con `/incidents/[id]`, igual que ya contiene `/properties/[id]` y `/reservations/[id]`. El test "covers exactly..." compara `routeRegistry.map(r => r.pattern).sort()` con la lista: si el descriptor entra al registro y la lista no se actualiza, el test falla en rojo por construcción. [R1.5, D6]
- [ ] 1.4 Correr `cd frontend && npm test -- --run features/shell/navigation` y verificar que `route-registry.test.ts`, `route-metadata.test.ts` y `breadcrumbs.test.ts` pasan en verde. [R1.2, R1.3, R1.5]

## 2. Locale nuevo `incidents.json`

- [ ] 2.1 Crear `frontend/locales/es/incidents.json` con cinco secciones. **`status`** (las 9 etiquetas del enum `IncidentStatus` declarado en `openapi.d.ts:1823`: `OPEN`, `CLASSIFIED`, `AWAITING_OWNER_APPROVAL`, `ASSIGNED`, `ACCEPTED`, `IN_PROGRESS`, `WAITING_EXTERNAL_PARTS`, `RESOLVED`, `CANCELLED`). **`severity`** (las 4 de `IncidentSeverity:1811`: `LOW`, `MEDIUM`, `HIGH`, `CRITICAL`). **`category`** (las **13** de `IncidentCategory:1708`: `ACCESS`, `LOCK`, `WIFI`, `ELECTRICITY`, `WATER`, `PLUMBING`, `HVAC`, `APPLIANCE`, `NOISE`, `CLEANING`, `DAMAGE`, `SAFETY`, `OTHER`). **`source`** (las **6** de `IncidentSource:1818`: `GUEST`, `CLEANER`, `OWNER`, `SYSTEM`, `PMS`, `LOCK_ALERT`). **`fields`** (etiquetas de las **6 columnas** de la lista —D5—: `severity: "Severidad"`, `status: "Estado"`, `title: "Título"`, `category: "Categoría"`, `source: "Origen"`, `createdAt: "Creada"`; etiquetas del detalle: `id: "Identificador"`, `property: "Propiedad"`, `reservation: "Reserva"`, `assignedTechnician: "Técnico asignado"`, `assignedTechnicianNote: "El identificador no se puede resolver a nombre dentro de esta vista"`, `aiSummary: "Resumen del clasificador"`, `description: "Descripción"`, `estimatedCost: "Coste estimado"`, `approvedCost: "Coste aprobado"`, `finalCost: "Coste final"`, `resolvedAt: "Resuelta el"`, `createdAt: "Creada el"`, `updatedAt: "Actualizada el"`, `ownerApprovalRequired: "Requiere aprobación de la propietaria"`, `notFound: "Incidencia no encontrada"`, `backToList: "Volver al listado"`, `clearFilters: "Limpiar filtros"`, `prevPage: "Página anterior"`, `nextPage: "Página siguiente"`, `forbidden: "No tienes permiso para ver esta incidencia"`, `validation: "Los datos enviados no son válidos"`). [R4.1, R4.2, R4.3, R4.4, R4.6, R5.2, R5.4, D2]
- [ ] 2.2 Crear `frontend/locales/en/incidents.json` con la misma estructura y las etiquetas en inglés. **`status`**: `OPEN: "Open"`, `CLASSIFIED: "Classified"`, `AWAITING_OWNER_APPROVAL: "Awaiting owner approval"`, `ASSIGNED: "Assigned"`, `ACCEPTED: "Accepted"`, `IN_PROGRESS: "In progress"`, `WAITING_EXTERNAL_PARTS: "Waiting for parts"`, `RESOLVED: "Resolved"`, `CANCELLED: "Cancelled"`. **`severity`**: `LOW: "Low"`, `MEDIUM: "Medium"`, `HIGH: "High"`, `CRITICAL: "Critical"`. **`category`** (las 13 con traducción natural: `ACCESS: "Access"`, `LOCK: "Lock"`, `WIFI: "WiFi"`, `ELECTRICITY: "Electricity"`, `WATER: "Water"`, `PLUMBING: "Plumbing"`, `HVAC: "HVAC"`, `APPLIANCE: "Appliance"`, `NOISE: "Noise"`, `CLEANING: "Cleaning"`, `DAMAGE: "Damage"`, `SAFETY: "Safety"`, `OTHER: "Other"`). **`source`**: `GUEST: "Guest"`, `CLEANER: "Cleaner"`, `OWNER: "Owner"`, `SYSTEM: "System"`, `PMS: "PMS"`, `LOCK_ALERT: "Lock alert"`. **`fields`** (traducciones naturales: `severity: "Severity"`, `status: "Status"`, `title: "Title"`, `category: "Category"`, `source: "Source"`, `createdAt: "Created"`, `id: "Identifier"`, `property: "Property"`, `reservation: "Reservation"`, `assignedTechnician: "Assigned technician"`, `assignedTechnicianNote: "The identifier cannot be resolved to a name in this view"`, `aiSummary: "Classifier summary"`, `description: "Description"`, `estimatedCost: "Estimated cost"`, `approvedCost: "Approved cost"`, `finalCost: "Final cost"`, `resolvedAt: "Resolved at"`, `createdAt: "Created at"`, `updatedAt: "Updated at"`, `ownerApprovalRequired: "Owner approval required"`, `notFound: "Incident not found"`, `backToList: "Back to list"`, `clearFilters: "Clear filters"`, `prevPage: "Previous page"`, `nextPage: "Next page"`, `forbidden: "You don't have permission to view this incident"`, `validation: "The submitted data is not valid"`). [R4.1, R4.2, R4.3, R4.4, R4.6, R5.2, R5.4, D2]
- [ ] 2.3 Registrar `esIncidents` / `enIncidents` en `frontend/lib/i18n/resources.ts` junto a `esReservations` / `enReservations` (líneas 13-14): `import esIncidents from "@/locales/es/incidents.json"; import enIncidents from "@/locales/en/incidents.json";` y añadirlos al diccionario que `initI18next` consume. Sin este paso los locales son ficheros huérfanos que `t("incidents:...")` no resuelve. [R4.5]
- [ ] 2.4 Verificar que `cd frontend && npm test -- --run` pasa en verde (los locales nuevos no deben romper ningún test que enumere claves i18n). [R4.5]

## 3. Data layer: DTOs y composición

- [ ] 3.1 Crear `frontend/features/incidents/data/dto.ts` exportando: `IncidentStatus` / `IncidentSeverity` / `IncidentCategory` / `IncidentSource` (re-exports de `components["schemas"]["IncidentStatus"]` etc., `openapi.d.ts:1705-1825`); `IncidentSummaryDto` (`{ id: string; status: IncidentStatus; severity: IncidentSeverity; category: IncidentCategory; source: IncidentSource; title: string; createdAt: string }` — **sin** `propertyId`, coherente con D5 que elimina la columna de la lista); `IncidentDetailDto` (los **18** campos de `IncidentResponse` en camelCase: `id`, `propertyId`, `reservationId`, `source`, `category`, `severity`, `status`, `title`, `description`, `aiSummary`, `assignedTechnicianId`, `ownerApprovalRequired`, `estimatedCost: string | null`, `approvedCost: string | null`, `finalCost: string | null`, `resolvedAt: string | null`, `createdAt`, `updatedAt`); `IncidentList` (`{ items: IncidentSummaryDto[]; total: number; page: number; perPage: number }` — el sobre del backend renombrado a camelCase, **sin** `totalPages`, por D3); `IncidentFilters` (`{ status?: IncidentStatus; severity?: IncidentSeverity; page?: number; perPage?: number }` — **sin** `propertyId`, por D4); `IncidentPagination` (`{ page: number; perPage: number; total: number; lastPage: number }` — tipo derivado en el cliente, NO viene del backend, con `lastPage = max(1, ceil(total / perPage))` y `total = 0 → lastPage = 1`, por R2.5). [R2.3, R2.6, R3.2, R5.3, D3, D5]
- [ ] 3.2 Crear `frontend/features/incidents/data/index.ts` con la composición del source: invoca `createAuthenticatedClients` con `apiBaseUrl: ""` y `onSessionExpired: notifySessionExpired` (mismo patrón que `frontend/features/reservations/data/index.ts`), instancia un singleton `HttpIncidentsSource` con el `apiClient` resultante, y exporta `getIncidentsDataSource()` y los tipos del `dto.ts`. Sin seam `IncidentsDataSource` (D1). [R2.1, R5.3, D1]

## 4. HTTP source + tests del mapper

- [ ] 4.1 Crear `frontend/features/incidents/data/http/http-incidents-source.ts` con la clase `HttpIncidentsSource` (recibe `ApiClient` por constructor) y los mappers `mapIncidentSummary`, `mapIncidentDetail`. **Listado (`listIncidents`)** llama a `client.request("/api/v1/incidents", { query })` donde `query` se construye **exclusivamente** con los nombres admitidos en v1 (D4): `status`, `severity`, `page`, `per_page` —derivados uno a uno de `filters.status`, `filters.severity`, `filters.page`, `filters.perPage`—. Si una clave de `filters` viene `undefined`, se omite del objeto `query`. **El nombre `property_id` no aparece en el objeto query bajo ninguna rama** —ni en una entrada, ni en una salida, ni en una rama condicional—. **Detalle (`getIncident`)** llama a `client.request("/api/v1/incidents/{incident_id}", { pathParams: { incident_id: id } })`. Cada mapper enumera campos explícitamente (D3) y devuelve un objeto UI `camelCase`. El método de listado mapea cada elemento con `mapIncidentSummary` y devuelve el sobre `IncidentList` con las claves renombradas a camelCase (sin `totalPages`). El método de detalle mapea con `mapIncidentDetail` y devuelve `IncidentDetailDto`. [R2.1, R2.3, R3.1, R3.2, R5.3, D1, D3, D4]
- [ ] 4.2 Crear `frontend/features/incidents/data/http/http-incidents-source.test.ts` con tests unitarios del source mockeando `ApiClient.request` con `vi.fn().mockResolvedValue(...)`, replicando el patrón de `http-reservations-source.test.ts`: (a) `listIncidents` mapea correctamente `IncidentPageResponse` con `created_at`/`ai_summary` snake_case al sobre UI `IncidentList` camelCase, preservando el sobre `{items, total, page, per_page}` (sin `total_pages`); (b) **verificación exacta de la query v1**: se llama a `listIncidents` con un objeto `IncidentFilters` válido (tipado) —`{ status: "OPEN", severity: "HIGH", page: 2, perPage: 20 }`— y se hace `expect(request).toHaveBeenCalledWith("/api/v1/incidents", { query: { status: "OPEN", severity: "HIGH", page: 2, per_page: 20 } })` —comparación de objeto completa, sin `expect.objectContaining` ni `toMatchObject`, para que cualquier clave extra (incluida `property_id`) haga fallar el test en rojo—; (c) **verificación de ausencia de `property_id`**: el mismo test (b) afirma `expect(request.mock.calls[0][1].query).not.toHaveProperty("property_id")`; un cuarto test independiente llama a `listIncidents` con un objeto que **sólo** contiene `severity` y verifica que la query emitida contiene `severity: <valor>` y nada más (cero claves, no `toEqual({})` con propiedades extra); (d) `listIncidents` con `filters: {}` produce `query: {}` (no `page: 1` ni `per_page: 20` por defecto — los defaults los pone el backend, ver `openapi.d.ts:4925-4935`); (e) `getIncident` mapea `IncidentResponse` con los 18 campos; (f) `getIncident` con `description` que contiene `<script>alert(1)</script>` lo deja tal cual en el DTO (el mapper no transforma `description`); (g) `getIncident` pasa el `id` por `pathParams: { incident_id }`. [R2.1, R2.3, R2.6, R3.1, R3.2, R5.3, D1, D3, D4]
- [ ] 4.3 Correr `cd frontend && npm test -- --run features/incidents/data` y verificar verde. [R2.1, R3.1]

## 5. Hooks: query keys + useIncidents/useIncident + retry policy compartida

- [ ] 5.1 Reutilizar `retryPolicy` desde `frontend/lib/api/retry-policy.ts` (verificado en `main`: exporta `retryPolicy(failureCount, error): boolean`; no reintenta `ApiError` 4xx, reintenta hasta 2 veces para 5xx/red). **No** se crea ningún fichero local en `frontend/features/incidents/hooks/`; el import en 5.3 se hace desde la ruta directa `@/lib/api/retry-policy`, replicando el precedent de `frontend/features/dashboard/hooks/use-dashboard-data.ts:6` y `frontend/features/guest-portal/hooks/use-checkin.ts:5` — `frontend/lib/api/index.ts` **no** re-exporta `retryPolicy` y no se modifica. [R5.1]
- [ ] 5.2 Crear `frontend/features/incidents/hooks/query-keys.ts` exportando `incidentsKeys` con la misma forma que `reservationsKeys` (`frontend/features/reservations/hooks/query-keys.ts:18-21`): `list(tenantId, filters)` usa `tenantScopedKey(tenantId, "incidents-list", filters)`; `detail(tenantId, incidentId)` usa `tenantScopedKey(tenantId, "incidents-detail", incidentId)`. `filters` se pasa como **objeto normalizado** (claves en orden estable: `status`, `severity`, `page`, `perPage`), **no** se serializa con `JSON.stringify`. El orden estable es lo que garantiza que dos renders con los mismos filtros produzcan la misma key y TanStack Query no duplique cache. [R2.7, R5.1, D4]
- [ ] 5.3 Crear `frontend/features/incidents/hooks/use-incidents.ts` con dos hooks. `useTenantId()` lanza si no hay sesión. `useIncidents(filters)` usa `incidentsKeys.list(tenantId, filters)` como `queryKey` y llama `getIncidentsDataSource().listIncidents(tenantId, filters)`. `useIncident(incidentId)` usa `incidentsKeys.detail(tenantId, incidentId)` y llama `getIncidentsDataSource().getIncident(tenantId, incidentId)`. Ambos pasan `retry: retryPolicy`, importado desde `@/lib/api/retry-policy` (ver 5.1) — no se crea copia local. [R2.1, R3.1, R5.1, R5.4]
- [ ] 5.4 Crear `frontend/features/incidents/hooks/use-incidents.test.tsx` con un `QueryClient` de test, mockeando `getIncidentsDataSource()` con `vi.mock(...)` sobre `../data` (precedent: tests de hooks del dashboard y de reservations). Cubre: (a) `useIncidents` llama al source con los filtros recibidos; (b) `useIncident(id)` llama al source con el id; (c) las keys de cache incluyen el `tenantId` y los filtros (verificable inspeccionando `queryClient.getQueryData` con la misma key); (d) **smoke test del wiring** — `useIncidents` y `useIncident` no reintentan ante un `ApiError status: 404`, confirmando que `retry: retryPolicy` está en su config; la cobertura detallada de la política vive en `frontend/features/dashboard/hooks/use-dashboard-data.test.tsx` y **no se duplica** aquí. [R2.1, R5.1, R5.4]
- [ ] 5.5 Correr `cd frontend && npm test -- --run features/incidents/hooks` y verificar verde. [R2.1, R3.1, R5.1]

## 6. Componente de filtros (lista)

- [ ] 6.1 Crear `frontend/features/incidents/components/list/incidents-filters.tsx` como componente controlado: recibe `value: IncidentFilters` y `onChange: (next: IncidentFilters) => void` por props. Renderiza dos campos de selección tipados: `status` (los 9 valores del enum `IncidentStatus` con etiqueta localizada `t("status.<KEY>")`) y `severity` (los 4 valores del enum `IncidentSeverity` con etiqueta localizada `t("severity.<KEY>")`), más un botón con texto `t("incidents:fields.clearFilters")` que resetea a `{}`. **No** renderiza campo de propiedad (D4, fuera de v1). La normalización del objeto `next` ordena las claves en el orden estable de D4 (`status`, `severity`, `page`, `perPage`) antes de llamar a `onChange`. Usa componentes shadcn/ui del precedent (`frontend/features/dashboard/components/...`). [R2.2, R2.6, R5.1, R5.2, D4]
- [ ] 6.2 Crear `frontend/features/incidents/components/list/incidents-filters.test.tsx` con Testing Library: (a) renderiza con `value={}` y muestra los campos vacíos; (b) al cambiar `status` llama `onChange` con `{ status: "OPEN" }` (sin `severity`/`page`/`perPage` si no estaban); (c) al cambiar `severity` llama `onChange` con `{ severity: "HIGH" }` (orden de claves estable); (d) al cambiar ambos, el objeto `next` lleva las dos claves en el orden `status`, `severity`; (e) al pulsar el botón "Limpiar filtros" llama `onChange({})`; (f) **ningún** string de UI (`"Limpiar"`, `"Estado"`, `"Severidad"`, `"Categoría"`, `"Origen"`) está hardcodeado: cada test lee el texto esperado desde `frontend/locales/es/incidents.json` y compara contra el DOM — si mañana se renombra la clave, los tests fallan en rojo en vez de seguir pasando; (g) **ningún** `property_id` aparece en el DOM (ni como filtro, ni como etiqueta, ni como placeholder). [R2.2, R2.6, R4.6, R5.1, R5.2, D4]
- [ ] 6.3 Correr `cd frontend && npm test -- --run features/incidents/components/list/incidents-filters` y verificar verde. [R2.2, R5.2]

## 7. Mapeo de errores HTTP (módulo compartido de feature)

- [ ] 7.1 Crear `frontend/features/incidents/lib/error-mapping.ts` exportando tipos discriminados y un mapeador puro `mapIncidentsError<TData, TError = Error>(queryResult)` (precedent: `frontend/features/reservations/lib/error-mapping.ts`): variante `loading` / `forbidden` / `not-found` / `validation` / `error` / `ok`. Reglas: (a) **`status: 401`**: **no** se renderiza estado localizado —se delega al mecanismo existente de expiración de sesión (`notifySessionExpired` ya invocado por `createAuthenticatedClients` en 3.2 y propagado al `auth-provider`); el mapeador devuelve `{ kind: "loading" }` mientras el flujo de refresh está en curso; (b) **`status: 403`**: `{ kind: "forbidden" }`; (c) **`status: 404`**: `{ kind: "not-found" }` (en la lista se considera `error` genérico —el endpoint de lista no debería producir 404, ver 8.1—; el `not-found` se usa en el detalle); (d) **`status: 422`**: `{ kind: "validation" }` **sin payload** —ni `error.message` ni `error.details` se leen, ni se exponen al componente, ni se mapean a nada—; la lista y el detalle mostrarán únicamente el texto localizado `t("incidents:fields.validation")` (R5.4); (e) **5xx o red** (`TypeError`/network): `{ kind: "error" }`; (f) **`ok`**: `{ kind: "ok"; data: ... }`. El módulo **no** contiene strings de UI —sólo la forma de los datos—; los textos se leen de `incidents:fields.*` y `states:*` en el componente. [R5.4]
- [ ] 7.2 Crear `frontend/features/incidents/lib/error-mapping.test.ts` con tests unitarios del mapeador (sin React): (a) `ApiError status: 401` no devuelve `forbidden`/`not-found`/`validation`/`error` y entra en la rama de "manejada fuera"; (b) `ApiError status: 403` → `forbidden`; (c) `ApiError status: 404` → `not-found`; (d) `ApiError status: 422` con `message: "cualquier cosa"`, `details: { status: "invalid" }` y `code: "validation_error"` → `validation`, **sin** exponer `message`/`details`/`code` en la variante devuelta (la variante es exactamente `{ kind: "validation" }`); (e) `ApiError status: 500` → `error`; (f) `TypeError` (red) → `error`; (g) resultado con `data` → `ok` con `data`. [R5.4]

## 8. Vista de lista + tests

- [ ] 8.1 Crear `frontend/features/incidents/components/list/incidents-view.tsx` como `'use client'`. Mantiene estado local de `filters: IncidentFilters` (inicial `{}`), llama `useIncidents(filters)`, pasa el resultado por `mapIncidentsError` (de 7.1), y renderiza según la variante: header con título localizado (`navigation:routes.incidents.title`); `<IncidentsFilters value={filters} onChange={...} />`; tabla con las **6 columnas** del D5 (`severity`, `status`, `title`, `category`, `source`, `createdAt`) en ese orden; estados localizados: `loading` → `states:loading.label`; `forbidden` → `incidents:fields.forbidden`; `validation` → `incidents:fields.validation`; `error` → `states:error.title` + `states:error.description` + botón "Reintentar" con `states:error.retry`; `ok` con `data.items.length === 0` → `states:empty.title` + `states:empty.description`. **El `not-found` (404) no se renderiza en la lista** —un 404 sobre un listado se considera `error` genérico, porque el endpoint de lista no debería producir 404; los tests verifican que en la lista no aparece el texto `t("incidents:fields.notFound")`. Paginación con dos botones etiquetados `t("incidents:fields.prevPage")` y `t("incidents:fields.nextPage")`, deshabilitados en los extremos según `lastPage = max(1, ceil(total / perPage))` (R2.5) — el `disabled` del botón "siguiente" usa `page >= lastPage`, y el `disabled` del botón "anterior" usa `page <= 1`; cuando `lastPage === 1` ambos quedan deshabilitados. La columna `severity` se pinta con color semántico (`LOW` gris, `MEDIUM` azul, `HIGH` ámbar, `CRITICAL` rojo — precedent `dashboard-web` §Property operational states). La columna `title` se trunca a los primeros 60 caracteres con `title` HTML nativo para texto completo. La columna `createdAt` se pinta en formato `YYYY-MM-DD HH:mm` localizado. Cada fila enlaza a `/incidents/${id}` con un `<Link>` del precedent (router del shell). [R2.1, R2.4, R2.5, R3.2, R3.4, R4.1, R4.2, R4.3, R4.6, R5.2, R5.4, D5]
- [ ] 8.2 Crear `frontend/features/incidents/components/list/incidents-view.test.tsx` con Testing Library, mockeando `@/lib/api` para que `useIncidents` devuelva un resultado controlado en cada test: (a) estado de carga muestra `states:loading.label` y no muestra tabla; (b) estado con datos renderiza una fila por `summary` con las **6 columnas** en el orden de D5 (severity, status, title, category, source, createdAt) y los textos vienen de `frontend/locales/es/incidents.json`; (c) estado con `items: []` muestra `states:empty.title`; (d) estado con `ApiError status: 500` muestra `states:error.title` y un botón "Reintentar"; (e) estado con `ApiError status: 403` muestra `t("incidents:fields.forbidden")`; (f) estado con `ApiError status: 422` cuyo `message: "cualquier cosa"`, `details: { status: "invalid" }` y `code: "validation_error"` muestra `t("incidents:fields.validation")` y **no** contiene las cadenas `"cualquier cosa"`, `"status"` (de details), `"validation_error"` ni ningún eco del payload; (g) estado con `ApiError status: 404` se renderiza como `error` genérico (no aparece `notFound`); (h) al cambiar filtro de `status` el hook recibe los nuevos filtros; (i) al cambiar filtro de `severity` el hook recibe los nuevos filtros; (j) **la columna de propiedad NO aparece en la tabla** — el test busca cualquier texto de etiqueta de propiedad (`t("incidents:fields.property")`, `propertyId`, etc.) y falla si lo encuentra; (k) los botones de paginación usan `t("incidents:fields.prevPage")` y `t("incidents:fields.nextPage")`; (l) el borde superior `page === lastPage` deshabilita el botón "siguiente" — caso del test con `total: 5, perPage: 20, page: 1 → lastPage = 1`, ambos botones deshabilitados; caso con `total: 100, perPage: 20, page: 5 → lastPage = 5`, sólo el botón "siguiente" deshabilitado; (m) cuando `total: 0` el botón "anterior" está deshabilitado y `lastPage = 1` se calcula correctamente. [R2.1, R2.2, R2.4, R2.5, R2.6, R3.4, R4.1, R4.2, R4.3, R5.2, R5.4, D4, D5]
- [ ] 8.3 Correr `cd frontend && npm test -- --run features/incidents/components/list features/incidents/lib` y verificar verde. [R2.1, R4.3, R5.2, R5.4]

## 9. Componente del detalle + tests

- [ ] 9.1 Crear `frontend/features/incidents/components/detail/incident-detail-sections.tsx` con sub-componentes puros. `<DetailHeader />` recibe `severity`, `status`, `category`, `source` y los pinta con etiqueta localizada, color semántico en `severity` (D5), y bloque destacado cuando `status === "AWAITING_OWNER_APPROVAL"` o `ownerApprovalRequired === true` (muestra `t("incidents:fields.ownerApprovalRequired")` pero **sin** botones de respuesta — la aprobación vive en `/approvals`, R5 del proposal). `<DetailIdentifyingBlock />` recibe `id`, `propertyId`, `reservationId` y los pinta con etiqueta localizada; el `propertyId` y `reservationId` se muestran como `propertyId` y `reservationId` UUIDs (sin nombre) — la asociación "qué propiedad" se limita a esto en esta vista, y un manager que necesite el nombre navega al detalle de la propiedad. `<DetailAssignedTechnicianBlock />` recibe `assignedTechnicianId`. **Si `assignedTechnicianId` es `null`**, no se renderiza. **Si es no `null`**, se renderiza **bajo una sección secundaria etiquetada** (no en la cabecera de identificación), con el UUID como texto plano y una nota localizada única `t("incidents:fields.assignedTechnicianNote")` que documenta la limitación (R3.6): sin tooltip de copia, sin botón "Copiar UUID", sin elemento de UI específico para su valor. `<DetailDescriptionBlock />` recibe `description`: lo renderiza con `whitespace-pre-wrap` y `max-w-prose`, **sin** `dangerouslySetInnerHTML` (D7, regla 11 de `steering/security.md`); si es `null` o vacío no se renderiza el bloque. `<DetailCostsBlock />` recibe `estimatedCost`, `approvedCost`, `finalCost` (todos `string | null`); los pinta como número decimal con dos cifras y separadores del locale activo (coma/punto decimal según `es`/`en`), **sin** código ni símbolo de moneda (R5.5: sin fuente de moneda en el scope — `IncidentResponse`, `TenantConfigResponse` y `TenantResponse` no exponen moneda, ver `openapi.d.ts:1758-1799, 2821-2843, 2849-2867`); los `null` se pintan como `—`. `<DetailMetadataBlock />` recibe `aiSummary`, `createdAt`, `updatedAt`, `resolvedAt`: muestra `aiSummary` si no es `null`, y los timestamps en formato `YYYY-MM-DD HH:mm` localizado; `resolvedAt` `null` se omite. Las claves de texto se leen de `incidents:fields.*` excepto donde se indique. [R3.1, R3.2, R3.3, R3.5, R3.6, R3.7, R3.8, R4.2, R4.3, R5.2, R5.5, D5, D7]
- [ ] 9.2 Crear `frontend/features/incidents/components/detail/incident-detail-view.tsx` como `'use client'`. Recibe `incidentId: string` por props, llama `useIncident(incidentId)`, pasa el resultado por `mapIncidentsError` (de 7.1), y renderiza según la variante: breadcrumb `« ${t("incidents:fields.backToList")} »` enlazando a `/incidents`; `loading` → `states:loading.label`; `forbidden` → `incidents:fields.forbidden`; `not-found` → `incidents:fields.notFound` con un enlace de vuelta al listado; `validation` → `incidents:fields.validation`; `error` → estado de error genérico localizado. Cuando hay datos (`ok`), compone los bloques de 9.1 en orden: `<DetailHeader />`, `<DetailIdentifyingBlock />`, `<DetailAssignedTechnicianBlock />` (cuando aplique), `<DetailDescriptionBlock />` (cuando aplique), `<DetailCostsBlock />`, `<DetailMetadataBlock />`. **No** muestra `reported_by_guest_token`, `reported_by_user_id` ni `ai_classification` —ni en la maqueta ni en un log— (R3.3). [R3.1, R3.2, R3.3, R3.4, R3.5, R3.6, R3.7, R3.8, R4.2, R5.2, R5.4, D7]
- [ ] 9.3 Crear `frontend/features/incidents/components/detail/incident-detail-view.test.tsx` con Testing Library: (a) estado de carga muestra `states:loading.label`; (b) con datos renderiza un bloque por sección y los textos localizados desde `frontend/locales/es/incidents.json`; (c) **`assignedTechnicianId: null`** NO renderiza el bloque de técnico asignado (el bloque es condicional); (d) **`assignedTechnicianId: "uuid-123"`** renderiza el bloque bajo una sección secundaria (no en la cabecera), con la nota localizada `t("incidents:fields.assignedTechnicianNote")`; el test verifica que la cadena "Técnico asignado" o equivalente aparece **después** del bloque de identificación, no antes — y que NO hay botón "Copiar" ni tooltip accesible de copia; (e) `description: "<script>alert(1)</script>\n\nLínea 2"` se renderiza como texto literal (no como `<script>`); `document.querySelector("script")` es `null`; el `\n` se respeta con `whitespace-pre-wrap`; (f) `description: null` no renderiza el bloque de descripción; (g) **`ownerApprovalRequired: true`** renderiza el bloque destacado con `t("incidents:fields.ownerApprovalRequired")` **sin** botones de respuesta (verifica que el DOM no contiene elementos `<button>` con texto "Aprobar", "Rechazar" o equivalentes); (h) `ApiError status: 404` muestra `t("incidents:fields.notFound")` y un enlace de vuelta; (i) `ApiError status: 403` muestra `t("incidents:fields.forbidden")`; (j) `ApiError status: 422` cuyo `message: "cualquier cosa"`, `details: { status: "invalid" }` y `code: "validation_error"` muestra `t("incidents:fields.validation")` y **no** contiene las cadenas `"cualquier cosa"`, `"validation_error"` ni ningún eco del payload; (k) `ApiError status: 500` muestra el estado de error genérico localizado; (l) los **tres campos de coste** (`estimatedCost`, `approvedCost`, `finalCost`) se renderizan como número decimal con dos cifras y los separadores del locale, **sin** símbolo ni código de moneda (`EUR`, `€`, `$`, etc.); los `null` se renderizan como `—`; (m) los textos esperados se leen de `frontend/locales/es/incidents.json` y no hay literales duplicados en los tests. [R3.1, R3.3, R3.4, R3.5, R3.6, R3.7, R3.8, R4.2, R5.2, R5.4, R5.5, D7]
- [ ] 9.4 Correr `cd frontend && npm test -- --run features/incidents/components/detail` y verificar verde. [R3.1, R3.5, R4.2, D7]

## 10. Barrel export y páginas del App Router

- [ ] 10.1 Crear `frontend/features/incidents/index.ts` como barrel: re-exporta los componentes de la lista y el detalle, los hooks, y los tipos del `dto.ts`. [R1.1, R3.1]
- [ ] 10.2 Reemplazar `frontend/app/(workspace)/incidents/page.tsx`: importar `IncidentsView` desde `@/features/incidents` y `routeMetadata` desde `@/features/shell`; `generateMetadata` sigue devolviendo `routeMetadata("incidents")`; el `default export` es `<IncidentsView />`. [R1.1, R1.3]
- [ ] 10.3 Crear `frontend/app/(workspace)/incidents/[id]/page.tsx` como server component: `generateMetadata` devuelve `routeMetadata("incident-detail")`; el `default export` recibe `params: Promise<{ id: string }>`, hace `await params`, y renderiza `<IncidentDetailView incidentId={id} />`. [R1.1, R1.4, R3.1]
- [ ] 10.4 Correr `cd frontend && npm test -- --run` y verificar que toda la suite pasa (los tests del shell, los nuevos de incidents, y los de App Router si los hay). [R1.1, R3.1, R5.5]

## 11. Verification

- [ ] 11.1 Suite completa del frontend: `cd frontend && npm test -- --run` — verde. [R5.5]
- [ ] 11.2 Typecheck: `cd frontend && npx tsc --noEmit` — verde. (Ya está cubierto por `frontend-ci`; el comando exacto de `project.md`.) [R2.6, R3.4, R5.1]
- [ ] 11.3 Lint: `cd frontend && npm run lint` — verde. [R5.5]
- [ ] 11.4 Comprobación manual contra el stack local (`make up` desde raíz; login con un usuario seed; navegar a `/incidents` y a `/incidents/<id>`). Cubrir: (a) la lista carga y muestra al menos una incidencia del seed de REDES11/PAJARITOS8 (las tres sembradas: WIFI LOW CLASSIFIED, ACCESS HIGH ASSIGNED, APPLIANCE MEDIUM CLASSIFIED); (b) el filtro por `status` y por `severity` funciona; (c) la paginación avanza y retrocede sin reusar la respuesta cacheada de otra página; (d) el detalle abre con todos los campos y muestra `description` como texto plano (incluso si contiene `<script>` o saltos de línea); (e) `ownerApprovalRequired: true` (caso de la ACCESS HIGH con `estimated_cost` por encima del umbral) muestra el bloque destacado **sin** botones de respuesta; (f) `assignedTechnicianId` aparece bajo una sección secundaria con la nota localizada; (g) los tres campos de coste se ven como número decimal con dos cifras, **sin** símbolo ni código de moneda; (h) un id inexistente muestra "Incidencia no encontrada" (caso del detalle, **no** de la lista); (i) las traducciones cambian al alternar ES/EN (verificar las 9 de `status`, las 4 de `severity`, las 13 de `category`, las 6 de `source`); (j) los botones de paginación y el de limpiar filtros muestran los textos localizados; (k) un error 403 muestra el estado de falta de permiso; (l) un 422 con `status: "invalid"` en `details` no filtra el nombre del parámetro al UI. [R1.1, R2.1, R2.2, R2.4, R2.5, R3.1, R3.2, R3.3, R3.5, R3.6, R3.7, R3.8, R4.1, R4.2, R4.3, R4.4, R4.6, R5.2, R5.4, R5.5]
- [ ] 11.5 `cd frontend && npm run api:check` (verificación del contrato) — verde. El change no regenera `openapi.d.ts` (el contrato ya lo incluye desde `maintenance`), así que el check debe pasar sin diff. [R5.3]
- [ ] 11.6 Grep de hardcodeos en el código nuevo: `grep -RnE '"(Limpiar|Página anterior|Página siguiente|Reintentar|Sin resultados|Incidencia no encontrada|Volver al listado|No tienes permiso|Los datos enviados|Técnico asignado|Copiar)"' frontend/features/incidents frontend/app/\(workspace\)/incidents` — debe devolver cero coincidencias. Si devuelve alguna, se reemplaza por la clave i18n correspondiente en el mismo commit. [R5.2, R3.8]

## Cobertura de requirements (matriz de verificación)

| Req | Tareas que lo satisfacen |
|---|---|
| R1.1 (ruta `/incidents` muestra contenido) | 10.1, 10.2, 10.4 |
| R1.2 (descriptor `incident-detail` registrado) | 1.1, 1.3, 1.4, 10.3 |
| R1.3 (claves i18n del descriptor) | 1.2, 1.4, 10.2 |
| R1.4 (detalle navegable) | 10.3, 10.4 |
| R1.5 (`PRD_24_SURFACES` extendido) | 1.3, 1.4 |
| R2.1 (lista llama al endpoint) | 3.2, 4.1, 4.2, 4.3, 5.2, 5.3, 5.4, 5.5, 8.1, 8.2, 8.3, 11.4 |
| R2.2 (filtros v1: status + severity + page + per_page; sin property_id) | 4.1, 4.2 (caso b, c, d), 6.1, 6.2, 6.3, 8.2 (caso h, i, j), 11.4 |
| R2.3 (sobre `{items, total, page, per_page}`) | 3.1, 4.1, 4.2, 4.3 |
| R2.4 (estados localizados carga/error/vacío) | 8.1, 8.2, 8.3, 11.4 |
| R2.5 (paginación con `lastPage` derivado) | 3.1, 8.1, 8.2 (caso l, m), 11.4 |
| R2.6 (status/severity como enums tipados) | 3.1, 4.2, 5.2, 6.1, 6.2, 8.2, 11.2 |
| R2.7 (query key con objeto normalizado) | 5.2 |
| R3.1 (detalle llama y renderiza) | 4.1, 4.2, 4.3, 5.3, 5.4, 5.5, 9.1, 9.2, 9.3, 9.4, 10.3, 11.4 |
| R3.2 (18 campos del detalle) | 3.1, 4.1, 4.2, 9.1, 9.2, 9.3, 9.4, 11.4 |
| R3.3 (no PII: no `reported_by_*`, no `ai_classification`) | 9.1, 9.2, 9.3, 9.4, 11.2, 11.4 |
| R3.4 (404 distinguido en detalle, no en lista) | 7.1, 7.2, 8.1 (404 → error), 8.2 (caso g), 9.2, 9.3 (caso h), 11.4 (caso h) |
| R3.5 (`owner_approval_required` destacado, sin botones) | 9.1, 9.2, 9.3 (caso g), 11.4 (caso e) |
| R3.6 (`description` texto plano, una sola vez en detalle) | 4.2 (caso f), 9.1, 9.2, 9.3 (caso e), 11.4 (caso d) |
| R3.7 (costes formateados sin moneda, `null` como `—`) | 9.1, 9.2, 9.3 (caso l), 11.4 (caso g) |
| R3.8 (`assignedTechnicianId` en sección secundaria, sin UX específica) | 9.1, 9.2, 9.3 (caso c, d), 11.4 (caso f), 11.6 |
| R4.1 (9 etiquetas `IncidentStatus`) | 2.1, 2.2, 2.4, 8.1, 8.2, 8.3, 9.1, 9.2, 9.3, 11.4 |
| R4.2 (4 etiquetas `IncidentSeverity`) | 2.1, 2.2, 2.4, 8.1, 8.2, 8.3, 9.1, 9.2, 9.3, 11.4 |
| R4.3 (13 etiquetas `IncidentCategory`) | 2.1, 2.2, 2.4, 8.1, 8.2, 8.3, 9.1, 9.2, 9.3, 11.4 |
| R4.4 (6 etiquetas `IncidentSource`) | 2.1, 2.2, 2.4, 8.1, 8.2, 8.3, 9.1, 9.2, 9.3, 11.4 |
| R4.5 (claves en locales + registro en `resources.ts`) | 2.1, 2.2, 2.3, 2.4 |
| R4.6 (misma etiqueta en lista y detalle) | 8.1, 8.2, 9.1, 9.2, 9.3, 11.4 |
| R5.1 (TanStack Query, sin Zustand) | 5.1, 5.2, 5.3, 5.4, 5.5, 6.1, 6.2, 11.2 |
| R5.2 (i18n sin hardcode) | 2.1, 2.2, 2.4, 6.1, 6.2, 6.3, 8.1, 8.2, 8.3, 9.1, 9.2, 9.3, 9.4, 11.2, 11.4, 11.6 |
| R5.3 (cliente HTTP centralizado) | 3.2, 4.1, 4.2, 4.3, 11.5 |
| R5.4 — 401 delegado a sesión | 3.2, 7.1 (caso a), 7.2 (caso a) |
| R5.4 — 403 falta de permiso | 2.1, 2.2, 7.1 (caso b), 7.2 (caso b), 8.1, 8.2 (caso e), 9.2, 9.3 (caso i), 11.4 (caso k) |
| R5.4 — 404 reserva no encontrada (sólo en detalle) | 2.1, 2.2, 7.1 (caso c), 7.2 (caso c), 8.1 (404 → error), 8.2 (caso g), 9.2, 9.3 (caso h), 11.4 (caso h) |
| R5.4 — 422 validación, sólo texto localizado, sin payload | 2.1, 2.2, 7.1 (caso d), 7.2 (caso d), 8.1, 8.2 (caso f), 9.2, 9.3 (caso j), 11.4 (caso l) |
| R5.4 — 5xx / red error genérico | 7.1 (caso e), 7.2 (caso e, f), 8.1, 8.2 (caso d), 9.2, 9.3 (caso k), 11.4 |
| R5.5 (tests con Testing Library) | 5.4, 5.5, 6.2, 6.3, 7.2, 8.2, 8.3, 9.3, 9.4, 10.4, 11.1 |
| R5.6 (costes sin moneda, separadores del locale) | 9.1, 9.2, 9.3 (caso l), 11.4 (caso g) |

## Cobertura de design decisions (matriz de verificación)

| Decisión | Tareas que la aterrizan |
|---|---|
| D1 (sin seam `IncidentsDataSource`) | 3.1, 3.2, 4.1, 4.2 |
| D2 (locale único `incidents.json` con 5 secciones) | 2.1, 2.2, 2.3, 2.4 |
| D3 (DTOs camelCase + mappers explícitos, sin genérico de sobre, 18 campos) | 3.1, 4.1, 4.2, 4.3 |
| D4 (filtros v1: status + severity + page + perPage; sin propertyId; query key objeto normalizado) | 3.1, 4.1, 4.2 (caso b, c, d), 5.2, 6.1, 6.2 (caso g), 8.2 (caso j) |
| D5 (6 columnas mínimas: severity, status, title, category, source, createdAt; sin property) | 3.1, 8.1, 8.2 (caso b, j), 9.1 |
| D6 (descriptor `incident-detail` sin `href`/`navigationGroup`) | 1.1, 1.3, 1.4 |
| D7 (`description` texto plano una sola vez en detalle) | 4.2 (caso f), 9.1, 9.2, 9.3 (caso e) |