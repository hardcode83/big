# Tasks: dashboard-activity-feed

## 1. Timeline read port + adapter (repository layer) <!-- panel: PASS 2026-09-04 -->

- [x] 1.1 Add `list_for_tenant(tenant_id, *, filters, page, per_page) -> Page` to the `TimelineEventReader` Protocol — `backend/app/timeline/domain/repositories.py`, next to `list_for_property`/`last_for_properties`, documenting the same order/tiebreaker/`total` contract as its sibling (design D2) [R1.1, R1.2, R1.4]
- [x] 1.2 Generalize `_conditions` in `backend/app/timeline/infrastructure/repositories.py` to `_conditions(tenant_id, filters, property_id=None)` — the `property_id` clause is appended only when given; update `list_for_property`'s call site to the new argument order. `_ordered(...)` stays untouched (design D3) [R1.1, R2.1]
- [x] 1.3 Implement `SqlAlchemyTimelineEventReader.list_for_tenant`: same `count` + paged `select` shape as `list_for_property`, conditions from `_conditions(tenant_id, filters)` (no `property_id`), ordered by the shared `_ordered(...)` (design D2, D3) [R1.1, R1.2, R2.1, R2.2]
- [x] 1.4 New Alembic revision `backend/alembic/versions/<rev>_timeline_tenant_created_at_index.py`, `down_revision = "4ba1f499f7c2"` (current single head), with only `create_index`/`drop_index` for `ix_timeline_events_tenant_id_created_at` on `(tenant_id, created_at DESC)`; add the matching `Index(...)` to `TimelineEventModel.__table_args__` — `backend/app/timeline/infrastructure/models.py` (design D4) [R1.1, R1.2]
- [x] 1.5 Tests — `backend/tests/timeline/test_read_repository.py`: `list_for_tenant` merges events of several properties of one tenant, newest first with `id` as tiebreaker; a burst of events sharing one `created_at` paged in slices neither repeats nor omits any (same technique as `test_paging_through_identical_timestamps_neither_repeats_nor_omits`); each of `event_type`/`severity`/`actor_type`/`from`+`to` narrows the result and all four AND-combine; `total` counts the filtered set, never the whole tenant; a neighbour tenant's events — including one with the exact same `event_type`/`severity` as the tenant under test — never appear; a tenant with no properties or no events returns `items=()`, `total=0` [R1.1, R1.2, R1.3, R2.1, R4.3]

## 2. Tenant activity read model + use case (application layer) <!-- panel: PASS 2026-09-04 -->

- [x] 2.1 New file `backend/app/timeline/domain/read_models.py`: frozen dataclass `TenantActivityEntry` — the seven fields of `RenderedEntry` plus `property_id: uuid.UUID`, `property_name: str | None`, `property_internal_code: str | None` — and `from_rendered(entry: RenderedEntry, *, property_id, property_name, property_internal_code) -> TenantActivityEntry` (design D6, D7) [R3.1, R3.2]
- [x] 2.2 Add `RenderedTenantPage` (`entries: tuple[TenantActivityEntry, ...]`, `total: int`) and `ListTenantActivityUseCase` to `backend/app/timeline/application/use_cases.py`. Constructor takes `properties: PropertyRepository`, `events: TimelineEventReader`. `execute(*, tenant_id, filters, page, per_page, locale)`: one call to `events.list_for_tenant`, `render(event, locale)` per item, the distinct `property_id`s of that page resolved in ONE call to `properties.list_for_ids(tenant_id, property_ids)`, then `TenantActivityEntry.from_rendered(...)` per entry with `property_name`/`property_internal_code` taken from the batch result and `None` when the id is absent from it. No `PropertyRepository.get`, no `PropertyNotFoundError`, no `404` path (design D5, D6, D9) [R1.1, R1.2, R1.3, R3.1, R3.2, R4.2, R4.3]
- [x] 2.3 Add `get_tenant_activity_use_case(session)` to `backend/app/timeline/api/dependencies.py`, wiring the same two adapters (`SqlAlchemyPropertyRepository`, `SqlAlchemyTimelineEventReader`) as `get_property_timeline_use_case` [R1]
- [x] 2.4 Statement-count test — new `backend/tests/timeline/test_tenant_activity_identity_queries.py`, same technique as `tests/reservations/test_list_identity_queries.py`: seed events across 3 distinct properties of one tenant (10+ events), call `ListTenantActivityUseCase.execute` directly (real adapters) inside `count_statements(test_engine)`, parametrized over two different `per_page` values — assert the `properties` batch query runs exactly once per call, independent of `per_page`; a page whose rows all share one `property_id` still queries `properties` exactly once (not zero, not once-per-row) [R3.2]
- [x] 2.5 Unit test for the read model — new `backend/tests/timeline/test_read_models.py`: `TenantActivityEntry.from_rendered` maps all ten fields correctly; `property_name`/`property_internal_code` come through as `None` (never dropped) when passed as `None` (design D6) [R3.1]

## 3. `GET /api/v1/timeline` route (API layer) <!-- panel: PASS 2026-09-04 -->

- [x] 3.1 Add `TenantTimelineEntryResponse(TimelineEntryResponse)` (adds `property_id`, `property_name`, `property_internal_code`, with a classmethod building it from `TenantActivityEntry`, mirroring `from_rendered`) and `TenantTimelinePageResponse` (same envelope shape as `TimelinePageResponse`, built from a `RenderedTenantPage`) to `backend/app/timeline/api/schemas.py`; `metadata` stays structurally absent from both (design D8) [R3.1, R3.3]
- [x] 3.2 Add `@router.get("", response_model=TenantTimelinePageResponse, ...)` to `backend/app/timeline/api/router.py` — same `ReadDep`, the same query params as `get_property_timeline` (`page`, `per_page`, `event_type`, `severity`, `actor_type`, `from`/`to` alias), no `property_id` path parameter, calling `ListTenantActivityUseCase.execute` via `get_tenant_activity_use_case` (design D1, D9, D10) [R1.1, R1.2, R1.3, R1.4, R2.1, R2.2, R4.1, R4.2, R4.3]
- [x] 3.3 Correct the module docstring of `router.py`: remove the "the global variant ... is out of scope" paragraph and describe both routes (design D1, D11 — code correction, not a spec edit)
- [x] 3.4 Route tests — new `backend/tests/timeline/test_tenant_feed_api.py` (structure mirrors `test_api.py`): pagination envelope shape with the three identity fields present on every entry; `metadata` never in the body; canonical literals untranslated; newest-first across properties with `id` tiebreak; title composed in the caller's language, `description` verbatim; each filter narrows and all AND-combine; `from`/`to` alias plus inverted-range `422`; out-of-range pagination `422`; an empty tenant returns `200` with an empty page (never `404`); the route accepts no `property_id`; `401` anonymous; per-role matrix (`TENANT_OWNER`/`PROPERTY_MANAGER` → 200, `CLEANER`/`TECHNICIAN`/`SUPER_ADMIN` → 403) [R1.1, R1.2, R1.3, R1.4, R2.1, R2.2, R3.1, R3.3, R4.1, R4.2]
- [x] 3.5 Tenant isolation test in `test_tenant_feed_api.py`: seed a neighbour tenant with its own property **and events**, assert none of it appears in the caller's feed or its `total` (R4.3) [R4.3]
- [x] 3.6 Identity-resolution edge case in `test_tenant_feed_api.py`: an event whose `property_id` does not resolve within the tenant (dangling/cross-tenant reference inserted directly, same technique `reservation-property-identity`'s tests use) still appears in the feed with `property_name`/`property_internal_code: null`, never a `500` and never dropped (design D6) [R3.1]
- [x] 3.7 Add `"/api/v1/timeline"` to the protected-endpoints snapshot in `backend/tests/test_route_authorization.py` (`test_the_protected_endpoints_are_the_ones_expected`), with a comment naming `dashboard-activity-feed` and pointing at `test_tenant_feed_api.py` for the per-role matrix [R4.1]
- [x] 3.8 Regenerate `backend/openapi.json` (`make openapi`) and the frontend contract (`cd frontend && npm run api:generate`); commit both alongside the route (`steering/documentation.md`)

## 4. Documentation

- [x] 4.1 Update `docs/dashboard.md`: add `GET /api/v1/timeline` as a sixth row of the API table (update the lead sentence "Cinco rutas" → "Seis rutas"), rewrite or remove the "Cronología global" bullet under "Deuda pendiente" (the backend route now exists; only the `[FE]` widget that consumes it remains pending, per the proposal's Out of scope), and note the `property_name`/`property_internal_code` identity fields the new route's entries carry (`steering/documentation.md`)

## 5. Verification

- [x] 5.1 Full backend test suite passes: `docker compose exec backend uv run pytest` — 10228 passed, 43 skipped, 0 failed (936.98s)
- [x] 5.2 Backend static tooling passes: `cd backend && uv run pyright .` — preflight (`command -v pyright`, `uv run pyright --version`) OK, tool started and ran to completion (899 pre-existing findings, repo-wide baseline per `sdd/specs/backend-tooling.md` §Resultado — none introduced by this change: the two in-scope lines, `router.py`'s new route and `test_filters.py`, reproduce patterns already present byte-for-byte in unmodified sibling code)
- [x] 5.3 `cd frontend && npm run api:check` confirms no drift in the regenerated contract (via the worktree workaround in `sdd/project.md` § Worktree bootstrap) — "api: generated types are up to date"
- [x] 5.4 Manual check: with the stack up and a tenant seeded (`make bootstrap` + `make seed-demo`) with 2 properties (Redes 11/REDES11, Pajaritos 8/PAJARITOS8) each with events, `GET /api/v1/timeline` as `TENANT_OWNER` returns `200` with a single page (`total: 25`) merging both properties' events with correct identity per entry, `metadata` absent from every entry; anonymous request `401`; `CLEANER` (no `READ_PROPERTIES`) gets `403`

## Implementation Notes

<!-- Append-only, written by the implementer of each section for the next one:
     decisions taken, names chosen, gotchas found. One bullet each, no prose. -->

- `list_for_tenant` final signature: `async def list_for_tenant(self, tenant_id: uuid.UUID, *, filters: TimelineFilters, page: int, per_page: int) -> Page` on both `TimelineEventReader` (Protocol, `backend/app/timeline/domain/repositories.py`) and `SqlAlchemyTimelineEventReader` (`backend/app/timeline/infrastructure/repositories.py`) — no `property_id` param.
- `_conditions` final signature: `_conditions(tenant_id: uuid.UUID, filters: TimelineFilters, property_id: uuid.UUID | None = None) -> list` — `property_id` moved to keyword-only-by-convention trailing position (not enforced with `*`, matches existing style of the file which doesn't use `*` on private helpers), default `None`; `list_for_property` now calls it as `_conditions(tenant_id, filters, property_id=property_id)`.
- Alembic revision chosen: `ecc25b9ad299` (`backend/alembic/versions/ecc25b9ad299_timeline_tenant_created_at_index.py`), `down_revision = "4ba1f499f7c2"`. Confirmed `4ba1f499f7c2` was the single head both by manual `down_revision` graph traversal across all files in `backend/alembic/versions/` and by `docker compose exec backend uv run alembic heads` before creating the new revision. After this task the single head is `ecc25b9ad299`.
- Migration verified: `alembic upgrade head` → `downgrade -1` → `upgrade head` all ran cleanly against the dev DB; DB left at head (`ecc25b9ad299`).
- New index `ix_timeline_events_tenant_id_created_at` on `(tenant_id, created_at DESC)` added to both the migration (`op.create_index(..., ["tenant_id", sa.text("created_at DESC")])`, mirroring `e5c9b1f47a28_notifications_read_at.py`'s exact call shape) and `TimelineEventModel.__table_args__`.
- Docker/pytest quirk: inside the `backend` container the workdir is `/app` (= `backend/`), so pytest paths are relative to `backend/` (e.g. `docker compose exec backend uv run pytest tests/timeline/test_read_repository.py`), not prefixed with `backend/`.
- This worktree is a linked worktree (`IS_WORKTREE=yes` per the Makefile's own check) — `make up` publishes no host ports, which is expected and fine for `docker compose exec` based test runs.
- `make up` transiently reported a container-name conflict for `dashboard-activity-feed-migrate-1` (likely a race from an earlier aborted invocation) but the stack still converged correctly — `docker ps` and `alembic heads` afterward confirmed all services healthy and migrate exited 0; no action needed, but don't be alarmed by that specific transient error if seen again.
- All 30 tests in `backend/tests/timeline/test_read_repository.py` pass (20 pre-existing + 10 new for `list_for_tenant`).
- Had to update the pre-existing structural guard `test_the_reader_is_a_separate_port_and_writes_nothing` in `backend/tests/timeline/test_filters.py` — it asserts the exact method set of `TimelineEventReader`, so adding `list_for_tenant` (task 1.1) required adding it to that set too; full `tests/timeline/` (227 tests) passes.
- `TenantActivityEntry`/`from_rendered` final home: `backend/app/timeline/domain/read_models.py` — `from_rendered` is a MODULE-LEVEL function (not a classmethod), mirroring `rendering.render(event, locale) -> RenderedEntry`'s own style. Signature: `from_rendered(entry: RenderedEntry, *, property_id: uuid.UUID, property_name: str | None, property_internal_code: str | None) -> TenantActivityEntry`. `TenantActivityEntry` field order: the 7 `RenderedEntry` fields first (`id`, `occurred_at`, `actor_type`, `event_type`, `severity`, `title`, `description`), then `property_id`, `property_name`, `property_internal_code`.
- `ListTenantActivityUseCase`/`RenderedTenantPage` final home: `backend/app/timeline/application/use_cases.py`, appended after `GetPropertyTimelineUseCase`/`RenderedPage` (both left untouched). `RenderedTenantPage`: `entries: tuple[TenantActivityEntry, ...]`, `total: int`. `ListTenantActivityUseCase(*, properties: PropertyRepository, events: TimelineEventReader).execute(*, tenant_id: uuid.UUID, filters: TimelineFilters, page: int, per_page: int, locale: Locale) -> RenderedTenantPage`. No property pre-check, no 404 — an empty tenant returns `entries=(), total=0`.
- `get_tenant_activity_use_case(session: SessionDep) -> ListTenantActivityUseCase` added to `backend/app/timeline/api/dependencies.py`, right after `get_property_timeline_use_case`, wiring the same `SqlAlchemyPropertyRepository`/`SqlAlchemyTimelineEventReader` pair. Section 3's route dependency is `get_tenant_activity_use_case`, imported the same way `get_property_timeline_use_case` is in the router today.
- `TimelineEvent.property_id` is non-optional (`uuid.UUID`, never `None`) per `backend/app/timeline/domain/entities.py:13` — so the distinct-id set built in `ListTenantActivityUseCase.execute` needs no `is not None` filter, unlike `ListReservationsUseCase`'s `property_id`/`guest_id` (which ARE optional on `Reservation`). `properties.list_for_ids` is still called with that set unconditionally; its own contract already no-ops on empty input.
- New tests use `zip(found.items, rendered, strict=True)` to pair each `TimelineEvent` (for its `property_id`) with its own `RenderedEntry` — both iterate `found.items` in the same order, so no extra bookkeeping dict was needed.
- Full `tests/timeline/` suite: 232 passed (227 pre-existing + 5 new: 3 in `test_tenant_activity_identity_queries.py`, 2 in `test_read_models.py`).
- Review-panel fix (R3.1, finding 1): `test_the_listing_with_ten_events_emits_a_constant_number_of_statements` in `backend/tests/timeline/test_tenant_activity_identity_queries.py` now also asserts each `TenantActivityEntry.property_name`/`property_internal_code` matches the seeded property whose `id` equals `entry.property_id` (a `{property.id: property}` lookup), closing the gap where a mixed-up identity mapping would have passed.
- Review-panel fix (R4.2, finding 2): added `test_the_locale_argument_changes_the_title_but_not_the_description` to `backend/tests/timeline/test_tenant_activity_identity_queries.py` — calls `ListTenantActivityUseCase.execute` with `Locale.ES` and `Locale.EN` on the same seeded `CLEANING_COMPLETED` event and asserts `entry.title` differs by locale while `entry.description` passes through verbatim in both; no production code touched.
- Review-panel fix (R2.1, finding 3): added `test_list_for_tenant_a_single_date_bound_alone_still_filters` to `backend/tests/timeline/test_read_repository.py`, covering `occurred_from` alone and `occurred_to` alone (previously only tested together) for `list_for_tenant`.
- `TenantTimelineEntryResponse(TimelineEntryResponse)` (`backend/app/timeline/api/schemas.py`): adds `property_id: uuid.UUID`, `property_name: str | None`, `property_internal_code: str | None`. Builder is a classmethod named `from_rendered(entry: TenantActivityEntry) -> "TenantTimelineEntryResponse"` (same name as the parent's own `from_rendered`, overriding it — `# type: ignore[override]` on the signature since the parameter type narrows to a different input type). `metadata` is not mentioned anywhere in the class.
- `TenantTimelinePageResponse` (`backend/app/timeline/api/schemas.py`): same envelope fields as `TimelinePageResponse` (`data`, `total`, `page`, `per_page`, `total_pages`). Builder: `build(cls, result: RenderedTenantPage, *, page: int, per_page: int) -> "TenantTimelinePageResponse"` — takes the whole `RenderedTenantPage` (not `entries`/`total` split apart, unlike the sibling) since the design called for building it "from a `RenderedTenantPage`"; `page`/`per_page` still come from the caller since `RenderedTenantPage` doesn't carry them.
- Route: `GET /api/v1/timeline` (no path parameter), handler `list_tenant_activity` in `backend/app/timeline/api/router.py`, registered via `@router.get("", response_model=TenantTimelinePageResponse, ...)` placed BEFORE `@router.get("/{property_id}", ...)` in the file (collection route before the parameterized one — purely a style choice, Starlette does not care since `""` and `"/{property_id}"` are structurally distinct paths; both routes tested and pass in the same suite). Signature mirrors `get_property_timeline` exactly minus the `property_id: uuid.UUID` parameter and the property pre-check. OpenAPI `operationId`: `list_tenant_activity_api_v1_timeline_get`.
- Final route count in the module: 2 (`GET /api/v1/timeline/{property_id}` and `GET /api/v1/timeline`), both gated by `require(Permission.READ_PROPERTIES)` (`ReadDep`), no permission of the module's own.
- Identity field names on the wire (for docs, task 4.1): `property_id`, `property_name`, `property_internal_code` — same names as `reservation-property-identity`'s `ReservationResponse` fields, `null` (with the key present) when the FK does not resolve within the tenant.
- `backend/openapi.json` and `frontend/lib/api/generated/openapi.d.ts` regenerated via `make openapi` plus the worktree `docker compose cp`/`ln -sfn` workaround (`sdd/project.md` § Worktree bootstrap); `npm run api:check` inside the `frontend` container confirms no drift. Both files are modified but NOT committed by this section, per the task's own scope.
- Full `tests/timeline/ tests/test_route_authorization.py` suite: 283 passed — 256 in `tests/timeline/` (232 pre-existing from sections 1-2 + 24 new in `test_tenant_feed_api.py`) + 27 in `tests/test_route_authorization.py`. No regressions.
- `docs/dashboard.md` (task 4.1): "Cinco rutas" → "Seis rutas" and a new table row for `GET /api/v1/timeline`; added a bullet under "Lo que conviene saber al operarlas" documenting `property_id`/`property_name`/`property_internal_code` (null when the FK doesn't resolve within the tenant, same names as `reservation-property-identity`) plus a bullet on the `200`-with-empty-page exception to the `404` rule; reworded the `/timeline` screen bullet and the "Cronología global" debt bullet to say the backend route now exists (delivered by `dashboard-activity-feed`) and only the `[FE]` «Actividad Reciente» widget (Decisión 4 of `visual-restyle-workspace`) remains pending — no stale "no existe"/"backend sirve una por petición" claims left.
