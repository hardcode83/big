<!-- Markers, read by /sdd:run and the lifecycle gates (HTML comments, invisible
     when rendered). On a section heading: "hard" makes that section's
     implementer run on the stronger model; "panel: PASS <date> receipt:<id>"
     is written by the panel gate (reviewer_panel.py) when the section's review
     panel passes — never by hand; "panel: skipped — <reason>" records a
     deliberate skip (scaffolding, docs, config). On a task line:
     "manual" marks a task only a human can perform — run leaves it to you and
     it may travel with the PR as a deferred entry; it may sit on any line of
     the task item, not only the checkbox line. -->

# Tasks: guest-scheduled-comms

## 1. Check-in reminders (24h and 2h) <!-- panel: PASS 2026-09-13 receipt:35fd0df5 -->

- [x] 1.1 Add `render_checkin_reminder_email(language, property_name, check_in_date, check_in_time_local) -> tuple[str, str]` to new `backend/app/reservations/domain/notifications.py`, following `guests/domain/notifications.py`'s `"es"`/`"en"` fallback convention (design D6) — fixed template plus property name and dates only, never guest- or operator-authored text. Unit tests in new `backend/tests/reservations/test_notifications.py` covering both languages and the unknown-language fallback. [R1, R5]
- [x] 1.2 Add `SendCheckinRemindersUseCase` to `backend/app/reservations/application/use_cases.py`: per-tenant candidate query of `CONFIRMED` reservations bounded to `check_in_date` within ±1 day of `now` (design D4); for each candidate, `properties.domain.clock_triggers.effective_bounds(property, reservation)` + the threshold-crossing check of design D3 (`checkin_instant - lead <= now < checkin_instant`) for both `CHECKIN_REMINDER_24H` (24h lead) and `CHECKIN_REMINDER_2H` (2h lead); resolve the guest's email (skip-and-count the candidate if blank, design D5); `exists_for(tenant_id, related_type="reservation", related_id=reservation.id, notification_type=...)` dedup per type (design D8) before building; write one `PENDING` `NotificationLog` row per due type via `render_checkin_reminder_email`, `channel=EMAIL`, `sla_deadline_at=None` (design D11/D12). Catch `IncompatibleTransitionContextError` per candidate and skip-and-count rather than aborting the tenant's sweep. [R1, R4, R5]
- [x] 1.3 Unit tests for `SendCheckinRemindersUseCase` with in-memory fakes for its ports (per `backend-architecture.md`'s `application/` testing convention — no real DB, no SQLAlchemy mocks): each window fires exactly once, a second run does not duplicate either type, a reservation with no guest email is skipped and counted, a property with a DST-invalid local time is skipped and counted rather than raising out of the sweep. [R1, R4]
- [x] 1.4 Register `send_checkin_reminders` in `CADENCES` (`backend/app/scheduler/schedule.py`, `timedelta(minutes=15)`, design D2) and add its `@celery_app.task` in `backend/app/scheduler/tasks.py`, wiring `SendCheckinRemindersUseCase` through the existing `_guarded`/`run_for_every_tenant` shape (design D1) — mirror `check_checkin_windows`'s wiring at `tasks.py:461-467`. [R1]
- [x] 1.5 In `backend/tests/notifications/test_writer_census.py`: move `CHECKIN_REMINDER_24H` and `CHECKIN_REMINDER_2H` from `WITHOUT_WRITER` to `WITH_WRITER`; add `reservations/domain/notifications.py` and `reservations/application/use_cases.py` to `CONSTRUCTION_SITES` (only if not already present). In `backend/tests/scheduler/test_schedule.py`: register `send_checkin_reminders` in the "every registered task is in exactly one of the three tables" assertion and the "calendar is PRD §8.3 plus exactly the declared additions" assertion. [R1, R5]

## 2. Checkout reminder <!-- panel: PASS 2026-09-13 receipt:d210dff4 -->

- [x] 2.1 Add `render_checkout_reminder_email(language, property_name, check_out_date, check_out_time_local) -> tuple[str, str]` to `backend/app/reservations/domain/notifications.py` (same file as 1.1). Unit tests alongside 1.1's in `backend/tests/reservations/test_notifications.py`. [R2, R5]
- [x] 2.2 Add `SendCheckoutRemindersUseCase` to `backend/app/reservations/application/use_cases.py`: same shape as `SendCheckinRemindersUseCase` (1.2) but against `effective_bounds`'s `end` instant and `check_out_date`, a 2h lead time (design D3 — reused from `CHECKIN_REMINDER_2H` rather than inventing a new number), and `notification_type=CHECKOUT_REMINDER`. Same skip-and-count rules for missing email and DST-invalid times. [R2, R4, R5]
- [x] 2.3 Unit tests for `SendCheckoutRemindersUseCase`, same coverage shape as 1.3 (fires once, dedup on repeat run, missing-email skip, DST-invalid skip). [R2, R4]
- [x] 2.4 Register `send_checkout_reminders` in `CADENCES` (15 min) and its `@celery_app.task`, same wiring shape as 1.4. [R2]
- [x] 2.5 In `test_writer_census.py`: move `CHECKOUT_REMINDER` from `WITHOUT_WRITER` to `WITH_WRITER` (the module is already in `CONSTRUCTION_SITES` from 1.5). In `test_schedule.py`: register `send_checkout_reminders` in both assertions from 1.5. [R2, R5]

## 3. Automatic access-instructions delivery <!-- panel: PASS 2026-09-13 receipt:665dfb16 -->

- [x] 3.1 Add `ACCESS_INSTRUCTIONS_SENT = "ACCESS_INSTRUCTIONS_SENT"` to `NotificationType` in `backend/app/notifications/domain/enums.py`, commented as a declared divergence from PRD §14's sixteen (design D10), the same way `GUEST_PORTAL_LINK_DELIVERED`/`PASSWORD_RESET_REQUESTED` are already commented. [R3, R5]
- [x] 3.2 Add `render_access_instructions_email(language, code_masked) -> tuple[str, str]` to new `backend/app/access/domain/notifications.py` — fixed template plus `code_masked` only (rule 11 exception 1), never `access_records.notes` or any other free text. Unit tests in new `backend/tests/access/test_notifications.py` covering both languages, the fallback, and that only the masked form appears in the rendered output. [R3, R5]
- [x] 3.3 Add `DeliverAccessInstructionsUseCase` to `backend/app/access/application/use_cases.py`: per-tenant candidate query over `AccessRecord`s with `status` in `{MANUAL_ADDED, CREATED_EXTERNAL}` and a non-null `code_masked`; resolve the linked reservation's guest email (skip-and-count if blank, design D5); `exists_for(tenant_id, related_type="access_record", related_id=record.id, notification_type=ACCESS_INSTRUCTIONS_SENT)` dedup (design D8); write one `PENDING` row via `render_access_instructions_email`, `channel=EMAIL`, `sla_deadline_at=None`; MUST NOT call `mark_delivered` or otherwise mutate `AccessRecord.status` (design D9, proposal R3.4 — this stays the operator's own independent confirmation). [R3, R4, R5]
- [x] 3.4 Unit tests for `DeliverAccessInstructionsUseCase`: fires exactly once per record, a second run does not duplicate, missing-email skip, and an explicit assertion that `AccessRecord.status`/`mark_delivered` is untouched by this use case. [R3, R4]
- [x] 3.5 Register `deliver_access_instructions` in `CADENCES` (15 min, design D9) and its `@celery_app.task`, wiring `DeliverAccessInstructionsUseCase` through the same `_guarded` shape — mirror `provision_access_records`'s wiring at `tasks.py:666-684`. [R3]
- [x] 3.6 In `test_writer_census.py`: add `ACCESS_INSTRUCTIONS_SENT` to `WITH_WRITER`; add `access/domain/notifications.py` and `access/application/use_cases.py` to `CONSTRUCTION_SITES`; update `test_exactly_four_types_have_no_writer` (retitle if needed) so `WITHOUT_WRITER` now asserts exactly `{"LOCK_ALERT"}`, since all three reminder types moved out in sections 1-2. In `test_schedule.py`: register `deliver_access_instructions` in both assertions. [R3, R5]

## 4. Verification

- [ ] 4.1 Full backend test suite passes: `docker compose exec backend uv run pytest` (or `docker compose run --rm backend uv run pytest` with the stack down).
- [ ] 4.2 Static tooling passes: from `backend`, `uv sync --frozen` then `uv run pyright .`.
- [ ] 4.3 `make check-rule11-ownership` passes (host, `python3`, no Docker, no stack) — this change is the first living writer of rule 11's exception 1 and touches backend docstrings that reference it.
- [ ] 4.4 Manual: with the dev stack up and seed/demo data giving a reservation inside each of the 24h/2h/checkout windows and an `AccessRecord` with a masked code, trigger each of the three new Celery tasks (or wait a tick) and confirm a real email arrives via the dev SMTP relay, each `NotificationLog` row lands `SENT` with the expected template/language and, for the access-instructions email, the masked code and nothing else; confirm a second tick does not send any of them twice. <!-- manual -->

## Implementation Notes

<!-- Append-only, written by the implementer of each section for the next one:
     decisions taken, names chosen, gotchas found. One bullet each, no prose. -->

- Section 1 (2026-09-13): `render_checkin_reminder_email(language, property_name, check_in_date: date, check_in_time_local: time) -> tuple[str, str]` lives in new `backend/app/reservations/domain/notifications.py`. Section 2 adds `render_checkout_reminder_email(...)` to the **same file** (design D6) — keep the `_CHECKIN_SUBJECT`/`_CHECKIN_BODY` dicts as-is and add sibling `_CHECKOUT_*` dicts, don't rename anything.
- `SendCheckinRemindersUseCase` (`reservations/application/use_cases.py`) ports: `properties: PropertyRepository`, `reservations: ReservationRepository`, `guests: GuestRepository`, `notifications: NotificationLogRepository`, `uow: UnitOfWork`. `execute(*, tenant_id, now) -> CheckinReminderReport` (fields: `candidates`, `written`, `skipped_missing_email`, `skipped_invalid_local_time`).
- Candidate query: loads `properties.list_all(tenant_id)` first, then `reservations.list_for_properties(tenant_id, property_ids, today-1, today+1)` (module constant `_CANDIDATE_WINDOW = timedelta(days=1)`) — **no new repository method was added**; `list_for_properties`'s stay-overlap semantics are deliberately over-inclusive, and the precise `effective_bounds` + threshold check in Python is what actually decides (design D3/D4). `CONFIRMED` status is filtered in the Python loop, not in SQL (mirrors `list_for_properties`'s own "no status filter" precedent). Reuse this same shape for `SendCheckoutRemindersUseCase`'s candidate query (swap the threshold to `checkout_instant`).
- Recipient resolution is a private `_recipient(tenant_id, guest_id) -> tuple[str, str] | None` method (email, `preferred_language`), mirroring `SendGuestAccessTokenUseCase._recipient` but returning `None` instead of raising. Reusable as-is for the checkout use case (same guest, same reservation shape).
- **Gotcha, AST census (`test_writer_census.py`)**: `NotificationLog(...)` must be built with a **literal** `notification_type=NotificationType.<X>.value` at the call site — a loop variable holding the enum member is invisible to the census (measured: it silently dropped both new types from `WITH_WRITER`). Solved here with a private `_write_if_new(...)` helper that has one `if/elif` branch per `NotificationType` member, each with its own literal `NotificationLog(...)` construction. Section 2's `CHECKOUT_REMINDER` writer needs the same shape (either add a third branch here or a parallel helper — either way, the literal must appear textually per type).
- **Gotcha, second AST guard not mentioned in tasks.md**: `tests/notifications/test_channel_literals.py` allowlists every file that references `NotificationChannel.<X>` as a bare literal (D6 of `notification-channel-routing`). Since design D12 has each writer target `NotificationChannel.EMAIL` directly (no `resolve_channels`/`dispatch_channels`), `app/reservations/application/use_cases.py` had to be added to `CHANNEL_LITERAL_WHITELIST` in that test file (both the set and its `test_whitelist_is_exactly_the_design_d6_set` pin) — cited to this change's own design D12. Section 2 does not need a second entry (same file, already whitelisted); section 3's `access/application/use_cases.py` will need its own entry there.
- `effective_bounds(property, reservation)` raises `IncompatibleTransitionContextError` (from `app.properties.domain.exceptions`) — caught per-candidate, counted in `skipped_invalid_local_time`, sweep continues.
- `check_in_time_local` passed to the renderer is `reservation.check_in_time or property.default_check_in_time` — a plain fallback expression written directly in the use case (not a call into `ContextualStateResolver`'s private DST arithmetic, which stays untouched).
- Celery wiring: `CADENCES["send_checkin_reminders"] = timedelta(minutes=15)` in `scheduler/schedule.py`; `_send_checkin_reminders(session, tenant_id, now)` wiring function + `@celery_app.task(name="send_checkin_reminders")` in `scheduler/tasks.py`, both placed beside `check_checkin_windows`'s equivalents and using `_guarded` directly (not `_clock_task`, since there is no `PropertyStateTrigger` involved).
- Commands run (backend container, `docker compose -f docker-compose.yml -f docker-compose.worktree.yml run --rm backend uv run pytest ...` — the worktree overlay is required or `postgres`'s published port collides with the main checkout's stack):
  - `tests/reservations/test_notifications.py` — 5 passed
  - `tests/reservations/test_checkin_reminders.py` — 11 passed
  - `tests/reservations/ tests/notifications/test_writer_census.py tests/scheduler/ tests/test_layering.py` — 1835 passed
  - `tests/notifications/ tests/guests/ tests/properties/` — 1 pre-existing failure (`test_channel_literals.py`, fixed by the whitelist edit above), then 1735 passed/41 skipped before the fix; all green after
  - Full suite `uv run pytest -q` — 11097 passed, 44 skipped, 1 failed (`tests/messaging/test_repositories.py::test_two_concurrent_callers_end_with_one_thread_and_both_see_it`, a lock-contention timing test unrelated to this section; re-run alone passed in 0.63s, confirming host-contention flake, not a regression)
  - `uv run pyright <touched files>` — 0 errors on `app/reservations/application/use_cases.py`, `app/reservations/domain/notifications.py`, `app/scheduler/tasks.py`, `app/scheduler/schedule.py`, `tests/reservations/test_notifications.py`; 4 errors on `tests/reservations/test_checkin_reminders.py`, all the same pre-existing `Fake*Repository`-vs-`Protocol` structural-typing noise already present in `tests/reservations/test_use_cases.py` (verified by running pyright on that file too)
  - `make check-rule11-ownership` (host, no Docker) — passed
- Review fix (sdd-qa FAIL on R1/D3, 2026-09-13): added two boundary tests to `tests/reservations/test_checkin_reminders.py` — `test_the_24h_lower_bound_is_inclusive_and_fires` (`now == checkin_instant - timedelta(hours=24)` fires `CHECKIN_REMINDER_24H`) and `test_the_upper_bound_is_exclusive_at_the_checkin_instant` (`now == checkin_instant` fires nothing). `tests/reservations/test_checkin_reminders.py` — 13 passed; `uv run pyright tests/reservations/test_checkin_reminders.py` — 0 errors.
- Review fix (sdd-review-tenancy FAIL on `sdd/steering/security.md` rule 1, 2026-09-13): `tests/reservations/test_checkout_reminders.py` had no isolation test for `SendCheckoutRemindersUseCase`. Added `test_a_second_tenants_reservation_is_neither_read_nor_written`, introducing `OTHER_TENANT` (mirroring the constant already present but unused in `test_checkin_reminders.py`) — seeds an `OTHER_TENANT` property/guest/reservation that would be a candidate in the identical checkout window, runs `execute(tenant_id=TENANT, ...)`, and asserts `report.candidates == 1`/`report.written == 1` and the only `NotificationLog` row belongs to `TENANT`'s own reservation. `tests/reservations/test_checkout_reminders.py` — 13 passed; `uv run pyright tests/reservations/test_checkout_reminders.py` — 0 errors.
- Section 2 (2026-09-13): `render_checkout_reminder_email(language, property_name, check_out_date, check_out_time_local) -> tuple[str, str]` added to `reservations/domain/notifications.py`, sibling `_CHECKOUT_SUBJECT`/`_CHECKOUT_BODY` dicts, same fallback shape as the check-in renderer — nothing renamed there per the section-1 note.
- `SendCheckoutRemindersUseCase` (`reservations/application/use_cases.py`) has the identical port shape and `execute(*, tenant_id, now) -> CheckoutReminderReport` signature as the check-in use case, but evaluates a single due type against `effective_bounds`'s **second** (`checkout_instant`) return value, at `_CHECKOUT_REMINDER_LEAD` — a module constant defined as `_CHECKIN_REMINDER_LEADS[NotificationType.CHECKIN_REMINDER_2H]`, i.e. a literal reuse of the same `timedelta(hours=2)` value rather than a fresh magic number (design D3). `check_out_time_local` is `reservation.check_out_time or property.default_check_out_time`, the same plain-fallback shape as check-in's `check_in_time`.
- Candidate query is the exact same shape/window as section 1's (`properties.list_all` then `reservations.list_for_properties(tenant_id, property_ids, now-1d, now+1d)`) — `list_for_properties`'s stay-overlap semantics are over-inclusive enough to also cover checkout candidates; the `effective_bounds` + threshold check in Python is still what decides (design D4).
- `_write_if_new` here has only one `NotificationType` branch (`CHECKOUT_REMINDER`), since there is only one due type for checkout — still a literal `NotificationLog(notification_type=NotificationType.CHECKOUT_REMINDER.value, ...)` construction at the call site, per the AST-census gotcha section 1 flagged. `_recipient` is duplicated verbatim from the check-in use case rather than shared (no common base class, four lines).
- **`test_channel_literals.py` needed no new whitelist entry**: `app/reservations/application/use_cases.py` was already whitelisted by section 1 (confirmed, not re-added) — both `CHANNEL_LITERAL_WHITELIST` and its pinned-set test stayed green untouched.
- `test_writer_census.py`: `CHECKOUT_REMINDER` moved from `WITHOUT_WRITER` to `WITH_WRITER`; `reservations/application/use_cases.py` and `reservations/domain/notifications.py` were already in `CONSTRUCTION_SITES` (confirmed, not re-added). `WITHOUT_WRITER` is now just `{"LOCK_ALERT"}`. `test_exactly_four_types_have_no_writer`'s docstring/body updated to match — the function name is still stale (kept for section 3 to retitle alongside `ACCESS_INSTRUCTIONS_SENT`, per its own note).
- `test_schedule.py`: `send_checkout_reminders` added to `BEYOND_PRD_8_3` (15 min) with an explanatory comment; both `test_the_calendar_is_prd_8_3_plus_exactly_the_declared_additions` and `test_every_registered_task_is_in_exactly_one_of_the_three_tables` derive from that same dict, so one edit covers both assertions.
- Celery wiring: `CADENCES["send_checkout_reminders"] = timedelta(minutes=15)` in `scheduler/schedule.py`; `_send_checkout_reminders(session, tenant_id, now)` + `@celery_app.task(name="send_checkout_reminders")` in `scheduler/tasks.py`, placed right after `send_checkin_reminders`'s equivalents, same `_guarded` (not `_clock_task`) wiring.
- Commands run (backend container, `docker compose -f docker-compose.yml -f docker-compose.worktree.yml run --rm backend uv run pytest ...`):
  - `tests/reservations/test_notifications.py` — 11 passed (task 2.1)
  - `tests/reservations/test_checkout_reminders.py` — 12 passed (task 2.2/2.3, including the D3 lower/upper-bound pair pinned from the start, per the section-1 review lesson)
  - `tests/reservations/ tests/notifications/test_writer_census.py tests/notifications/test_channel_literals.py tests/scheduler/test_schedule.py` — 279 passed
  - Full suite `uv run pytest -q` — 11118 passed, 44 skipped, 0 failed (no flake this run; section 1's `tests/messaging/test_repositories.py` concurrency test noted as occasionally flaky did not reproduce)
  - `uv run pyright app/reservations app/scheduler tests/reservations tests/notifications tests/scheduler` — 88 pre-existing errors, all in `tests/reservations/test_entities.py`/`test_entity_mutations.py` (untouched by this section, confirmed via `git status`/`git log`, same `**overrides`-vs-`Protocol` structural noise section 1 already found elsewhere); scoped to only the files this section touched (`app/reservations/application/use_cases.py`, `app/reservations/domain/notifications.py`, `app/scheduler/tasks.py`, `app/scheduler/schedule.py`, `tests/reservations/test_notifications.py`, `tests/reservations/test_checkout_reminders.py`, `tests/reservations/test_checkin_reminders.py`, `tests/notifications/test_writer_census.py`, `tests/notifications/test_channel_literals.py`, `tests/scheduler/test_schedule.py`) — 0 errors
  - `make check-rule11-ownership` (host, no Docker) — passed
- For section 3: `WITHOUT_WRITER` in `test_writer_census.py` is now `{"LOCK_ALERT"}` only — `ACCESS_INSTRUCTIONS_SENT` is not in it (check whether it already has a writer or belongs elsewhere before assuming it needs one). `test_exactly_four_types_have_no_writer`'s name is stale per its own docstring; retitle it if convenient when touching that file. `app/access/application/use_cases.py` will need its own entry in `test_channel_literals.py`'s `CHANNEL_LITERAL_WHITELIST` (and its pinned-set test) if it targets `NotificationChannel.EMAIL` directly the same way `reservations/application/use_cases.py` does — confirm the design's channel choice before assuming this pattern applies.
- Section 3 (2026-09-13): `render_access_instructions_email(language, code_masked) -> tuple[str, str]` lives in new `backend/app/access/domain/notifications.py` — its own file per design D6 (`guests/domain/notifications.py` is guest-identity-scoped, not the `AccessRecord` owner). `code_masked` is the only variable input, never `notes`; unit tests in new `backend/tests/access/test_notifications.py` (6 tests) pin fallback + both languages + exact-body equality against the module's own private template dict (imported directly — a test reaching into a "private" name it owns, not a layering violation).
- **`AccessRecordRepository` gained a new port method**, `list_awaiting_instructions(tenant_id, *, limit) -> Sequence[AccessRecord]` (`domain/repositories.py` + `infrastructure/repositories.py`): `status IN (MANUAL_ADDED, CREATED_EXTERNAL)` AND `code_masked IS NOT NULL`. Deliberately approximate the same way `list_reservations_missing_records` is — a record already notified is still returned, and `exists_for` in the use case is what actually stops a repeat send. `DELIVERED` is excluded even though it also carries a mask: an operator's own confirmation means this sweep has nothing left to do.
- `DeliverAccessInstructionsUseCase` (`access/application/use_cases.py`) does **NOT** extend `_AccessOperationBase` — confirmed by reading it first, per the task brief's own hint: that base class moves an `AccessRecord` through its state machine and persists timeline/audit rows, and this use case touches neither. Ports: `records: AccessRecordRepository`, `reservations: ReservationRepository`, `guests: GuestRepository`, `notifications: NotificationLogRepository`, `uow: UnitOfWork`, `batch_size: int` (same knob as `ProvisionAccessRecordsUseCase`, since the candidate query has no natural time window the way the reminder use cases' `_CANDIDATE_WINDOW` does — a `MANUAL_ADDED`/`CREATED_EXTERNAL` record with a mask stays a candidate indefinitely until `DELIVERED`/`REVOKED`/`EXPIRED`, so `exists_for` dedup plus a batch cap is the only bound). `execute(*, tenant_id, now) -> DeliverAccessInstructionsReport` (fields: `candidates`, `written`, `skipped_missing_email` — no `skipped_invalid_local_time`, since there is no DST/timezone computation in this path at all).
- Recipient resolution is a two-hop private `_recipient(tenant_id, reservation_id) -> tuple[str, str] | None`: `AccessRecord` names a `reservation_id`, not a `guest_id` directly, so the reservation is loaded first via `ReservationRepository.get`, then the guest via `GuestRepository.get` on `reservation.guest_id` — unlike the reminder use cases, which already have the reservation (and its `guest_id`) in hand from their own candidate query.
- `_write_if_new` writes one literal `NotificationLog(notification_type=NotificationType.ACCESS_INSTRUCTIONS_SENT.value, ...)` call site (only one `NotificationType` member for this use case, so no `if/elif` branching needed — same shape as `SendCheckoutRemindersUseCase`'s single-branch writer). `related_type="access_record"`, `related_id=record.id` (design D8) — a different dedup key from the reservation-scoped reminders, so a stay's check-in/checkout reminders and its access instructions never collide even sharing a `reservation_id`.
- `app/access/application/use_cases.py` needed its own entry in `test_channel_literals.py`'s `CHANNEL_LITERAL_WHITELIST` (set + pinned-set test) — confirmed design D12 applies the same way it does to `reservations/application/use_cases.py` (a guest recipient, no `User` row, no channel-resolver fan-out).
- `test_writer_census.py`: `ACCESS_INSTRUCTIONS_SENT` added to `WITH_WRITER`; `access/domain/notifications.py` and `access/application/use_cases.py` added to `CONSTRUCTION_SITES`. `WITHOUT_WRITER` stays `{"LOCK_ALERT"}` unchanged (it was never carrying `ACCESS_INSTRUCTIONS_SENT` — the enum member simply had no writer yet, so `test_the_two_lists_partition_the_enum` needed the new member added to `WITH_WRITER`, not removed from anywhere). `test_exactly_four_types_have_no_writer` retitled to `test_exactly_one_type_has_no_writer` per its own docstring's standing instruction.
- Unit tests for the use case are in new `backend/tests/access/test_delivery.py` (14 tests), fakes-only per `backend-architecture.md`'s `application/` convention: `FakeReservationRepository`/`FakeGuestRepository`/`FakeUnitOfWork` are **reused from `tests.reservations.doubles`** (cross-package test import — precedented by `tests/properties/doubles.py` re-exporting from the same module) rather than duplicated; a new local `FakeAccessRecordRepository` (only `list_awaiting_instructions` reachable) and `FakeNotificationLogRepository` (identical shape to the reminder tests' own) are defined in the new file. Covers: fires once per `MANUAL_ADDED`/`CREATED_EXTERNAL` candidate, no duplicate on a second run, missing-email skip (absent email, absent `guest_id`, absent `reservation_id`, a reservation id that resolves to nothing), `DELIVERED`/no-mask records are not candidates at all, an explicit assertion that `AccessRecord.status` is never mutated and that the use case holds no `_provider`/`_timeline`/`_audit` collaborator, a cross-tenant isolation test (a second tenant's qualifying record is neither read nor written), and a batch-size bound.
- **New integration tests** in `tests/access/test_repositories.py` for `list_awaiting_instructions` itself (5 tests, against the real DB/session, following that file's own convention): both qualifying statuses are candidates, a null mask excludes a record even in a qualifying status, `PENDING`/`DELIVERED`/`REVOKED`/`EXPIRED` are all excluded, cross-tenant isolation, and the `limit` bound. `tests/access/conftest.py`'s `insert_access_record` helper gained a `code_masked` kwarg (default `None`, backward compatible) to support these.
- Commands run (backend container, `docker compose -f docker-compose.yml -f docker-compose.worktree.yml run --rm backend uv run pytest ...`):
  - `tests/access/ tests/notifications/test_writer_census.py tests/notifications/test_channel_literals.py tests/scheduler/test_schedule.py` — 189 passed
  - `uv run pyright app/access tests/access tests/notifications/test_writer_census.py tests/notifications/test_channel_literals.py tests/scheduler/test_schedule.py app/notifications/domain/enums.py app/scheduler/tasks.py app/scheduler/schedule.py` — 7 pre-existing errors, none in any file this section touched (`app/access/api/router.py` ×5, `app/access/infrastructure/repositories.py:156` in the untouched `save` method, `tests/access/test_provisioning.py`'s pre-existing `_Uow`/`Protocol` noise) — confirmed via `git diff --stat` that none of those three files' diffs (or a zero-line diff, for `router.py`/`test_provisioning.py`) touch the erroring lines
  - `make check-rule11-ownership` (host, no Docker) — passed
- No CONFLICT/BLOCKER. Section 4 should know: the full-suite run (4.1) and the broader `uv run pyright .` (4.2) were intentionally left to section 4/verification rather than run here, per this section's own scope; nothing in this section's own scoped runs suggested either would find anything new.
