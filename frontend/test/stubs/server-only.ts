// Vitest stub for the `server-only` guard, which throws when imported outside a
// React Server Component. Aliased in vitest.config.ts so server modules can be
// unit-tested. Production builds use the real package.
export {};
