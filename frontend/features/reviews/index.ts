/**
 * Public entry point for the `/reviews` screen. Only `ReviewsView` is exposed:
 * hooks, types, and modules are internal — tests reach them by relative path.
 */
export { ReviewsView } from "./components/reviews-view";